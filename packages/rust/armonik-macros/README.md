# ArmoniK.Api macros

Internal derive macros for the [`armonik`](https://crates.io/crates/armonik) crate.

This crate is an implementation detail of `armonik`: the attribute grammar and
the emitted code offer no stability guarantee of their own. It must only be
used through the `armonik` crate, which depends on it with an exact version pin.
