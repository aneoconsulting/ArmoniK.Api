// @generated by ffi/poc/codec/gen/cpp_native.py (plan from ffi/corpus/corpus.json (reader view) over ffi/schema/shapes.json). Do not edit.

// Arm `core-native-cpp` (unknown fields: RETAIN): the codec rendered from the SAME plans
// as the core behind the C ABI (`poc/codec/gen/plan.py`), over the facade objects, with no
// boundary at all. README R3's no-boundary control; R-E1: the arms differ only in where a
// value comes from.
//
// R5's second half: an arm named "no boundary" is only a control if it is not FUSED INTO
// THE BENCHMARK LOOP. In C++ that question is `-flto` over this file, and the slice's
// `gen/boundary.sh` answers it from the built artifact.
#include "generated/core_native_retain.h"

#include <cstring>

namespace corpus {
namespace native_retain {

static const uint32_t kLimit = 100;

static void enc_timestamp(const Timestamp &o, ak::Enc *e);
static void dec_timestamp(ak::Dec *d, Timestamp *out, uint32_t depth);
static void enc_duration(const Duration &o, ak::Enc *e);
static void dec_duration(ak::Dec *d, Duration *out, uint32_t depth);
static void enc_result_raw(const ResultRaw &o, ak::Enc *e);
static void dec_result_raw(ak::Dec *d, ResultRaw *out, uint32_t depth);
static void enc_task_options(const TaskOptions &o, ak::Enc *e);
static void dec_task_options(ak::Dec *d, TaskOptions *out, uint32_t depth);
static void enc_task_output(const TaskOutput &o, ak::Enc *e);
static void dec_task_output(ak::Dec *d, TaskOutput *out, uint32_t depth);
static void enc_task_detailed(const TaskDetailed &o, ak::Enc *e);
static void dec_task_detailed(ak::Dec *d, TaskDetailed *out, uint32_t depth);
static void enc_task_summary(const TaskSummary &o, ak::Enc *e);
static void dec_task_summary(ak::Dec *d, TaskSummary *out, uint32_t depth);
static void enc_probe(const Probe &o, ak::Enc *e);
static void dec_probe(ak::Dec *d, Probe *out, uint32_t depth);
static void enc_empty(const Empty &o, ak::Enc *e);
static void dec_empty(ak::Dec *d, Empty *out, uint32_t depth);
static void enc_upload_result_data(const UploadResultData &o, ak::Enc *e);
static void dec_upload_result_data(ak::Dec *d, UploadResultData *out, uint32_t depth);
static void enc_metrics_batch(const MetricsBatch &o, ak::Enc *e);
static void dec_metrics_batch(ak::Dec *d, MetricsBatch *out, uint32_t depth);
static void enc_pair(const Pair &o, ak::Enc *e);
static void dec_pair(ak::Dec *d, Pair *out, uint32_t depth);
static void enc_list_results_response(const ListResultsResponse &o, ak::Enc *e);
static void dec_list_results_response(ak::Dec *d, ListResultsResponse *out, uint32_t depth);
static void enc_list_tasks_detailed_response(const ListTasksDetailedResponse &o, ak::Enc *e);
static void dec_list_tasks_detailed_response(ak::Dec *d, ListTasksDetailedResponse *out, uint32_t depth);
static void enc_list_task_summary_response(const ListTaskSummaryResponse &o, ak::Enc *e);
static void dec_list_task_summary_response(ak::Dec *d, ListTaskSummaryResponse *out, uint32_t depth);
static void enc_list_probe_response(const ListProbeResponse &o, ak::Enc *e);
static void dec_list_probe_response(ak::Dec *d, ListProbeResponse *out, uint32_t depth);
static void enc_list_metrics_response(const ListMetricsResponse &o, ak::Enc *e);
static void dec_list_metrics_response(ak::Dec *d, ListMetricsResponse *out, uint32_t depth);
static void enc_upload_result_data_message(const UploadResultDataMessage &o, ak::Enc *e);
static void dec_upload_result_data_message(ak::Dec *d, UploadResultDataMessage *out, uint32_t depth);
static void enc_dual_response(const DualResponse &o, ak::Enc *e);
static void dec_dual_response(ak::Dec *d, DualResponse *out, uint32_t depth);
static void enc_chunk_leaf(const ChunkLeaf &o, ak::Enc *e);
static void dec_chunk_leaf(ak::Dec *d, ChunkLeaf *out, uint32_t depth);
static void enc_chunk_inner(const ChunkInner &o, ak::Enc *e);
static void dec_chunk_inner(ak::Dec *d, ChunkInner *out, uint32_t depth);
static void enc_chunk_element(const ChunkElement &o, ak::Enc *e);
static void dec_chunk_element(ak::Dec *d, ChunkElement *out, uint32_t depth);
static void enc_chunked_response(const ChunkedResponse &o, ak::Enc *e);
static void dec_chunked_response(ak::Dec *d, ChunkedResponse *out, uint32_t depth);
static void enc_chunked_response_wide(const ChunkedResponseWide &o, ak::Enc *e);
static void dec_chunked_response_wide(ak::Dec *d, ChunkedResponseWide *out, uint32_t depth);
static void enc_leaf_element(const LeafElement &o, ak::Enc *e);
static void dec_leaf_element(ak::Dec *d, LeafElement *out, uint32_t depth);
static void enc_leaf_response(const LeafResponse &o, ak::Enc *e);
static void dec_leaf_response(ak::Dec *d, LeafResponse *out, uint32_t depth);
static void enc_surrogate(const Surrogate &o, ak::Enc *e);
static void dec_surrogate(ak::Dec *d, Surrogate *out, uint32_t depth);
static void enc_surrogate_inner(const SurrogateInner &o, ak::Enc *e);
static void dec_surrogate_inner(ak::Dec *d, SurrogateInner *out, uint32_t depth);
static void enc_nest(const Nest &o, ak::Enc *e);
static void dec_nest(ak::Dec *d, Nest *out, uint32_t depth);
static void enc_wire_zoo(const WireZoo &o, ak::Enc *e);
static void dec_wire_zoo(ak::Dec *d, WireZoo *out, uint32_t depth);

static void enc_timestamp(const Timestamp &o, ak::Enc *e) {
  (void)o; (void)e;
  if ((o.seconds) != 0) {
    e->varint_field(1, (uint64_t)(o.seconds));
  }
  if ((o.nanos) != 0) {
    e->varint_field(2, (uint64_t)(int64_t)(o.nanos));
  }
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_duration(const Duration &o, ak::Enc *e) {
  (void)o; (void)e;
  if ((o.seconds) != 0) {
    e->varint_field(1, (uint64_t)(o.seconds));
  }
  if ((o.nanos) != 0) {
    e->varint_field(2, (uint64_t)(int64_t)(o.nanos));
  }
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_result_raw(const ResultRaw &o, ak::Enc *e) {
  (void)o; (void)e;
  if (!o.session_id.empty()) e->blob_field(1, o.session_id);
  if (!o.name.empty()) e->blob_field(2, o.name);
  if (!o.owner_task_id.empty()) e->blob_field(3, o.owner_task_id);
  if ((o.status).v != 0) {
    e->varint_field(4, (uint64_t)(int64_t)((o.status).v));
  }
  if (o.created_at.has_value()) {
    ak::Mark mk = e->begin(5, 0);
    enc_timestamp(*o.created_at, e);
    e->end(mk);
  }
  if (o.completed_at.has_value()) {
    ak::Mark mk = e->begin(6, 1);
    enc_timestamp(*o.completed_at, e);
    e->end(mk);
  }
  if (!o.result_id.empty()) e->blob_field(8, o.result_id);
  if ((o.size) != 0) {
    e->varint_field(9, (uint64_t)(o.size));
  }
  if (!o.created_by.empty()) e->blob_field(10, o.created_by);
  if (!o.opaque_id.empty()) e->blob_field(11, o.opaque_id);
  if (o.manual_deletion) {
    e->varint_field(12, (uint64_t)((o.manual_deletion) ? 1 : 0));
  }
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_task_options(const TaskOptions &o, ak::Enc *e) {
  (void)o; (void)e;
  for (std::map<std::string, std::string>::const_iterator it = o.options.begin();
       it != o.options.end(); ++it) {
    ak::Mark mk = e->begin(1, 2);
    if (!it->first.empty()) e->blob_field(1, it->first);
    if (!it->second.empty()) e->blob_field(2, it->second);
    e->end(mk);
  }
  if (o.max_duration.has_value()) {
    ak::Mark mk = e->begin(2, 3);
    enc_duration(*o.max_duration, e);
    e->end(mk);
  }
  if ((o.max_retries) != 0) {
    e->varint_field(3, (uint64_t)(int64_t)(o.max_retries));
  }
  if ((o.priority) != 0) {
    e->varint_field(4, (uint64_t)(int64_t)(o.priority));
  }
  if (!o.partition_id.empty()) e->blob_field(5, o.partition_id);
  if (!o.application_name.empty()) e->blob_field(6, o.application_name);
  if (!o.application_version.empty()) e->blob_field(7, o.application_version);
  if (!o.application_namespace.empty()) e->blob_field(8, o.application_namespace);
  if (!o.application_service.empty()) e->blob_field(9, o.application_service);
  if (!o.engine_type.empty()) e->blob_field(10, o.engine_type);
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_task_output(const TaskOutput &o, ak::Enc *e) {
  (void)o; (void)e;
  if (o.success) {
    e->varint_field(1, (uint64_t)((o.success) ? 1 : 0));
  }
  if (!o.error.empty()) e->blob_field(2, o.error);
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_task_detailed(const TaskDetailed &o, ak::Enc *e) {
  (void)o; (void)e;
  if (!o.id.empty()) e->blob_field(1, o.id);
  if (!o.session_id.empty()) e->blob_field(2, o.session_id);
  if (!o.owner_pod_id.empty()) e->blob_field(3, o.owner_pod_id);
  for (size_t i = 0; i < o.parent_task_ids.size(); ++i) e->blob_field(4, o.parent_task_ids[i]);
  for (size_t i = 0; i < o.data_dependencies.size(); ++i) e->blob_field(5, o.data_dependencies[i]);
  for (size_t i = 0; i < o.expected_output_ids.size(); ++i) e->blob_field(6, o.expected_output_ids[i]);
  for (size_t i = 0; i < o.retry_of_ids.size(); ++i) e->blob_field(7, o.retry_of_ids[i]);
  if ((o.status).v != 0) {
    e->varint_field(8, (uint64_t)(int64_t)((o.status).v));
  }
  if (!o.status_message.empty()) e->blob_field(9, o.status_message);
  if (o.options.has_value()) {
    ak::Mark mk = e->begin(10, 4);
    enc_task_options(*o.options, e);
    e->end(mk);
  }
  if (o.created_at.has_value()) {
    ak::Mark mk = e->begin(11, 5);
    enc_timestamp(*o.created_at, e);
    e->end(mk);
  }
  if (o.submitted_at.has_value()) {
    ak::Mark mk = e->begin(12, 6);
    enc_timestamp(*o.submitted_at, e);
    e->end(mk);
  }
  if (o.started_at.has_value()) {
    ak::Mark mk = e->begin(13, 7);
    enc_timestamp(*o.started_at, e);
    e->end(mk);
  }
  if (o.ended_at.has_value()) {
    ak::Mark mk = e->begin(14, 8);
    enc_timestamp(*o.ended_at, e);
    e->end(mk);
  }
  if (o.pod_ttl.has_value()) {
    ak::Mark mk = e->begin(15, 9);
    enc_timestamp(*o.pod_ttl, e);
    e->end(mk);
  }
  if (o.output.has_value()) {
    ak::Mark mk = e->begin(16, 10);
    enc_task_output(*o.output, e);
    e->end(mk);
  }
  if (!o.pod_hostname.empty()) e->blob_field(17, o.pod_hostname);
  if (o.received_at.has_value()) {
    ak::Mark mk = e->begin(18, 11);
    enc_timestamp(*o.received_at, e);
    e->end(mk);
  }
  if (o.acquired_at.has_value()) {
    ak::Mark mk = e->begin(19, 12);
    enc_timestamp(*o.acquired_at, e);
    e->end(mk);
  }
  if (o.creation_to_end_duration.has_value()) {
    ak::Mark mk = e->begin(20, 13);
    enc_duration(*o.creation_to_end_duration, e);
    e->end(mk);
  }
  if (o.processing_to_end_duration.has_value()) {
    ak::Mark mk = e->begin(21, 14);
    enc_duration(*o.processing_to_end_duration, e);
    e->end(mk);
  }
  if (!o.initial_task_id.empty()) e->blob_field(22, o.initial_task_id);
  if (o.received_to_end_duration.has_value()) {
    ak::Mark mk = e->begin(23, 15);
    enc_duration(*o.received_to_end_duration, e);
    e->end(mk);
  }
  if (o.processed_at.has_value()) {
    ak::Mark mk = e->begin(24, 16);
    enc_timestamp(*o.processed_at, e);
    e->end(mk);
  }
  if (o.fetched_at.has_value()) {
    ak::Mark mk = e->begin(25, 17);
    enc_timestamp(*o.fetched_at, e);
    e->end(mk);
  }
  if (!o.payload_id.empty()) e->blob_field(26, o.payload_id);
  if (!o.created_by.empty()) e->blob_field(27, o.created_by);
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_task_summary(const TaskSummary &o, ak::Enc *e) {
  (void)o; (void)e;
  if (!o.id.empty()) e->blob_field(1, o.id);
  if (!o.session_id.empty()) e->blob_field(2, o.session_id);
  if (o.options.has_value()) {
    ak::Mark mk = e->begin(3, 18);
    enc_task_options(*o.options, e);
    e->end(mk);
  }
  if ((o.status).v != 0) {
    e->varint_field(4, (uint64_t)(int64_t)((o.status).v));
  }
  if (o.created_at.has_value()) {
    ak::Mark mk = e->begin(5, 19);
    enc_timestamp(*o.created_at, e);
    e->end(mk);
  }
  if (!o.error.empty()) e->blob_field(8, o.error);
  if (!o.status_message.empty()) e->blob_field(9, o.status_message);
  if ((o.count_data_dependencies) != 0) {
    e->varint_field(11, (uint64_t)(o.count_data_dependencies));
  }
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_probe(const Probe &o, ak::Enc *e) {
  (void)o; (void)e;
  if (!o.id.empty()) e->blob_field(1, o.id);
  if (o.opt_count.has_value()) {
    e->varint_field(2, (uint64_t)(int64_t)(*o.opt_count));
  }
  if (o.opt_label.has_value()) e->blob_field(3, *o.opt_label);
  if (o.opt_flag.has_value()) {
    e->varint_field(4, (uint64_t)((*o.opt_flag) ? 1 : 0));
  }
  if (o.body.which() == ProbeBody::kAsInt) {
    // Plan rule: a oneof member is written whatever its value.
    e->varint_field(10, (uint64_t)(o.body.as_int()));
  }
  if (o.body.which() == ProbeBody::kAsText) {
    // Plan rule: a oneof member is written whatever its value.
    e->blob_field(11, o.body.as_text());
  }
  if (o.body.which() == ProbeBody::kAsBlob) {
    // Plan rule: a oneof member is written whatever its value.
    e->blob_field(12, o.body.as_blob());
  }
  if (o.body.which() == ProbeBody::kAsStamp) {
    // Plan rule: a oneof member is written whatever its value.
    ak::Mark mk = e->begin(13, 20);
    enc_timestamp(o.body.as_stamp(), e);
    e->end(mk);
  }
  if (o.body.which() == ProbeBody::kAsNothing) {
    // Plan rule: a oneof member is written whatever its value.
    ak::Mark mk = e->begin(14, 21);
    enc_empty(o.body.as_nothing(), e);
    e->end(mk);
  }
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_empty(const Empty &o, ak::Enc *e) {
  (void)o; (void)e;
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_upload_result_data(const UploadResultData &o, ak::Enc *e) {
  (void)o; (void)e;
  if (!o.session_id.empty()) e->blob_field(1, o.session_id);
  if (!o.result_id.empty()) e->blob_field(2, o.result_id);
  if (!o.data_chunk.empty()) e->blob_field(3, o.data_chunk);
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_metrics_batch(const MetricsBatch &o, ak::Enc *e) {
  (void)o; (void)e;
  if (!o.id.empty()) e->blob_field(1, o.id);
  if (!o.ticks.empty()) {
    ak::Mark mk = e->begin(2, 22);
    for (size_t i = 0; i < o.ticks.size(); ++i) {
      e->varint((uint64_t)(o.ticks[i]));
    }
    e->end(mk);
  }
  if (!o.values.empty()) {
    ak::Mark mk = e->begin(3, 23);
    for (size_t i = 0; i < o.values.size(); ++i) {
      e->f64_raw(o.values[i]);
    }
    e->end(mk);
  }
  if (!o.codes.empty()) {
    ak::Mark mk = e->begin(4, 24);
    for (size_t i = 0; i < o.codes.size(); ++i) {
      e->varint((uint64_t)(int64_t)(o.codes[i]));
    }
    e->end(mk);
  }
  if (!o.flags.empty()) {
    ak::Mark mk = e->begin(5, 25);
    for (size_t i = 0; i < o.flags.size(); ++i) {
      e->varint((uint64_t)((o.flags[i]) ? 1 : 0));
    }
    e->end(mk);
  }
  if (!o.statuses.empty()) {
    ak::Mark mk = e->begin(6, 26);
    for (size_t i = 0; i < o.statuses.size(); ++i) {
      e->varint((uint64_t)(int64_t)((o.statuses[i]).v));
    }
    e->end(mk);
  }
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_pair(const Pair &o, ak::Enc *e) {
  (void)o; (void)e;
  if (!o.key.empty()) e->blob_field(1, o.key);
  if ((o.value) != 0) {
    e->varint_field(2, (uint64_t)(int64_t)(o.value));
  }
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_list_results_response(const ListResultsResponse &o, ak::Enc *e) {
  (void)o; (void)e;
  for (size_t i = 0; i < o.results.size(); ++i) {
    ak::Mark mk = e->begin(1, 27);
    enc_result_raw(o.results[i], e);
    e->end(mk);
  }
  if ((o.page) != 0) {
    e->varint_field(2, (uint64_t)(int64_t)(o.page));
  }
  if ((o.total) != 0) {
    e->varint_field(3, (uint64_t)(int64_t)(o.total));
  }
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_list_tasks_detailed_response(const ListTasksDetailedResponse &o, ak::Enc *e) {
  (void)o; (void)e;
  for (size_t i = 0; i < o.tasks.size(); ++i) {
    ak::Mark mk = e->begin(1, 28);
    enc_task_detailed(o.tasks[i], e);
    e->end(mk);
  }
  if ((o.page) != 0) {
    e->varint_field(2, (uint64_t)(int64_t)(o.page));
  }
  if ((o.total) != 0) {
    e->varint_field(3, (uint64_t)(int64_t)(o.total));
  }
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_list_task_summary_response(const ListTaskSummaryResponse &o, ak::Enc *e) {
  (void)o; (void)e;
  for (size_t i = 0; i < o.tasks.size(); ++i) {
    ak::Mark mk = e->begin(1, 29);
    enc_task_summary(o.tasks[i], e);
    e->end(mk);
  }
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_list_probe_response(const ListProbeResponse &o, ak::Enc *e) {
  (void)o; (void)e;
  for (size_t i = 0; i < o.probes.size(); ++i) {
    ak::Mark mk = e->begin(1, 30);
    enc_probe(o.probes[i], e);
    e->end(mk);
  }
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_list_metrics_response(const ListMetricsResponse &o, ak::Enc *e) {
  (void)o; (void)e;
  for (size_t i = 0; i < o.batches.size(); ++i) {
    ak::Mark mk = e->begin(1, 31);
    enc_metrics_batch(o.batches[i], e);
    e->end(mk);
  }
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_upload_result_data_message(const UploadResultDataMessage &o, ak::Enc *e) {
  (void)o; (void)e;
  if (o.upload.has_value()) {
    ak::Mark mk = e->begin(1, 32);
    enc_upload_result_data(*o.upload, e);
    e->end(mk);
  }
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_dual_response(const DualResponse &o, ak::Enc *e) {
  (void)o; (void)e;
  for (size_t i = 0; i < o.left.size(); ++i) {
    ak::Mark mk = e->begin(1, 33);
    enc_pair(o.left[i], e);
    e->end(mk);
  }
  for (size_t i = 0; i < o.right.size(); ++i) {
    ak::Mark mk = e->begin(2, 34);
    enc_pair(o.right[i], e);
    e->end(mk);
  }
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_chunk_leaf(const ChunkLeaf &o, ak::Enc *e) {
  (void)o; (void)e;
  if (!o.k.empty()) e->blob_field(1, o.k);
  if ((o.v) != 0) {
    e->varint_field(2, (uint64_t)(int64_t)(o.v));
  }
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_chunk_inner(const ChunkInner &o, ak::Enc *e) {
  (void)o; (void)e;
  if (!o.marks.empty()) {
    ak::Mark mk = e->begin(1, 35);
    for (size_t i = 0; i < o.marks.size(); ++i) {
      e->varint((uint64_t)(o.marks[i]));
    }
    e->end(mk);
  }
  for (size_t i = 0; i < o.leaves.size(); ++i) {
    ak::Mark mk = e->begin(2, 36);
    enc_chunk_leaf(o.leaves[i], e);
    e->end(mk);
  }
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_chunk_element(const ChunkElement &o, ak::Enc *e) {
  (void)o; (void)e;
  for (size_t i = 0; i < o.labels.size(); ++i) e->blob_field(1, o.labels[i]);
  for (std::map<std::string, std::string>::const_iterator it = o.attrs.begin();
       it != o.attrs.end(); ++it) {
    ak::Mark mk = e->begin(2, 37);
    if (!it->first.empty()) e->blob_field(1, it->first);
    if (!it->second.empty()) e->blob_field(2, it->second);
    e->end(mk);
  }
  if (!o.id.empty()) e->blob_field(3, o.id);
  if (o.inner.has_value()) {
    ak::Mark mk = e->begin(4, 38);
    enc_chunk_inner(*o.inner, e);
    e->end(mk);
  }
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_chunked_response(const ChunkedResponse &o, ak::Enc *e) {
  (void)o; (void)e;
  for (size_t i = 0; i < o.items.size(); ++i) {
    ak::Mark mk = e->begin(7, 39);
    enc_chunk_element(o.items[i], e);
    e->end(mk);
  }
  if ((o.page) != 0) {
    e->varint_field(8, (uint64_t)(int64_t)(o.page));
  }
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_chunked_response_wide(const ChunkedResponseWide &o, ak::Enc *e) {
  (void)o; (void)e;
  for (size_t i = 0; i < o.items.size(); ++i) {
    ak::Mark mk = e->begin(70000, 40);
    enc_chunk_element(o.items[i], e);
    e->end(mk);
  }
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_leaf_element(const LeafElement &o, ak::Enc *e) {
  (void)o; (void)e;
  if (!o.id.empty()) e->blob_field(1, o.id);
  if ((o.n) != 0) {
    e->varint_field(2, (uint64_t)(o.n));
  }
  if (o.stamp.has_value()) {
    ak::Mark mk = e->begin(3, 41);
    enc_timestamp(*o.stamp, e);
    e->end(mk);
  }
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_leaf_response(const LeafResponse &o, ak::Enc *e) {
  (void)o; (void)e;
  for (size_t i = 0; i < o.items.size(); ++i) {
    ak::Mark mk = e->begin(9, 42);
    enc_leaf_element(o.items[i], e);
    e->end(mk);
  }
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_surrogate(const Surrogate &o, ak::Enc *e) {
  (void)o; (void)e;
  if (!o.text.empty()) e->blob_field(1, o.text);
  if (o.nested.has_value()) {
    ak::Mark mk = e->begin(2, 43);
    enc_surrogate_inner(*o.nested, e);
    e->end(mk);
  }
  for (std::map<std::string, std::string>::const_iterator it = o.attrs.begin();
       it != o.attrs.end(); ++it) {
    ak::Mark mk = e->begin(3, 44);
    if (!it->first.empty()) e->blob_field(1, it->first);
    if (!it->second.empty()) e->blob_field(2, it->second);
    e->end(mk);
  }
  for (size_t i = 0; i < o.texts.size(); ++i) e->blob_field(4, o.texts[i]);
  if (!o.raw.empty()) e->blob_field(5, o.raw);
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_surrogate_inner(const SurrogateInner &o, ak::Enc *e) {
  (void)o; (void)e;
  if (!o.text.empty()) e->blob_field(1, o.text);
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_nest(const Nest &o, ak::Enc *e) {
  (void)o; (void)e;
  if (o.child.has_value()) {
    ak::Mark mk = e->begin(1, 45);
    enc_nest(*o.child, e);
    e->end(mk);
  }
  if (!o.leaf.empty()) e->blob_field(2, o.leaf);
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void enc_wire_zoo(const WireZoo &o, ak::Enc *e) {
  (void)o; (void)e;
  if ((o.v_int32) != 0) {
    e->varint_field(1, (uint64_t)(int64_t)(o.v_int32));
  }
  if ((o.v_int64) != 0) {
    e->varint_field(2, (uint64_t)(o.v_int64));
  }
  if (o.v_bool) {
    e->varint_field(3, (uint64_t)((o.v_bool) ? 1 : 0));
  }
  if (ak::f64_bits(o.v_double) != 0) {
    e->f64_field(4, o.v_double);
  }
  if ((o.v_fixed32) != 0) {
    e->fixed32_field(5, o.v_fixed32);
  }
  if (!o.v_string.empty()) e->blob_field(6, o.v_string);
  if (!o.v_bytes.empty()) e->blob_field(7, o.v_bytes);
  if ((o.v_enum).v != 0) {
    e->varint_field(8, (uint64_t)(int64_t)((o.v_enum).v));
  }
  if (o.v_msg.has_value()) {
    ak::Mark mk = e->begin(9, 46);
    enc_timestamp(*o.v_msg, e);
    e->end(mk);
  }
  if ((o.v_big_tag) != 0) {
    e->varint_field(536870911, (uint64_t)(int64_t)(o.v_big_tag));
  }
  // Plan rule (retain): the captured unknown runs, verbatim, AFTER
  // every known field.
  if (!o.unknown_fields.empty())
    e->raw((const uint8_t *)o.unknown_fields.data(), o.unknown_fields.size());
}

static void dec_timestamp(ak::Dec *d, Timestamp *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 8ull: {  // field 1, wire type 0: set_scalar
        out->seconds = (int64_t)d->varint();
        break;
      }
      case 16ull: {  // field 2, wire type 0: set_scalar
        out->nanos = (int32_t)d->varint();
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_duration(ak::Dec *d, Duration *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 8ull: {  // field 1, wire type 0: set_scalar
        out->seconds = (int64_t)d->varint();
        break;
      }
      case 16ull: {  // field 2, wire type 0: set_scalar
        out->nanos = (int32_t)d->varint();
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_result_raw(ak::Dec *d, ResultRaw *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 10ull: {  // field 1, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->session_id);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 18ull: {  // field 2, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->name);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 26ull: {  // field 3, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->owner_task_id);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 32ull: {  // field 4, wire type 0: set_scalar
        out->status = ResultStatus((int32_t)d->varint());
        break;
      }
      case 42ull: {  // field 5, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_timestamp(&sub, &out->created_at.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 50ull: {  // field 6, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_timestamp(&sub, &out->completed_at.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 66ull: {  // field 8, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->result_id);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 72ull: {  // field 9, wire type 0: set_scalar
        out->size = (int64_t)d->varint();
        break;
      }
      case 82ull: {  // field 10, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->created_by);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 90ull: {  // field 11, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        out->opaque_id.assign((const char *)(d->buf + off), n);
        break;
      }
      case 96ull: {  // field 12, wire type 0: set_scalar
        out->manual_deletion = (d->varint() != 0);
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_task_options(ak::Dec *d, TaskOptions *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 10ull: {  // field 1, wire type 2: append_message
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        if (depth + 1 > kLimit) { d->err = ak::ERR_DEPTH; return; }
        ak::Dec sub(d->buf + off, n);
        std::string k_, v_;
        while (!sub.at_end()) {
          uint64_t kk = sub.varint();
          if (sub.err != 0) break;
          uint32_t et = (uint32_t)(kk >> 3), ew = (uint32_t)(kk & 7);
          if (et == 0 || (kk >> 3) > 536870911uLL) { sub.err = ak::ERR_MALFORMED; break; }
          switch (((uint64_t)et << 3) | ew) {
            case 10ull: {
              size_t a, b; sub.len_body(&a, &b);
              if (sub.err != 0) break;
              { int32_t rc = ak::decode_str_checked(sub.buf + a, b, &k_);
                if (rc != 0) { d->err = rc; return; } }
              break;
            }
            case 18ull: {
              size_t a, b; sub.len_body(&a, &b);
              if (sub.err != 0) break;
              { int32_t rc = ak::decode_str_checked(sub.buf + a, b, &v_);
                if (rc != 0) { d->err = rc; return; } }
              break;
            }
            // A facade map entry has no bag: an unknown field inside an
            // entry is skipped in both modes (`U-map-entry`).
            default: sub.skip(et, ew); break;
          }
        }
        if (sub.err != 0) { d->err = sub.err; return; }
        // Plan rule: a duplicate key replaces the earlier value.
#if AK_CXX17
        out->options.insert_or_assign(std::move(k_), std::move(v_));
#else
        out->options[k_] = v_;
#endif
        break;
      }
      case 18ull: {  // field 2, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_duration(&sub, &out->max_duration.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 24ull: {  // field 3, wire type 0: set_scalar
        out->max_retries = (int32_t)d->varint();
        break;
      }
      case 32ull: {  // field 4, wire type 0: set_scalar
        out->priority = (int32_t)d->varint();
        break;
      }
      case 42ull: {  // field 5, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->partition_id);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 50ull: {  // field 6, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->application_name);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 58ull: {  // field 7, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->application_version);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 66ull: {  // field 8, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->application_namespace);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 74ull: {  // field 9, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->application_service);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 82ull: {  // field 10, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->engine_type);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_task_output(ak::Dec *d, TaskOutput *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 8ull: {  // field 1, wire type 0: set_scalar
        out->success = (d->varint() != 0);
        break;
      }
      case 18ull: {  // field 2, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->error);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_task_detailed(ak::Dec *d, TaskDetailed *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 10ull: {  // field 1, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->id);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 18ull: {  // field 2, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->session_id);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 26ull: {  // field 3, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->owner_pod_id);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 34ull: {  // field 4, wire type 2: append_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
#if AK_CXX17
        std::string &b_ = out->parent_task_ids.emplace_back();
#else
        out->parent_task_ids.push_back(std::string());
        std::string &b_ = out->parent_task_ids.back();
#endif
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &b_);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 42ull: {  // field 5, wire type 2: append_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
#if AK_CXX17
        std::string &b_ = out->data_dependencies.emplace_back();
#else
        out->data_dependencies.push_back(std::string());
        std::string &b_ = out->data_dependencies.back();
#endif
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &b_);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 50ull: {  // field 6, wire type 2: append_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
#if AK_CXX17
        std::string &b_ = out->expected_output_ids.emplace_back();
#else
        out->expected_output_ids.push_back(std::string());
        std::string &b_ = out->expected_output_ids.back();
#endif
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &b_);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 58ull: {  // field 7, wire type 2: append_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
#if AK_CXX17
        std::string &b_ = out->retry_of_ids.emplace_back();
#else
        out->retry_of_ids.push_back(std::string());
        std::string &b_ = out->retry_of_ids.back();
#endif
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &b_);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 64ull: {  // field 8, wire type 0: set_scalar
        out->status = TaskStatus((int32_t)d->varint());
        break;
      }
      case 74ull: {  // field 9, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->status_message);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 82ull: {  // field 10, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_task_options(&sub, &out->options.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 90ull: {  // field 11, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_timestamp(&sub, &out->created_at.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 98ull: {  // field 12, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_timestamp(&sub, &out->submitted_at.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 106ull: {  // field 13, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_timestamp(&sub, &out->started_at.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 114ull: {  // field 14, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_timestamp(&sub, &out->ended_at.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 122ull: {  // field 15, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_timestamp(&sub, &out->pod_ttl.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 130ull: {  // field 16, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_task_output(&sub, &out->output.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 138ull: {  // field 17, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->pod_hostname);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 146ull: {  // field 18, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_timestamp(&sub, &out->received_at.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 154ull: {  // field 19, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_timestamp(&sub, &out->acquired_at.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 162ull: {  // field 20, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_duration(&sub, &out->creation_to_end_duration.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 170ull: {  // field 21, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_duration(&sub, &out->processing_to_end_duration.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 178ull: {  // field 22, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->initial_task_id);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 186ull: {  // field 23, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_duration(&sub, &out->received_to_end_duration.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 194ull: {  // field 24, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_timestamp(&sub, &out->processed_at.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 202ull: {  // field 25, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_timestamp(&sub, &out->fetched_at.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 210ull: {  // field 26, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->payload_id);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 218ull: {  // field 27, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->created_by);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_task_summary(ak::Dec *d, TaskSummary *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 10ull: {  // field 1, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->id);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 18ull: {  // field 2, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->session_id);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 26ull: {  // field 3, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_task_options(&sub, &out->options.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 32ull: {  // field 4, wire type 0: set_scalar
        out->status = TaskStatus((int32_t)d->varint());
        break;
      }
      case 42ull: {  // field 5, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_timestamp(&sub, &out->created_at.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 66ull: {  // field 8, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->error);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 74ull: {  // field 9, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->status_message);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 88ull: {  // field 11, wire type 0: set_scalar
        out->count_data_dependencies = (int64_t)d->varint();
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_probe(ak::Dec *d, Probe *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 10ull: {  // field 1, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->id);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 16ull: {  // field 2, wire type 0: set_scalar
        out->opt_count.set((int32_t)d->varint());
        break;
      }
      case 26ull: {  // field 3, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        std::string &s_ = out->opt_label.emplace();
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &s_);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 32ull: {  // field 4, wire type 0: set_scalar
        out->opt_flag.set((d->varint() != 0));
        break;
      }
      case 80ull: {  // field 10, wire type 0: oneof_set
        int64_t v_ = (int64_t)d->varint();
        if (d->err != 0) return;
        out->body.set_as_int() = v_;
        break;
      }
      case 90ull: {  // field 11, wire type 2: oneof_set
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        std::string &s_ = out->body.set_as_text();
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &s_);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 98ull: {  // field 12, wire type 2: oneof_set
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        std::string &s_ = out->body.set_as_blob();
        s_.assign((const char *)(d->buf + off), n);
        break;
      }
      case 106ull: {  // field 13, wire type 2: oneof_set
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: the SAME member merges; another one starts empty (R-E4).
        Timestamp &c_ = out->body.which() == ProbeBody::kAsStamp ? out->body.mutable_as_stamp() : out->body.set_as_stamp();
        dec_timestamp(&sub, &c_, depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 114ull: {  // field 14, wire type 2: oneof_set
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: the SAME member merges; another one starts empty (R-E4).
        Empty &c_ = out->body.which() == ProbeBody::kAsNothing ? out->body.mutable_as_nothing() : out->body.set_as_nothing();
        dec_empty(&sub, &c_, depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_empty(ak::Dec *d, Empty *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_upload_result_data(ak::Dec *d, UploadResultData *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 10ull: {  // field 1, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->session_id);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 18ull: {  // field 2, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->result_id);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 26ull: {  // field 3, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        out->data_chunk.assign((const char *)(d->buf + off), n);
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_metrics_batch(ak::Dec *d, MetricsBatch *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 10ull: {  // field 1, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->id);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 16ull: {  // field 2, wire type 0: packed_one
        // The unpacked form, at the kind's own wire type only (R-E2).
        int64_t v_ = (int64_t)d->varint();
        if (d->err != 0) return;
        out->ticks.push_back(v_);
        break;
      }
      case 18ull: {  // field 2, wire type 2: packed_run
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        out->ticks.reserve(out->ticks.size() + n);  // n bytes of varints: at most n elements
        while (!sub.at_end()) {
          int64_t v_ = (int64_t)(&sub)->varint();
          if (sub.err != 0) break;
          out->ticks.push_back(v_);
        }
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 25ull: {  // field 3, wire type 1: packed_one
        // The unpacked form, at the kind's own wire type only (R-E2).
        double v_ = d->f64();
        if (d->err != 0) return;
        out->values.push_back(v_);
        break;
      }
      case 26ull: {  // field 3, wire type 2: packed_run
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        out->values.reserve(out->values.size() + n / 8);
        while (!sub.at_end()) {
          double v_ = (&sub)->f64();
          if (sub.err != 0) break;
          out->values.push_back(v_);
        }
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 32ull: {  // field 4, wire type 0: packed_one
        // The unpacked form, at the kind's own wire type only (R-E2).
        int32_t v_ = (int32_t)d->varint();
        if (d->err != 0) return;
        out->codes.push_back(v_);
        break;
      }
      case 34ull: {  // field 4, wire type 2: packed_run
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        out->codes.reserve(out->codes.size() + n);  // n bytes of varints: at most n elements
        while (!sub.at_end()) {
          int32_t v_ = (int32_t)(&sub)->varint();
          if (sub.err != 0) break;
          out->codes.push_back(v_);
        }
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 40ull: {  // field 5, wire type 0: packed_one
        // The unpacked form, at the kind's own wire type only (R-E2).
        bool v_ = (d->varint() != 0);
        if (d->err != 0) return;
        out->flags.push_back(v_);
        break;
      }
      case 42ull: {  // field 5, wire type 2: packed_run
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        out->flags.reserve(out->flags.size() + n);  // n bytes of varints: at most n elements
        while (!sub.at_end()) {
          bool v_ = ((&sub)->varint() != 0);
          if (sub.err != 0) break;
          out->flags.push_back(v_);
        }
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 48ull: {  // field 6, wire type 0: packed_one
        // The unpacked form, at the kind's own wire type only (R-E2).
        TaskStatus v_ = TaskStatus((int32_t)d->varint());
        if (d->err != 0) return;
        out->statuses.push_back(v_);
        break;
      }
      case 50ull: {  // field 6, wire type 2: packed_run
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        out->statuses.reserve(out->statuses.size() + n);  // n bytes of varints: at most n elements
        while (!sub.at_end()) {
          TaskStatus v_ = TaskStatus((int32_t)(&sub)->varint());
          if (sub.err != 0) break;
          out->statuses.push_back(v_);
        }
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_pair(ak::Dec *d, Pair *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 10ull: {  // field 1, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->key);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 16ull: {  // field 2, wire type 0: set_scalar
        out->value = (int32_t)d->varint();
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_list_results_response(ak::Dec *d, ListResultsResponse *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 10ull: {  // field 1, wire type 2: append_message
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        out->results.push_back(ResultRaw());
        dec_result_raw(&sub, &out->results.back(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 16ull: {  // field 2, wire type 0: set_scalar
        out->page = (int32_t)d->varint();
        break;
      }
      case 24ull: {  // field 3, wire type 0: set_scalar
        out->total = (int32_t)d->varint();
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_list_tasks_detailed_response(ak::Dec *d, ListTasksDetailedResponse *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 10ull: {  // field 1, wire type 2: append_message
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        out->tasks.push_back(TaskDetailed());
        dec_task_detailed(&sub, &out->tasks.back(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 16ull: {  // field 2, wire type 0: set_scalar
        out->page = (int32_t)d->varint();
        break;
      }
      case 24ull: {  // field 3, wire type 0: set_scalar
        out->total = (int32_t)d->varint();
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_list_task_summary_response(ak::Dec *d, ListTaskSummaryResponse *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 10ull: {  // field 1, wire type 2: append_message
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        out->tasks.push_back(TaskSummary());
        dec_task_summary(&sub, &out->tasks.back(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_list_probe_response(ak::Dec *d, ListProbeResponse *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 10ull: {  // field 1, wire type 2: append_message
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        out->probes.push_back(Probe());
        dec_probe(&sub, &out->probes.back(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_list_metrics_response(ak::Dec *d, ListMetricsResponse *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 10ull: {  // field 1, wire type 2: append_message
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        out->batches.push_back(MetricsBatch());
        dec_metrics_batch(&sub, &out->batches.back(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_upload_result_data_message(ak::Dec *d, UploadResultDataMessage *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 10ull: {  // field 1, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_upload_result_data(&sub, &out->upload.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_dual_response(ak::Dec *d, DualResponse *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 10ull: {  // field 1, wire type 2: append_message
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        out->left.push_back(Pair());
        dec_pair(&sub, &out->left.back(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 18ull: {  // field 2, wire type 2: append_message
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        out->right.push_back(Pair());
        dec_pair(&sub, &out->right.back(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_chunk_leaf(ak::Dec *d, ChunkLeaf *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 10ull: {  // field 1, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->k);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 16ull: {  // field 2, wire type 0: set_scalar
        out->v = (int32_t)d->varint();
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_chunk_inner(ak::Dec *d, ChunkInner *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 8ull: {  // field 1, wire type 0: packed_one
        // The unpacked form, at the kind's own wire type only (R-E2).
        int64_t v_ = (int64_t)d->varint();
        if (d->err != 0) return;
        out->marks.push_back(v_);
        break;
      }
      case 10ull: {  // field 1, wire type 2: packed_run
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        out->marks.reserve(out->marks.size() + n);  // n bytes of varints: at most n elements
        while (!sub.at_end()) {
          int64_t v_ = (int64_t)(&sub)->varint();
          if (sub.err != 0) break;
          out->marks.push_back(v_);
        }
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 18ull: {  // field 2, wire type 2: append_message
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        out->leaves.push_back(ChunkLeaf());
        dec_chunk_leaf(&sub, &out->leaves.back(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_chunk_element(ak::Dec *d, ChunkElement *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 10ull: {  // field 1, wire type 2: append_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
#if AK_CXX17
        std::string &b_ = out->labels.emplace_back();
#else
        out->labels.push_back(std::string());
        std::string &b_ = out->labels.back();
#endif
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &b_);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 18ull: {  // field 2, wire type 2: append_message
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        if (depth + 1 > kLimit) { d->err = ak::ERR_DEPTH; return; }
        ak::Dec sub(d->buf + off, n);
        std::string k_, v_;
        while (!sub.at_end()) {
          uint64_t kk = sub.varint();
          if (sub.err != 0) break;
          uint32_t et = (uint32_t)(kk >> 3), ew = (uint32_t)(kk & 7);
          if (et == 0 || (kk >> 3) > 536870911uLL) { sub.err = ak::ERR_MALFORMED; break; }
          switch (((uint64_t)et << 3) | ew) {
            case 10ull: {
              size_t a, b; sub.len_body(&a, &b);
              if (sub.err != 0) break;
              { int32_t rc = ak::decode_str_checked(sub.buf + a, b, &k_);
                if (rc != 0) { d->err = rc; return; } }
              break;
            }
            case 18ull: {
              size_t a, b; sub.len_body(&a, &b);
              if (sub.err != 0) break;
              { int32_t rc = ak::decode_str_checked(sub.buf + a, b, &v_);
                if (rc != 0) { d->err = rc; return; } }
              break;
            }
            // A facade map entry has no bag: an unknown field inside an
            // entry is skipped in both modes (`U-map-entry`).
            default: sub.skip(et, ew); break;
          }
        }
        if (sub.err != 0) { d->err = sub.err; return; }
        // Plan rule: a duplicate key replaces the earlier value.
#if AK_CXX17
        out->attrs.insert_or_assign(std::move(k_), std::move(v_));
#else
        out->attrs[k_] = v_;
#endif
        break;
      }
      case 26ull: {  // field 3, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->id);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 34ull: {  // field 4, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_chunk_inner(&sub, &out->inner.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_chunked_response(ak::Dec *d, ChunkedResponse *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 58ull: {  // field 7, wire type 2: append_message
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        out->items.push_back(ChunkElement());
        dec_chunk_element(&sub, &out->items.back(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 64ull: {  // field 8, wire type 0: set_scalar
        out->page = (int32_t)d->varint();
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_chunked_response_wide(ak::Dec *d, ChunkedResponseWide *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 560002ull: {  // field 70000, wire type 2: append_message
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        out->items.push_back(ChunkElement());
        dec_chunk_element(&sub, &out->items.back(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_leaf_element(ak::Dec *d, LeafElement *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 10ull: {  // field 1, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->id);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 16ull: {  // field 2, wire type 0: set_scalar
        out->n = (int64_t)d->varint();
        break;
      }
      case 26ull: {  // field 3, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_timestamp(&sub, &out->stamp.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_leaf_response(ak::Dec *d, LeafResponse *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 74ull: {  // field 9, wire type 2: append_message
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        out->items.push_back(LeafElement());
        dec_leaf_element(&sub, &out->items.back(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_surrogate(ak::Dec *d, Surrogate *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 10ull: {  // field 1, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->text);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 18ull: {  // field 2, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_surrogate_inner(&sub, &out->nested.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 26ull: {  // field 3, wire type 2: append_message
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        if (depth + 1 > kLimit) { d->err = ak::ERR_DEPTH; return; }
        ak::Dec sub(d->buf + off, n);
        std::string k_, v_;
        while (!sub.at_end()) {
          uint64_t kk = sub.varint();
          if (sub.err != 0) break;
          uint32_t et = (uint32_t)(kk >> 3), ew = (uint32_t)(kk & 7);
          if (et == 0 || (kk >> 3) > 536870911uLL) { sub.err = ak::ERR_MALFORMED; break; }
          switch (((uint64_t)et << 3) | ew) {
            case 10ull: {
              size_t a, b; sub.len_body(&a, &b);
              if (sub.err != 0) break;
              { int32_t rc = ak::decode_str_checked(sub.buf + a, b, &k_);
                if (rc != 0) { d->err = rc; return; } }
              break;
            }
            case 18ull: {
              size_t a, b; sub.len_body(&a, &b);
              if (sub.err != 0) break;
              { int32_t rc = ak::decode_str_checked(sub.buf + a, b, &v_);
                if (rc != 0) { d->err = rc; return; } }
              break;
            }
            // A facade map entry has no bag: an unknown field inside an
            // entry is skipped in both modes (`U-map-entry`).
            default: sub.skip(et, ew); break;
          }
        }
        if (sub.err != 0) { d->err = sub.err; return; }
        // Plan rule: a duplicate key replaces the earlier value.
#if AK_CXX17
        out->attrs.insert_or_assign(std::move(k_), std::move(v_));
#else
        out->attrs[k_] = v_;
#endif
        break;
      }
      case 34ull: {  // field 4, wire type 2: append_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
#if AK_CXX17
        std::string &b_ = out->texts.emplace_back();
#else
        out->texts.push_back(std::string());
        std::string &b_ = out->texts.back();
#endif
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &b_);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 42ull: {  // field 5, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        out->raw.assign((const char *)(d->buf + off), n);
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_surrogate_inner(ak::Dec *d, SurrogateInner *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 10ull: {  // field 1, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->text);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_nest(ak::Dec *d, Nest *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 10ull: {  // field 1, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_nest(&sub, &out->child.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 18ull: {  // field 2, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->leaf);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

static void dec_wire_zoo(ak::Dec *d, WireZoo *out, uint32_t depth) {
  (void)out;
  // Plan rule: a message more than kLimit levels below the root is refused.
  if (depth > kLimit) { d->err = ak::ERR_DEPTH; return; }
  while (!d->at_end()) {
    size_t s0 = d->pos;
    (void)s0;
    uint64_t k = d->varint();
    if (d->err != 0) return;
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0 || (k >> 3) > 536870911uLL) { d->err = ak::ERR_MALFORMED; return; }
    switch (((uint64_t)tag << 3) | wire) {
      case 8ull: {  // field 1, wire type 0: set_scalar
        out->v_int32 = (int32_t)d->varint();
        break;
      }
      case 16ull: {  // field 2, wire type 0: set_scalar
        out->v_int64 = (int64_t)d->varint();
        break;
      }
      case 24ull: {  // field 3, wire type 0: set_scalar
        out->v_bool = (d->varint() != 0);
        break;
      }
      case 33ull: {  // field 4, wire type 1: set_scalar
        out->v_double = d->f64();
        break;
      }
      case 45ull: {  // field 5, wire type 5: set_scalar
        out->v_fixed32 = d->fixed32();
        break;
      }
      case 50ull: {  // field 6, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        { int32_t rc = ak::decode_str_checked(d->buf + off, n, &out->v_string);
          if (rc != 0) { d->err = rc; return; } }
        break;
      }
      case 58ull: {  // field 7, wire type 2: set_blob
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        out->v_bytes.assign((const char *)(d->buf + off), n);
        break;
      }
      case 64ull: {  // field 8, wire type 0: set_scalar
        out->v_enum = ResultStatus((int32_t)d->varint());
        break;
      }
      case 74ull: {  // field 9, wire type 2: merge_child
        size_t off, n; d->len_body(&off, &n);
        if (d->err != 0) return;
        ak::Dec sub(d->buf + off, n);
        // Plan rule: a repeated occurrence MERGES into the one decoded (R-E4).
        dec_timestamp(&sub, &out->v_msg.get_or_insert(), depth + 1);
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      }
      case 4294967288ull: {  // field 536870911, wire type 0: set_scalar
        out->v_big_tag = (int32_t)d->varint();
        break;
      }
      // Plan rule (retain): an unknown field is captured verbatim, key
      // included, and re-emitted after the known fields on encode.
      default:
        d->skip(tag, wire);
        if (d->err == 0) out->unknown_fields.append((const char *)(d->buf + s0), d->pos - s0);
        break;
    }
  }
}

void encode_into_timestamp(const Timestamp &o, ak::Enc *e) {
  e->reset();
  enc_timestamp(o, e);
}

int32_t decode_timestamp(const uint8_t *b, size_t n, Timestamp *out) {
  ak::Dec d(b, n);
  dec_timestamp(&d, out, 0);
  return d.err;
}

void encode_into_duration(const Duration &o, ak::Enc *e) {
  e->reset();
  enc_duration(o, e);
}

int32_t decode_duration(const uint8_t *b, size_t n, Duration *out) {
  ak::Dec d(b, n);
  dec_duration(&d, out, 0);
  return d.err;
}

void encode_into_result_raw(const ResultRaw &o, ak::Enc *e) {
  e->reset();
  enc_result_raw(o, e);
}

int32_t decode_result_raw(const uint8_t *b, size_t n, ResultRaw *out) {
  ak::Dec d(b, n);
  dec_result_raw(&d, out, 0);
  return d.err;
}

void encode_into_task_options(const TaskOptions &o, ak::Enc *e) {
  e->reset();
  enc_task_options(o, e);
}

int32_t decode_task_options(const uint8_t *b, size_t n, TaskOptions *out) {
  ak::Dec d(b, n);
  dec_task_options(&d, out, 0);
  return d.err;
}

void encode_into_task_output(const TaskOutput &o, ak::Enc *e) {
  e->reset();
  enc_task_output(o, e);
}

int32_t decode_task_output(const uint8_t *b, size_t n, TaskOutput *out) {
  ak::Dec d(b, n);
  dec_task_output(&d, out, 0);
  return d.err;
}

void encode_into_task_detailed(const TaskDetailed &o, ak::Enc *e) {
  e->reset();
  enc_task_detailed(o, e);
}

int32_t decode_task_detailed(const uint8_t *b, size_t n, TaskDetailed *out) {
  ak::Dec d(b, n);
  dec_task_detailed(&d, out, 0);
  return d.err;
}

void encode_into_task_summary(const TaskSummary &o, ak::Enc *e) {
  e->reset();
  enc_task_summary(o, e);
}

int32_t decode_task_summary(const uint8_t *b, size_t n, TaskSummary *out) {
  ak::Dec d(b, n);
  dec_task_summary(&d, out, 0);
  return d.err;
}

void encode_into_probe(const Probe &o, ak::Enc *e) {
  e->reset();
  enc_probe(o, e);
}

int32_t decode_probe(const uint8_t *b, size_t n, Probe *out) {
  ak::Dec d(b, n);
  dec_probe(&d, out, 0);
  return d.err;
}

void encode_into_empty(const Empty &o, ak::Enc *e) {
  e->reset();
  enc_empty(o, e);
}

int32_t decode_empty(const uint8_t *b, size_t n, Empty *out) {
  ak::Dec d(b, n);
  dec_empty(&d, out, 0);
  return d.err;
}

void encode_into_upload_result_data(const UploadResultData &o, ak::Enc *e) {
  e->reset();
  enc_upload_result_data(o, e);
}

int32_t decode_upload_result_data(const uint8_t *b, size_t n, UploadResultData *out) {
  ak::Dec d(b, n);
  dec_upload_result_data(&d, out, 0);
  return d.err;
}

void encode_into_metrics_batch(const MetricsBatch &o, ak::Enc *e) {
  e->reset();
  enc_metrics_batch(o, e);
}

int32_t decode_metrics_batch(const uint8_t *b, size_t n, MetricsBatch *out) {
  ak::Dec d(b, n);
  dec_metrics_batch(&d, out, 0);
  return d.err;
}

void encode_into_pair(const Pair &o, ak::Enc *e) {
  e->reset();
  enc_pair(o, e);
}

int32_t decode_pair(const uint8_t *b, size_t n, Pair *out) {
  ak::Dec d(b, n);
  dec_pair(&d, out, 0);
  return d.err;
}

void encode_into_list_results_response(const ListResultsResponse &o, ak::Enc *e) {
  e->reset();
  enc_list_results_response(o, e);
}

int32_t decode_list_results_response(const uint8_t *b, size_t n, ListResultsResponse *out) {
  ak::Dec d(b, n);
  dec_list_results_response(&d, out, 0);
  return d.err;
}

void encode_into_list_tasks_detailed_response(const ListTasksDetailedResponse &o, ak::Enc *e) {
  e->reset();
  enc_list_tasks_detailed_response(o, e);
}

int32_t decode_list_tasks_detailed_response(const uint8_t *b, size_t n, ListTasksDetailedResponse *out) {
  ak::Dec d(b, n);
  dec_list_tasks_detailed_response(&d, out, 0);
  return d.err;
}

void encode_into_list_task_summary_response(const ListTaskSummaryResponse &o, ak::Enc *e) {
  e->reset();
  enc_list_task_summary_response(o, e);
}

int32_t decode_list_task_summary_response(const uint8_t *b, size_t n, ListTaskSummaryResponse *out) {
  ak::Dec d(b, n);
  dec_list_task_summary_response(&d, out, 0);
  return d.err;
}

void encode_into_list_probe_response(const ListProbeResponse &o, ak::Enc *e) {
  e->reset();
  enc_list_probe_response(o, e);
}

int32_t decode_list_probe_response(const uint8_t *b, size_t n, ListProbeResponse *out) {
  ak::Dec d(b, n);
  dec_list_probe_response(&d, out, 0);
  return d.err;
}

void encode_into_list_metrics_response(const ListMetricsResponse &o, ak::Enc *e) {
  e->reset();
  enc_list_metrics_response(o, e);
}

int32_t decode_list_metrics_response(const uint8_t *b, size_t n, ListMetricsResponse *out) {
  ak::Dec d(b, n);
  dec_list_metrics_response(&d, out, 0);
  return d.err;
}

void encode_into_upload_result_data_message(const UploadResultDataMessage &o, ak::Enc *e) {
  e->reset();
  enc_upload_result_data_message(o, e);
}

int32_t decode_upload_result_data_message(const uint8_t *b, size_t n, UploadResultDataMessage *out) {
  ak::Dec d(b, n);
  dec_upload_result_data_message(&d, out, 0);
  return d.err;
}

void encode_into_dual_response(const DualResponse &o, ak::Enc *e) {
  e->reset();
  enc_dual_response(o, e);
}

int32_t decode_dual_response(const uint8_t *b, size_t n, DualResponse *out) {
  ak::Dec d(b, n);
  dec_dual_response(&d, out, 0);
  return d.err;
}

void encode_into_chunk_leaf(const ChunkLeaf &o, ak::Enc *e) {
  e->reset();
  enc_chunk_leaf(o, e);
}

int32_t decode_chunk_leaf(const uint8_t *b, size_t n, ChunkLeaf *out) {
  ak::Dec d(b, n);
  dec_chunk_leaf(&d, out, 0);
  return d.err;
}

void encode_into_chunk_inner(const ChunkInner &o, ak::Enc *e) {
  e->reset();
  enc_chunk_inner(o, e);
}

int32_t decode_chunk_inner(const uint8_t *b, size_t n, ChunkInner *out) {
  ak::Dec d(b, n);
  dec_chunk_inner(&d, out, 0);
  return d.err;
}

void encode_into_chunk_element(const ChunkElement &o, ak::Enc *e) {
  e->reset();
  enc_chunk_element(o, e);
}

int32_t decode_chunk_element(const uint8_t *b, size_t n, ChunkElement *out) {
  ak::Dec d(b, n);
  dec_chunk_element(&d, out, 0);
  return d.err;
}

void encode_into_chunked_response(const ChunkedResponse &o, ak::Enc *e) {
  e->reset();
  enc_chunked_response(o, e);
}

int32_t decode_chunked_response(const uint8_t *b, size_t n, ChunkedResponse *out) {
  ak::Dec d(b, n);
  dec_chunked_response(&d, out, 0);
  return d.err;
}

void encode_into_chunked_response_wide(const ChunkedResponseWide &o, ak::Enc *e) {
  e->reset();
  enc_chunked_response_wide(o, e);
}

int32_t decode_chunked_response_wide(const uint8_t *b, size_t n, ChunkedResponseWide *out) {
  ak::Dec d(b, n);
  dec_chunked_response_wide(&d, out, 0);
  return d.err;
}

void encode_into_leaf_element(const LeafElement &o, ak::Enc *e) {
  e->reset();
  enc_leaf_element(o, e);
}

int32_t decode_leaf_element(const uint8_t *b, size_t n, LeafElement *out) {
  ak::Dec d(b, n);
  dec_leaf_element(&d, out, 0);
  return d.err;
}

void encode_into_leaf_response(const LeafResponse &o, ak::Enc *e) {
  e->reset();
  enc_leaf_response(o, e);
}

int32_t decode_leaf_response(const uint8_t *b, size_t n, LeafResponse *out) {
  ak::Dec d(b, n);
  dec_leaf_response(&d, out, 0);
  return d.err;
}

void encode_into_surrogate(const Surrogate &o, ak::Enc *e) {
  e->reset();
  enc_surrogate(o, e);
}

int32_t decode_surrogate(const uint8_t *b, size_t n, Surrogate *out) {
  ak::Dec d(b, n);
  dec_surrogate(&d, out, 0);
  return d.err;
}

void encode_into_surrogate_inner(const SurrogateInner &o, ak::Enc *e) {
  e->reset();
  enc_surrogate_inner(o, e);
}

int32_t decode_surrogate_inner(const uint8_t *b, size_t n, SurrogateInner *out) {
  ak::Dec d(b, n);
  dec_surrogate_inner(&d, out, 0);
  return d.err;
}

void encode_into_nest(const Nest &o, ak::Enc *e) {
  e->reset();
  enc_nest(o, e);
}

int32_t decode_nest(const uint8_t *b, size_t n, Nest *out) {
  ak::Dec d(b, n);
  dec_nest(&d, out, 0);
  return d.err;
}

void encode_into_wire_zoo(const WireZoo &o, ak::Enc *e) {
  e->reset();
  enc_wire_zoo(o, e);
}

int32_t decode_wire_zoo(const uint8_t *b, size_t n, WireZoo *out) {
  ak::Dec d(b, n);
  dec_wire_zoo(&d, out, 0);
  return d.err;
}

const size_t kSites = 47;
const char *const kUnknownFields = "retain";
const char *const kUtf8Policy = "reject";
const uint32_t kRecursionLimit = kLimit;

}  // namespace native_retain
}  // namespace corpus
