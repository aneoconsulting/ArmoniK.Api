// @generated by ffi/poc/cpp/gen/generate.py from ffi/schema/shapes.json. Do not edit.

// ffi/corpus/CONTRACT.md obligation C2: what a reader must SEE, which byte identity does
// not imply. The presence rules of section 3 are the whole content of this file; the JSON
// spellings are in src/projjson.h.
#include "generated/project.h"

#include <map>
#include <string>
#include <vector>

#include "ak/projjson.h"
#include "ak/rt.h"

namespace corpus {
namespace project {

static void proj_timestamp(const Timestamp &o, std::string *out);
static void proj_duration(const Duration &o, std::string *out);
static void proj_result_raw(const ResultRaw &o, std::string *out);
static void proj_task_options(const TaskOptions &o, std::string *out);
static void proj_task_output(const TaskOutput &o, std::string *out);
static void proj_task_detailed(const TaskDetailed &o, std::string *out);
static void proj_task_summary(const TaskSummary &o, std::string *out);
static void proj_probe(const Probe &o, std::string *out);
static void proj_empty(const Empty &o, std::string *out);
static void proj_upload_result_data(const UploadResultData &o, std::string *out);
static void proj_metrics_batch(const MetricsBatch &o, std::string *out);
static void proj_pair(const Pair &o, std::string *out);
static void proj_list_results_response(const ListResultsResponse &o, std::string *out);
static void proj_list_tasks_detailed_response(const ListTasksDetailedResponse &o, std::string *out);
static void proj_list_task_summary_response(const ListTaskSummaryResponse &o, std::string *out);
static void proj_list_probe_response(const ListProbeResponse &o, std::string *out);
static void proj_list_metrics_response(const ListMetricsResponse &o, std::string *out);
static void proj_upload_result_data_message(const UploadResultDataMessage &o, std::string *out);
static void proj_dual_response(const DualResponse &o, std::string *out);
static void proj_chunk_leaf(const ChunkLeaf &o, std::string *out);
static void proj_chunk_inner(const ChunkInner &o, std::string *out);
static void proj_chunk_element(const ChunkElement &o, std::string *out);
static void proj_chunked_response(const ChunkedResponse &o, std::string *out);
static void proj_chunked_response_wide(const ChunkedResponseWide &o, std::string *out);
static void proj_leaf_element(const LeafElement &o, std::string *out);
static void proj_leaf_response(const LeafResponse &o, std::string *out);
static void proj_surrogate(const Surrogate &o, std::string *out);
static void proj_surrogate_inner(const SurrogateInner &o, std::string *out);
static void proj_nest(const Nest &o, std::string *out);
static void proj_wire_zoo(const WireZoo &o, std::string *out);

static void proj_timestamp(const Timestamp &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (o.seconds != 0) {
    ak::proj::key("seconds", &first, out);
    ak::proj::i64((long long)o.seconds, out);
  }
  if (o.nanos != 0) {
    ak::proj::key("nanos", &first, out);
    ak::proj::i64((long long)o.nanos, out);
  }
  out->push_back('}');
}

static void proj_duration(const Duration &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (o.seconds != 0) {
    ak::proj::key("seconds", &first, out);
    ak::proj::i64((long long)o.seconds, out);
  }
  if (o.nanos != 0) {
    ak::proj::key("nanos", &first, out);
    ak::proj::i64((long long)o.nanos, out);
  }
  out->push_back('}');
}

static void proj_result_raw(const ResultRaw &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (!o.session_id.empty()) {
    ak::proj::key("session_id", &first, out);
    ak::proj::str(o.session_id, out);
  }
  if (!o.name.empty()) {
    ak::proj::key("name", &first, out);
    ak::proj::str(o.name, out);
  }
  if (!o.owner_task_id.empty()) {
    ak::proj::key("owner_task_id", &first, out);
    ak::proj::str(o.owner_task_id, out);
  }
  if (o.status.v != 0) {
    ak::proj::key("status", &first, out);
    ak::proj::i64((long long)(o.status).v, out);
  }
  if (o.created_at.has_value()) {
    ak::proj::key("created_at", &first, out);
    proj_timestamp(*o.created_at, out);
  }
  if (o.completed_at.has_value()) {
    ak::proj::key("completed_at", &first, out);
    proj_timestamp(*o.completed_at, out);
  }
  if (!o.result_id.empty()) {
    ak::proj::key("result_id", &first, out);
    ak::proj::str(o.result_id, out);
  }
  if (o.size != 0) {
    ak::proj::key("size", &first, out);
    ak::proj::i64((long long)o.size, out);
  }
  if (!o.created_by.empty()) {
    ak::proj::key("created_by", &first, out);
    ak::proj::str(o.created_by, out);
  }
  if (!o.opaque_id.empty()) {
    ak::proj::key("opaque_id", &first, out);
    ak::proj::hex(o.opaque_id, out);
  }
  if (o.manual_deletion) {
    ak::proj::key("manual_deletion", &first, out);
    ak::proj::boolean(o.manual_deletion, out);
  }
  out->push_back('}');
}

static void proj_task_options(const TaskOptions &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (!o.options.empty()) {
    ak::proj::key("options", &first, out);
    out->push_back('{');
    bool mf = true;
    for (std::map<AkStrT, AkStrT>::const_iterator it = o.options.begin();
         it != o.options.end(); ++it) {
      ak::proj::sep(&mf, out);
      ak::proj::str(it->first, out);
      out->push_back(':');
      ak::proj::str(it->second, out);
    }
    out->push_back('}');
  }
  if (o.max_duration.has_value()) {
    ak::proj::key("max_duration", &first, out);
    proj_duration(*o.max_duration, out);
  }
  if (o.max_retries != 0) {
    ak::proj::key("max_retries", &first, out);
    ak::proj::i64((long long)o.max_retries, out);
  }
  if (o.priority != 0) {
    ak::proj::key("priority", &first, out);
    ak::proj::i64((long long)o.priority, out);
  }
  if (!o.partition_id.empty()) {
    ak::proj::key("partition_id", &first, out);
    ak::proj::str(o.partition_id, out);
  }
  if (!o.application_name.empty()) {
    ak::proj::key("application_name", &first, out);
    ak::proj::str(o.application_name, out);
  }
  if (!o.application_version.empty()) {
    ak::proj::key("application_version", &first, out);
    ak::proj::str(o.application_version, out);
  }
  if (!o.application_namespace.empty()) {
    ak::proj::key("application_namespace", &first, out);
    ak::proj::str(o.application_namespace, out);
  }
  if (!o.application_service.empty()) {
    ak::proj::key("application_service", &first, out);
    ak::proj::str(o.application_service, out);
  }
  if (!o.engine_type.empty()) {
    ak::proj::key("engine_type", &first, out);
    ak::proj::str(o.engine_type, out);
  }
  out->push_back('}');
}

static void proj_task_output(const TaskOutput &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (o.success) {
    ak::proj::key("success", &first, out);
    ak::proj::boolean(o.success, out);
  }
  if (!o.error.empty()) {
    ak::proj::key("error", &first, out);
    ak::proj::str(o.error, out);
  }
  out->push_back('}');
}

static void proj_task_detailed(const TaskDetailed &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (!o.id.empty()) {
    ak::proj::key("id", &first, out);
    ak::proj::str(o.id, out);
  }
  if (!o.session_id.empty()) {
    ak::proj::key("session_id", &first, out);
    ak::proj::str(o.session_id, out);
  }
  if (!o.owner_pod_id.empty()) {
    ak::proj::key("owner_pod_id", &first, out);
    ak::proj::str(o.owner_pod_id, out);
  }
  if (!o.parent_task_ids.empty()) {
    ak::proj::key("parent_task_ids", &first, out);
    out->push_back('[');
    for (size_t i = 0; i < o.parent_task_ids.size(); ++i) {
      if (i) out->push_back(',');
      ak::proj::str(o.parent_task_ids[i], out);
    }
    out->push_back(']');
  }
  if (!o.data_dependencies.empty()) {
    ak::proj::key("data_dependencies", &first, out);
    out->push_back('[');
    for (size_t i = 0; i < o.data_dependencies.size(); ++i) {
      if (i) out->push_back(',');
      ak::proj::str(o.data_dependencies[i], out);
    }
    out->push_back(']');
  }
  if (!o.expected_output_ids.empty()) {
    ak::proj::key("expected_output_ids", &first, out);
    out->push_back('[');
    for (size_t i = 0; i < o.expected_output_ids.size(); ++i) {
      if (i) out->push_back(',');
      ak::proj::str(o.expected_output_ids[i], out);
    }
    out->push_back(']');
  }
  if (!o.retry_of_ids.empty()) {
    ak::proj::key("retry_of_ids", &first, out);
    out->push_back('[');
    for (size_t i = 0; i < o.retry_of_ids.size(); ++i) {
      if (i) out->push_back(',');
      ak::proj::str(o.retry_of_ids[i], out);
    }
    out->push_back(']');
  }
  if (o.status.v != 0) {
    ak::proj::key("status", &first, out);
    ak::proj::i64((long long)(o.status).v, out);
  }
  if (!o.status_message.empty()) {
    ak::proj::key("status_message", &first, out);
    ak::proj::str(o.status_message, out);
  }
  if (o.options.has_value()) {
    ak::proj::key("options", &first, out);
    proj_task_options(*o.options, out);
  }
  if (o.created_at.has_value()) {
    ak::proj::key("created_at", &first, out);
    proj_timestamp(*o.created_at, out);
  }
  if (o.submitted_at.has_value()) {
    ak::proj::key("submitted_at", &first, out);
    proj_timestamp(*o.submitted_at, out);
  }
  if (o.started_at.has_value()) {
    ak::proj::key("started_at", &first, out);
    proj_timestamp(*o.started_at, out);
  }
  if (o.ended_at.has_value()) {
    ak::proj::key("ended_at", &first, out);
    proj_timestamp(*o.ended_at, out);
  }
  if (o.pod_ttl.has_value()) {
    ak::proj::key("pod_ttl", &first, out);
    proj_timestamp(*o.pod_ttl, out);
  }
  if (o.output.has_value()) {
    ak::proj::key("output", &first, out);
    proj_task_output(*o.output, out);
  }
  if (!o.pod_hostname.empty()) {
    ak::proj::key("pod_hostname", &first, out);
    ak::proj::str(o.pod_hostname, out);
  }
  if (o.received_at.has_value()) {
    ak::proj::key("received_at", &first, out);
    proj_timestamp(*o.received_at, out);
  }
  if (o.acquired_at.has_value()) {
    ak::proj::key("acquired_at", &first, out);
    proj_timestamp(*o.acquired_at, out);
  }
  if (o.creation_to_end_duration.has_value()) {
    ak::proj::key("creation_to_end_duration", &first, out);
    proj_duration(*o.creation_to_end_duration, out);
  }
  if (o.processing_to_end_duration.has_value()) {
    ak::proj::key("processing_to_end_duration", &first, out);
    proj_duration(*o.processing_to_end_duration, out);
  }
  if (!o.initial_task_id.empty()) {
    ak::proj::key("initial_task_id", &first, out);
    ak::proj::str(o.initial_task_id, out);
  }
  if (o.received_to_end_duration.has_value()) {
    ak::proj::key("received_to_end_duration", &first, out);
    proj_duration(*o.received_to_end_duration, out);
  }
  if (o.processed_at.has_value()) {
    ak::proj::key("processed_at", &first, out);
    proj_timestamp(*o.processed_at, out);
  }
  if (o.fetched_at.has_value()) {
    ak::proj::key("fetched_at", &first, out);
    proj_timestamp(*o.fetched_at, out);
  }
  if (!o.payload_id.empty()) {
    ak::proj::key("payload_id", &first, out);
    ak::proj::str(o.payload_id, out);
  }
  if (!o.created_by.empty()) {
    ak::proj::key("created_by", &first, out);
    ak::proj::str(o.created_by, out);
  }
  out->push_back('}');
}

static void proj_task_summary(const TaskSummary &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (!o.id.empty()) {
    ak::proj::key("id", &first, out);
    ak::proj::str(o.id, out);
  }
  if (!o.session_id.empty()) {
    ak::proj::key("session_id", &first, out);
    ak::proj::str(o.session_id, out);
  }
  if (o.options.has_value()) {
    ak::proj::key("options", &first, out);
    proj_task_options(*o.options, out);
  }
  if (o.status.v != 0) {
    ak::proj::key("status", &first, out);
    ak::proj::i64((long long)(o.status).v, out);
  }
  if (o.created_at.has_value()) {
    ak::proj::key("created_at", &first, out);
    proj_timestamp(*o.created_at, out);
  }
  if (!o.error.empty()) {
    ak::proj::key("error", &first, out);
    ak::proj::str(o.error, out);
  }
  if (!o.status_message.empty()) {
    ak::proj::key("status_message", &first, out);
    ak::proj::str(o.status_message, out);
  }
  if (o.count_data_dependencies != 0) {
    ak::proj::key("count_data_dependencies", &first, out);
    ak::proj::i64((long long)o.count_data_dependencies, out);
  }
  out->push_back('}');
}

static void proj_probe(const Probe &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (!o.id.empty()) {
    ak::proj::key("id", &first, out);
    ak::proj::str(o.id, out);
  }
  if (o.opt_count.has_value()) {
    ak::proj::key("opt_count", &first, out);
    ak::proj::i64((long long)*o.opt_count, out);
  }
  if (o.opt_label.has_value()) {
    ak::proj::key("opt_label", &first, out);
    ak::proj::str(*o.opt_label, out);
  }
  if (o.opt_flag.has_value()) {
    ak::proj::key("opt_flag", &first, out);
    ak::proj::boolean(*o.opt_flag, out);
  }
  switch (o.body.which()) {
    case ProbeBody::kAsInt: {
      ak::proj::key("as_int", &first, out);
      ak::proj::i64((long long)o.body.as_int(), out);
      break;
    }
    case ProbeBody::kAsText: {
      ak::proj::key("as_text", &first, out);
      ak::proj::str(o.body.as_text(), out);
      break;
    }
    case ProbeBody::kAsBlob: {
      ak::proj::key("as_blob", &first, out);
      ak::proj::hex(o.body.as_blob(), out);
      break;
    }
    case ProbeBody::kAsStamp: {
      ak::proj::key("as_stamp", &first, out);
      proj_timestamp(o.body.as_stamp(), out);
      break;
    }
    case ProbeBody::kAsNothing: {
      ak::proj::key("as_nothing", &first, out);
      proj_empty(o.body.as_nothing(), out);
      break;
    }
    case ProbeBody::kNotSet: break;
  }
  out->push_back('}');
}

static void proj_empty(const Empty &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  out->push_back('}');
}

static void proj_upload_result_data(const UploadResultData &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (!o.session_id.empty()) {
    ak::proj::key("session_id", &first, out);
    ak::proj::str(o.session_id, out);
  }
  if (!o.result_id.empty()) {
    ak::proj::key("result_id", &first, out);
    ak::proj::str(o.result_id, out);
  }
  if (!o.data_chunk.empty()) {
    ak::proj::key("data_chunk", &first, out);
    ak::proj::hex(o.data_chunk, out);
  }
  out->push_back('}');
}

static void proj_metrics_batch(const MetricsBatch &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (!o.id.empty()) {
    ak::proj::key("id", &first, out);
    ak::proj::str(o.id, out);
  }
  if (!o.ticks.empty()) {
    ak::proj::key("ticks", &first, out);
    out->push_back('[');
    for (size_t i = 0; i < o.ticks.size(); ++i) {
      if (i) out->push_back(',');
      ak::proj::i64((long long)o.ticks[i], out);
    }
    out->push_back(']');
  }
  if (!o.values.empty()) {
    ak::proj::key("values", &first, out);
    out->push_back('[');
    for (size_t i = 0; i < o.values.size(); ++i) {
      if (i) out->push_back(',');
      ak::proj::f64(o.values[i], out);
    }
    out->push_back(']');
  }
  if (!o.codes.empty()) {
    ak::proj::key("codes", &first, out);
    out->push_back('[');
    for (size_t i = 0; i < o.codes.size(); ++i) {
      if (i) out->push_back(',');
      ak::proj::i64((long long)o.codes[i], out);
    }
    out->push_back(']');
  }
  if (!o.flags.empty()) {
    ak::proj::key("flags", &first, out);
    out->push_back('[');
    for (size_t i = 0; i < o.flags.size(); ++i) {
      if (i) out->push_back(',');
      ak::proj::boolean(o.flags[i], out);
    }
    out->push_back(']');
  }
  if (!o.statuses.empty()) {
    ak::proj::key("statuses", &first, out);
    out->push_back('[');
    for (size_t i = 0; i < o.statuses.size(); ++i) {
      if (i) out->push_back(',');
      ak::proj::i64((long long)(o.statuses[i]).v, out);
    }
    out->push_back(']');
  }
  out->push_back('}');
}

static void proj_pair(const Pair &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (!o.key.empty()) {
    ak::proj::key("key", &first, out);
    ak::proj::str(o.key, out);
  }
  if (o.value != 0) {
    ak::proj::key("value", &first, out);
    ak::proj::i64((long long)o.value, out);
  }
  out->push_back('}');
}

static void proj_list_results_response(const ListResultsResponse &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (!o.results.empty()) {
    ak::proj::key("results", &first, out);
    out->push_back('[');
    for (size_t i = 0; i < o.results.size(); ++i) {
      if (i) out->push_back(',');
      proj_result_raw(o.results[i], out);
    }
    out->push_back(']');
  }
  if (o.page != 0) {
    ak::proj::key("page", &first, out);
    ak::proj::i64((long long)o.page, out);
  }
  if (o.total != 0) {
    ak::proj::key("total", &first, out);
    ak::proj::i64((long long)o.total, out);
  }
  out->push_back('}');
}

static void proj_list_tasks_detailed_response(const ListTasksDetailedResponse &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (!o.tasks.empty()) {
    ak::proj::key("tasks", &first, out);
    out->push_back('[');
    for (size_t i = 0; i < o.tasks.size(); ++i) {
      if (i) out->push_back(',');
      proj_task_detailed(o.tasks[i], out);
    }
    out->push_back(']');
  }
  if (o.page != 0) {
    ak::proj::key("page", &first, out);
    ak::proj::i64((long long)o.page, out);
  }
  if (o.total != 0) {
    ak::proj::key("total", &first, out);
    ak::proj::i64((long long)o.total, out);
  }
  out->push_back('}');
}

static void proj_list_task_summary_response(const ListTaskSummaryResponse &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (!o.tasks.empty()) {
    ak::proj::key("tasks", &first, out);
    out->push_back('[');
    for (size_t i = 0; i < o.tasks.size(); ++i) {
      if (i) out->push_back(',');
      proj_task_summary(o.tasks[i], out);
    }
    out->push_back(']');
  }
  out->push_back('}');
}

static void proj_list_probe_response(const ListProbeResponse &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (!o.probes.empty()) {
    ak::proj::key("probes", &first, out);
    out->push_back('[');
    for (size_t i = 0; i < o.probes.size(); ++i) {
      if (i) out->push_back(',');
      proj_probe(o.probes[i], out);
    }
    out->push_back(']');
  }
  out->push_back('}');
}

static void proj_list_metrics_response(const ListMetricsResponse &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (!o.batches.empty()) {
    ak::proj::key("batches", &first, out);
    out->push_back('[');
    for (size_t i = 0; i < o.batches.size(); ++i) {
      if (i) out->push_back(',');
      proj_metrics_batch(o.batches[i], out);
    }
    out->push_back(']');
  }
  out->push_back('}');
}

static void proj_upload_result_data_message(const UploadResultDataMessage &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (o.upload.has_value()) {
    ak::proj::key("upload", &first, out);
    proj_upload_result_data(*o.upload, out);
  }
  out->push_back('}');
}

static void proj_dual_response(const DualResponse &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (!o.left.empty()) {
    ak::proj::key("left", &first, out);
    out->push_back('[');
    for (size_t i = 0; i < o.left.size(); ++i) {
      if (i) out->push_back(',');
      proj_pair(o.left[i], out);
    }
    out->push_back(']');
  }
  if (!o.right.empty()) {
    ak::proj::key("right", &first, out);
    out->push_back('[');
    for (size_t i = 0; i < o.right.size(); ++i) {
      if (i) out->push_back(',');
      proj_pair(o.right[i], out);
    }
    out->push_back(']');
  }
  out->push_back('}');
}

static void proj_chunk_leaf(const ChunkLeaf &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (!o.k.empty()) {
    ak::proj::key("k", &first, out);
    ak::proj::str(o.k, out);
  }
  if (o.v != 0) {
    ak::proj::key("v", &first, out);
    ak::proj::i64((long long)o.v, out);
  }
  out->push_back('}');
}

static void proj_chunk_inner(const ChunkInner &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (!o.marks.empty()) {
    ak::proj::key("marks", &first, out);
    out->push_back('[');
    for (size_t i = 0; i < o.marks.size(); ++i) {
      if (i) out->push_back(',');
      ak::proj::i64((long long)o.marks[i], out);
    }
    out->push_back(']');
  }
  if (!o.leaves.empty()) {
    ak::proj::key("leaves", &first, out);
    out->push_back('[');
    for (size_t i = 0; i < o.leaves.size(); ++i) {
      if (i) out->push_back(',');
      proj_chunk_leaf(o.leaves[i], out);
    }
    out->push_back(']');
  }
  out->push_back('}');
}

static void proj_chunk_element(const ChunkElement &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (!o.labels.empty()) {
    ak::proj::key("labels", &first, out);
    out->push_back('[');
    for (size_t i = 0; i < o.labels.size(); ++i) {
      if (i) out->push_back(',');
      ak::proj::str(o.labels[i], out);
    }
    out->push_back(']');
  }
  if (!o.attrs.empty()) {
    ak::proj::key("attrs", &first, out);
    out->push_back('{');
    bool mf = true;
    for (std::map<AkStrT, AkStrT>::const_iterator it = o.attrs.begin();
         it != o.attrs.end(); ++it) {
      ak::proj::sep(&mf, out);
      ak::proj::str(it->first, out);
      out->push_back(':');
      ak::proj::str(it->second, out);
    }
    out->push_back('}');
  }
  if (!o.id.empty()) {
    ak::proj::key("id", &first, out);
    ak::proj::str(o.id, out);
  }
  if (o.inner.has_value()) {
    ak::proj::key("inner", &first, out);
    proj_chunk_inner(*o.inner, out);
  }
  out->push_back('}');
}

static void proj_chunked_response(const ChunkedResponse &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (!o.items.empty()) {
    ak::proj::key("items", &first, out);
    out->push_back('[');
    for (size_t i = 0; i < o.items.size(); ++i) {
      if (i) out->push_back(',');
      proj_chunk_element(o.items[i], out);
    }
    out->push_back(']');
  }
  if (o.page != 0) {
    ak::proj::key("page", &first, out);
    ak::proj::i64((long long)o.page, out);
  }
  out->push_back('}');
}

static void proj_chunked_response_wide(const ChunkedResponseWide &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (!o.items.empty()) {
    ak::proj::key("items", &first, out);
    out->push_back('[');
    for (size_t i = 0; i < o.items.size(); ++i) {
      if (i) out->push_back(',');
      proj_chunk_element(o.items[i], out);
    }
    out->push_back(']');
  }
  out->push_back('}');
}

static void proj_leaf_element(const LeafElement &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (!o.id.empty()) {
    ak::proj::key("id", &first, out);
    ak::proj::str(o.id, out);
  }
  if (o.n != 0) {
    ak::proj::key("n", &first, out);
    ak::proj::i64((long long)o.n, out);
  }
  if (o.stamp.has_value()) {
    ak::proj::key("stamp", &first, out);
    proj_timestamp(*o.stamp, out);
  }
  out->push_back('}');
}

static void proj_leaf_response(const LeafResponse &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (!o.items.empty()) {
    ak::proj::key("items", &first, out);
    out->push_back('[');
    for (size_t i = 0; i < o.items.size(); ++i) {
      if (i) out->push_back(',');
      proj_leaf_element(o.items[i], out);
    }
    out->push_back(']');
  }
  out->push_back('}');
}

static void proj_surrogate(const Surrogate &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (!o.text.empty()) {
    ak::proj::key("text", &first, out);
    ak::proj::str(o.text, out);
  }
  if (o.nested.has_value()) {
    ak::proj::key("nested", &first, out);
    proj_surrogate_inner(*o.nested, out);
  }
  if (!o.attrs.empty()) {
    ak::proj::key("attrs", &first, out);
    out->push_back('{');
    bool mf = true;
    for (std::map<AkStrT, AkStrT>::const_iterator it = o.attrs.begin();
         it != o.attrs.end(); ++it) {
      ak::proj::sep(&mf, out);
      ak::proj::str(it->first, out);
      out->push_back(':');
      ak::proj::str(it->second, out);
    }
    out->push_back('}');
  }
  if (!o.texts.empty()) {
    ak::proj::key("texts", &first, out);
    out->push_back('[');
    for (size_t i = 0; i < o.texts.size(); ++i) {
      if (i) out->push_back(',');
      ak::proj::str(o.texts[i], out);
    }
    out->push_back(']');
  }
  if (!o.raw.empty()) {
    ak::proj::key("raw", &first, out);
    ak::proj::hex(o.raw, out);
  }
  out->push_back('}');
}

static void proj_surrogate_inner(const SurrogateInner &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (!o.text.empty()) {
    ak::proj::key("text", &first, out);
    ak::proj::str(o.text, out);
  }
  out->push_back('}');
}

static void proj_nest(const Nest &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (o.child.has_value()) {
    ak::proj::key("child", &first, out);
    proj_nest(*o.child, out);
  }
  if (!o.leaf.empty()) {
    ak::proj::key("leaf", &first, out);
    ak::proj::str(o.leaf, out);
  }
  out->push_back('}');
}

static void proj_wire_zoo(const WireZoo &o, std::string *out) {
  (void)o;
  out->push_back('{');
  bool first = true;
  (void)first;
  if (o.v_int32 != 0) {
    ak::proj::key("v_int32", &first, out);
    ak::proj::i64((long long)o.v_int32, out);
  }
  if (o.v_int64 != 0) {
    ak::proj::key("v_int64", &first, out);
    ak::proj::i64((long long)o.v_int64, out);
  }
  if (o.v_bool) {
    ak::proj::key("v_bool", &first, out);
    ak::proj::boolean(o.v_bool, out);
  }
  if (ak::f64_bits(o.v_double) != 0) {
    ak::proj::key("v_double", &first, out);
    ak::proj::f64(o.v_double, out);
  }
  if (o.v_fixed32 != 0) {
    ak::proj::key("v_fixed32", &first, out);
    ak::proj::i64((long long)o.v_fixed32, out);
  }
  if (!o.v_string.empty()) {
    ak::proj::key("v_string", &first, out);
    ak::proj::str(o.v_string, out);
  }
  if (!o.v_bytes.empty()) {
    ak::proj::key("v_bytes", &first, out);
    ak::proj::hex(o.v_bytes, out);
  }
  if (o.v_enum.v != 0) {
    ak::proj::key("v_enum", &first, out);
    ak::proj::i64((long long)(o.v_enum).v, out);
  }
  if (o.v_msg.has_value()) {
    ak::proj::key("v_msg", &first, out);
    proj_timestamp(*o.v_msg, out);
  }
  if (o.v_big_tag != 0) {
    ak::proj::key("v_big_tag", &first, out);
    ak::proj::i64((long long)o.v_big_tag, out);
  }
  out->push_back('}');
}

std::string project_timestamp(const Timestamp &o) {
  std::string out;
  proj_timestamp(o, &out);
  return out;
}

std::string project_duration(const Duration &o) {
  std::string out;
  proj_duration(o, &out);
  return out;
}

std::string project_result_raw(const ResultRaw &o) {
  std::string out;
  proj_result_raw(o, &out);
  return out;
}

std::string project_task_options(const TaskOptions &o) {
  std::string out;
  proj_task_options(o, &out);
  return out;
}

std::string project_task_output(const TaskOutput &o) {
  std::string out;
  proj_task_output(o, &out);
  return out;
}

std::string project_task_detailed(const TaskDetailed &o) {
  std::string out;
  proj_task_detailed(o, &out);
  return out;
}

std::string project_task_summary(const TaskSummary &o) {
  std::string out;
  proj_task_summary(o, &out);
  return out;
}

std::string project_probe(const Probe &o) {
  std::string out;
  proj_probe(o, &out);
  return out;
}

std::string project_empty(const Empty &o) {
  std::string out;
  proj_empty(o, &out);
  return out;
}

std::string project_upload_result_data(const UploadResultData &o) {
  std::string out;
  proj_upload_result_data(o, &out);
  return out;
}

std::string project_metrics_batch(const MetricsBatch &o) {
  std::string out;
  proj_metrics_batch(o, &out);
  return out;
}

std::string project_pair(const Pair &o) {
  std::string out;
  proj_pair(o, &out);
  return out;
}

std::string project_list_results_response(const ListResultsResponse &o) {
  std::string out;
  proj_list_results_response(o, &out);
  return out;
}

std::string project_list_tasks_detailed_response(const ListTasksDetailedResponse &o) {
  std::string out;
  proj_list_tasks_detailed_response(o, &out);
  return out;
}

std::string project_list_task_summary_response(const ListTaskSummaryResponse &o) {
  std::string out;
  proj_list_task_summary_response(o, &out);
  return out;
}

std::string project_list_probe_response(const ListProbeResponse &o) {
  std::string out;
  proj_list_probe_response(o, &out);
  return out;
}

std::string project_list_metrics_response(const ListMetricsResponse &o) {
  std::string out;
  proj_list_metrics_response(o, &out);
  return out;
}

std::string project_upload_result_data_message(const UploadResultDataMessage &o) {
  std::string out;
  proj_upload_result_data_message(o, &out);
  return out;
}

std::string project_dual_response(const DualResponse &o) {
  std::string out;
  proj_dual_response(o, &out);
  return out;
}

std::string project_chunk_leaf(const ChunkLeaf &o) {
  std::string out;
  proj_chunk_leaf(o, &out);
  return out;
}

std::string project_chunk_inner(const ChunkInner &o) {
  std::string out;
  proj_chunk_inner(o, &out);
  return out;
}

std::string project_chunk_element(const ChunkElement &o) {
  std::string out;
  proj_chunk_element(o, &out);
  return out;
}

std::string project_chunked_response(const ChunkedResponse &o) {
  std::string out;
  proj_chunked_response(o, &out);
  return out;
}

std::string project_chunked_response_wide(const ChunkedResponseWide &o) {
  std::string out;
  proj_chunked_response_wide(o, &out);
  return out;
}

std::string project_leaf_element(const LeafElement &o) {
  std::string out;
  proj_leaf_element(o, &out);
  return out;
}

std::string project_leaf_response(const LeafResponse &o) {
  std::string out;
  proj_leaf_response(o, &out);
  return out;
}

std::string project_surrogate(const Surrogate &o) {
  std::string out;
  proj_surrogate(o, &out);
  return out;
}

std::string project_surrogate_inner(const SurrogateInner &o) {
  std::string out;
  proj_surrogate_inner(o, &out);
  return out;
}

std::string project_nest(const Nest &o) {
  std::string out;
  proj_nest(o, &out);
  return out;
}

std::string project_wire_zoo(const WireZoo &o) {
  std::string out;
  proj_wire_zoo(o, &out);
  return out;
}

}  // namespace project
}  // namespace corpus
