// @generated by ffi/poc/cpp/gen/generate.py from ffi/schema/shapes.json. Do not edit.

// Build any payload of shapes.json as facade values.
#include "generated/build.h"

#include <cstdio>

namespace shapes {
namespace build {

namespace v = ak::values;

// `explicit_present()`: presence cycles per field, and one element in seven is present AND
// zero, which a by-value group cannot tell from absent unless it is designed to.
static inline bool explicit_present(int64_t tag, int64_t idx) {
  return idx % (tag + 1) != 0;
}

static inline AdapterState adapter_state(int64_t idx) {
  switch (idx % 3) {
    case 0: return kAdapterError;
    case 1: return kAdapterOk;
    default: return kAdapterInvalid;
  }
}

static Timestamp mk_timestamp(int idx) {
  ak::values::Stamp s = v::timestamp(idx);
  Timestamp t;
  t.seconds = s.seconds;
  t.nanos = s.nanos;
  return t;
}

static Duration mk_duration(int idx) {
  ak::values::Stamp s = v::duration(idx);
  Duration t;
  t.seconds = s.seconds;
  t.nanos = s.nanos;
  return t;
}

Timestamp build_timestamp(const std::string &path, int64_t idx, Mode mode, int64_t repeats, size_t bulk) {
  (void)path; (void)idx; (void)mode; (void)repeats; (void)bulk;
  Timestamp r;
  if (!(mode == kAllAbsent)) {
    r.seconds = v::scalar_i64((path + ".seconds"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.nanos = v::scalar_i32((path + ".nanos"), (int)(idx));
  }
  return r;
}

Duration build_duration(const std::string &path, int64_t idx, Mode mode, int64_t repeats, size_t bulk) {
  (void)path; (void)idx; (void)mode; (void)repeats; (void)bulk;
  Duration r;
  if (!(mode == kAllAbsent)) {
    r.seconds = v::scalar_i64((path + ".seconds"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.nanos = v::scalar_i32((path + ".nanos"), (int)(idx));
  }
  return r;
}

ResultRaw build_result_raw(const std::string &path, int64_t idx, Mode mode, int64_t repeats, size_t bulk) {
  (void)path; (void)idx; (void)mode; (void)repeats; (void)bulk;
  ResultRaw r;
  if (!(mode == kAllAbsent)) {
    r.session_id = v::guid((path + ".session_id"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.name = v::word((path + ".name"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.owner_task_id = v::guid((path + ".owner_task_id"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.status = shapes::ResultStatus(v::enum_value(shapes::tbl_ResultStatus(), (int)idx));
  }
  if (!(mode == kAllAbsent)) {
    r.created_at.set(mk_timestamp((int)(idx)));
  }
  if (!(mode == kAllAbsent || mode == kHalfAbsent)) {
    r.completed_at.set(mk_timestamp((int)(idx)));
  }
  if (!(mode == kAllAbsent)) {
    r.result_id = v::guid((path + ".result_id"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.size = v::scalar_i64((path + ".size"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.created_by = v::guid((path + ".created_by"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.opaque_id = v::blob((path + ".opaque_id"), (int)idx, 16);
  }
  if (!(mode == kAllAbsent)) {
    r.manual_deletion = v::scalar_bool((path + ".manual_deletion"), (int)(idx));
  }
  return r;
}

TaskOptions build_task_options(const std::string &path, int64_t idx, Mode mode, int64_t repeats, size_t bulk) {
  (void)path; (void)idx; (void)mode; (void)repeats; (void)bulk;
  TaskOptions r;
  if (!(mode == kAllAbsent)) {
    for (int64_t k = 0; k < 4; ++k) {
      char kb[8];
      std::snprintf(kb, sizeof(kb), "k%02d-", (int)k);
      std::string key = std::string(kb) + v::word(path + ".options.key", (int)(idx * 31 + k));
      std::string val;
      if (!(mode == kHalfAbsent && k % 2 == 0))
        val = v::word(path + ".options.value", (int)(idx * 31 + k));
      r.options[key] = val;
    }
  }
  if (!(mode == kAllAbsent)) {
    r.max_duration.set(mk_duration((int)(idx)));
  }
  if (!(mode == kAllAbsent)) {
    r.max_retries = v::scalar_i32((path + ".max_retries"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.priority = v::scalar_i32((path + ".priority"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.partition_id = v::word((path + ".partition_id"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.application_name = v::word((path + ".application_name"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.application_version = v::word((path + ".application_version"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.application_namespace = v::word((path + ".application_namespace"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.application_service = v::word((path + ".application_service"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.engine_type = v::word((path + ".engine_type"), (int)(idx));
  }
  return r;
}

TaskOutput build_task_output(const std::string &path, int64_t idx, Mode mode, int64_t repeats, size_t bulk) {
  (void)path; (void)idx; (void)mode; (void)repeats; (void)bulk;
  TaskOutput r;
  if (!(mode == kAllAbsent)) {
    r.success = v::scalar_bool((path + ".success"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.error = v::sentence((path + ".error"), (int)(idx));
  }
  return r;
}

TaskDetailed build_task_detailed(const std::string &path, int64_t idx, Mode mode, int64_t repeats, size_t bulk) {
  (void)path; (void)idx; (void)mode; (void)repeats; (void)bulk;
  TaskDetailed r;
  if (!(mode == kAllAbsent)) {
    r.id = v::guid((path + ".id"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.session_id = v::guid((path + ".session_id"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.owner_pod_id = v::guid((path + ".owner_pod_id"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.parent_task_ids.reserve((size_t)repeats);
    for (int64_t j = 0; j < repeats; ++j)
      r.parent_task_ids.push_back(v::guid((path + ".parent_task_ids"), (int)(idx * 211 + j)));
  }
  if (!(mode == kAllAbsent)) {
    r.data_dependencies.reserve((size_t)repeats);
    for (int64_t j = 0; j < repeats; ++j)
      r.data_dependencies.push_back(v::guid((path + ".data_dependencies"), (int)(idx * 211 + j)));
  }
  if (!(mode == kAllAbsent)) {
    r.expected_output_ids.reserve((size_t)repeats);
    for (int64_t j = 0; j < repeats; ++j)
      r.expected_output_ids.push_back(v::guid((path + ".expected_output_ids"), (int)(idx * 211 + j)));
  }
  if (!(mode == kAllAbsent)) {
    r.retry_of_ids.reserve((size_t)repeats);
    for (int64_t j = 0; j < repeats; ++j)
      r.retry_of_ids.push_back(v::guid((path + ".retry_of_ids"), (int)(idx * 211 + j)));
  }
  if (!(mode == kAllAbsent)) {
    r.status = shapes::TaskStatus(v::enum_value(shapes::tbl_TaskStatus(), (int)idx));
  }
  if (!(mode == kAllAbsent)) {
    r.status_message = v::sentence((path + ".status_message"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.options.set(build_task_options((path + ".options"), idx, mode, repeats, bulk));
  }
  if (!(mode == kAllAbsent)) {
    r.created_at.set(mk_timestamp((int)(idx)));
  }
  if (!(mode == kAllAbsent || mode == kHalfAbsent)) {
    r.submitted_at.set(mk_timestamp((int)(idx)));
  }
  if (!(mode == kAllAbsent)) {
    r.started_at.set(mk_timestamp((int)(idx)));
  }
  if (!(mode == kAllAbsent || mode == kHalfAbsent)) {
    r.ended_at.set(mk_timestamp((int)(idx)));
  }
  if (!(mode == kAllAbsent)) {
    r.pod_ttl.set(mk_timestamp((int)(idx)));
  }
  if (!(mode == kAllAbsent || mode == kHalfAbsent)) {
    switch (adapter_state(idx)) {
      case kAdapterInvalid: break;
      case kAdapterOk: {
        r.output.emplace().success = true;
        break;
      }
      case kAdapterError: {
        shapes::TaskOutput &c = r.output.emplace();
        c.error = v::sentence(path + ".output.error", (int)idx);
        break;
      }
    }
  }
  if (!(mode == kAllAbsent)) {
    r.pod_hostname = v::word((path + ".pod_hostname"), (int)(idx));
  }
  if (!(mode == kAllAbsent || mode == kHalfAbsent)) {
    r.received_at.set(mk_timestamp((int)(idx)));
  }
  if (!(mode == kAllAbsent)) {
    r.acquired_at.set(mk_timestamp((int)(idx)));
  }
  if (!(mode == kAllAbsent)) {
    r.creation_to_end_duration.set(mk_duration((int)(idx)));
  }
  if (!(mode == kAllAbsent)) {
    r.processing_to_end_duration.set(mk_duration((int)(idx)));
  }
  if (!(mode == kAllAbsent)) {
    r.initial_task_id = v::guid((path + ".initial_task_id"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.received_to_end_duration.set(mk_duration((int)(idx)));
  }
  if (!(mode == kAllAbsent || mode == kHalfAbsent)) {
    r.processed_at.set(mk_timestamp((int)(idx)));
  }
  if (!(mode == kAllAbsent)) {
    r.fetched_at.set(mk_timestamp((int)(idx)));
  }
  if (!(mode == kAllAbsent)) {
    r.payload_id = v::guid((path + ".payload_id"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.created_by = v::guid((path + ".created_by"), (int)(idx));
  }
  return r;
}

TaskSummary build_task_summary(const std::string &path, int64_t idx, Mode mode, int64_t repeats, size_t bulk) {
  (void)path; (void)idx; (void)mode; (void)repeats; (void)bulk;
  TaskSummary r;
  if (!(mode == kAllAbsent)) {
    r.id = v::guid((path + ".id"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.session_id = v::guid((path + ".session_id"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.options.set(build_task_options((path + ".options"), idx, mode, repeats, bulk));
  }
  if (!(mode == kAllAbsent)) {
    r.status = shapes::TaskStatus(v::enum_value(shapes::tbl_TaskStatus(), (int)idx));
  }
  if (!(mode == kAllAbsent)) {
    r.created_at.set(mk_timestamp((int)(idx)));
  }
  if (!(mode == kAllAbsent) && adapter_state(idx) == kAdapterError) r.error = v::sentence((path + ".error"), (int)(idx));
  if (!(mode == kAllAbsent)) {
    r.status_message = v::sentence((path + ".status_message"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.count_data_dependencies = v::scalar_i64((path + ".count_data_dependencies"), (int)(idx));
  }
  return r;
}

Probe build_probe(const std::string &path, int64_t idx, Mode mode, int64_t repeats, size_t bulk) {
  (void)path; (void)idx; (void)mode; (void)repeats; (void)bulk;
  Probe r;
  if (!(mode == kAllAbsent)) {
    r.id = v::guid((path + ".id"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    if (explicit_present(2, idx))
      r.opt_count.set(idx % 7 == 0 ? 0 : v::scalar_i32((path + ".opt_count"), (int)(idx)));
  }
  if (!(mode == kAllAbsent)) {
    if (explicit_present(3, idx))
      r.opt_label.set(idx % 7 == 0 ? std::string() : v::word((path + ".opt_label"), (int)(idx)));
  }
  if (!(mode == kAllAbsent)) {
    if (explicit_present(4, idx))
      r.opt_flag.set(idx % 7 == 0 ? false : v::scalar_bool((path + ".opt_flag"), (int)(idx)));
  }
  switch (idx % 5) {
    case 0: {
      r.body.set_as_int() = v::scalar_i64("Probe.as_int", (int)idx);
      break;
    }
    case 1: {
      r.body.set_as_text() = v::word("Probe.as_text", (int)idx);
      break;
    }
    case 2: {
      r.body.set_as_blob() = v::blob("Probe.as_blob", (int)idx, 16);
      break;
    }
    case 3: {
      r.body.set_as_stamp() = mk_timestamp((int)idx);
      break;
    }
    default: {
      (void)r.body.set_as_nothing();
      break;
    }
  }
  return r;
}

Empty build_empty(const std::string &path, int64_t idx, Mode mode, int64_t repeats, size_t bulk) {
  (void)path; (void)idx; (void)mode; (void)repeats; (void)bulk;
  Empty r;
  return r;
}

UploadResultData build_upload_result_data(const std::string &path, int64_t idx, Mode mode, int64_t repeats, size_t bulk) {
  (void)path; (void)idx; (void)mode; (void)repeats; (void)bulk;
  UploadResultData r;
  if (!(mode == kAllAbsent)) {
    r.session_id = v::guid((path + ".session_id"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.result_id = v::guid((path + ".result_id"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.data_chunk = v::bulk(bulk);
  }
  return r;
}

MetricsBatch build_metrics_batch(const std::string &path, int64_t idx, Mode mode, int64_t repeats, size_t bulk) {
  (void)path; (void)idx; (void)mode; (void)repeats; (void)bulk;
  MetricsBatch r;
  if (!(mode == kAllAbsent)) {
    r.id = v::guid((path + ".id"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.ticks.reserve(30);
    for (int64_t j = 0; j < 30; ++j)
      r.ticks.push_back(v::scalar_i64((path + ".ticks"), (int)(idx * 97 + j)));
  }
  if (!(mode == kAllAbsent)) {
    r.values.reserve(30);
    for (int64_t j = 0; j < 30; ++j)
      r.values.push_back(v::scalar_double((path + ".values"), (int)(idx * 97 + j)));
  }
  if (!(mode == kAllAbsent)) {
    r.codes.reserve(30);
    for (int64_t j = 0; j < 30; ++j)
      r.codes.push_back(v::scalar_i32((path + ".codes"), (int)(idx * 97 + j)));
  }
  if (!(mode == kAllAbsent)) {
    r.flags.reserve(30);
    for (int64_t j = 0; j < 30; ++j)
      r.flags.push_back(v::scalar_bool((path + ".flags"), (int)(idx * 97 + j)));
  }
  if (!(mode == kAllAbsent)) {
    r.statuses.reserve(30);
    for (int64_t j = 0; j < 30; ++j)
      r.statuses.push_back(shapes::TaskStatus(v::enum_value(shapes::tbl_TaskStatus(), (int)(idx * 97 + j))));
  }
  return r;
}

Pair build_pair(const std::string &path, int64_t idx, Mode mode, int64_t repeats, size_t bulk) {
  (void)path; (void)idx; (void)mode; (void)repeats; (void)bulk;
  Pair r;
  if (!(mode == kAllAbsent)) {
    r.key = v::word((path + ".key"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.value = v::scalar_i32((path + ".value"), (int)(idx));
  }
  return r;
}

ListResultsResponse build_list_results_response(const std::string &path, int64_t idx, Mode mode, int64_t repeats, size_t bulk) {
  (void)path; (void)idx; (void)mode; (void)repeats; (void)bulk;
  ListResultsResponse r;
  if (!(mode == kAllAbsent)) {
    r.results.reserve((size_t)repeats);
    for (int64_t j = 0; j < repeats; ++j)
      r.results.push_back(build_result_raw((path + ".results"), j, mode, repeats, bulk));
  }
  if (!(mode == kAllAbsent)) {
    r.page = v::scalar_i32((path + ".page"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.total = v::scalar_i32((path + ".total"), (int)(idx));
  }
  return r;
}

ListTasksDetailedResponse build_list_tasks_detailed_response(const std::string &path, int64_t idx, Mode mode, int64_t repeats, size_t bulk) {
  (void)path; (void)idx; (void)mode; (void)repeats; (void)bulk;
  ListTasksDetailedResponse r;
  if (!(mode == kAllAbsent)) {
    r.tasks.reserve((size_t)repeats);
    for (int64_t j = 0; j < repeats; ++j)
      r.tasks.push_back(build_task_detailed((path + ".tasks"), j, mode, repeats, bulk));
  }
  if (!(mode == kAllAbsent)) {
    r.page = v::scalar_i32((path + ".page"), (int)(idx));
  }
  if (!(mode == kAllAbsent)) {
    r.total = v::scalar_i32((path + ".total"), (int)(idx));
  }
  return r;
}

ListTaskSummaryResponse build_list_task_summary_response(const std::string &path, int64_t idx, Mode mode, int64_t repeats, size_t bulk) {
  (void)path; (void)idx; (void)mode; (void)repeats; (void)bulk;
  ListTaskSummaryResponse r;
  if (!(mode == kAllAbsent)) {
    r.tasks.reserve((size_t)repeats);
    for (int64_t j = 0; j < repeats; ++j)
      r.tasks.push_back(build_task_summary((path + ".tasks"), j, mode, repeats, bulk));
  }
  return r;
}

ListProbeResponse build_list_probe_response(const std::string &path, int64_t idx, Mode mode, int64_t repeats, size_t bulk) {
  (void)path; (void)idx; (void)mode; (void)repeats; (void)bulk;
  ListProbeResponse r;
  if (!(mode == kAllAbsent)) {
    r.probes.reserve((size_t)repeats);
    for (int64_t j = 0; j < repeats; ++j)
      r.probes.push_back(build_probe((path + ".probes"), j, mode, repeats, bulk));
  }
  return r;
}

ListMetricsResponse build_list_metrics_response(const std::string &path, int64_t idx, Mode mode, int64_t repeats, size_t bulk) {
  (void)path; (void)idx; (void)mode; (void)repeats; (void)bulk;
  ListMetricsResponse r;
  if (!(mode == kAllAbsent)) {
    r.batches.reserve((size_t)repeats);
    for (int64_t j = 0; j < repeats; ++j)
      r.batches.push_back(build_metrics_batch((path + ".batches"), j, mode, repeats, bulk));
  }
  return r;
}

UploadResultDataMessage build_upload_result_data_message(const std::string &path, int64_t idx, Mode mode, int64_t repeats, size_t bulk) {
  (void)path; (void)idx; (void)mode; (void)repeats; (void)bulk;
  UploadResultDataMessage r;
  if (!(mode == kAllAbsent)) {
    r.upload.set(build_upload_result_data((path + ".upload"), idx, mode, repeats, bulk));
  }
  return r;
}

DualResponse build_dual_response(const std::string &path, int64_t idx, Mode mode, int64_t repeats, size_t bulk) {
  (void)path; (void)idx; (void)mode; (void)repeats; (void)bulk;
  DualResponse r;
  if (!(mode == kAllAbsent)) {
    r.left.reserve((size_t)repeats);
    for (int64_t j = 0; j < repeats; ++j)
      r.left.push_back(build_pair((path + ".left"), j, mode, repeats, bulk));
  }
  if (!(mode == kAllAbsent)) {
    r.right.reserve((size_t)repeats);
    for (int64_t j = 0; j < repeats; ++j)
      r.right.push_back(build_pair((path + ".right"), j, mode, repeats, bulk));
  }
  return r;
}

// ---- payloads -------------------------------------------------------

// P1.1: ListResultsResponse, full
ListResultsResponse payload_p1_1() {
  ListResultsResponse r;
  static const int64_t kReps[1] = {3};
  r.results.reserve(4);
  for (int64_t j = 0; j < 4; ++j)
    r.results.push_back(build_result_raw("ResultRaw", j, kFull, kReps[j % 1], 0));
  r.page = 1;
  r.total = 4;
  return r;
}

// P1.2: ListResultsResponse, full
ListResultsResponse payload_p1_2() {
  ListResultsResponse r;
  static const int64_t kReps[1] = {3};
  r.results.reserve(1000);
  for (int64_t j = 0; j < 1000; ++j)
    r.results.push_back(build_result_raw("ResultRaw", j, kFull, kReps[j % 1], 0));
  r.page = 1;
  r.total = 1000;
  return r;
}

// P1.3: ListResultsResponse, all_absent
ListResultsResponse payload_p1_3() {
  ListResultsResponse r;
  static const int64_t kReps[1] = {3};
  r.results.reserve(300);
  for (int64_t j = 0; j < 300; ++j)
    r.results.push_back(build_result_raw("ResultRaw", j, kAllAbsent, kReps[j % 1], 0));
  r.page = 1;
  r.total = 300;
  return r;
}

// P2.1: ListTasksDetailedResponse, full
ListTasksDetailedResponse payload_p2_1() {
  ListTasksDetailedResponse r;
  static const int64_t kReps[1] = {3};
  r.tasks.reserve(1);
  for (int64_t j = 0; j < 1; ++j)
    r.tasks.push_back(build_task_detailed("TaskDetailed", j, kFull, kReps[j % 1], 0));
  r.page = 1;
  r.total = 1;
  return r;
}

// P2.2: ListTasksDetailedResponse, full
ListTasksDetailedResponse payload_p2_2() {
  ListTasksDetailedResponse r;
  static const int64_t kReps[1] = {3};
  r.tasks.reserve(500);
  for (int64_t j = 0; j < 500; ++j)
    r.tasks.push_back(build_task_detailed("TaskDetailed", j, kFull, kReps[j % 1], 0));
  r.page = 1;
  r.total = 500;
  return r;
}

// P2.3: ListTasksDetailedResponse, full
ListTasksDetailedResponse payload_p2_3() {
  ListTasksDetailedResponse r;
  static const int64_t kReps[1] = {30};
  r.tasks.reserve(125);
  for (int64_t j = 0; j < 125; ++j)
    r.tasks.push_back(build_task_detailed("TaskDetailed", j, kFull, kReps[j % 1], 0));
  r.page = 1;
  r.total = 125;
  return r;
}

// P2.4: ListTasksDetailedResponse, full
ListTasksDetailedResponse payload_p2_4() {
  ListTasksDetailedResponse r;
  static const int64_t kReps[2] = {3, 150};
  r.tasks.reserve(80);
  for (int64_t j = 0; j < 80; ++j)
    r.tasks.push_back(build_task_detailed("TaskDetailed", j, kFull, kReps[j % 2], 0));
  r.page = 1;
  r.total = 80;
  return r;
}

// P2.5: ListTasksDetailedResponse, half_absent
ListTasksDetailedResponse payload_p2_5() {
  ListTasksDetailedResponse r;
  static const int64_t kReps[1] = {3};
  r.tasks.reserve(20);
  for (int64_t j = 0; j < 20; ++j)
    r.tasks.push_back(build_task_detailed("TaskDetailed", j, kHalfAbsent, kReps[j % 1], 0));
  r.page = 1;
  r.total = 20;
  return r;
}

// P3.1: ListProbeResponse, full
ListProbeResponse payload_p3_1() {
  ListProbeResponse r;
  static const int64_t kReps[1] = {3};
  r.probes.reserve(200);
  for (int64_t j = 0; j < 200; ++j)
    r.probes.push_back(build_probe("Probe", j, kFull, kReps[j % 1], 0));
  return r;
}

// P4.1: ListTaskSummaryResponse, full
ListTaskSummaryResponse payload_p4_1() {
  ListTaskSummaryResponse r;
  static const int64_t kReps[1] = {3};
  r.tasks.reserve(200);
  for (int64_t j = 0; j < 200; ++j)
    r.tasks.push_back(build_task_summary("TaskSummary", j, kFull, kReps[j % 1], 0));
  return r;
}

// P5.1: UploadResultDataMessage, full
UploadResultDataMessage payload_p5_1() {
  UploadResultDataMessage r;
  r.upload.set(build_upload_result_data("UploadResultData", 0, kFull, 3, 36));
  return r;
}

// P5.2: UploadResultDataMessage, full
UploadResultDataMessage payload_p5_2() {
  UploadResultDataMessage r;
  r.upload.set(build_upload_result_data("UploadResultData", 0, kFull, 3, 65536));
  return r;
}

// P5.3: UploadResultDataMessage, full
UploadResultDataMessage payload_p5_3() {
  UploadResultDataMessage r;
  r.upload.set(build_upload_result_data("UploadResultData", 0, kFull, 3, 1048576));
  return r;
}

// P5.4: UploadResultDataMessage, full
UploadResultDataMessage payload_p5_4() {
  UploadResultDataMessage r;
  r.upload.set(build_upload_result_data("UploadResultData", 0, kFull, 3, 4194304));
  return r;
}

// P6.1: ListMetricsResponse, full
ListMetricsResponse payload_p6_1() {
  ListMetricsResponse r;
  static const int64_t kReps[1] = {3};
  r.batches.reserve(200);
  for (int64_t j = 0; j < 200; ++j)
    r.batches.push_back(build_metrics_batch("MetricsBatch", j, kFull, kReps[j % 1], 0));
  return r;
}

// P7.1 has no builder: no canonical writer can produce it, so M7 is
// validated by DECODE (design/SHAPES.md, P7.1's row).

const char *const kCovered[] = {"P1.1", "P1.2", "P1.3", "P2.1", "P2.2", "P2.3", "P2.4", "P2.5", "P3.1", "P4.1", "P5.1", "P5.2", "P5.3", "P5.4", "P6.1"};
const int kNumCovered = 15;

}  // namespace build
}  // namespace shapes
