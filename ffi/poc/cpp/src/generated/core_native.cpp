// @generated by ffi/poc/cpp/gen/generate.py from ffi/schema/shapes.json. Do not edit.

// Arm `core-native-cpp`: the same generated traversal as the core behind the C ABI, over
// the same facade objects, with no boundary at all. README R3's no-boundary control.
//
// R5's second half applies to this arm in the opposite direction from the FFI one: an arm
// named "no boundary" is only a control if it is not FUSED INTO THE BENCHMARK LOOP. In C++
// that question is `-flto` over this file, and `gen/boundary.sh` answers it from the built
// artifact rather than from a claim.
#include "generated/core_native.h"

#include <cstring>

namespace shapes {
namespace native {

static void enc_timestamp(const Timestamp &o, ak::Enc *e);
static void dec_timestamp(ak::Dec *d, Timestamp *out);
static void enc_duration(const Duration &o, ak::Enc *e);
static void dec_duration(ak::Dec *d, Duration *out);
static void enc_result_raw(const ResultRaw &o, ak::Enc *e);
static void dec_result_raw(ak::Dec *d, ResultRaw *out);
static void enc_task_options(const TaskOptions &o, ak::Enc *e);
static void dec_task_options(ak::Dec *d, TaskOptions *out);
static void enc_task_output(const TaskOutput &o, ak::Enc *e);
static void dec_task_output(ak::Dec *d, TaskOutput *out);
static void enc_task_detailed(const TaskDetailed &o, ak::Enc *e);
static void dec_task_detailed(ak::Dec *d, TaskDetailed *out);
static void enc_task_summary(const TaskSummary &o, ak::Enc *e);
static void dec_task_summary(ak::Dec *d, TaskSummary *out);
static void enc_probe(const Probe &o, ak::Enc *e);
static void dec_probe(ak::Dec *d, Probe *out);
static void enc_empty(const Empty &o, ak::Enc *e);
static void dec_empty(ak::Dec *d, Empty *out);
static void enc_upload_result_data(const UploadResultData &o, ak::Enc *e);
static void dec_upload_result_data(ak::Dec *d, UploadResultData *out);
static void enc_metrics_batch(const MetricsBatch &o, ak::Enc *e);
static void dec_metrics_batch(ak::Dec *d, MetricsBatch *out);
static void enc_pair(const Pair &o, ak::Enc *e);
static void dec_pair(ak::Dec *d, Pair *out);
static void enc_list_results_response(const ListResultsResponse &o, ak::Enc *e);
static void dec_list_results_response(ak::Dec *d, ListResultsResponse *out);
static void enc_list_tasks_detailed_response(const ListTasksDetailedResponse &o, ak::Enc *e);
static void dec_list_tasks_detailed_response(ak::Dec *d, ListTasksDetailedResponse *out);
static void enc_list_task_summary_response(const ListTaskSummaryResponse &o, ak::Enc *e);
static void dec_list_task_summary_response(ak::Dec *d, ListTaskSummaryResponse *out);
static void enc_list_probe_response(const ListProbeResponse &o, ak::Enc *e);
static void dec_list_probe_response(ak::Dec *d, ListProbeResponse *out);
static void enc_list_metrics_response(const ListMetricsResponse &o, ak::Enc *e);
static void dec_list_metrics_response(ak::Dec *d, ListMetricsResponse *out);
static void enc_upload_result_data_message(const UploadResultDataMessage &o, ak::Enc *e);
static void dec_upload_result_data_message(ak::Dec *d, UploadResultDataMessage *out);
static void enc_dual_response(const DualResponse &o, ak::Enc *e);
static void dec_dual_response(ak::Dec *d, DualResponse *out);

static void enc_timestamp(const Timestamp &o, ak::Enc *e) {
  (void)o; (void)e;
  if (o.seconds != 0) e->varint_field(1, (uint64_t)o.seconds);
  if (o.nanos != 0) e->varint_field(2, (uint64_t)(int64_t)o.nanos);
}

static void enc_duration(const Duration &o, ak::Enc *e) {
  (void)o; (void)e;
  if (o.seconds != 0) e->varint_field(1, (uint64_t)o.seconds);
  if (o.nanos != 0) e->varint_field(2, (uint64_t)(int64_t)o.nanos);
}

static void enc_result_raw(const ResultRaw &o, ak::Enc *e) {
  (void)o; (void)e;
  if (!o.session_id.empty()) e->blob_field(1, o.session_id);
  if (!o.name.empty()) e->blob_field(2, o.name);
  if (!o.owner_task_id.empty()) e->blob_field(3, o.owner_task_id);
  if (o.status.v != 0) e->varint_field(4, (uint64_t)(int64_t)o.status.v);
  if (o.created_at.has_value()) {
    ak::Mark mk = e->begin(5, 0);
    enc_timestamp(*o.created_at, e);
    e->end(mk);
  }
  if (o.completed_at.has_value()) {
    ak::Mark mk = e->begin(6, 1);
    enc_timestamp(*o.completed_at, e);
    e->end(mk);
  }
  if (!o.result_id.empty()) e->blob_field(8, o.result_id);
  if (o.size != 0) e->varint_field(9, (uint64_t)o.size);
  if (!o.created_by.empty()) e->blob_field(10, o.created_by);
  if (!o.opaque_id.empty()) e->blob_field(11, o.opaque_id);
  if (o.manual_deletion) e->varint_field(12, 1);
}

static void enc_task_options(const TaskOptions &o, ak::Enc *e) {
  (void)o; (void)e;
  for (std::map<std::string, std::string>::const_iterator it = o.options.begin();
       it != o.options.end(); ++it) {
    ak::Mark mk = e->begin(1, 2);
    if (!it->first.empty()) e->blob_field(1, it->first);
    if (!it->second.empty()) e->blob_field(2, it->second);
    e->end(mk);
  }
  if (o.max_duration.has_value()) {
    ak::Mark mk = e->begin(2, 3);
    enc_duration(*o.max_duration, e);
    e->end(mk);
  }
  if (o.max_retries != 0) e->varint_field(3, (uint64_t)(int64_t)o.max_retries);
  if (o.priority != 0) e->varint_field(4, (uint64_t)(int64_t)o.priority);
  if (!o.partition_id.empty()) e->blob_field(5, o.partition_id);
  if (!o.application_name.empty()) e->blob_field(6, o.application_name);
  if (!o.application_version.empty()) e->blob_field(7, o.application_version);
  if (!o.application_namespace.empty()) e->blob_field(8, o.application_namespace);
  if (!o.application_service.empty()) e->blob_field(9, o.application_service);
  if (!o.engine_type.empty()) e->blob_field(10, o.engine_type);
}

static void enc_task_output(const TaskOutput &o, ak::Enc *e) {
  (void)o; (void)e;
  if (o.success) e->varint_field(1, 1);
  if (!o.error.empty()) e->blob_field(2, o.error);
}

static void enc_task_detailed(const TaskDetailed &o, ak::Enc *e) {
  (void)o; (void)e;
  if (!o.id.empty()) e->blob_field(1, o.id);
  if (!o.session_id.empty()) e->blob_field(2, o.session_id);
  if (!o.owner_pod_id.empty()) e->blob_field(3, o.owner_pod_id);
  for (size_t i = 0; i < o.parent_task_ids.size(); ++i) e->blob_field(4, o.parent_task_ids[i]);
  for (size_t i = 0; i < o.data_dependencies.size(); ++i) e->blob_field(5, o.data_dependencies[i]);
  for (size_t i = 0; i < o.expected_output_ids.size(); ++i) e->blob_field(6, o.expected_output_ids[i]);
  for (size_t i = 0; i < o.retry_of_ids.size(); ++i) e->blob_field(7, o.retry_of_ids[i]);
  if (o.status.v != 0) e->varint_field(8, (uint64_t)(int64_t)o.status.v);
  if (!o.status_message.empty()) e->blob_field(9, o.status_message);
  if (o.options.has_value()) {
    ak::Mark mk = e->begin(10, 4);
    enc_task_options(*o.options, e);
    e->end(mk);
  }
  if (o.created_at.has_value()) {
    ak::Mark mk = e->begin(11, 5);
    enc_timestamp(*o.created_at, e);
    e->end(mk);
  }
  if (o.submitted_at.has_value()) {
    ak::Mark mk = e->begin(12, 6);
    enc_timestamp(*o.submitted_at, e);
    e->end(mk);
  }
  if (o.started_at.has_value()) {
    ak::Mark mk = e->begin(13, 7);
    enc_timestamp(*o.started_at, e);
    e->end(mk);
  }
  if (o.ended_at.has_value()) {
    ak::Mark mk = e->begin(14, 8);
    enc_timestamp(*o.ended_at, e);
    e->end(mk);
  }
  if (o.pod_ttl.has_value()) {
    ak::Mark mk = e->begin(15, 9);
    enc_timestamp(*o.pod_ttl, e);
    e->end(mk);
  }
  if (o.output.has_value()) {
    ak::Mark mk = e->begin(16, 10);
    enc_task_output(*o.output, e);
    e->end(mk);
  }
  if (!o.pod_hostname.empty()) e->blob_field(17, o.pod_hostname);
  if (o.received_at.has_value()) {
    ak::Mark mk = e->begin(18, 11);
    enc_timestamp(*o.received_at, e);
    e->end(mk);
  }
  if (o.acquired_at.has_value()) {
    ak::Mark mk = e->begin(19, 12);
    enc_timestamp(*o.acquired_at, e);
    e->end(mk);
  }
  if (o.creation_to_end_duration.has_value()) {
    ak::Mark mk = e->begin(20, 13);
    enc_duration(*o.creation_to_end_duration, e);
    e->end(mk);
  }
  if (o.processing_to_end_duration.has_value()) {
    ak::Mark mk = e->begin(21, 14);
    enc_duration(*o.processing_to_end_duration, e);
    e->end(mk);
  }
  if (!o.initial_task_id.empty()) e->blob_field(22, o.initial_task_id);
  if (o.received_to_end_duration.has_value()) {
    ak::Mark mk = e->begin(23, 15);
    enc_duration(*o.received_to_end_duration, e);
    e->end(mk);
  }
  if (o.processed_at.has_value()) {
    ak::Mark mk = e->begin(24, 16);
    enc_timestamp(*o.processed_at, e);
    e->end(mk);
  }
  if (o.fetched_at.has_value()) {
    ak::Mark mk = e->begin(25, 17);
    enc_timestamp(*o.fetched_at, e);
    e->end(mk);
  }
  if (!o.payload_id.empty()) e->blob_field(26, o.payload_id);
  if (!o.created_by.empty()) e->blob_field(27, o.created_by);
}

static void enc_task_summary(const TaskSummary &o, ak::Enc *e) {
  (void)o; (void)e;
  if (!o.id.empty()) e->blob_field(1, o.id);
  if (!o.session_id.empty()) e->blob_field(2, o.session_id);
  if (o.options.has_value()) {
    ak::Mark mk = e->begin(3, 18);
    enc_task_options(*o.options, e);
    e->end(mk);
  }
  if (o.status.v != 0) e->varint_field(4, (uint64_t)(int64_t)o.status.v);
  if (o.created_at.has_value()) {
    ak::Mark mk = e->begin(5, 19);
    enc_timestamp(*o.created_at, e);
    e->end(mk);
  }
  if (!o.error.empty()) e->blob_field(8, o.error);
  if (!o.status_message.empty()) e->blob_field(9, o.status_message);
  if (o.count_data_dependencies != 0) e->varint_field(11, (uint64_t)o.count_data_dependencies);
}

static void enc_probe(const Probe &o, ak::Enc *e) {
  (void)o; (void)e;
  if (!o.id.empty()) e->blob_field(1, o.id);
  if (o.opt_count.has_value()) e->varint_field(2, (uint64_t)(int64_t)*o.opt_count);
  if (o.opt_label.has_value()) e->blob_field(3, *o.opt_label);
  if (o.opt_flag.has_value()) e->varint_field(4, (uint64_t)(*o.opt_flag ? 1 : 0));
  switch (o.body.which()) {
    case ProbeBody::kAsInt: {
      e->varint_field(10, (uint64_t)o.body.as_int());
      break;
    }
    case ProbeBody::kAsText: {
      e->blob_field(11, o.body.as_text());
      break;
    }
    case ProbeBody::kAsBlob: {
      e->blob_field(12, o.body.as_blob());
      break;
    }
    case ProbeBody::kAsStamp: {
      { ak::Mark mk = e->begin(13, 20);
        enc_timestamp(o.body.as_stamp(), e);
        e->end(mk); }
      break;
    }
    case ProbeBody::kAsNothing: {
      { ak::Mark mk = e->begin(14, 21);
        enc_empty(o.body.as_nothing(), e);
        e->end(mk); }
      break;
    }
    default: break;
  }
}

static void enc_empty(const Empty &o, ak::Enc *e) {
  (void)o; (void)e;
}

static void enc_upload_result_data(const UploadResultData &o, ak::Enc *e) {
  (void)o; (void)e;
  if (!o.session_id.empty()) e->blob_field(1, o.session_id);
  if (!o.result_id.empty()) e->blob_field(2, o.result_id);
  if (!o.data_chunk.empty()) e->blob_field(3, o.data_chunk);
}

static void enc_metrics_batch(const MetricsBatch &o, ak::Enc *e) {
  (void)o; (void)e;
  if (!o.id.empty()) e->blob_field(1, o.id);
  if (!o.ticks.empty()) {
    ak::Mark mk = e->begin(2, 22);
    for (size_t i = 0; i < o.ticks.size(); ++i)
      e->varint((uint64_t)o.ticks[i]);
    e->end(mk);
  }
  if (!o.values.empty()) {
    ak::Mark mk = e->begin(3, 23);
    for (size_t i = 0; i < o.values.size(); ++i)
      { double dv = o.values[i]; uint64_t bits; std::memcpy(&bits, &dv, 8);
        for (int b = 0; b < 8; ++b) e->buf.push_back((uint8_t)(bits >> (8*b))); }
    e->end(mk);
  }
  if (!o.codes.empty()) {
    ak::Mark mk = e->begin(4, 24);
    for (size_t i = 0; i < o.codes.size(); ++i)
      e->varint((uint64_t)(int64_t)o.codes[i]);
    e->end(mk);
  }
  if (!o.flags.empty()) {
    ak::Mark mk = e->begin(5, 25);
    for (size_t i = 0; i < o.flags.size(); ++i)
      e->varint(o.flags[i] ? 1 : 0);
    e->end(mk);
  }
  if (!o.statuses.empty()) {
    ak::Mark mk = e->begin(6, 26);
    for (size_t i = 0; i < o.statuses.size(); ++i)
      e->varint((uint64_t)(int64_t)o.statuses[i].v);
    e->end(mk);
  }
}

static void enc_pair(const Pair &o, ak::Enc *e) {
  (void)o; (void)e;
  if (!o.key.empty()) e->blob_field(1, o.key);
  if (o.value != 0) e->varint_field(2, (uint64_t)(int64_t)o.value);
}

static void enc_list_results_response(const ListResultsResponse &o, ak::Enc *e) {
  (void)o; (void)e;
  for (size_t i = 0; i < o.results.size(); ++i) {
    ak::Mark mk = e->begin(1, 27);
    enc_result_raw(o.results[i], e);
    e->end(mk);
  }
  if (o.page != 0) e->varint_field(2, (uint64_t)(int64_t)o.page);
  if (o.total != 0) e->varint_field(3, (uint64_t)(int64_t)o.total);
}

static void enc_list_tasks_detailed_response(const ListTasksDetailedResponse &o, ak::Enc *e) {
  (void)o; (void)e;
  for (size_t i = 0; i < o.tasks.size(); ++i) {
    ak::Mark mk = e->begin(1, 28);
    enc_task_detailed(o.tasks[i], e);
    e->end(mk);
  }
  if (o.page != 0) e->varint_field(2, (uint64_t)(int64_t)o.page);
  if (o.total != 0) e->varint_field(3, (uint64_t)(int64_t)o.total);
}

static void enc_list_task_summary_response(const ListTaskSummaryResponse &o, ak::Enc *e) {
  (void)o; (void)e;
  for (size_t i = 0; i < o.tasks.size(); ++i) {
    ak::Mark mk = e->begin(1, 29);
    enc_task_summary(o.tasks[i], e);
    e->end(mk);
  }
}

static void enc_list_probe_response(const ListProbeResponse &o, ak::Enc *e) {
  (void)o; (void)e;
  for (size_t i = 0; i < o.probes.size(); ++i) {
    ak::Mark mk = e->begin(1, 30);
    enc_probe(o.probes[i], e);
    e->end(mk);
  }
}

static void enc_list_metrics_response(const ListMetricsResponse &o, ak::Enc *e) {
  (void)o; (void)e;
  for (size_t i = 0; i < o.batches.size(); ++i) {
    ak::Mark mk = e->begin(1, 31);
    enc_metrics_batch(o.batches[i], e);
    e->end(mk);
  }
}

static void enc_upload_result_data_message(const UploadResultDataMessage &o, ak::Enc *e) {
  (void)o; (void)e;
  if (o.upload.has_value()) {
    ak::Mark mk = e->begin(1, 32);
    enc_upload_result_data(*o.upload, e);
    e->end(mk);
  }
}

static void enc_dual_response(const DualResponse &o, ak::Enc *e) {
  (void)o; (void)e;
  for (size_t i = 0; i < o.left.size(); ++i) {
    ak::Mark mk = e->begin(1, 33);
    enc_pair(o.left[i], e);
    e->end(mk);
  }
  for (size_t i = 0; i < o.right.size(); ++i) {
    ak::Mark mk = e->begin(2, 34);
    enc_pair(o.right[i], e);
    e->end(mk);
  }
}

static void dec_timestamp(ak::Dec *d, Timestamp *out) {
  (void)out;
  while (!d->at_end()) {
    uint64_t k = d->varint();
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0) { d->err = ak::ERR_MALFORMED; return; }
    switch (tag) {
      case 1: if (wire == 0) {
        out->seconds = (int64_t)d->varint();
        break; } else { d->skip(wire); break; }
      case 2: if (wire == 0) {
        out->nanos = (int32_t)d->varint();
        break; } else { d->skip(wire); break; }
      default: d->skip(wire); break;
    }
  }
}

static void dec_duration(ak::Dec *d, Duration *out) {
  (void)out;
  while (!d->at_end()) {
    uint64_t k = d->varint();
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0) { d->err = ak::ERR_MALFORMED; return; }
    switch (tag) {
      case 1: if (wire == 0) {
        out->seconds = (int64_t)d->varint();
        break; } else { d->skip(wire); break; }
      case 2: if (wire == 0) {
        out->nanos = (int32_t)d->varint();
        break; } else { d->skip(wire); break; }
      default: d->skip(wire); break;
    }
  }
}

static void dec_result_raw(ak::Dec *d, ResultRaw *out) {
  (void)out;
  while (!d->at_end()) {
    uint64_t k = d->varint();
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0) { d->err = ak::ERR_MALFORMED; return; }
    switch (tag) {
      case 1: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->session_id);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 2: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->name);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 3: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->owner_task_id);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 4: if (wire == 0) {
        out->status = ResultStatus((int32_t)d->varint());
        break; } else { d->skip(wire); break; }
      case 5: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        Timestamp &dst_ = out->created_at.emplace();
        dec_timestamp(&sub, &dst_);
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      case 6: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        Timestamp &dst_ = out->completed_at.emplace();
        dec_timestamp(&sub, &dst_);
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      case 8: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->result_id);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 9: if (wire == 0) {
        out->size = (int64_t)d->varint();
        break; } else { d->skip(wire); break; }
      case 10: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->created_by);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 11: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        out->opaque_id.assign((const char *)(d->buf + off), n);
        break; } else { d->skip(wire); break; }
      case 12: if (wire == 0) {
        out->manual_deletion = (d->varint() != 0);
        break; } else { d->skip(wire); break; }
      default: d->skip(wire); break;
    }
  }
}

static void dec_task_options(ak::Dec *d, TaskOptions *out) {
  (void)out;
  while (!d->at_end()) {
    uint64_t k = d->varint();
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0) { d->err = ak::ERR_MALFORMED; return; }
    switch (tag) {
      case 1: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        std::string k_, v_;
        while (!sub.at_end()) {
          uint64_t kk = sub.varint();
          uint32_t et = (uint32_t)(kk >> 3), ew = (uint32_t)(kk & 7);
          if (et == 1 && ew == 2) {
            size_t a, b; sub.len_body(&a, &b);
            int32_t rc = ak::decode_str(sub.buf + a, b, &k_);
            if (rc != 0) { d->err = rc; return; }
          } else if (et == 2 && ew == 2) {
            size_t a, b; sub.len_body(&a, &b);
            int32_t rc = ak::decode_str(sub.buf + a, b, &v_);
            if (rc != 0) { d->err = rc; return; }
          } else {
            sub.skip(ew);
          }
        }
        if (sub.err != 0) { d->err = sub.err; return; }
#if AK_CXX17
        // C++17: one insertion, no default-construct-then-assign.
        out->options.insert_or_assign(std::move(k_), std::move(v_));
#else
        out->options[k_] = v_;
#endif
        break; } else { d->skip(wire); break; }
      case 2: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        Duration &dst_ = out->max_duration.emplace();
        dec_duration(&sub, &dst_);
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      case 3: if (wire == 0) {
        out->max_retries = (int32_t)d->varint();
        break; } else { d->skip(wire); break; }
      case 4: if (wire == 0) {
        out->priority = (int32_t)d->varint();
        break; } else { d->skip(wire); break; }
      case 5: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->partition_id);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 6: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->application_name);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 7: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->application_version);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 8: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->application_namespace);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 9: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->application_service);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 10: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->engine_type);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      default: d->skip(wire); break;
    }
  }
}

static void dec_task_output(ak::Dec *d, TaskOutput *out) {
  (void)out;
  while (!d->at_end()) {
    uint64_t k = d->varint();
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0) { d->err = ak::ERR_MALFORMED; return; }
    switch (tag) {
      case 1: if (wire == 0) {
        out->success = (d->varint() != 0);
        break; } else { d->skip(wire); break; }
      case 2: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->error);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      default: d->skip(wire); break;
    }
  }
}

static void dec_task_detailed(ak::Dec *d, TaskDetailed *out) {
  (void)out;
  while (!d->at_end()) {
    uint64_t k = d->varint();
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0) { d->err = ak::ERR_MALFORMED; return; }
    switch (tag) {
      case 1: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->id);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 2: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->session_id);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 3: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->owner_pod_id);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 4: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
#if AK_CXX17
        // C++17: emplace_back returns the reference the writer needs.
        std::string &b_ = out->parent_task_ids.emplace_back();
#else
        out->parent_task_ids.push_back(std::string());
        std::string &b_ = out->parent_task_ids.back();
#endif
        int32_t rc = ak::decode_str(d->buf + off, n, &b_);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 5: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
#if AK_CXX17
        // C++17: emplace_back returns the reference the writer needs.
        std::string &b_ = out->data_dependencies.emplace_back();
#else
        out->data_dependencies.push_back(std::string());
        std::string &b_ = out->data_dependencies.back();
#endif
        int32_t rc = ak::decode_str(d->buf + off, n, &b_);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 6: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
#if AK_CXX17
        // C++17: emplace_back returns the reference the writer needs.
        std::string &b_ = out->expected_output_ids.emplace_back();
#else
        out->expected_output_ids.push_back(std::string());
        std::string &b_ = out->expected_output_ids.back();
#endif
        int32_t rc = ak::decode_str(d->buf + off, n, &b_);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 7: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
#if AK_CXX17
        // C++17: emplace_back returns the reference the writer needs.
        std::string &b_ = out->retry_of_ids.emplace_back();
#else
        out->retry_of_ids.push_back(std::string());
        std::string &b_ = out->retry_of_ids.back();
#endif
        int32_t rc = ak::decode_str(d->buf + off, n, &b_);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 8: if (wire == 0) {
        out->status = TaskStatus((int32_t)d->varint());
        break; } else { d->skip(wire); break; }
      case 9: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->status_message);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 10: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        TaskOptions &dst_ = out->options.emplace();
        dec_task_options(&sub, &dst_);
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      case 11: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        Timestamp &dst_ = out->created_at.emplace();
        dec_timestamp(&sub, &dst_);
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      case 12: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        Timestamp &dst_ = out->submitted_at.emplace();
        dec_timestamp(&sub, &dst_);
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      case 13: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        Timestamp &dst_ = out->started_at.emplace();
        dec_timestamp(&sub, &dst_);
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      case 14: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        Timestamp &dst_ = out->ended_at.emplace();
        dec_timestamp(&sub, &dst_);
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      case 15: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        Timestamp &dst_ = out->pod_ttl.emplace();
        dec_timestamp(&sub, &dst_);
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      case 16: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        TaskOutput &dst_ = out->output.emplace();
        dec_task_output(&sub, &dst_);
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      case 17: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->pod_hostname);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 18: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        Timestamp &dst_ = out->received_at.emplace();
        dec_timestamp(&sub, &dst_);
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      case 19: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        Timestamp &dst_ = out->acquired_at.emplace();
        dec_timestamp(&sub, &dst_);
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      case 20: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        Duration &dst_ = out->creation_to_end_duration.emplace();
        dec_duration(&sub, &dst_);
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      case 21: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        Duration &dst_ = out->processing_to_end_duration.emplace();
        dec_duration(&sub, &dst_);
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      case 22: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->initial_task_id);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 23: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        Duration &dst_ = out->received_to_end_duration.emplace();
        dec_duration(&sub, &dst_);
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      case 24: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        Timestamp &dst_ = out->processed_at.emplace();
        dec_timestamp(&sub, &dst_);
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      case 25: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        Timestamp &dst_ = out->fetched_at.emplace();
        dec_timestamp(&sub, &dst_);
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      case 26: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->payload_id);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 27: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->created_by);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      default: d->skip(wire); break;
    }
  }
}

static void dec_task_summary(ak::Dec *d, TaskSummary *out) {
  (void)out;
  while (!d->at_end()) {
    uint64_t k = d->varint();
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0) { d->err = ak::ERR_MALFORMED; return; }
    switch (tag) {
      case 1: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->id);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 2: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->session_id);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 3: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        TaskOptions &dst_ = out->options.emplace();
        dec_task_options(&sub, &dst_);
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      case 4: if (wire == 0) {
        out->status = TaskStatus((int32_t)d->varint());
        break; } else { d->skip(wire); break; }
      case 5: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        Timestamp &dst_ = out->created_at.emplace();
        dec_timestamp(&sub, &dst_);
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      case 8: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->error);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 9: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->status_message);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 11: if (wire == 0) {
        out->count_data_dependencies = (int64_t)d->varint();
        break; } else { d->skip(wire); break; }
      default: d->skip(wire); break;
    }
  }
}

static void dec_probe(ak::Dec *d, Probe *out) {
  (void)out;
  while (!d->at_end()) {
    uint64_t k = d->varint();
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0) { d->err = ak::ERR_MALFORMED; return; }
    switch (tag) {
      case 1: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->id);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 2: if (wire == 0) {
        out->opt_count.set((int32_t)d->varint());
        break; } else { d->skip(wire); break; }
      case 3: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        std::string &s_ = out->opt_label.emplace();
        int32_t rc = ak::decode_str(d->buf + off, n, &s_);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 4: if (wire == 0) {
        out->opt_flag.set((d->varint() != 0));
        break; } else { d->skip(wire); break; }
      case 10: if (wire == 0) { out->body.set_as_int() = (int64_t)d->varint(); break; } else { d->skip(wire); break; }
      case 11: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        std::string &s_ = out->body.set_as_text();
        int32_t rc = ak::decode_str(d->buf + off, n, &s_);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 12: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        std::string &s_ = out->body.set_as_blob();
        s_.assign((const char *)(d->buf + off), n);
        break; } else { d->skip(wire); break; }
      case 13: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        Timestamp &c_ = out->body.set_as_stamp();
        dec_timestamp(&sub, &c_);
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      case 14: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        Empty &c_ = out->body.set_as_nothing();
        dec_empty(&sub, &c_);
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      default: d->skip(wire); break;
    }
  }
}

static void dec_empty(ak::Dec *d, Empty *out) {
  (void)out;
  while (!d->at_end()) {
    uint64_t k = d->varint();
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0) { d->err = ak::ERR_MALFORMED; return; }
    switch (tag) {
      default: d->skip(wire); break;
    }
  }
}

static void dec_upload_result_data(ak::Dec *d, UploadResultData *out) {
  (void)out;
  while (!d->at_end()) {
    uint64_t k = d->varint();
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0) { d->err = ak::ERR_MALFORMED; return; }
    switch (tag) {
      case 1: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->session_id);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 2: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->result_id);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 3: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        out->data_chunk.assign((const char *)(d->buf + off), n);
        break; } else { d->skip(wire); break; }
      default: d->skip(wire); break;
    }
  }
}

static void dec_metrics_batch(ak::Dec *d, MetricsBatch *out) {
  (void)out;
  while (!d->at_end()) {
    uint64_t k = d->varint();
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0) { d->err = ak::ERR_MALFORMED; return; }
    switch (tag) {
      case 1: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->id);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 2: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        while (!sub.at_end()) out->ticks.push_back((int64_t)sub.varint());
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      } else {
        // A decoder must accept the unpacked form too: proto3 makes packed
        // the default, it does not make the other form illegal.
        out->ticks.push_back((int64_t)d->varint()); break;
      }
      case 3: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        while (!sub.at_end()) out->values.push_back(sub.f64());
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      } else {
        // A decoder must accept the unpacked form too: proto3 makes packed
        // the default, it does not make the other form illegal.
        out->values.push_back(d->f64()); break;
      }
      case 4: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        while (!sub.at_end()) out->codes.push_back((int32_t)sub.varint());
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      } else {
        // A decoder must accept the unpacked form too: proto3 makes packed
        // the default, it does not make the other form illegal.
        out->codes.push_back((int32_t)d->varint()); break;
      }
      case 5: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        while (!sub.at_end()) out->flags.push_back((sub.varint() != 0));
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      } else {
        // A decoder must accept the unpacked form too: proto3 makes packed
        // the default, it does not make the other form illegal.
        out->flags.push_back((d->varint() != 0)); break;
      }
      case 6: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        while (!sub.at_end()) out->statuses.push_back(TaskStatus((int32_t)sub.varint()));
        if (sub.err != 0) { d->err = sub.err; return; }
        break;
      } else {
        // A decoder must accept the unpacked form too: proto3 makes packed
        // the default, it does not make the other form illegal.
        out->statuses.push_back(TaskStatus((int32_t)d->varint())); break;
      }
      default: d->skip(wire); break;
    }
  }
}

static void dec_pair(ak::Dec *d, Pair *out) {
  (void)out;
  while (!d->at_end()) {
    uint64_t k = d->varint();
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0) { d->err = ak::ERR_MALFORMED; return; }
    switch (tag) {
      case 1: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        int32_t rc = ak::decode_str(d->buf + off, n, &out->key);
        if (rc != 0) { d->err = rc; return; }
        break; } else { d->skip(wire); break; }
      case 2: if (wire == 0) {
        out->value = (int32_t)d->varint();
        break; } else { d->skip(wire); break; }
      default: d->skip(wire); break;
    }
  }
}

static void dec_list_results_response(ak::Dec *d, ListResultsResponse *out) {
  (void)out;
  while (!d->at_end()) {
    uint64_t k = d->varint();
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0) { d->err = ak::ERR_MALFORMED; return; }
    switch (tag) {
      case 1: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        out->results.push_back(ResultRaw());
        dec_result_raw(&sub, &out->results.back());
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      case 2: if (wire == 0) {
        out->page = (int32_t)d->varint();
        break; } else { d->skip(wire); break; }
      case 3: if (wire == 0) {
        out->total = (int32_t)d->varint();
        break; } else { d->skip(wire); break; }
      default: d->skip(wire); break;
    }
  }
}

static void dec_list_tasks_detailed_response(ak::Dec *d, ListTasksDetailedResponse *out) {
  (void)out;
  while (!d->at_end()) {
    uint64_t k = d->varint();
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0) { d->err = ak::ERR_MALFORMED; return; }
    switch (tag) {
      case 1: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        out->tasks.push_back(TaskDetailed());
        dec_task_detailed(&sub, &out->tasks.back());
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      case 2: if (wire == 0) {
        out->page = (int32_t)d->varint();
        break; } else { d->skip(wire); break; }
      case 3: if (wire == 0) {
        out->total = (int32_t)d->varint();
        break; } else { d->skip(wire); break; }
      default: d->skip(wire); break;
    }
  }
}

static void dec_list_task_summary_response(ak::Dec *d, ListTaskSummaryResponse *out) {
  (void)out;
  while (!d->at_end()) {
    uint64_t k = d->varint();
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0) { d->err = ak::ERR_MALFORMED; return; }
    switch (tag) {
      case 1: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        out->tasks.push_back(TaskSummary());
        dec_task_summary(&sub, &out->tasks.back());
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      default: d->skip(wire); break;
    }
  }
}

static void dec_list_probe_response(ak::Dec *d, ListProbeResponse *out) {
  (void)out;
  while (!d->at_end()) {
    uint64_t k = d->varint();
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0) { d->err = ak::ERR_MALFORMED; return; }
    switch (tag) {
      case 1: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        out->probes.push_back(Probe());
        dec_probe(&sub, &out->probes.back());
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      default: d->skip(wire); break;
    }
  }
}

static void dec_list_metrics_response(ak::Dec *d, ListMetricsResponse *out) {
  (void)out;
  while (!d->at_end()) {
    uint64_t k = d->varint();
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0) { d->err = ak::ERR_MALFORMED; return; }
    switch (tag) {
      case 1: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        out->batches.push_back(MetricsBatch());
        dec_metrics_batch(&sub, &out->batches.back());
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      default: d->skip(wire); break;
    }
  }
}

static void dec_upload_result_data_message(ak::Dec *d, UploadResultDataMessage *out) {
  (void)out;
  while (!d->at_end()) {
    uint64_t k = d->varint();
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0) { d->err = ak::ERR_MALFORMED; return; }
    switch (tag) {
      case 1: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        UploadResultData &dst_ = out->upload.emplace();
        dec_upload_result_data(&sub, &dst_);
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      default: d->skip(wire); break;
    }
  }
}

static void dec_dual_response(ak::Dec *d, DualResponse *out) {
  (void)out;
  while (!d->at_end()) {
    uint64_t k = d->varint();
    uint32_t tag = (uint32_t)(k >> 3), wire = (uint32_t)(k & 7);
    if (tag == 0) { d->err = ak::ERR_MALFORMED; return; }
    switch (tag) {
      case 1: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        out->left.push_back(Pair());
        dec_pair(&sub, &out->left.back());
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      case 2: if (wire == 2) {
        size_t off, n; d->len_body(&off, &n);
        ak::Dec sub(d->buf + off, n);
        out->right.push_back(Pair());
        dec_pair(&sub, &out->right.back());
        if (sub.err != 0) { d->err = sub.err; return; }
        break; } else { d->skip(wire); break; }
      default: d->skip(wire); break;
    }
  }
}

void encode_into_list_results_response(const ListResultsResponse &o, ak::Enc *e) {
  e->reset();
  enc_list_results_response(o, e);
}

int32_t decode_list_results_response(const uint8_t *b, size_t n, ListResultsResponse *out) {
  ak::Dec d(b, n);
  dec_list_results_response(&d, out);
  return d.err;
}

void encode_into_list_tasks_detailed_response(const ListTasksDetailedResponse &o, ak::Enc *e) {
  e->reset();
  enc_list_tasks_detailed_response(o, e);
}

int32_t decode_list_tasks_detailed_response(const uint8_t *b, size_t n, ListTasksDetailedResponse *out) {
  ak::Dec d(b, n);
  dec_list_tasks_detailed_response(&d, out);
  return d.err;
}

void encode_into_list_probe_response(const ListProbeResponse &o, ak::Enc *e) {
  e->reset();
  enc_list_probe_response(o, e);
}

int32_t decode_list_probe_response(const uint8_t *b, size_t n, ListProbeResponse *out) {
  ak::Dec d(b, n);
  dec_list_probe_response(&d, out);
  return d.err;
}

void encode_into_list_task_summary_response(const ListTaskSummaryResponse &o, ak::Enc *e) {
  e->reset();
  enc_list_task_summary_response(o, e);
}

int32_t decode_list_task_summary_response(const uint8_t *b, size_t n, ListTaskSummaryResponse *out) {
  ak::Dec d(b, n);
  dec_list_task_summary_response(&d, out);
  return d.err;
}

void encode_into_upload_result_data_message(const UploadResultDataMessage &o, ak::Enc *e) {
  e->reset();
  enc_upload_result_data_message(o, e);
}

int32_t decode_upload_result_data_message(const uint8_t *b, size_t n, UploadResultDataMessage *out) {
  ak::Dec d(b, n);
  dec_upload_result_data_message(&d, out);
  return d.err;
}

void encode_into_list_metrics_response(const ListMetricsResponse &o, ak::Enc *e) {
  e->reset();
  enc_list_metrics_response(o, e);
}

int32_t decode_list_metrics_response(const uint8_t *b, size_t n, ListMetricsResponse *out) {
  ak::Dec d(b, n);
  dec_list_metrics_response(&d, out);
  return d.err;
}

void encode_into_dual_response(const DualResponse &o, ak::Enc *e) {
  e->reset();
  enc_dual_response(o, e);
}

int32_t decode_dual_response(const uint8_t *b, size_t n, DualResponse *out) {
  ak::Dec d(b, n);
  dec_dual_response(&d, out);
  return d.err;
}

const size_t kSites = 35;

}  // namespace native
}  // namespace shapes
