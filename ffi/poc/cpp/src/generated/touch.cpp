// @generated by ffi/poc/cpp/gen/cpp_touch.py (campaign glue). Do not edit.

#include "generated/touch.h"
#include "ak/rt.h"

namespace shapes {
namespace touch {

static inline uint64_t mix(uint64_t h, uint64_t v) { return (h ^ v) * 0x100000001b3ull; }
template <class S> static inline uint64_t fs(const S &s) {
  uint64_t n = (uint64_t)s.size();
  if (n) n ^= ((uint64_t)(unsigned char)s.data()[0] << 32) ^
              ((uint64_t)(unsigned char)s.data()[n - 1] << 40);
  return n;
}

uint64_t touch(const Timestamp &o);
uint64_t touch(const Duration &o);
uint64_t touch(const ResultRaw &o);
uint64_t touch(const TaskOptions &o);
uint64_t touch(const TaskOutput &o);
uint64_t touch(const TaskDetailed &o);
uint64_t touch(const TaskSummary &o);
uint64_t touch(const Probe &o);
uint64_t touch(const Empty &o);
uint64_t touch(const UploadResultData &o);
uint64_t touch(const MetricsBatch &o);
uint64_t touch(const Pair &o);
uint64_t touch(const ListResultsResponse &o);
uint64_t touch(const ListTasksDetailedResponse &o);
uint64_t touch(const ListTaskSummaryResponse &o);
uint64_t touch(const ListProbeResponse &o);
uint64_t touch(const ListMetricsResponse &o);
uint64_t touch(const UploadResultDataMessage &o);
uint64_t touch(const DualResponse &o);
uint64_t touch(const Timestamp &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  h = mix(h, (uint64_t)(int64_t)(o.seconds));
  h = mix(h, (uint64_t)(int64_t)(o.nanos));
  return h;
}

uint64_t touch(const Duration &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  h = mix(h, (uint64_t)(int64_t)(o.seconds));
  h = mix(h, (uint64_t)(int64_t)(o.nanos));
  return h;
}

uint64_t touch(const ResultRaw &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  h = mix(h, fs(o.session_id));
  h = mix(h, fs(o.name));
  h = mix(h, fs(o.owner_task_id));
  h = mix(h, (uint64_t)(int64_t)(o.status).v);
  if (o.created_at.has_value()) { h = mix(h, touch(*o.created_at)); }
  if (o.completed_at.has_value()) { h = mix(h, touch(*o.completed_at)); }
  h = mix(h, fs(o.result_id));
  h = mix(h, (uint64_t)(int64_t)(o.size));
  h = mix(h, fs(o.created_by));
  h = mix(h, fs(o.opaque_id));
  h = mix(h, (o.manual_deletion) ? 1u : 0u);
  return h;
}

uint64_t touch(const TaskOptions &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  { uint64_t acc = 0;
    for (std::map<std::string, std::string>::const_iterator it = o.options.begin(); it != o.options.end(); ++it)
      acc += mix(fs(it->first) * 31u, fs(it->second));
    h = mix(h, acc); }
  if (o.max_duration.has_value()) { h = mix(h, touch(*o.max_duration)); }
  h = mix(h, (uint64_t)(int64_t)(o.max_retries));
  h = mix(h, (uint64_t)(int64_t)(o.priority));
  h = mix(h, fs(o.partition_id));
  h = mix(h, fs(o.application_name));
  h = mix(h, fs(o.application_version));
  h = mix(h, fs(o.application_namespace));
  h = mix(h, fs(o.application_service));
  h = mix(h, fs(o.engine_type));
  return h;
}

uint64_t touch(const TaskOutput &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  h = mix(h, (o.success) ? 1u : 0u);
  h = mix(h, fs(o.error));
  return h;
}

uint64_t touch(const TaskDetailed &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  h = mix(h, fs(o.id));
  h = mix(h, fs(o.session_id));
  h = mix(h, fs(o.owner_pod_id));
  for (size_t i = 0; i < o.parent_task_ids.size(); ++i) { h = mix(h, fs(o.parent_task_ids[i])); }
  for (size_t i = 0; i < o.data_dependencies.size(); ++i) { h = mix(h, fs(o.data_dependencies[i])); }
  for (size_t i = 0; i < o.expected_output_ids.size(); ++i) { h = mix(h, fs(o.expected_output_ids[i])); }
  for (size_t i = 0; i < o.retry_of_ids.size(); ++i) { h = mix(h, fs(o.retry_of_ids[i])); }
  h = mix(h, (uint64_t)(int64_t)(o.status).v);
  h = mix(h, fs(o.status_message));
  if (o.options.has_value()) { h = mix(h, touch(*o.options)); }
  if (o.created_at.has_value()) { h = mix(h, touch(*o.created_at)); }
  if (o.submitted_at.has_value()) { h = mix(h, touch(*o.submitted_at)); }
  if (o.started_at.has_value()) { h = mix(h, touch(*o.started_at)); }
  if (o.ended_at.has_value()) { h = mix(h, touch(*o.ended_at)); }
  if (o.pod_ttl.has_value()) { h = mix(h, touch(*o.pod_ttl)); }
  if (o.output.has_value()) { h = mix(h, touch(*o.output)); }
  h = mix(h, fs(o.pod_hostname));
  if (o.received_at.has_value()) { h = mix(h, touch(*o.received_at)); }
  if (o.acquired_at.has_value()) { h = mix(h, touch(*o.acquired_at)); }
  if (o.creation_to_end_duration.has_value()) { h = mix(h, touch(*o.creation_to_end_duration)); }
  if (o.processing_to_end_duration.has_value()) { h = mix(h, touch(*o.processing_to_end_duration)); }
  h = mix(h, fs(o.initial_task_id));
  if (o.received_to_end_duration.has_value()) { h = mix(h, touch(*o.received_to_end_duration)); }
  if (o.processed_at.has_value()) { h = mix(h, touch(*o.processed_at)); }
  if (o.fetched_at.has_value()) { h = mix(h, touch(*o.fetched_at)); }
  h = mix(h, fs(o.payload_id));
  h = mix(h, fs(o.created_by));
  return h;
}

uint64_t touch(const TaskSummary &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  h = mix(h, fs(o.id));
  h = mix(h, fs(o.session_id));
  if (o.options.has_value()) { h = mix(h, touch(*o.options)); }
  h = mix(h, (uint64_t)(int64_t)(o.status).v);
  if (o.created_at.has_value()) { h = mix(h, touch(*o.created_at)); }
  h = mix(h, fs(o.error));
  h = mix(h, fs(o.status_message));
  h = mix(h, (uint64_t)(int64_t)(o.count_data_dependencies));
  return h;
}

uint64_t touch(const Probe &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  h = mix(h, fs(o.id));
  if (o.opt_count.has_value()) { h = mix(h, (uint64_t)(int64_t)(*o.opt_count)); }
  if (o.opt_label.has_value()) { h = mix(h, fs(*o.opt_label)); }
  if (o.opt_flag.has_value()) { h = mix(h, (*o.opt_flag) ? 1u : 0u); }
  h = mix(h, (uint64_t)o.body.which());
  switch (o.body.which()) {
    case ProbeBody::kAsInt: h = mix(h, (uint64_t)(int64_t)(o.body.as_int())); break;
    case ProbeBody::kAsText: h = mix(h, fs(o.body.as_text())); break;
    case ProbeBody::kAsBlob: h = mix(h, fs(o.body.as_blob())); break;
    case ProbeBody::kAsStamp: h = mix(h, touch(o.body.as_stamp())); break;
    case ProbeBody::kAsNothing: h = mix(h, touch(o.body.as_nothing())); break;
    default: break;
  }
  return h;
}

uint64_t touch(const Empty &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  return h;
}

uint64_t touch(const UploadResultData &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  h = mix(h, fs(o.session_id));
  h = mix(h, fs(o.result_id));
  h = mix(h, fs(o.data_chunk));
  return h;
}

uint64_t touch(const MetricsBatch &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  h = mix(h, fs(o.id));
  for (size_t i = 0; i < o.ticks.size(); ++i) { h = mix(h, (uint64_t)(int64_t)(o.ticks[i])); }
  for (size_t i = 0; i < o.values.size(); ++i) { h = mix(h, ak::f64_bits(o.values[i])); }
  for (size_t i = 0; i < o.codes.size(); ++i) { h = mix(h, (uint64_t)(int64_t)(o.codes[i])); }
  for (size_t i = 0; i < o.flags.size(); ++i) { h = mix(h, (o.flags[i]) ? 1u : 0u); }
  for (size_t i = 0; i < o.statuses.size(); ++i) { h = mix(h, (uint64_t)(int64_t)(o.statuses[i]).v); }
  return h;
}

uint64_t touch(const Pair &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  h = mix(h, fs(o.key));
  h = mix(h, (uint64_t)(int64_t)(o.value));
  return h;
}

uint64_t touch(const ListResultsResponse &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  for (size_t i = 0; i < o.results.size(); ++i) { h = mix(h, touch(o.results[i])); }
  h = mix(h, (uint64_t)(int64_t)(o.page));
  h = mix(h, (uint64_t)(int64_t)(o.total));
  return h;
}

uint64_t touch(const ListTasksDetailedResponse &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  for (size_t i = 0; i < o.tasks.size(); ++i) { h = mix(h, touch(o.tasks[i])); }
  h = mix(h, (uint64_t)(int64_t)(o.page));
  h = mix(h, (uint64_t)(int64_t)(o.total));
  return h;
}

uint64_t touch(const ListTaskSummaryResponse &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  for (size_t i = 0; i < o.tasks.size(); ++i) { h = mix(h, touch(o.tasks[i])); }
  return h;
}

uint64_t touch(const ListProbeResponse &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  for (size_t i = 0; i < o.probes.size(); ++i) { h = mix(h, touch(o.probes[i])); }
  return h;
}

uint64_t touch(const ListMetricsResponse &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  for (size_t i = 0; i < o.batches.size(); ++i) { h = mix(h, touch(o.batches[i])); }
  return h;
}

uint64_t touch(const UploadResultDataMessage &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  if (o.upload.has_value()) { h = mix(h, touch(*o.upload)); }
  return h;
}

uint64_t touch(const DualResponse &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  for (size_t i = 0; i < o.left.size(); ++i) { h = mix(h, touch(o.left[i])); }
  for (size_t i = 0; i < o.right.size(); ++i) { h = mix(h, touch(o.right[i])); }
  return h;
}

}  // namespace touch
}  // namespace shapes

namespace pbtouch {

static inline uint64_t mix(uint64_t h, uint64_t v) { return (h ^ v) * 0x100000001b3ull; }
template <class S> static inline uint64_t fs(const S &s) {
  uint64_t n = (uint64_t)s.size();
  if (n) n ^= ((uint64_t)(unsigned char)s.data()[0] << 32) ^
              ((uint64_t)(unsigned char)s.data()[n - 1] << 40);
  return n;
}

namespace pb = armonik::ffi::shapes::v1;
uint64_t touch(const pb::Timestamp &o);
uint64_t touch(const pb::Duration &o);
uint64_t touch(const pb::ResultRaw &o);
uint64_t touch(const pb::TaskOptions &o);
uint64_t touch(const pb::TaskOutput &o);
uint64_t touch(const pb::TaskDetailed &o);
uint64_t touch(const pb::TaskSummary &o);
uint64_t touch(const pb::Probe &o);
uint64_t touch(const pb::Empty &o);
uint64_t touch(const pb::UploadResultData &o);
uint64_t touch(const pb::MetricsBatch &o);
uint64_t touch(const pb::Pair &o);
uint64_t touch(const pb::ListResultsResponse &o);
uint64_t touch(const pb::ListTasksDetailedResponse &o);
uint64_t touch(const pb::ListTaskSummaryResponse &o);
uint64_t touch(const pb::ListProbeResponse &o);
uint64_t touch(const pb::ListMetricsResponse &o);
uint64_t touch(const pb::UploadResultDataMessage &o);
uint64_t touch(const pb::DualResponse &o);
uint64_t touch(const pb::Timestamp &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  h = mix(h, (uint64_t)(int64_t)(o.seconds()));
  h = mix(h, (uint64_t)(int64_t)(o.nanos()));
  return h;
}

uint64_t touch(const pb::Duration &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  h = mix(h, (uint64_t)(int64_t)(o.seconds()));
  h = mix(h, (uint64_t)(int64_t)(o.nanos()));
  return h;
}

uint64_t touch(const pb::ResultRaw &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  h = mix(h, fs(o.session_id()));
  h = mix(h, fs(o.name()));
  h = mix(h, fs(o.owner_task_id()));
  h = mix(h, (uint64_t)(int64_t)(int)(o.status()));
  if (o.has_created_at()) { h = mix(h, touch(o.created_at())); }
  if (o.has_completed_at()) { h = mix(h, touch(o.completed_at())); }
  h = mix(h, fs(o.result_id()));
  h = mix(h, (uint64_t)(int64_t)(o.size()));
  h = mix(h, fs(o.created_by()));
  h = mix(h, fs(o.opaque_id()));
  h = mix(h, (o.manual_deletion()) ? 1u : 0u);
  return h;
}

uint64_t touch(const pb::TaskOptions &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  { uint64_t acc = 0;
    for (auto it = o.options().begin(); it != o.options().end(); ++it)
      acc += mix(fs(it->first) * 31u, fs(it->second));
    h = mix(h, acc); }
  if (o.has_max_duration()) { h = mix(h, touch(o.max_duration())); }
  h = mix(h, (uint64_t)(int64_t)(o.max_retries()));
  h = mix(h, (uint64_t)(int64_t)(o.priority()));
  h = mix(h, fs(o.partition_id()));
  h = mix(h, fs(o.application_name()));
  h = mix(h, fs(o.application_version()));
  h = mix(h, fs(o.application_namespace()));
  h = mix(h, fs(o.application_service()));
  h = mix(h, fs(o.engine_type()));
  return h;
}

uint64_t touch(const pb::TaskOutput &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  h = mix(h, (o.success()) ? 1u : 0u);
  h = mix(h, fs(o.error()));
  return h;
}

uint64_t touch(const pb::TaskDetailed &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  h = mix(h, fs(o.id()));
  h = mix(h, fs(o.session_id()));
  h = mix(h, fs(o.owner_pod_id()));
  for (int i = 0; i < o.parent_task_ids_size(); ++i) { h = mix(h, fs(o.parent_task_ids(i))); }
  for (int i = 0; i < o.data_dependencies_size(); ++i) { h = mix(h, fs(o.data_dependencies(i))); }
  for (int i = 0; i < o.expected_output_ids_size(); ++i) { h = mix(h, fs(o.expected_output_ids(i))); }
  for (int i = 0; i < o.retry_of_ids_size(); ++i) { h = mix(h, fs(o.retry_of_ids(i))); }
  h = mix(h, (uint64_t)(int64_t)(int)(o.status()));
  h = mix(h, fs(o.status_message()));
  if (o.has_options()) { h = mix(h, touch(o.options())); }
  if (o.has_created_at()) { h = mix(h, touch(o.created_at())); }
  if (o.has_submitted_at()) { h = mix(h, touch(o.submitted_at())); }
  if (o.has_started_at()) { h = mix(h, touch(o.started_at())); }
  if (o.has_ended_at()) { h = mix(h, touch(o.ended_at())); }
  if (o.has_pod_ttl()) { h = mix(h, touch(o.pod_ttl())); }
  if (o.has_output()) { h = mix(h, touch(o.output())); }
  h = mix(h, fs(o.pod_hostname()));
  if (o.has_received_at()) { h = mix(h, touch(o.received_at())); }
  if (o.has_acquired_at()) { h = mix(h, touch(o.acquired_at())); }
  if (o.has_creation_to_end_duration()) { h = mix(h, touch(o.creation_to_end_duration())); }
  if (o.has_processing_to_end_duration()) { h = mix(h, touch(o.processing_to_end_duration())); }
  h = mix(h, fs(o.initial_task_id()));
  if (o.has_received_to_end_duration()) { h = mix(h, touch(o.received_to_end_duration())); }
  if (o.has_processed_at()) { h = mix(h, touch(o.processed_at())); }
  if (o.has_fetched_at()) { h = mix(h, touch(o.fetched_at())); }
  h = mix(h, fs(o.payload_id()));
  h = mix(h, fs(o.created_by()));
  return h;
}

uint64_t touch(const pb::TaskSummary &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  h = mix(h, fs(o.id()));
  h = mix(h, fs(o.session_id()));
  if (o.has_options()) { h = mix(h, touch(o.options())); }
  h = mix(h, (uint64_t)(int64_t)(int)(o.status()));
  if (o.has_created_at()) { h = mix(h, touch(o.created_at())); }
  h = mix(h, fs(o.error()));
  h = mix(h, fs(o.status_message()));
  h = mix(h, (uint64_t)(int64_t)(o.count_data_dependencies()));
  return h;
}

uint64_t touch(const pb::Probe &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  h = mix(h, fs(o.id()));
  if (o.has_opt_count()) { h = mix(h, (uint64_t)(int64_t)(o.opt_count())); }
  if (o.has_opt_label()) { h = mix(h, fs(o.opt_label())); }
  if (o.has_opt_flag()) { h = mix(h, (o.opt_flag()) ? 1u : 0u); }
  h = mix(h, (uint64_t)o.body_case());
  switch (o.body_case()) {
    case pb::Probe::kAsInt: h = mix(h, (uint64_t)(int64_t)(o.as_int())); break;
    case pb::Probe::kAsText: h = mix(h, fs(o.as_text())); break;
    case pb::Probe::kAsBlob: h = mix(h, fs(o.as_blob())); break;
    case pb::Probe::kAsStamp: h = mix(h, touch(o.as_stamp())); break;
    case pb::Probe::kAsNothing: h = mix(h, touch(o.as_nothing())); break;
    default: break;
  }
  return h;
}

uint64_t touch(const pb::Empty &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  return h;
}

uint64_t touch(const pb::UploadResultData &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  h = mix(h, fs(o.session_id()));
  h = mix(h, fs(o.result_id()));
  h = mix(h, fs(o.data_chunk()));
  return h;
}

uint64_t touch(const pb::MetricsBatch &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  h = mix(h, fs(o.id()));
  for (int i = 0; i < o.ticks_size(); ++i) { h = mix(h, (uint64_t)(int64_t)(o.ticks(i))); }
  for (int i = 0; i < o.values_size(); ++i) { h = mix(h, ak::f64_bits(o.values(i))); }
  for (int i = 0; i < o.codes_size(); ++i) { h = mix(h, (uint64_t)(int64_t)(o.codes(i))); }
  for (int i = 0; i < o.flags_size(); ++i) { h = mix(h, (o.flags(i)) ? 1u : 0u); }
  for (int i = 0; i < o.statuses_size(); ++i) { h = mix(h, (uint64_t)(int64_t)(int)(o.statuses(i))); }
  return h;
}

uint64_t touch(const pb::Pair &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  h = mix(h, fs(o.key()));
  h = mix(h, (uint64_t)(int64_t)(o.value()));
  return h;
}

uint64_t touch(const pb::ListResultsResponse &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  for (int i = 0; i < o.results_size(); ++i) { h = mix(h, touch(o.results(i))); }
  h = mix(h, (uint64_t)(int64_t)(o.page()));
  h = mix(h, (uint64_t)(int64_t)(o.total()));
  return h;
}

uint64_t touch(const pb::ListTasksDetailedResponse &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  for (int i = 0; i < o.tasks_size(); ++i) { h = mix(h, touch(o.tasks(i))); }
  h = mix(h, (uint64_t)(int64_t)(o.page()));
  h = mix(h, (uint64_t)(int64_t)(o.total()));
  return h;
}

uint64_t touch(const pb::ListTaskSummaryResponse &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  for (int i = 0; i < o.tasks_size(); ++i) { h = mix(h, touch(o.tasks(i))); }
  return h;
}

uint64_t touch(const pb::ListProbeResponse &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  for (int i = 0; i < o.probes_size(); ++i) { h = mix(h, touch(o.probes(i))); }
  return h;
}

uint64_t touch(const pb::ListMetricsResponse &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  for (int i = 0; i < o.batches_size(); ++i) { h = mix(h, touch(o.batches(i))); }
  return h;
}

uint64_t touch(const pb::UploadResultDataMessage &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  if (o.has_upload()) { h = mix(h, touch(o.upload())); }
  return h;
}

uint64_t touch(const pb::DualResponse &o) {
  uint64_t h = 0xcbf29ce484222325ull;
  for (int i = 0; i < o.left_size(); ++i) { h = mix(h, touch(o.left(i))); }
  for (int i = 0; i < o.right_size(); ++i) { h = mix(h, touch(o.right(i))); }
  return h;
}

}  // namespace pbtouch
