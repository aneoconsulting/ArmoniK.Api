"""Harness glue shared by this slice's generator modules: the banner of a glue file.

FIX-PLAN WP5 step 4: this directory keeps project and harness glue only (payload builders,
the arm tables, the corpus projection and dispatch). Every codec and binding -- every wire
rule and every layout -- is rendered by the shared backends in `ffi/poc/codec/gen/`
(`cs_types`, `cs_managed`, `cs_binding`, `cs_host`) from `plan.py`'s plans. A glue module
reads the plan's descriptor view (names, kinds, cardinality, payload definitions); it never
decides a wire rule or a layout.
"""
import cs_names as N


class Head(N.Head):
    def __init__(self, what, module, source="ffi/schema/shapes.json"):
        N.Head.__init__(self, what, source)
        self.lines[0] = "// @generated by ffi/poc/csharp/gen/%s.py (harness glue, driven by gen/generate.py)" % module
