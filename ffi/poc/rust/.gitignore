# Build output.
target/

# The repository-wide .gitignore excludes any directory named `gen` (line 397), which would
# commit this slice's generator as nothing at all -- the exact trap ffi/schema/README.md
# records for itself. Undo it here, for this slice only.
!gen/
