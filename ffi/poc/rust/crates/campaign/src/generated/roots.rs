// @generated by ffi/poc/rust/gen/rust_campaign.py from the plan. Do not edit.

#![allow(clippy::all, non_camel_case_types, unused_variables, unused_mut)]
use crate::{mix, Ops};
use harness::arms::core_ffi_arm::Ctx;
use harness::generated::binding;
use facade::generated::{build, core_native, core_native_retain};
use shapes_prost::shapes as p;

#[inline(never)]
pub fn touch_f_timestamp(o: &facade::Timestamp, mut h: u64) -> u64 {
    h = mix(h, (o.seconds) as u64);
    h = mix(h, (o.nanos) as u64);
    h = mix(h, o.unknown_fields.len() as u64);
    h
}

#[inline(never)]
pub fn touch_f_duration(o: &facade::Duration, mut h: u64) -> u64 {
    h = mix(h, (o.seconds) as u64);
    h = mix(h, (o.nanos) as u64);
    h = mix(h, o.unknown_fields.len() as u64);
    h
}

#[inline(never)]
pub fn touch_f_result_raw(o: &facade::ResultRaw, mut h: u64) -> u64 {
    { let b: &[u8] = o.session_id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.name.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.owner_task_id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    h = mix(h, (o.status).to_i32() as u64);
    if let Some(c) = o.created_at.as_ref() { h = touch_f_timestamp(&(*c), h); }
    if let Some(c) = o.completed_at.as_ref() { h = touch_f_timestamp(&(*c), h); }
    { let b: &[u8] = o.result_id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    h = mix(h, (o.size) as u64);
    { let b: &[u8] = o.created_by.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.opaque_id.as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    h = mix(h, (o.manual_deletion) as u64);
    h = mix(h, o.unknown_fields.len() as u64);
    h
}

#[inline(never)]
pub fn touch_f_task_options(o: &facade::TaskOptions, mut h: u64) -> u64 {
    for (k, v) in o.options.iter() { { let b: &[u8] = (*k).as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } } { let b: &[u8] = (*v).as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } } }
    if let Some(c) = o.max_duration.as_ref() { h = touch_f_duration(&(*c), h); }
    h = mix(h, (o.max_retries) as u64);
    h = mix(h, (o.priority) as u64);
    { let b: &[u8] = o.partition_id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.application_name.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.application_version.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.application_namespace.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.application_service.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.engine_type.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    h = mix(h, o.unknown_fields.len() as u64);
    h
}

#[inline(never)]
pub fn touch_f_task_output(o: &facade::TaskOutput, mut h: u64) -> u64 {
    h = mix(h, (o.success) as u64);
    { let b: &[u8] = o.error.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    h = mix(h, o.unknown_fields.len() as u64);
    h
}

#[inline(never)]
pub fn touch_f_task_detailed(o: &facade::TaskDetailed, mut h: u64) -> u64 {
    { let b: &[u8] = o.id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.session_id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.owner_pod_id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    h = mix(h, o.parent_task_ids.len() as u64);
    for x in o.parent_task_ids.iter() { { let b: &[u8] = (*x).as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } } }
    h = mix(h, o.data_dependencies.len() as u64);
    for x in o.data_dependencies.iter() { { let b: &[u8] = (*x).as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } } }
    h = mix(h, o.expected_output_ids.len() as u64);
    for x in o.expected_output_ids.iter() { { let b: &[u8] = (*x).as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } } }
    h = mix(h, o.retry_of_ids.len() as u64);
    for x in o.retry_of_ids.iter() { { let b: &[u8] = (*x).as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } } }
    h = mix(h, (o.status).to_i32() as u64);
    { let b: &[u8] = o.status_message.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    if let Some(c) = o.options.as_ref() { h = touch_f_task_options(&(*c), h); }
    if let Some(c) = o.created_at.as_ref() { h = touch_f_timestamp(&(*c), h); }
    if let Some(c) = o.submitted_at.as_ref() { h = touch_f_timestamp(&(*c), h); }
    if let Some(c) = o.started_at.as_ref() { h = touch_f_timestamp(&(*c), h); }
    if let Some(c) = o.ended_at.as_ref() { h = touch_f_timestamp(&(*c), h); }
    if let Some(c) = o.pod_ttl.as_ref() { h = touch_f_timestamp(&(*c), h); }
    if let Some(c) = o.output.as_ref() { h = touch_f_task_output(&(*c), h); }
    { let b: &[u8] = o.pod_hostname.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    if let Some(c) = o.received_at.as_ref() { h = touch_f_timestamp(&(*c), h); }
    if let Some(c) = o.acquired_at.as_ref() { h = touch_f_timestamp(&(*c), h); }
    if let Some(c) = o.creation_to_end_duration.as_ref() { h = touch_f_duration(&(*c), h); }
    if let Some(c) = o.processing_to_end_duration.as_ref() { h = touch_f_duration(&(*c), h); }
    { let b: &[u8] = o.initial_task_id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    if let Some(c) = o.received_to_end_duration.as_ref() { h = touch_f_duration(&(*c), h); }
    if let Some(c) = o.processed_at.as_ref() { h = touch_f_timestamp(&(*c), h); }
    if let Some(c) = o.fetched_at.as_ref() { h = touch_f_timestamp(&(*c), h); }
    { let b: &[u8] = o.payload_id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.created_by.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    h = mix(h, o.unknown_fields.len() as u64);
    h
}

#[inline(never)]
pub fn touch_f_task_summary(o: &facade::TaskSummary, mut h: u64) -> u64 {
    { let b: &[u8] = o.id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.session_id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    if let Some(c) = o.options.as_ref() { h = touch_f_task_options(&(*c), h); }
    h = mix(h, (o.status).to_i32() as u64);
    if let Some(c) = o.created_at.as_ref() { h = touch_f_timestamp(&(*c), h); }
    { let b: &[u8] = o.error.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.status_message.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    h = mix(h, (o.count_data_dependencies) as u64);
    h = mix(h, o.unknown_fields.len() as u64);
    h
}

#[inline(never)]
pub fn touch_f_probe(o: &facade::Probe, mut h: u64) -> u64 {
    { let b: &[u8] = o.id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    if let Some(x) = o.opt_count.as_ref() { h = mix(h, ((*x)) as u64); }
    if let Some(x) = o.opt_label.as_ref() { { let b: &[u8] = (*x).as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } } }
    if let Some(x) = o.opt_flag.as_ref() { h = mix(h, ((*x)) as u64); }
    match o.body.as_ref() {
        Some(facade::ProbeBody::AsInt(x)) => { h = mix(h, 10); h = mix(h, ((*x)) as u64); }
        Some(facade::ProbeBody::AsText(x)) => { h = mix(h, 11); { let b: &[u8] = (*x).as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } } }
        Some(facade::ProbeBody::AsBlob(x)) => { h = mix(h, 12); { let b: &[u8] = (*x).as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } } }
        Some(facade::ProbeBody::AsStamp(x)) => { h = mix(h, 13); h = touch_f_timestamp(&(*x), h); }
        Some(facade::ProbeBody::AsNothing(x)) => { h = mix(h, 14); h = touch_f_empty(&(*x), h); }
        None => {}
    }
    h = mix(h, o.unknown_fields.len() as u64);
    h
}

#[inline(never)]
pub fn touch_f_empty(o: &facade::Empty, mut h: u64) -> u64 {
    h = mix(h, o.unknown_fields.len() as u64);
    h
}

#[inline(never)]
pub fn touch_f_upload_result_data(o: &facade::UploadResultData, mut h: u64) -> u64 {
    { let b: &[u8] = o.session_id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.result_id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.data_chunk.as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    h = mix(h, o.unknown_fields.len() as u64);
    h
}

#[inline(never)]
pub fn touch_f_metrics_batch(o: &facade::MetricsBatch, mut h: u64) -> u64 {
    { let b: &[u8] = o.id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    h = mix(h, o.ticks.len() as u64);
    for x in o.ticks.iter() { h = mix(h, ((*x)) as u64); }
    h = mix(h, o.values.len() as u64);
    for x in o.values.iter() { h = mix(h, ((*x)).to_bits()); }
    h = mix(h, o.codes.len() as u64);
    for x in o.codes.iter() { h = mix(h, ((*x)) as u64); }
    h = mix(h, o.flags.len() as u64);
    for x in o.flags.iter() { h = mix(h, ((*x)) as u64); }
    h = mix(h, o.statuses.len() as u64);
    for x in o.statuses.iter() { h = mix(h, ((*x)).to_i32() as u64); }
    h = mix(h, o.unknown_fields.len() as u64);
    h
}

#[inline(never)]
pub fn touch_f_pair(o: &facade::Pair, mut h: u64) -> u64 {
    { let b: &[u8] = o.key.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    h = mix(h, (o.value) as u64);
    h = mix(h, o.unknown_fields.len() as u64);
    h
}

#[inline(never)]
pub fn touch_f_list_results_response(o: &facade::ListResultsResponse, mut h: u64) -> u64 {
    h = mix(h, o.results.len() as u64);
    for x in o.results.iter() { h = touch_f_result_raw(&(*x), h); }
    h = mix(h, (o.page) as u64);
    h = mix(h, (o.total) as u64);
    h = mix(h, o.unknown_fields.len() as u64);
    h
}

#[inline(never)]
pub fn touch_f_list_tasks_detailed_response(o: &facade::ListTasksDetailedResponse, mut h: u64) -> u64 {
    h = mix(h, o.tasks.len() as u64);
    for x in o.tasks.iter() { h = touch_f_task_detailed(&(*x), h); }
    h = mix(h, (o.page) as u64);
    h = mix(h, (o.total) as u64);
    h = mix(h, o.unknown_fields.len() as u64);
    h
}

#[inline(never)]
pub fn touch_f_list_task_summary_response(o: &facade::ListTaskSummaryResponse, mut h: u64) -> u64 {
    h = mix(h, o.tasks.len() as u64);
    for x in o.tasks.iter() { h = touch_f_task_summary(&(*x), h); }
    h = mix(h, o.unknown_fields.len() as u64);
    h
}

#[inline(never)]
pub fn touch_f_list_probe_response(o: &facade::ListProbeResponse, mut h: u64) -> u64 {
    h = mix(h, o.probes.len() as u64);
    for x in o.probes.iter() { h = touch_f_probe(&(*x), h); }
    h = mix(h, o.unknown_fields.len() as u64);
    h
}

#[inline(never)]
pub fn touch_f_list_metrics_response(o: &facade::ListMetricsResponse, mut h: u64) -> u64 {
    h = mix(h, o.batches.len() as u64);
    for x in o.batches.iter() { h = touch_f_metrics_batch(&(*x), h); }
    h = mix(h, o.unknown_fields.len() as u64);
    h
}

#[inline(never)]
pub fn touch_f_upload_result_data_message(o: &facade::UploadResultDataMessage, mut h: u64) -> u64 {
    if let Some(c) = o.upload.as_ref() { h = touch_f_upload_result_data(&(*c), h); }
    h = mix(h, o.unknown_fields.len() as u64);
    h
}

#[inline(never)]
pub fn touch_f_dual_response(o: &facade::DualResponse, mut h: u64) -> u64 {
    h = mix(h, o.left.len() as u64);
    for x in o.left.iter() { h = touch_f_pair(&(*x), h); }
    h = mix(h, o.right.len() as u64);
    for x in o.right.iter() { h = touch_f_pair(&(*x), h); }
    h = mix(h, o.unknown_fields.len() as u64);
    h
}

#[inline(never)]
pub fn touch_p_timestamp(o: &p::Timestamp, mut h: u64) -> u64 {
    h = mix(h, (o.seconds) as u64);
    h = mix(h, (o.nanos) as u64);
    h
}

#[inline(never)]
pub fn touch_p_duration(o: &p::Duration, mut h: u64) -> u64 {
    h = mix(h, (o.seconds) as u64);
    h = mix(h, (o.nanos) as u64);
    h
}

#[inline(never)]
pub fn touch_p_result_raw(o: &p::ResultRaw, mut h: u64) -> u64 {
    { let b: &[u8] = o.session_id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.name.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.owner_task_id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    h = mix(h, o.status as u64);
    if let Some(c) = o.created_at.as_ref() { h = touch_p_timestamp(&(*c), h); }
    if let Some(c) = o.completed_at.as_ref() { h = touch_p_timestamp(&(*c), h); }
    { let b: &[u8] = o.result_id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    h = mix(h, (o.size) as u64);
    { let b: &[u8] = o.created_by.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.opaque_id.as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    h = mix(h, (o.manual_deletion) as u64);
    h
}

#[inline(never)]
pub fn touch_p_task_options(o: &p::TaskOptions, mut h: u64) -> u64 {
    for (k, v) in o.options.iter() { { let b: &[u8] = (*k).as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } } { let b: &[u8] = (*v).as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } } }
    if let Some(c) = o.max_duration.as_ref() { h = touch_p_duration(&(*c), h); }
    h = mix(h, (o.max_retries) as u64);
    h = mix(h, (o.priority) as u64);
    { let b: &[u8] = o.partition_id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.application_name.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.application_version.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.application_namespace.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.application_service.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.engine_type.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    h
}

#[inline(never)]
pub fn touch_p_task_output(o: &p::TaskOutput, mut h: u64) -> u64 {
    h = mix(h, (o.success) as u64);
    { let b: &[u8] = o.error.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    h
}

#[inline(never)]
pub fn touch_p_task_detailed(o: &p::TaskDetailed, mut h: u64) -> u64 {
    { let b: &[u8] = o.id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.session_id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.owner_pod_id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    h = mix(h, o.parent_task_ids.len() as u64);
    for x in o.parent_task_ids.iter() { { let b: &[u8] = (*x).as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } } }
    h = mix(h, o.data_dependencies.len() as u64);
    for x in o.data_dependencies.iter() { { let b: &[u8] = (*x).as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } } }
    h = mix(h, o.expected_output_ids.len() as u64);
    for x in o.expected_output_ids.iter() { { let b: &[u8] = (*x).as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } } }
    h = mix(h, o.retry_of_ids.len() as u64);
    for x in o.retry_of_ids.iter() { { let b: &[u8] = (*x).as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } } }
    h = mix(h, o.status as u64);
    { let b: &[u8] = o.status_message.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    if let Some(c) = o.options.as_ref() { h = touch_p_task_options(&(*c), h); }
    if let Some(c) = o.created_at.as_ref() { h = touch_p_timestamp(&(*c), h); }
    if let Some(c) = o.submitted_at.as_ref() { h = touch_p_timestamp(&(*c), h); }
    if let Some(c) = o.started_at.as_ref() { h = touch_p_timestamp(&(*c), h); }
    if let Some(c) = o.ended_at.as_ref() { h = touch_p_timestamp(&(*c), h); }
    if let Some(c) = o.pod_ttl.as_ref() { h = touch_p_timestamp(&(*c), h); }
    if let Some(c) = o.output.as_ref() { h = touch_p_task_output(&(*c), h); }
    { let b: &[u8] = o.pod_hostname.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    if let Some(c) = o.received_at.as_ref() { h = touch_p_timestamp(&(*c), h); }
    if let Some(c) = o.acquired_at.as_ref() { h = touch_p_timestamp(&(*c), h); }
    if let Some(c) = o.creation_to_end_duration.as_ref() { h = touch_p_duration(&(*c), h); }
    if let Some(c) = o.processing_to_end_duration.as_ref() { h = touch_p_duration(&(*c), h); }
    { let b: &[u8] = o.initial_task_id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    if let Some(c) = o.received_to_end_duration.as_ref() { h = touch_p_duration(&(*c), h); }
    if let Some(c) = o.processed_at.as_ref() { h = touch_p_timestamp(&(*c), h); }
    if let Some(c) = o.fetched_at.as_ref() { h = touch_p_timestamp(&(*c), h); }
    { let b: &[u8] = o.payload_id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.created_by.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    h
}

#[inline(never)]
pub fn touch_p_task_summary(o: &p::TaskSummary, mut h: u64) -> u64 {
    { let b: &[u8] = o.id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.session_id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    if let Some(c) = o.options.as_ref() { h = touch_p_task_options(&(*c), h); }
    h = mix(h, o.status as u64);
    if let Some(c) = o.created_at.as_ref() { h = touch_p_timestamp(&(*c), h); }
    { let b: &[u8] = o.error.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.status_message.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    h = mix(h, (o.count_data_dependencies) as u64);
    h
}

#[inline(never)]
pub fn touch_p_probe(o: &p::Probe, mut h: u64) -> u64 {
    { let b: &[u8] = o.id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    if let Some(x) = o.opt_count.as_ref() { h = mix(h, ((*x)) as u64); }
    if let Some(x) = o.opt_label.as_ref() { { let b: &[u8] = (*x).as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } } }
    if let Some(x) = o.opt_flag.as_ref() { h = mix(h, ((*x)) as u64); }
    match o.body.as_ref() {
        Some(p::probe::Body::AsInt(x)) => { h = mix(h, 10); h = mix(h, ((*x)) as u64); }
        Some(p::probe::Body::AsText(x)) => { h = mix(h, 11); { let b: &[u8] = (*x).as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } } }
        Some(p::probe::Body::AsBlob(x)) => { h = mix(h, 12); { let b: &[u8] = (*x).as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } } }
        Some(p::probe::Body::AsStamp(x)) => { h = mix(h, 13); h = touch_p_timestamp(&(*x), h); }
        Some(p::probe::Body::AsNothing(x)) => { h = mix(h, 14); h = touch_p_empty(&(*x), h); }
        None => {}
    }
    h
}

#[inline(never)]
pub fn touch_p_empty(o: &p::Empty, mut h: u64) -> u64 {
    h
}

#[inline(never)]
pub fn touch_p_upload_result_data(o: &p::UploadResultData, mut h: u64) -> u64 {
    { let b: &[u8] = o.session_id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.result_id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    { let b: &[u8] = o.data_chunk.as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    h
}

#[inline(never)]
pub fn touch_p_metrics_batch(o: &p::MetricsBatch, mut h: u64) -> u64 {
    { let b: &[u8] = o.id.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    h = mix(h, o.ticks.len() as u64);
    for x in o.ticks.iter() { h = mix(h, ((*x)) as u64); }
    h = mix(h, o.values.len() as u64);
    for x in o.values.iter() { h = mix(h, ((*x)).to_bits()); }
    h = mix(h, o.codes.len() as u64);
    for x in o.codes.iter() { h = mix(h, ((*x)) as u64); }
    h = mix(h, o.flags.len() as u64);
    for x in o.flags.iter() { h = mix(h, ((*x)) as u64); }
    h = mix(h, o.statuses.len() as u64);
    for x in o.statuses.iter() { h = mix(h, (*x) as u64); }
    h
}

#[inline(never)]
pub fn touch_p_pair(o: &p::Pair, mut h: u64) -> u64 {
    { let b: &[u8] = o.key.as_bytes().as_ref(); h = mix(h, b.len() as u64); if let (Some(f), Some(l)) = (b.first(), b.last()) { h = mix(h, *f as u64); h = mix(h, *l as u64); } }
    h = mix(h, (o.value) as u64);
    h
}

#[inline(never)]
pub fn touch_p_list_results_response(o: &p::ListResultsResponse, mut h: u64) -> u64 {
    h = mix(h, o.results.len() as u64);
    for x in o.results.iter() { h = touch_p_result_raw(&(*x), h); }
    h = mix(h, (o.page) as u64);
    h = mix(h, (o.total) as u64);
    h
}

#[inline(never)]
pub fn touch_p_list_tasks_detailed_response(o: &p::ListTasksDetailedResponse, mut h: u64) -> u64 {
    h = mix(h, o.tasks.len() as u64);
    for x in o.tasks.iter() { h = touch_p_task_detailed(&(*x), h); }
    h = mix(h, (o.page) as u64);
    h = mix(h, (o.total) as u64);
    h
}

#[inline(never)]
pub fn touch_p_list_task_summary_response(o: &p::ListTaskSummaryResponse, mut h: u64) -> u64 {
    h = mix(h, o.tasks.len() as u64);
    for x in o.tasks.iter() { h = touch_p_task_summary(&(*x), h); }
    h
}

#[inline(never)]
pub fn touch_p_list_probe_response(o: &p::ListProbeResponse, mut h: u64) -> u64 {
    h = mix(h, o.probes.len() as u64);
    for x in o.probes.iter() { h = touch_p_probe(&(*x), h); }
    h
}

#[inline(never)]
pub fn touch_p_list_metrics_response(o: &p::ListMetricsResponse, mut h: u64) -> u64 {
    h = mix(h, o.batches.len() as u64);
    for x in o.batches.iter() { h = touch_p_metrics_batch(&(*x), h); }
    h
}

#[inline(never)]
pub fn touch_p_upload_result_data_message(o: &p::UploadResultDataMessage, mut h: u64) -> u64 {
    if let Some(c) = o.upload.as_ref() { h = touch_p_upload_result_data(&(*c), h); }
    h
}

#[inline(never)]
pub fn touch_p_dual_response(o: &p::DualResponse, mut h: u64) -> u64 {
    h = mix(h, o.left.len() as u64);
    for x in o.left.iter() { h = touch_p_pair(&(*x), h); }
    h = mix(h, o.right.len() as u64);
    for x in o.right.iter() { h = touch_p_pair(&(*x), h); }
    h
}

pub struct R_ListResultsResponse;
impl Ops for R_ListResultsResponse {
    const ROOT: &'static str = "ListResultsResponse";
    type F = facade::ListResultsResponse;
    type P = p::ListResultsResponse;
    fn build(pid: &str) -> Option<Self::F> {
        match pid {
            "P1.1" => Some(build::payload_p1_1()),
            "P1.2" => Some(build::payload_p1_2()),
            "P1.3" => Some(build::payload_p1_3()),
            _ => None,
        }
    }
    fn n_decode(b: &[u8], retain: bool) -> Result<Self::F, i32> {
        if retain { core_native_retain::decode_list_results_response(b) } else { core_native::decode_list_results_response(b) }
    }
    fn n_encode(v: &Self::F, e: &mut ak_rt::Enc, retain: bool) {
        if retain { core_native_retain::encode_into_list_results_response(v, e) } else { core_native::encode_into_list_results_response(v, e) }
    }
    fn f_decode(c: &Ctx, b: &[u8], retain: bool) -> Result<Self::F, i32> {
        if retain { binding::decode_with_list_results_response_unk(c.dec, b) } else { binding::decode_with_list_results_response(c.dec, b) }
    }
    fn f_encode(c: &Ctx, v: &Self::F, retain: bool) -> Result<usize, i32> {
        if retain { binding::encode_into_list_results_response_unk(c.enc, v, &c.tcs) } else { binding::encode_into_list_results_response(c.enc, v, &c.tcs) }
    }
    fn f_pull(c: &Ctx, b: &[u8], retain: bool, toks: &mut Vec<i64>) -> Result<Self::F, i32> {
        if retain { binding::parse_walk_with_list_results_response_unk(c.dec, b, toks) } else { binding::parse_walk_with_list_results_response(c.dec, b, toks) }
    }
    fn dec_ctx(c: &Ctx) -> *mut ak_abi::ak_dec_ctx { c.dec.list_results_response }
    fn touch_f(v: &Self::F) -> u64 { touch_f_list_results_response(v, 0) }
    fn touch_p(v: &Self::P) -> u64 { touch_p_list_results_response(v, 0) }
}

pub struct R_ListTasksDetailedResponse;
impl Ops for R_ListTasksDetailedResponse {
    const ROOT: &'static str = "ListTasksDetailedResponse";
    type F = facade::ListTasksDetailedResponse;
    type P = p::ListTasksDetailedResponse;
    fn build(pid: &str) -> Option<Self::F> {
        match pid {
            "P2.1" => Some(build::payload_p2_1()),
            "P2.2" => Some(build::payload_p2_2()),
            "P2.3" => Some(build::payload_p2_3()),
            "P2.4" => Some(build::payload_p2_4()),
            "P2.5" => Some(build::payload_p2_5()),
            _ => None,
        }
    }
    fn n_decode(b: &[u8], retain: bool) -> Result<Self::F, i32> {
        if retain { core_native_retain::decode_list_tasks_detailed_response(b) } else { core_native::decode_list_tasks_detailed_response(b) }
    }
    fn n_encode(v: &Self::F, e: &mut ak_rt::Enc, retain: bool) {
        if retain { core_native_retain::encode_into_list_tasks_detailed_response(v, e) } else { core_native::encode_into_list_tasks_detailed_response(v, e) }
    }
    fn f_decode(c: &Ctx, b: &[u8], retain: bool) -> Result<Self::F, i32> {
        if retain { binding::decode_with_list_tasks_detailed_response_unk(c.dec, b) } else { binding::decode_with_list_tasks_detailed_response(c.dec, b) }
    }
    fn f_encode(c: &Ctx, v: &Self::F, retain: bool) -> Result<usize, i32> {
        if retain { binding::encode_into_list_tasks_detailed_response_unk(c.enc, v, &c.tcs) } else { binding::encode_into_list_tasks_detailed_response(c.enc, v, &c.tcs) }
    }
    fn f_pull(c: &Ctx, b: &[u8], retain: bool, toks: &mut Vec<i64>) -> Result<Self::F, i32> {
        if retain { binding::parse_walk_with_list_tasks_detailed_response_unk(c.dec, b, toks) } else { binding::parse_walk_with_list_tasks_detailed_response(c.dec, b, toks) }
    }
    fn dec_ctx(c: &Ctx) -> *mut ak_abi::ak_dec_ctx { c.dec.list_tasks_detailed_response }
    fn touch_f(v: &Self::F) -> u64 { touch_f_list_tasks_detailed_response(v, 0) }
    fn touch_p(v: &Self::P) -> u64 { touch_p_list_tasks_detailed_response(v, 0) }
}

pub struct R_ListProbeResponse;
impl Ops for R_ListProbeResponse {
    const ROOT: &'static str = "ListProbeResponse";
    type F = facade::ListProbeResponse;
    type P = p::ListProbeResponse;
    fn build(pid: &str) -> Option<Self::F> {
        match pid {
            "P3.1" => Some(build::payload_p3_1()),
            _ => None,
        }
    }
    fn n_decode(b: &[u8], retain: bool) -> Result<Self::F, i32> {
        if retain { core_native_retain::decode_list_probe_response(b) } else { core_native::decode_list_probe_response(b) }
    }
    fn n_encode(v: &Self::F, e: &mut ak_rt::Enc, retain: bool) {
        if retain { core_native_retain::encode_into_list_probe_response(v, e) } else { core_native::encode_into_list_probe_response(v, e) }
    }
    fn f_decode(c: &Ctx, b: &[u8], retain: bool) -> Result<Self::F, i32> {
        if retain { binding::decode_with_list_probe_response_unk(c.dec, b) } else { binding::decode_with_list_probe_response(c.dec, b) }
    }
    fn f_encode(c: &Ctx, v: &Self::F, retain: bool) -> Result<usize, i32> {
        if retain { binding::encode_into_list_probe_response_unk(c.enc, v, &c.tcs) } else { binding::encode_into_list_probe_response(c.enc, v, &c.tcs) }
    }
    fn f_pull(c: &Ctx, b: &[u8], retain: bool, toks: &mut Vec<i64>) -> Result<Self::F, i32> {
        if retain { binding::parse_walk_with_list_probe_response_unk(c.dec, b, toks) } else { binding::parse_walk_with_list_probe_response(c.dec, b, toks) }
    }
    fn dec_ctx(c: &Ctx) -> *mut ak_abi::ak_dec_ctx { c.dec.list_probe_response }
    fn touch_f(v: &Self::F) -> u64 { touch_f_list_probe_response(v, 0) }
    fn touch_p(v: &Self::P) -> u64 { touch_p_list_probe_response(v, 0) }
}

pub struct R_ListTaskSummaryResponse;
impl Ops for R_ListTaskSummaryResponse {
    const ROOT: &'static str = "ListTaskSummaryResponse";
    type F = facade::ListTaskSummaryResponse;
    type P = p::ListTaskSummaryResponse;
    fn build(pid: &str) -> Option<Self::F> {
        match pid {
            "P4.1" => Some(build::payload_p4_1()),
            _ => None,
        }
    }
    fn n_decode(b: &[u8], retain: bool) -> Result<Self::F, i32> {
        if retain { core_native_retain::decode_list_task_summary_response(b) } else { core_native::decode_list_task_summary_response(b) }
    }
    fn n_encode(v: &Self::F, e: &mut ak_rt::Enc, retain: bool) {
        if retain { core_native_retain::encode_into_list_task_summary_response(v, e) } else { core_native::encode_into_list_task_summary_response(v, e) }
    }
    fn f_decode(c: &Ctx, b: &[u8], retain: bool) -> Result<Self::F, i32> {
        if retain { binding::decode_with_list_task_summary_response_unk(c.dec, b) } else { binding::decode_with_list_task_summary_response(c.dec, b) }
    }
    fn f_encode(c: &Ctx, v: &Self::F, retain: bool) -> Result<usize, i32> {
        if retain { binding::encode_into_list_task_summary_response_unk(c.enc, v, &c.tcs) } else { binding::encode_into_list_task_summary_response(c.enc, v, &c.tcs) }
    }
    fn f_pull(c: &Ctx, b: &[u8], retain: bool, toks: &mut Vec<i64>) -> Result<Self::F, i32> {
        if retain { binding::parse_walk_with_list_task_summary_response_unk(c.dec, b, toks) } else { binding::parse_walk_with_list_task_summary_response(c.dec, b, toks) }
    }
    fn dec_ctx(c: &Ctx) -> *mut ak_abi::ak_dec_ctx { c.dec.list_task_summary_response }
    fn touch_f(v: &Self::F) -> u64 { touch_f_list_task_summary_response(v, 0) }
    fn touch_p(v: &Self::P) -> u64 { touch_p_list_task_summary_response(v, 0) }
}

pub struct R_UploadResultDataMessage;
impl Ops for R_UploadResultDataMessage {
    const ROOT: &'static str = "UploadResultDataMessage";
    type F = facade::UploadResultDataMessage;
    type P = p::UploadResultDataMessage;
    fn build(pid: &str) -> Option<Self::F> {
        match pid {
            "P5.1" => Some(build::payload_p5_1()),
            "P5.2" => Some(build::payload_p5_2()),
            "P5.3" => Some(build::payload_p5_3()),
            "P5.4" => Some(build::payload_p5_4()),
            _ => None,
        }
    }
    fn n_decode(b: &[u8], retain: bool) -> Result<Self::F, i32> {
        if retain { core_native_retain::decode_upload_result_data_message(b) } else { core_native::decode_upload_result_data_message(b) }
    }
    fn n_encode(v: &Self::F, e: &mut ak_rt::Enc, retain: bool) {
        if retain { core_native_retain::encode_into_upload_result_data_message(v, e) } else { core_native::encode_into_upload_result_data_message(v, e) }
    }
    fn f_decode(c: &Ctx, b: &[u8], retain: bool) -> Result<Self::F, i32> {
        if retain { binding::decode_with_upload_result_data_message_unk(c.dec, b) } else { binding::decode_with_upload_result_data_message(c.dec, b) }
    }
    fn f_encode(c: &Ctx, v: &Self::F, retain: bool) -> Result<usize, i32> {
        if retain { binding::encode_into_upload_result_data_message_unk(c.enc, v, &c.tcs) } else { binding::encode_into_upload_result_data_message(c.enc, v, &c.tcs) }
    }
    fn f_pull(c: &Ctx, b: &[u8], retain: bool, toks: &mut Vec<i64>) -> Result<Self::F, i32> {
        if retain { binding::parse_walk_with_upload_result_data_message_unk(c.dec, b, toks) } else { binding::parse_walk_with_upload_result_data_message(c.dec, b, toks) }
    }
    fn dec_ctx(c: &Ctx) -> *mut ak_abi::ak_dec_ctx { c.dec.upload_result_data_message }
    fn touch_f(v: &Self::F) -> u64 { touch_f_upload_result_data_message(v, 0) }
    fn touch_p(v: &Self::P) -> u64 { touch_p_upload_result_data_message(v, 0) }
}

pub struct R_ListMetricsResponse;
impl Ops for R_ListMetricsResponse {
    const ROOT: &'static str = "ListMetricsResponse";
    type F = facade::ListMetricsResponse;
    type P = p::ListMetricsResponse;
    fn build(pid: &str) -> Option<Self::F> {
        match pid {
            "P6.1" => Some(build::payload_p6_1()),
            _ => None,
        }
    }
    fn n_decode(b: &[u8], retain: bool) -> Result<Self::F, i32> {
        if retain { core_native_retain::decode_list_metrics_response(b) } else { core_native::decode_list_metrics_response(b) }
    }
    fn n_encode(v: &Self::F, e: &mut ak_rt::Enc, retain: bool) {
        if retain { core_native_retain::encode_into_list_metrics_response(v, e) } else { core_native::encode_into_list_metrics_response(v, e) }
    }
    fn f_decode(c: &Ctx, b: &[u8], retain: bool) -> Result<Self::F, i32> {
        if retain { binding::decode_with_list_metrics_response_unk(c.dec, b) } else { binding::decode_with_list_metrics_response(c.dec, b) }
    }
    fn f_encode(c: &Ctx, v: &Self::F, retain: bool) -> Result<usize, i32> {
        if retain { binding::encode_into_list_metrics_response_unk(c.enc, v, &c.tcs) } else { binding::encode_into_list_metrics_response(c.enc, v, &c.tcs) }
    }
    fn f_pull(c: &Ctx, b: &[u8], retain: bool, toks: &mut Vec<i64>) -> Result<Self::F, i32> {
        if retain { binding::parse_walk_with_list_metrics_response_unk(c.dec, b, toks) } else { binding::parse_walk_with_list_metrics_response(c.dec, b, toks) }
    }
    fn dec_ctx(c: &Ctx) -> *mut ak_abi::ak_dec_ctx { c.dec.list_metrics_response }
    fn touch_f(v: &Self::F) -> u64 { touch_f_list_metrics_response(v, 0) }
    fn touch_p(v: &Self::P) -> u64 { touch_p_list_metrics_response(v, 0) }
}

pub struct R_DualResponse;
impl Ops for R_DualResponse {
    const ROOT: &'static str = "DualResponse";
    type F = facade::DualResponse;
    type P = p::DualResponse;
    fn build(pid: &str) -> Option<Self::F> {
        match pid {
            _ => None,
        }
    }
    fn n_decode(b: &[u8], retain: bool) -> Result<Self::F, i32> {
        if retain { core_native_retain::decode_dual_response(b) } else { core_native::decode_dual_response(b) }
    }
    fn n_encode(v: &Self::F, e: &mut ak_rt::Enc, retain: bool) {
        if retain { core_native_retain::encode_into_dual_response(v, e) } else { core_native::encode_into_dual_response(v, e) }
    }
    fn f_decode(c: &Ctx, b: &[u8], retain: bool) -> Result<Self::F, i32> {
        if retain { binding::decode_with_dual_response_unk(c.dec, b) } else { binding::decode_with_dual_response(c.dec, b) }
    }
    fn f_encode(c: &Ctx, v: &Self::F, retain: bool) -> Result<usize, i32> {
        if retain { binding::encode_into_dual_response_unk(c.enc, v, &c.tcs) } else { binding::encode_into_dual_response(c.enc, v, &c.tcs) }
    }
    fn f_pull(c: &Ctx, b: &[u8], retain: bool, toks: &mut Vec<i64>) -> Result<Self::F, i32> {
        if retain { binding::parse_walk_with_dual_response_unk(c.dec, b, toks) } else { binding::parse_walk_with_dual_response(c.dec, b, toks) }
    }
    fn dec_ctx(c: &Ctx) -> *mut ak_abi::ak_dec_ctx { c.dec.dual_response }
    fn touch_f(v: &Self::F) -> u64 { touch_f_dual_response(v, 0) }
    fn touch_p(v: &Self::P) -> u64 { touch_p_dual_response(v, 0) }
}

/// Call `v.visit::<R>()` for the root named `root`; false if this slice has no such root.
pub fn with_root<V: crate::Visit>(root: &str, v: &mut V) -> bool {
    match root {
        "ListResultsResponse" => { v.visit::<R_ListResultsResponse>(); true }
        "ListTasksDetailedResponse" => { v.visit::<R_ListTasksDetailedResponse>(); true }
        "ListProbeResponse" => { v.visit::<R_ListProbeResponse>(); true }
        "ListTaskSummaryResponse" => { v.visit::<R_ListTaskSummaryResponse>(); true }
        "UploadResultDataMessage" => { v.visit::<R_UploadResultDataMessage>(); true }
        "ListMetricsResponse" => { v.visit::<R_ListMetricsResponse>(); true }
        "DualResponse" => { v.visit::<R_DualResponse>(); true }
        _ => false,
    }
}

pub const ROOTS: &[&str] = &["ListResultsResponse", "ListTasksDetailedResponse", "ListProbeResponse", "ListTaskSummaryResponse", "UploadResultDataMessage", "ListMetricsResponse", "DualResponse"];
