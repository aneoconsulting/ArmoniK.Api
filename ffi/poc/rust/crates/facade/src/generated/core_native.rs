// @generated by ffi/poc/codec/gen/rust_native.py (plan from ffi/schema/shapes.json). Do not edit.

//! Arm `core-native` (unknown fields: DROP): the codec rendered into the host from the
//! SAME plans as the core behind the C ABI (`poc/codec/gen/plan.py`), over the facade
//! types, with no boundary at all. README R3's no-boundary control; R-E1: the two
//! arms differ only in where a value comes from.
#![allow(clippy::all)]
use super::types::*;
use ak_rt::dec::Dec;
use ak_rt::Enc;

/// One learned length-prefix slot per site in this file (ABI v1 section 6).
pub const SITES: usize = 35;
/// The plan's recursion limit (Options.recursion_limit).
pub const LIMIT: u32 = 100;
/// The plan's UTF-8 policy on decode, for a log to name.
pub const UTF8_POLICY: &str = "reject";
/// The plan's unknown-field mode this module renders.
pub const UNKNOWN_FIELDS: &str = "drop";

#[inline]
#[allow(unused_variables)]
fn enc_timestamp(o: &Timestamp, e: &mut Enc) {
    { let x = o.seconds; if x != 0 { e.varint_field(1, x as u64); } }
    { let x = o.nanos; if x != 0 { e.varint_field(2, x as i64 as u64); } }
}

#[inline]
#[allow(unused_variables)]
fn enc_duration(o: &Duration, e: &mut Enc) {
    { let x = o.seconds; if x != 0 { e.varint_field(1, x as u64); } }
    { let x = o.nanos; if x != 0 { e.varint_field(2, x as i64 as u64); } }
}

#[inline]
#[allow(unused_variables)]
fn enc_result_raw(o: &ResultRaw, e: &mut Enc) {
    if !o.session_id.is_empty() { e.blob_field(1, o.session_id.as_bytes()); }
    if !o.name.is_empty() { e.blob_field(2, o.name.as_bytes()); }
    if !o.owner_task_id.is_empty() { e.blob_field(3, o.owner_task_id.as_bytes()); }
    { let x = o.status.to_i32(); if x != 0 { e.varint_field(4, x as i64 as u64); } }
    if let Some(c) = &o.created_at {
        let mk = e.begin(5, 0);
        enc_timestamp(c, e);
        e.end(mk);
    }
    if let Some(c) = &o.completed_at {
        let mk = e.begin(6, 1);
        enc_timestamp(c, e);
        e.end(mk);
    }
    if !o.result_id.is_empty() { e.blob_field(8, o.result_id.as_bytes()); }
    { let x = o.size; if x != 0 { e.varint_field(9, x as u64); } }
    if !o.created_by.is_empty() { e.blob_field(10, o.created_by.as_bytes()); }
    if !o.opaque_id.is_empty() { e.blob_field(11, &o.opaque_id[..]); }
    { let x = o.manual_deletion; if x { e.varint_field(12, x as u64); } }
}

#[inline]
#[allow(unused_variables)]
fn enc_task_options(o: &TaskOptions, e: &mut Enc) {
    for (k, val) in &o.options {
        let mk = e.begin(1, 2);
        if !k.is_empty() { e.blob_field(1, k.as_bytes()); }
        if !val.is_empty() { e.blob_field(2, val.as_bytes()); }
        e.end(mk);
    }
    if let Some(c) = &o.max_duration {
        let mk = e.begin(2, 3);
        enc_duration(c, e);
        e.end(mk);
    }
    { let x = o.max_retries; if x != 0 { e.varint_field(3, x as i64 as u64); } }
    { let x = o.priority; if x != 0 { e.varint_field(4, x as i64 as u64); } }
    if !o.partition_id.is_empty() { e.blob_field(5, o.partition_id.as_bytes()); }
    if !o.application_name.is_empty() { e.blob_field(6, o.application_name.as_bytes()); }
    if !o.application_version.is_empty() { e.blob_field(7, o.application_version.as_bytes()); }
    if !o.application_namespace.is_empty() { e.blob_field(8, o.application_namespace.as_bytes()); }
    if !o.application_service.is_empty() { e.blob_field(9, o.application_service.as_bytes()); }
    if !o.engine_type.is_empty() { e.blob_field(10, o.engine_type.as_bytes()); }
}

#[inline]
#[allow(unused_variables)]
fn enc_task_output(o: &TaskOutput, e: &mut Enc) {
    { let x = o.success; if x { e.varint_field(1, x as u64); } }
    if !o.error.is_empty() { e.blob_field(2, o.error.as_bytes()); }
}

#[inline]
#[allow(unused_variables)]
fn enc_task_detailed(o: &TaskDetailed, e: &mut Enc) {
    if !o.id.is_empty() { e.blob_field(1, o.id.as_bytes()); }
    if !o.session_id.is_empty() { e.blob_field(2, o.session_id.as_bytes()); }
    if !o.owner_pod_id.is_empty() { e.blob_field(3, o.owner_pod_id.as_bytes()); }
    for x in &o.parent_task_ids { e.blob_field(4, x.as_bytes()); }
    for x in &o.data_dependencies { e.blob_field(5, x.as_bytes()); }
    for x in &o.expected_output_ids { e.blob_field(6, x.as_bytes()); }
    for x in &o.retry_of_ids { e.blob_field(7, x.as_bytes()); }
    { let x = o.status.to_i32(); if x != 0 { e.varint_field(8, x as i64 as u64); } }
    if !o.status_message.is_empty() { e.blob_field(9, o.status_message.as_bytes()); }
    if let Some(c) = &o.options {
        let mk = e.begin(10, 4);
        enc_task_options(c, e);
        e.end(mk);
    }
    if let Some(c) = &o.created_at {
        let mk = e.begin(11, 5);
        enc_timestamp(c, e);
        e.end(mk);
    }
    if let Some(c) = &o.submitted_at {
        let mk = e.begin(12, 6);
        enc_timestamp(c, e);
        e.end(mk);
    }
    if let Some(c) = &o.started_at {
        let mk = e.begin(13, 7);
        enc_timestamp(c, e);
        e.end(mk);
    }
    if let Some(c) = &o.ended_at {
        let mk = e.begin(14, 8);
        enc_timestamp(c, e);
        e.end(mk);
    }
    if let Some(c) = &o.pod_ttl {
        let mk = e.begin(15, 9);
        enc_timestamp(c, e);
        e.end(mk);
    }
    if let Some(c) = &o.output {
        let mk = e.begin(16, 10);
        enc_task_output(c, e);
        e.end(mk);
    }
    if !o.pod_hostname.is_empty() { e.blob_field(17, o.pod_hostname.as_bytes()); }
    if let Some(c) = &o.received_at {
        let mk = e.begin(18, 11);
        enc_timestamp(c, e);
        e.end(mk);
    }
    if let Some(c) = &o.acquired_at {
        let mk = e.begin(19, 12);
        enc_timestamp(c, e);
        e.end(mk);
    }
    if let Some(c) = &o.creation_to_end_duration {
        let mk = e.begin(20, 13);
        enc_duration(c, e);
        e.end(mk);
    }
    if let Some(c) = &o.processing_to_end_duration {
        let mk = e.begin(21, 14);
        enc_duration(c, e);
        e.end(mk);
    }
    if !o.initial_task_id.is_empty() { e.blob_field(22, o.initial_task_id.as_bytes()); }
    if let Some(c) = &o.received_to_end_duration {
        let mk = e.begin(23, 15);
        enc_duration(c, e);
        e.end(mk);
    }
    if let Some(c) = &o.processed_at {
        let mk = e.begin(24, 16);
        enc_timestamp(c, e);
        e.end(mk);
    }
    if let Some(c) = &o.fetched_at {
        let mk = e.begin(25, 17);
        enc_timestamp(c, e);
        e.end(mk);
    }
    if !o.payload_id.is_empty() { e.blob_field(26, o.payload_id.as_bytes()); }
    if !o.created_by.is_empty() { e.blob_field(27, o.created_by.as_bytes()); }
}

#[inline]
#[allow(unused_variables)]
fn enc_task_summary(o: &TaskSummary, e: &mut Enc) {
    if !o.id.is_empty() { e.blob_field(1, o.id.as_bytes()); }
    if !o.session_id.is_empty() { e.blob_field(2, o.session_id.as_bytes()); }
    if let Some(c) = &o.options {
        let mk = e.begin(3, 18);
        enc_task_options(c, e);
        e.end(mk);
    }
    { let x = o.status.to_i32(); if x != 0 { e.varint_field(4, x as i64 as u64); } }
    if let Some(c) = &o.created_at {
        let mk = e.begin(5, 19);
        enc_timestamp(c, e);
        e.end(mk);
    }
    if !o.error.is_empty() { e.blob_field(8, o.error.as_bytes()); }
    if !o.status_message.is_empty() { e.blob_field(9, o.status_message.as_bytes()); }
    { let x = o.count_data_dependencies; if x != 0 { e.varint_field(11, x as u64); } }
}

#[inline]
#[allow(unused_variables)]
fn enc_probe(o: &Probe, e: &mut Enc) {
    if !o.id.is_empty() { e.blob_field(1, o.id.as_bytes()); }
    if let Some(x) = &o.opt_count { e.varint_field(2, (*x) as i64 as u64); }
    if let Some(x) = &o.opt_label { e.blob_field(3, x.as_bytes()); }
    if let Some(x) = &o.opt_flag { e.varint_field(4, (*x) as u64); }
    if let Some(ProbeBody::AsInt(x)) = &o.body {
        e.varint_field(10, (*x) as u64);
    }
    if let Some(ProbeBody::AsText(x)) = &o.body {
        e.blob_field(11, x.as_bytes());
    }
    if let Some(ProbeBody::AsBlob(x)) = &o.body {
        e.blob_field(12, &x[..]);
    }
    if let Some(ProbeBody::AsStamp(x)) = &o.body {
        let mk = e.begin(13, 20);
        enc_timestamp(x, e);
        e.end(mk);
    }
    if let Some(ProbeBody::AsNothing(x)) = &o.body {
        let mk = e.begin(14, 21);
        enc_empty(x, e);
        e.end(mk);
    }
}

#[inline]
#[allow(unused_variables)]
fn enc_empty(o: &Empty, e: &mut Enc) {
}

#[inline]
#[allow(unused_variables)]
fn enc_upload_result_data(o: &UploadResultData, e: &mut Enc) {
    if !o.session_id.is_empty() { e.blob_field(1, o.session_id.as_bytes()); }
    if !o.result_id.is_empty() { e.blob_field(2, o.result_id.as_bytes()); }
    if !o.data_chunk.is_empty() { e.blob_field(3, &o.data_chunk[..]); }
}

#[inline]
#[allow(unused_variables)]
fn enc_metrics_batch(o: &MetricsBatch, e: &mut Enc) {
    if !o.id.is_empty() { e.blob_field(1, o.id.as_bytes()); }
    if !o.ticks.is_empty() {
        let mk = e.begin(2, 22);
        for x in &o.ticks { e.varint(*x as u64); }
        e.end(mk);
    }
    if !o.values.is_empty() {
        let mk = e.begin(3, 23);
        for x in &o.values { e.buf.extend_from_slice(&x.to_le_bytes()); }
        e.end(mk);
    }
    if !o.codes.is_empty() {
        let mk = e.begin(4, 24);
        for x in &o.codes { e.varint((*x) as i64 as u64); }
        e.end(mk);
    }
    if !o.flags.is_empty() {
        let mk = e.begin(5, 25);
        for x in &o.flags { e.varint(*x as u64); }
        e.end(mk);
    }
    if !o.statuses.is_empty() {
        let mk = e.begin(6, 26);
        for x in &o.statuses { e.varint((*x).to_i32() as i64 as u64); }
        e.end(mk);
    }
}

#[inline]
#[allow(unused_variables)]
fn enc_pair(o: &Pair, e: &mut Enc) {
    if !o.key.is_empty() { e.blob_field(1, o.key.as_bytes()); }
    { let x = o.value; if x != 0 { e.varint_field(2, x as i64 as u64); } }
}

#[inline]
#[allow(unused_variables)]
fn enc_list_results_response(o: &ListResultsResponse, e: &mut Enc) {
    for c in &o.results {
        let mk = e.begin(1, 27);
        enc_result_raw(c, e);
        e.end(mk);
    }
    { let x = o.page; if x != 0 { e.varint_field(2, x as i64 as u64); } }
    { let x = o.total; if x != 0 { e.varint_field(3, x as i64 as u64); } }
}

#[inline]
#[allow(unused_variables)]
fn enc_list_tasks_detailed_response(o: &ListTasksDetailedResponse, e: &mut Enc) {
    for c in &o.tasks {
        let mk = e.begin(1, 28);
        enc_task_detailed(c, e);
        e.end(mk);
    }
    { let x = o.page; if x != 0 { e.varint_field(2, x as i64 as u64); } }
    { let x = o.total; if x != 0 { e.varint_field(3, x as i64 as u64); } }
}

#[inline]
#[allow(unused_variables)]
fn enc_list_task_summary_response(o: &ListTaskSummaryResponse, e: &mut Enc) {
    for c in &o.tasks {
        let mk = e.begin(1, 29);
        enc_task_summary(c, e);
        e.end(mk);
    }
}

#[inline]
#[allow(unused_variables)]
fn enc_list_probe_response(o: &ListProbeResponse, e: &mut Enc) {
    for c in &o.probes {
        let mk = e.begin(1, 30);
        enc_probe(c, e);
        e.end(mk);
    }
}

#[inline]
#[allow(unused_variables)]
fn enc_list_metrics_response(o: &ListMetricsResponse, e: &mut Enc) {
    for c in &o.batches {
        let mk = e.begin(1, 31);
        enc_metrics_batch(c, e);
        e.end(mk);
    }
}

#[inline]
#[allow(unused_variables)]
fn enc_upload_result_data_message(o: &UploadResultDataMessage, e: &mut Enc) {
    if let Some(c) = &o.upload {
        let mk = e.begin(1, 32);
        enc_upload_result_data(c, e);
        e.end(mk);
    }
}

#[inline]
#[allow(unused_variables)]
fn enc_dual_response(o: &DualResponse, e: &mut Enc) {
    for c in &o.left {
        let mk = e.begin(1, 33);
        enc_pair(c, e);
        e.end(mk);
    }
    for c in &o.right {
        let mk = e.begin(2, 34);
        enc_pair(c, e);
        e.end(mk);
    }
}

#[inline]
#[allow(unused_variables, unused_mut)]
fn dec_timestamp(d: &mut Dec, out: &mut Timestamp, depth: u32) {
    // Plan rule: a message more than LIMIT levels below the root is refused.
    if depth > LIMIT { d.err = ak_rt::ERR_DEPTH; return; }
    let buf = d.buf;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { return; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; return; }
        match (tag, wire) {
            (1, 0) => {
                let v = d.varint() as i64;
                if d.err == 0 { out.seconds = v; }
            }
            (2, 0) => {
                let v = d.varint() as i32;
                if d.err == 0 { out.nanos = v; }
            }
            // Plan rule (drop): an unknown field -- including a known number at
            // a wire type the table has no arm for (R-E2) -- is skipped.
            _ => d.skip(tag, wire),
        }
    }
}

#[inline]
#[allow(unused_variables, unused_mut)]
fn dec_duration(d: &mut Dec, out: &mut Duration, depth: u32) {
    // Plan rule: a message more than LIMIT levels below the root is refused.
    if depth > LIMIT { d.err = ak_rt::ERR_DEPTH; return; }
    let buf = d.buf;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { return; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; return; }
        match (tag, wire) {
            (1, 0) => {
                let v = d.varint() as i64;
                if d.err == 0 { out.seconds = v; }
            }
            (2, 0) => {
                let v = d.varint() as i32;
                if d.err == 0 { out.nanos = v; }
            }
            // Plan rule (drop): an unknown field -- including a known number at
            // a wire type the table has no arm for (R-E2) -- is skipped.
            _ => d.skip(tag, wire),
        }
    }
}

#[inline]
#[allow(unused_variables, unused_mut)]
fn dec_result_raw(d: &mut Dec, out: &mut ResultRaw, depth: u32) {
    // Plan rule: a message more than LIMIT levels below the root is refused.
    if depth > LIMIT { d.err = ak_rt::ERR_DEPTH; return; }
    let buf = d.buf;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { return; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; return; }
        match (tag, wire) {
            (1, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.session_id = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (2, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.name = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (3, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.owner_task_id = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (4, 0) => {
                let v = ResultStatus::from_i32(d.varint() as i32);
                if d.err == 0 { out.status = v; }
            }
            (5, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                // Plan rule: a repeated occurrence MERGES into the one decoded.
                let c = out.created_at.get_or_insert_with(Default::default);
                dec_timestamp(&mut sub, c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (6, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                // Plan rule: a repeated occurrence MERGES into the one decoded.
                let c = out.completed_at.get_or_insert_with(Default::default);
                dec_timestamp(&mut sub, c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (8, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.result_id = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (9, 0) => {
                let v = d.varint() as i64;
                if d.err == 0 { out.size = v; }
            }
            (10, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.created_by = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (11, 2) => {
                let (off, n) = d.len_body();
                out.opaque_id = ::bytes::Bytes::copy_from_slice(&buf[off..off + n]);
            }
            (12, 0) => {
                let v = d.varint() != 0;
                if d.err == 0 { out.manual_deletion = v; }
            }
            // Plan rule (drop): an unknown field -- including a known number at
            // a wire type the table has no arm for (R-E2) -- is skipped.
            _ => d.skip(tag, wire),
        }
    }
}

#[inline]
#[allow(unused_variables, unused_mut)]
fn dec_task_options(d: &mut Dec, out: &mut TaskOptions, depth: u32) {
    // Plan rule: a message more than LIMIT levels below the root is refused.
    if depth > LIMIT { d.err = ak_rt::ERR_DEPTH; return; }
    let buf = d.buf;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { return; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; return; }
        match (tag, wire) {
            (1, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                if depth + 1 > LIMIT { d.err = ak_rt::ERR_DEPTH; return; }
                let (mut k, mut val) = (String::new(), String::new());
                let eb = sub.buf;
                while !sub.at_end() {
                    let kk = sub.varint();
                    if sub.err != 0 { break; }
                    let (et, ew) = ((kk >> 3) as u32, (kk & 7) as u32);
                    if et == 0 || (kk >> 3) > ak_rt::MAX_FIELD_NUMBER { sub.err = ak_rt::ERR_MALFORMED; break; }
                    match (et, ew) {
                        (1, 2) => {
                            let (a, b) = sub.len_body();
                            match ak_rt::strings::decode_str_reject(&eb[a..a + b]) {
                                Ok(s) => k = s,
                                Err(er) => { d.err = er; return; }
                            }
                        }
                        (2, 2) => {
                            let (a, b) = sub.len_body();
                            match ak_rt::strings::decode_str_reject(&eb[a..a + b]) {
                                Ok(s) => val = s,
                                Err(er) => { d.err = er; return; }
                            }
                        }
                        // A facade map entry has no bag: an unknown field inside
                        // an entry is skipped in both modes (`U-map-entry`, disputed).
                        _ => sub.skip(et, ew),
                    }
                }
                if sub.err != 0 { d.err = sub.err; return; }
                // Plan rule: a duplicate key replaces the earlier value.
                out.options.insert(k, val);
            }
            (2, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                // Plan rule: a repeated occurrence MERGES into the one decoded.
                let c = out.max_duration.get_or_insert_with(Default::default);
                dec_duration(&mut sub, c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (3, 0) => {
                let v = d.varint() as i32;
                if d.err == 0 { out.max_retries = v; }
            }
            (4, 0) => {
                let v = d.varint() as i32;
                if d.err == 0 { out.priority = v; }
            }
            (5, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.partition_id = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (6, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.application_name = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (7, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.application_version = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (8, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.application_namespace = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (9, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.application_service = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (10, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.engine_type = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            // Plan rule (drop): an unknown field -- including a known number at
            // a wire type the table has no arm for (R-E2) -- is skipped.
            _ => d.skip(tag, wire),
        }
    }
}

#[inline]
#[allow(unused_variables, unused_mut)]
fn dec_task_output(d: &mut Dec, out: &mut TaskOutput, depth: u32) {
    // Plan rule: a message more than LIMIT levels below the root is refused.
    if depth > LIMIT { d.err = ak_rt::ERR_DEPTH; return; }
    let buf = d.buf;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { return; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; return; }
        match (tag, wire) {
            (1, 0) => {
                let v = d.varint() != 0;
                if d.err == 0 { out.success = v; }
            }
            (2, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.error = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            // Plan rule (drop): an unknown field -- including a known number at
            // a wire type the table has no arm for (R-E2) -- is skipped.
            _ => d.skip(tag, wire),
        }
    }
}

#[inline]
#[allow(unused_variables, unused_mut)]
fn dec_task_detailed(d: &mut Dec, out: &mut TaskDetailed, depth: u32) {
    // Plan rule: a message more than LIMIT levels below the root is refused.
    if depth > LIMIT { d.err = ak_rt::ERR_DEPTH; return; }
    let buf = d.buf;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { return; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; return; }
        match (tag, wire) {
            (1, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.id = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (2, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.session_id = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (3, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.owner_pod_id = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (4, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.parent_task_ids.push(s),
                    Err(er) => { d.err = er; return; }
                }
            }
            (5, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.data_dependencies.push(s),
                    Err(er) => { d.err = er; return; }
                }
            }
            (6, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.expected_output_ids.push(s),
                    Err(er) => { d.err = er; return; }
                }
            }
            (7, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.retry_of_ids.push(s),
                    Err(er) => { d.err = er; return; }
                }
            }
            (8, 0) => {
                let v = TaskStatus::from_i32(d.varint() as i32);
                if d.err == 0 { out.status = v; }
            }
            (9, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.status_message = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (10, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                // Plan rule: a repeated occurrence MERGES into the one decoded.
                let c = out.options.get_or_insert_with(Default::default);
                dec_task_options(&mut sub, c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (11, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                // Plan rule: a repeated occurrence MERGES into the one decoded.
                let c = out.created_at.get_or_insert_with(Default::default);
                dec_timestamp(&mut sub, c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (12, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                // Plan rule: a repeated occurrence MERGES into the one decoded.
                let c = out.submitted_at.get_or_insert_with(Default::default);
                dec_timestamp(&mut sub, c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (13, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                // Plan rule: a repeated occurrence MERGES into the one decoded.
                let c = out.started_at.get_or_insert_with(Default::default);
                dec_timestamp(&mut sub, c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (14, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                // Plan rule: a repeated occurrence MERGES into the one decoded.
                let c = out.ended_at.get_or_insert_with(Default::default);
                dec_timestamp(&mut sub, c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (15, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                // Plan rule: a repeated occurrence MERGES into the one decoded.
                let c = out.pod_ttl.get_or_insert_with(Default::default);
                dec_timestamp(&mut sub, c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (16, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                // Plan rule: a repeated occurrence MERGES into the one decoded.
                let c = out.output.get_or_insert_with(Default::default);
                dec_task_output(&mut sub, c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (17, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.pod_hostname = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (18, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                // Plan rule: a repeated occurrence MERGES into the one decoded.
                let c = out.received_at.get_or_insert_with(Default::default);
                dec_timestamp(&mut sub, c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (19, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                // Plan rule: a repeated occurrence MERGES into the one decoded.
                let c = out.acquired_at.get_or_insert_with(Default::default);
                dec_timestamp(&mut sub, c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (20, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                // Plan rule: a repeated occurrence MERGES into the one decoded.
                let c = out.creation_to_end_duration.get_or_insert_with(Default::default);
                dec_duration(&mut sub, c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (21, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                // Plan rule: a repeated occurrence MERGES into the one decoded.
                let c = out.processing_to_end_duration.get_or_insert_with(Default::default);
                dec_duration(&mut sub, c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (22, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.initial_task_id = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (23, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                // Plan rule: a repeated occurrence MERGES into the one decoded.
                let c = out.received_to_end_duration.get_or_insert_with(Default::default);
                dec_duration(&mut sub, c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (24, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                // Plan rule: a repeated occurrence MERGES into the one decoded.
                let c = out.processed_at.get_or_insert_with(Default::default);
                dec_timestamp(&mut sub, c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (25, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                // Plan rule: a repeated occurrence MERGES into the one decoded.
                let c = out.fetched_at.get_or_insert_with(Default::default);
                dec_timestamp(&mut sub, c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (26, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.payload_id = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (27, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.created_by = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            // Plan rule (drop): an unknown field -- including a known number at
            // a wire type the table has no arm for (R-E2) -- is skipped.
            _ => d.skip(tag, wire),
        }
    }
}

#[inline]
#[allow(unused_variables, unused_mut)]
fn dec_task_summary(d: &mut Dec, out: &mut TaskSummary, depth: u32) {
    // Plan rule: a message more than LIMIT levels below the root is refused.
    if depth > LIMIT { d.err = ak_rt::ERR_DEPTH; return; }
    let buf = d.buf;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { return; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; return; }
        match (tag, wire) {
            (1, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.id = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (2, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.session_id = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (3, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                // Plan rule: a repeated occurrence MERGES into the one decoded.
                let c = out.options.get_or_insert_with(Default::default);
                dec_task_options(&mut sub, c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (4, 0) => {
                let v = TaskStatus::from_i32(d.varint() as i32);
                if d.err == 0 { out.status = v; }
            }
            (5, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                // Plan rule: a repeated occurrence MERGES into the one decoded.
                let c = out.created_at.get_or_insert_with(Default::default);
                dec_timestamp(&mut sub, c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (8, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.error = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (9, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.status_message = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (11, 0) => {
                let v = d.varint() as i64;
                if d.err == 0 { out.count_data_dependencies = v; }
            }
            // Plan rule (drop): an unknown field -- including a known number at
            // a wire type the table has no arm for (R-E2) -- is skipped.
            _ => d.skip(tag, wire),
        }
    }
}

#[inline]
#[allow(unused_variables, unused_mut)]
fn dec_probe(d: &mut Dec, out: &mut Probe, depth: u32) {
    // Plan rule: a message more than LIMIT levels below the root is refused.
    if depth > LIMIT { d.err = ak_rt::ERR_DEPTH; return; }
    let buf = d.buf;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { return; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; return; }
        match (tag, wire) {
            (1, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.id = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (2, 0) => {
                let v = d.varint() as i32;
                if d.err == 0 { out.opt_count = Some(v); }
            }
            (3, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.opt_label = Some(s),
                    Err(er) => { d.err = er; return; }
                }
            }
            (4, 0) => {
                let v = d.varint() != 0;
                if d.err == 0 { out.opt_flag = Some(v); }
            }
            (10, 0) => {
                let v = d.varint() as i64;
                if d.err == 0 { out.body = Some(ProbeBody::AsInt(v)); }
            }
            (11, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.body = Some(ProbeBody::AsText(s)),
                    Err(er) => { d.err = er; return; }
                }
            }
            (12, 2) => {
                let (off, n) = d.len_body();
                out.body = Some(ProbeBody::AsBlob(::bytes::Bytes::copy_from_slice(&buf[off..off + n])));
            }
            (13, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                // Plan rule: the SAME member merges; another one starts empty.
                if !matches!(out.body, Some(ProbeBody::AsStamp(_))) {
                    out.body = Some(ProbeBody::AsStamp(Default::default()));
                }
                if let Some(ProbeBody::AsStamp(c)) = &mut out.body {
                    dec_timestamp(&mut sub, c, depth + 1);
                }
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (14, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                // Plan rule: the SAME member merges; another one starts empty.
                if !matches!(out.body, Some(ProbeBody::AsNothing(_))) {
                    out.body = Some(ProbeBody::AsNothing(Default::default()));
                }
                if let Some(ProbeBody::AsNothing(c)) = &mut out.body {
                    dec_empty(&mut sub, c, depth + 1);
                }
                if sub.err != 0 { d.err = sub.err; return; }
            }
            // Plan rule (drop): an unknown field -- including a known number at
            // a wire type the table has no arm for (R-E2) -- is skipped.
            _ => d.skip(tag, wire),
        }
    }
}

#[inline]
#[allow(unused_variables, unused_mut)]
fn dec_empty(d: &mut Dec, out: &mut Empty, depth: u32) {
    // Plan rule: a message more than LIMIT levels below the root is refused.
    if depth > LIMIT { d.err = ak_rt::ERR_DEPTH; return; }
    let buf = d.buf;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { return; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; return; }
        match (tag, wire) {
            // Plan rule (drop): an unknown field -- including a known number at
            // a wire type the table has no arm for (R-E2) -- is skipped.
            _ => d.skip(tag, wire),
        }
    }
}

#[inline]
#[allow(unused_variables, unused_mut)]
fn dec_upload_result_data(d: &mut Dec, out: &mut UploadResultData, depth: u32) {
    // Plan rule: a message more than LIMIT levels below the root is refused.
    if depth > LIMIT { d.err = ak_rt::ERR_DEPTH; return; }
    let buf = d.buf;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { return; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; return; }
        match (tag, wire) {
            (1, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.session_id = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (2, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.result_id = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (3, 2) => {
                let (off, n) = d.len_body();
                out.data_chunk = ::bytes::Bytes::copy_from_slice(&buf[off..off + n]);
            }
            // Plan rule (drop): an unknown field -- including a known number at
            // a wire type the table has no arm for (R-E2) -- is skipped.
            _ => d.skip(tag, wire),
        }
    }
}

#[inline]
#[allow(unused_variables, unused_mut)]
fn dec_metrics_batch(d: &mut Dec, out: &mut MetricsBatch, depth: u32) {
    // Plan rule: a message more than LIMIT levels below the root is refused.
    if depth > LIMIT { d.err = ak_rt::ERR_DEPTH; return; }
    let buf = d.buf;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { return; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; return; }
        match (tag, wire) {
            (1, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.id = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (2, 0) => {
                let v = d.varint() as i64;
                if d.err == 0 { out.ticks.push(v); }
            }
            (2, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                while !sub.at_end() {
                    let v = sub.varint() as i64;
                    if sub.err != 0 { break; }
                    out.ticks.push(v);
                }
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (3, 1) => {
                let v = d.f64();
                if d.err == 0 { out.values.push(v); }
            }
            (3, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                while !sub.at_end() {
                    let v = sub.f64();
                    if sub.err != 0 { break; }
                    out.values.push(v);
                }
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (4, 0) => {
                let v = d.varint() as i32;
                if d.err == 0 { out.codes.push(v); }
            }
            (4, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                while !sub.at_end() {
                    let v = sub.varint() as i32;
                    if sub.err != 0 { break; }
                    out.codes.push(v);
                }
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (5, 0) => {
                let v = d.varint() != 0;
                if d.err == 0 { out.flags.push(v); }
            }
            (5, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                while !sub.at_end() {
                    let v = sub.varint() != 0;
                    if sub.err != 0 { break; }
                    out.flags.push(v);
                }
                if sub.err != 0 { d.err = sub.err; return; }
            }
            (6, 0) => {
                let v = TaskStatus::from_i32(d.varint() as i32);
                if d.err == 0 { out.statuses.push(v); }
            }
            (6, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                while !sub.at_end() {
                    let v = TaskStatus::from_i32(sub.varint() as i32);
                    if sub.err != 0 { break; }
                    out.statuses.push(v);
                }
                if sub.err != 0 { d.err = sub.err; return; }
            }
            // Plan rule (drop): an unknown field -- including a known number at
            // a wire type the table has no arm for (R-E2) -- is skipped.
            _ => d.skip(tag, wire),
        }
    }
}

#[inline]
#[allow(unused_variables, unused_mut)]
fn dec_pair(d: &mut Dec, out: &mut Pair, depth: u32) {
    // Plan rule: a message more than LIMIT levels below the root is refused.
    if depth > LIMIT { d.err = ak_rt::ERR_DEPTH; return; }
    let buf = d.buf;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { return; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; return; }
        match (tag, wire) {
            (1, 2) => {
                let (off, n) = d.len_body();
                match ak_rt::strings::decode_str_reject(&buf[off..off + n]) {
                    Ok(s) => out.key = s,
                    Err(er) => { d.err = er; return; }
                }
            }
            (2, 0) => {
                let v = d.varint() as i32;
                if d.err == 0 { out.value = v; }
            }
            // Plan rule (drop): an unknown field -- including a known number at
            // a wire type the table has no arm for (R-E2) -- is skipped.
            _ => d.skip(tag, wire),
        }
    }
}

#[inline]
#[allow(unused_variables, unused_mut)]
fn dec_list_results_response(d: &mut Dec, out: &mut ListResultsResponse, depth: u32) {
    // Plan rule: a message more than LIMIT levels below the root is refused.
    if depth > LIMIT { d.err = ak_rt::ERR_DEPTH; return; }
    let buf = d.buf;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { return; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; return; }
        match (tag, wire) {
            (1, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                let mut c = ResultRaw::default();
                dec_result_raw(&mut sub, &mut c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
                out.results.push(c);
            }
            (2, 0) => {
                let v = d.varint() as i32;
                if d.err == 0 { out.page = v; }
            }
            (3, 0) => {
                let v = d.varint() as i32;
                if d.err == 0 { out.total = v; }
            }
            // Plan rule (drop): an unknown field -- including a known number at
            // a wire type the table has no arm for (R-E2) -- is skipped.
            _ => d.skip(tag, wire),
        }
    }
}

#[inline]
#[allow(unused_variables, unused_mut)]
fn dec_list_tasks_detailed_response(d: &mut Dec, out: &mut ListTasksDetailedResponse, depth: u32) {
    // Plan rule: a message more than LIMIT levels below the root is refused.
    if depth > LIMIT { d.err = ak_rt::ERR_DEPTH; return; }
    let buf = d.buf;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { return; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; return; }
        match (tag, wire) {
            (1, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                let mut c = TaskDetailed::default();
                dec_task_detailed(&mut sub, &mut c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
                out.tasks.push(c);
            }
            (2, 0) => {
                let v = d.varint() as i32;
                if d.err == 0 { out.page = v; }
            }
            (3, 0) => {
                let v = d.varint() as i32;
                if d.err == 0 { out.total = v; }
            }
            // Plan rule (drop): an unknown field -- including a known number at
            // a wire type the table has no arm for (R-E2) -- is skipped.
            _ => d.skip(tag, wire),
        }
    }
}

#[inline]
#[allow(unused_variables, unused_mut)]
fn dec_list_task_summary_response(d: &mut Dec, out: &mut ListTaskSummaryResponse, depth: u32) {
    // Plan rule: a message more than LIMIT levels below the root is refused.
    if depth > LIMIT { d.err = ak_rt::ERR_DEPTH; return; }
    let buf = d.buf;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { return; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; return; }
        match (tag, wire) {
            (1, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                let mut c = TaskSummary::default();
                dec_task_summary(&mut sub, &mut c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
                out.tasks.push(c);
            }
            // Plan rule (drop): an unknown field -- including a known number at
            // a wire type the table has no arm for (R-E2) -- is skipped.
            _ => d.skip(tag, wire),
        }
    }
}

#[inline]
#[allow(unused_variables, unused_mut)]
fn dec_list_probe_response(d: &mut Dec, out: &mut ListProbeResponse, depth: u32) {
    // Plan rule: a message more than LIMIT levels below the root is refused.
    if depth > LIMIT { d.err = ak_rt::ERR_DEPTH; return; }
    let buf = d.buf;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { return; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; return; }
        match (tag, wire) {
            (1, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                let mut c = Probe::default();
                dec_probe(&mut sub, &mut c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
                out.probes.push(c);
            }
            // Plan rule (drop): an unknown field -- including a known number at
            // a wire type the table has no arm for (R-E2) -- is skipped.
            _ => d.skip(tag, wire),
        }
    }
}

#[inline]
#[allow(unused_variables, unused_mut)]
fn dec_list_metrics_response(d: &mut Dec, out: &mut ListMetricsResponse, depth: u32) {
    // Plan rule: a message more than LIMIT levels below the root is refused.
    if depth > LIMIT { d.err = ak_rt::ERR_DEPTH; return; }
    let buf = d.buf;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { return; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; return; }
        match (tag, wire) {
            (1, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                let mut c = MetricsBatch::default();
                dec_metrics_batch(&mut sub, &mut c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
                out.batches.push(c);
            }
            // Plan rule (drop): an unknown field -- including a known number at
            // a wire type the table has no arm for (R-E2) -- is skipped.
            _ => d.skip(tag, wire),
        }
    }
}

#[inline]
#[allow(unused_variables, unused_mut)]
fn dec_upload_result_data_message(d: &mut Dec, out: &mut UploadResultDataMessage, depth: u32) {
    // Plan rule: a message more than LIMIT levels below the root is refused.
    if depth > LIMIT { d.err = ak_rt::ERR_DEPTH; return; }
    let buf = d.buf;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { return; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; return; }
        match (tag, wire) {
            (1, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                // Plan rule: a repeated occurrence MERGES into the one decoded.
                let c = out.upload.get_or_insert_with(Default::default);
                dec_upload_result_data(&mut sub, c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
            }
            // Plan rule (drop): an unknown field -- including a known number at
            // a wire type the table has no arm for (R-E2) -- is skipped.
            _ => d.skip(tag, wire),
        }
    }
}

#[inline]
#[allow(unused_variables, unused_mut)]
fn dec_dual_response(d: &mut Dec, out: &mut DualResponse, depth: u32) {
    // Plan rule: a message more than LIMIT levels below the root is refused.
    if depth > LIMIT { d.err = ak_rt::ERR_DEPTH; return; }
    let buf = d.buf;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { return; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; return; }
        match (tag, wire) {
            (1, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                let mut c = Pair::default();
                dec_pair(&mut sub, &mut c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
                out.left.push(c);
            }
            (2, 2) => {
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf[off..off + n]);
                let mut c = Pair::default();
                dec_pair(&mut sub, &mut c, depth + 1);
                if sub.err != 0 { d.err = sub.err; return; }
                out.right.push(c);
            }
            // Plan rule (drop): an unknown field -- including a known number at
            // a wire type the table has no arm for (R-E2) -- is skipped.
            _ => d.skip(tag, wire),
        }
    }
}

pub fn encode_list_results_response(o: &ListResultsResponse) -> Vec<u8> {
    let mut e = Enc::new(SITES);
    enc_list_results_response(o, &mut e);
    e.buf
}

/// Encode into a context that is reused, so the learned widths survive and the
/// allocation is not part of what is timed. This is the shape a real host uses.
pub fn encode_into_list_results_response(o: &ListResultsResponse, e: &mut Enc) {
    e.reset();
    enc_list_results_response(o, e);
}

/// After an error the value is discarded, never returned (plan rule, R-G6).
pub fn decode_list_results_response(b: &[u8]) -> Result<ListResultsResponse, i32> {
    let mut d = Dec::new(b);
    let mut out = ListResultsResponse::default();
    dec_list_results_response(&mut d, &mut out, 0);
    if d.err != 0 { Err(d.err) } else { Ok(out) }
}

pub fn encode_list_tasks_detailed_response(o: &ListTasksDetailedResponse) -> Vec<u8> {
    let mut e = Enc::new(SITES);
    enc_list_tasks_detailed_response(o, &mut e);
    e.buf
}

/// Encode into a context that is reused, so the learned widths survive and the
/// allocation is not part of what is timed. This is the shape a real host uses.
pub fn encode_into_list_tasks_detailed_response(o: &ListTasksDetailedResponse, e: &mut Enc) {
    e.reset();
    enc_list_tasks_detailed_response(o, e);
}

/// After an error the value is discarded, never returned (plan rule, R-G6).
pub fn decode_list_tasks_detailed_response(b: &[u8]) -> Result<ListTasksDetailedResponse, i32> {
    let mut d = Dec::new(b);
    let mut out = ListTasksDetailedResponse::default();
    dec_list_tasks_detailed_response(&mut d, &mut out, 0);
    if d.err != 0 { Err(d.err) } else { Ok(out) }
}

pub fn encode_list_probe_response(o: &ListProbeResponse) -> Vec<u8> {
    let mut e = Enc::new(SITES);
    enc_list_probe_response(o, &mut e);
    e.buf
}

/// Encode into a context that is reused, so the learned widths survive and the
/// allocation is not part of what is timed. This is the shape a real host uses.
pub fn encode_into_list_probe_response(o: &ListProbeResponse, e: &mut Enc) {
    e.reset();
    enc_list_probe_response(o, e);
}

/// After an error the value is discarded, never returned (plan rule, R-G6).
pub fn decode_list_probe_response(b: &[u8]) -> Result<ListProbeResponse, i32> {
    let mut d = Dec::new(b);
    let mut out = ListProbeResponse::default();
    dec_list_probe_response(&mut d, &mut out, 0);
    if d.err != 0 { Err(d.err) } else { Ok(out) }
}

pub fn encode_list_task_summary_response(o: &ListTaskSummaryResponse) -> Vec<u8> {
    let mut e = Enc::new(SITES);
    enc_list_task_summary_response(o, &mut e);
    e.buf
}

/// Encode into a context that is reused, so the learned widths survive and the
/// allocation is not part of what is timed. This is the shape a real host uses.
pub fn encode_into_list_task_summary_response(o: &ListTaskSummaryResponse, e: &mut Enc) {
    e.reset();
    enc_list_task_summary_response(o, e);
}

/// After an error the value is discarded, never returned (plan rule, R-G6).
pub fn decode_list_task_summary_response(b: &[u8]) -> Result<ListTaskSummaryResponse, i32> {
    let mut d = Dec::new(b);
    let mut out = ListTaskSummaryResponse::default();
    dec_list_task_summary_response(&mut d, &mut out, 0);
    if d.err != 0 { Err(d.err) } else { Ok(out) }
}

pub fn encode_upload_result_data_message(o: &UploadResultDataMessage) -> Vec<u8> {
    let mut e = Enc::new(SITES);
    enc_upload_result_data_message(o, &mut e);
    e.buf
}

/// Encode into a context that is reused, so the learned widths survive and the
/// allocation is not part of what is timed. This is the shape a real host uses.
pub fn encode_into_upload_result_data_message(o: &UploadResultDataMessage, e: &mut Enc) {
    e.reset();
    enc_upload_result_data_message(o, e);
}

/// After an error the value is discarded, never returned (plan rule, R-G6).
pub fn decode_upload_result_data_message(b: &[u8]) -> Result<UploadResultDataMessage, i32> {
    let mut d = Dec::new(b);
    let mut out = UploadResultDataMessage::default();
    dec_upload_result_data_message(&mut d, &mut out, 0);
    if d.err != 0 { Err(d.err) } else { Ok(out) }
}

pub fn encode_list_metrics_response(o: &ListMetricsResponse) -> Vec<u8> {
    let mut e = Enc::new(SITES);
    enc_list_metrics_response(o, &mut e);
    e.buf
}

/// Encode into a context that is reused, so the learned widths survive and the
/// allocation is not part of what is timed. This is the shape a real host uses.
pub fn encode_into_list_metrics_response(o: &ListMetricsResponse, e: &mut Enc) {
    e.reset();
    enc_list_metrics_response(o, e);
}

/// After an error the value is discarded, never returned (plan rule, R-G6).
pub fn decode_list_metrics_response(b: &[u8]) -> Result<ListMetricsResponse, i32> {
    let mut d = Dec::new(b);
    let mut out = ListMetricsResponse::default();
    dec_list_metrics_response(&mut d, &mut out, 0);
    if d.err != 0 { Err(d.err) } else { Ok(out) }
}

pub fn encode_dual_response(o: &DualResponse) -> Vec<u8> {
    let mut e = Enc::new(SITES);
    enc_dual_response(o, &mut e);
    e.buf
}

/// Encode into a context that is reused, so the learned widths survive and the
/// allocation is not part of what is timed. This is the shape a real host uses.
pub fn encode_into_dual_response(o: &DualResponse, e: &mut Enc) {
    e.reset();
    enc_dual_response(o, e);
}

/// After an error the value is discarded, never returned (plan rule, R-G6).
pub fn decode_dual_response(b: &[u8]) -> Result<DualResponse, i32> {
    let mut d = Dec::new(b);
    let mut out = DualResponse::default();
    dec_dual_response(&mut d, &mut out, 0);
    if d.err != 0 { Err(d.err) } else { Ok(out) }
}
