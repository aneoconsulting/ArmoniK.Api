// @generated by ffi/poc/java/gen/generate.py from ffi/schema/shapes.json. Do not edit.

package ak.shapes;

import ak.Values;

/** Build any payload of shapes.json as facade values. */
public final class Build {
  private Build() {}

  public static final int FULL = 0, ALL_ABSENT = 1, HALF_ABSENT = 2;
  public static final int ADAPTER_ERROR = 0, ADAPTER_OK = 1, ADAPTER_INVALID = 2;

  /** Presence cycles per field, and one element in seven is present AND zero, which a
   *  by-value group cannot tell from absent unless it is designed to. */
  static boolean explicitPresent(int tag, int idx) {
    return idx % (tag + 1) != 0;
  }

  /** Cycled Error, Ok, Invalid rather than 0,1,2 so element 0 is Error: `half_absent`
   *  removes the nested site and P2.5 keeps a written one first. */
  static int adapterState(int idx) {
    switch (idx % 3) {
      case 0: return ADAPTER_ERROR;
      case 1: return ADAPTER_OK;
      default: return ADAPTER_INVALID;
    }
  }

  /** `"k%02d-"`, without String.format: on JDK 21 and later one String.format with a
   *  numeric conversion permanently deoptimises every char narrowing loop in the process
   *  (README R9), and protobuf-java's own encoder is such a loop. The builder runs in the
   *  same process as the benchmark. */
  static String pad2(int k) {
    return "k" + (k < 10 ? "0" : "") + k + "-";
  }

  static Timestamp mkTimestamp(int idx) {
    Timestamp t = new Timestamp();
    t.seconds = Values.stampSeconds(idx);
    t.nanos = Values.stampNanos(idx);
    return t;
  }

  static Duration mkDuration(int idx) {
    Duration t = new Duration();
    t.seconds = Values.durSeconds(idx);
    t.nanos = Values.durNanos(idx);
    return t;
  }


  public static Timestamp buildTimestamp(String path, int idx, int mode, int repeats, int bulk, int cs) {
    Timestamp r = new Timestamp();
    if (!(mode == ALL_ABSENT)) {
      r.seconds = Values.scalarI64((path + ".seconds"), (int) (idx));
    }
    if (!(mode == ALL_ABSENT)) {
      r.nanos = Values.scalarI32((path + ".nanos"), (int) (idx));
    }
    return r;
  }

  public static Duration buildDuration(String path, int idx, int mode, int repeats, int bulk, int cs) {
    Duration r = new Duration();
    if (!(mode == ALL_ABSENT)) {
      r.seconds = Values.scalarI64((path + ".seconds"), (int) (idx));
    }
    if (!(mode == ALL_ABSENT)) {
      r.nanos = Values.scalarI32((path + ".nanos"), (int) (idx));
    }
    return r;
  }

  public static ResultRaw buildResultRaw(String path, int idx, int mode, int repeats, int bulk, int cs) {
    ResultRaw r = new ResultRaw();
    if (!(mode == ALL_ABSENT)) {
      r.session_id = Values.recode(Values.guid((path + ".session_id"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.name = Values.recode(Values.word((path + ".name"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.owner_task_id = Values.recode(Values.guid((path + ".owner_task_id"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.status = Values.enumValue(ResultStatus.DECLARED, (int) idx);
    }
    if (!(mode == ALL_ABSENT)) {
      r.created_at = mkTimestamp((int) (idx));
    }
    if (!(mode == ALL_ABSENT || mode == HALF_ABSENT)) {
      r.completed_at = mkTimestamp((int) (idx));
    }
    if (!(mode == ALL_ABSENT)) {
      r.result_id = Values.recode(Values.guid((path + ".result_id"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.size = Values.scalarI64((path + ".size"), (int) (idx));
    }
    if (!(mode == ALL_ABSENT)) {
      r.created_by = Values.recode(Values.guid((path + ".created_by"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.opaque_id = Values.blob((path + ".opaque_id"), (int) idx, 16);
    }
    if (!(mode == ALL_ABSENT)) {
      r.manual_deletion = Values.scalarBool((path + ".manual_deletion"), (int) (idx));
    }
    return r;
  }

  public static TaskOptions buildTaskOptions(String path, int idx, int mode, int repeats, int bulk, int cs) {
    TaskOptions r = new TaskOptions();
    if (!(mode == ALL_ABSENT)) {
      for (int k = 0; k < 4; k++) {
        String key = pad2(k) + Values.recode(Values.word(path + ".options.key", idx * 31 + k), cs);
        String val = "";
        if (!(mode == HALF_ABSENT && k % 2 == 0))
          val = Values.recode(Values.word(path + ".options.value", idx * 31 + k), cs);
        r.options.put(key, val);
      }
    }
    if (!(mode == ALL_ABSENT)) {
      r.max_duration = mkDuration((int) (idx));
    }
    if (!(mode == ALL_ABSENT)) {
      r.max_retries = Values.scalarI32((path + ".max_retries"), (int) (idx));
    }
    if (!(mode == ALL_ABSENT)) {
      r.priority = Values.scalarI32((path + ".priority"), (int) (idx));
    }
    if (!(mode == ALL_ABSENT)) {
      r.partition_id = Values.recode(Values.word((path + ".partition_id"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.application_name = Values.recode(Values.word((path + ".application_name"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.application_version = Values.recode(Values.word((path + ".application_version"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.application_namespace = Values.recode(Values.word((path + ".application_namespace"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.application_service = Values.recode(Values.word((path + ".application_service"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.engine_type = Values.recode(Values.word((path + ".engine_type"), (int) (idx)), cs);
    }
    return r;
  }

  public static TaskOutput buildTaskOutput(String path, int idx, int mode, int repeats, int bulk, int cs) {
    TaskOutput r = new TaskOutput();
    if (!(mode == ALL_ABSENT)) {
      r.success = Values.scalarBool((path + ".success"), (int) (idx));
    }
    if (!(mode == ALL_ABSENT)) {
      r.error = Values.recode(Values.sentence((path + ".error"), (int) (idx)), cs);
    }
    return r;
  }

  public static TaskDetailed buildTaskDetailed(String path, int idx, int mode, int repeats, int bulk, int cs) {
    TaskDetailed r = new TaskDetailed();
    if (!(mode == ALL_ABSENT)) {
      r.id = Values.recode(Values.guid((path + ".id"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.session_id = Values.recode(Values.guid((path + ".session_id"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.owner_pod_id = Values.recode(Values.guid((path + ".owner_pod_id"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      for (int j = 0; j < repeats; j++)
        r.parent_task_ids.add(Values.recode(Values.guid((path + ".parent_task_ids"), (int) (idx * 211 + j)), cs));
    }
    if (!(mode == ALL_ABSENT)) {
      for (int j = 0; j < repeats; j++)
        r.data_dependencies.add(Values.recode(Values.guid((path + ".data_dependencies"), (int) (idx * 211 + j)), cs));
    }
    if (!(mode == ALL_ABSENT)) {
      for (int j = 0; j < repeats; j++)
        r.expected_output_ids.add(Values.recode(Values.guid((path + ".expected_output_ids"), (int) (idx * 211 + j)), cs));
    }
    if (!(mode == ALL_ABSENT)) {
      for (int j = 0; j < repeats; j++)
        r.retry_of_ids.add(Values.recode(Values.guid((path + ".retry_of_ids"), (int) (idx * 211 + j)), cs));
    }
    if (!(mode == ALL_ABSENT)) {
      r.status = Values.enumValue(TaskStatus.DECLARED, (int) idx);
    }
    if (!(mode == ALL_ABSENT)) {
      r.status_message = Values.recode(Values.sentence((path + ".status_message"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.options = buildTaskOptions((path + ".options"), idx, mode, repeats, bulk, cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.created_at = mkTimestamp((int) (idx));
    }
    if (!(mode == ALL_ABSENT || mode == HALF_ABSENT)) {
      r.submitted_at = mkTimestamp((int) (idx));
    }
    if (!(mode == ALL_ABSENT)) {
      r.started_at = mkTimestamp((int) (idx));
    }
    if (!(mode == ALL_ABSENT || mode == HALF_ABSENT)) {
      r.ended_at = mkTimestamp((int) (idx));
    }
    if (!(mode == ALL_ABSENT)) {
      r.pod_ttl = mkTimestamp((int) (idx));
    }
    if (!(mode == ALL_ABSENT || mode == HALF_ABSENT)) {
      switch (adapterState(idx)) {
        case ADAPTER_INVALID: break;
        case ADAPTER_OK: {
          r.output = new TaskOutput();
          r.output.success = true;
          break;
        }
        default: {
          r.output = new TaskOutput();
          r.output.error = Values.recode(Values.sentence(path + ".output.error", (int) idx), cs);
          break;
        }
      }
    }
    if (!(mode == ALL_ABSENT)) {
      r.pod_hostname = Values.recode(Values.word((path + ".pod_hostname"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT || mode == HALF_ABSENT)) {
      r.received_at = mkTimestamp((int) (idx));
    }
    if (!(mode == ALL_ABSENT)) {
      r.acquired_at = mkTimestamp((int) (idx));
    }
    if (!(mode == ALL_ABSENT)) {
      r.creation_to_end_duration = mkDuration((int) (idx));
    }
    if (!(mode == ALL_ABSENT)) {
      r.processing_to_end_duration = mkDuration((int) (idx));
    }
    if (!(mode == ALL_ABSENT)) {
      r.initial_task_id = Values.recode(Values.guid((path + ".initial_task_id"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.received_to_end_duration = mkDuration((int) (idx));
    }
    if (!(mode == ALL_ABSENT || mode == HALF_ABSENT)) {
      r.processed_at = mkTimestamp((int) (idx));
    }
    if (!(mode == ALL_ABSENT)) {
      r.fetched_at = mkTimestamp((int) (idx));
    }
    if (!(mode == ALL_ABSENT)) {
      r.payload_id = Values.recode(Values.guid((path + ".payload_id"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.created_by = Values.recode(Values.guid((path + ".created_by"), (int) (idx)), cs);
    }
    return r;
  }

  public static TaskSummary buildTaskSummary(String path, int idx, int mode, int repeats, int bulk, int cs) {
    TaskSummary r = new TaskSummary();
    if (!(mode == ALL_ABSENT)) {
      r.id = Values.recode(Values.guid((path + ".id"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.session_id = Values.recode(Values.guid((path + ".session_id"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.options = buildTaskOptions((path + ".options"), idx, mode, repeats, bulk, cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.status = Values.enumValue(TaskStatus.DECLARED, (int) idx);
    }
    if (!(mode == ALL_ABSENT)) {
      r.created_at = mkTimestamp((int) (idx));
    }
    if (!(mode == ALL_ABSENT) && adapterState(idx) == ADAPTER_ERROR) r.error = Values.recode(Values.sentence((path + ".error"), (int) (idx)), cs);
    if (!(mode == ALL_ABSENT)) {
      r.status_message = Values.recode(Values.sentence((path + ".status_message"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.count_data_dependencies = Values.scalarI64((path + ".count_data_dependencies"), (int) (idx));
    }
    return r;
  }

  public static Probe buildProbe(String path, int idx, int mode, int repeats, int bulk, int cs) {
    Probe r = new Probe();
    if (!(mode == ALL_ABSENT)) {
      r.id = Values.recode(Values.guid((path + ".id"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      if (explicitPresent(2, idx)) {
        r.opt_count = idx % 7 == 0 ? 0 : Values.scalarI32((path + ".opt_count"), (int) (idx));
        r.has_opt_count = true;
      }
    }
    if (!(mode == ALL_ABSENT)) {
      if (explicitPresent(3, idx))
        r.opt_label = idx % 7 == 0 ? "" : Values.recode(Values.word((path + ".opt_label"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      if (explicitPresent(4, idx)) {
        r.opt_flag = idx % 7 == 0 ? false : Values.scalarBool((path + ".opt_flag"), (int) (idx));
        r.has_opt_flag = true;
      }
    }
    switch (idx % 5) {
      case 0: {
        r.body_case = Probe.BODY_CASE_AS_INT;
        r.body_as_int = Values.scalarI64("Probe.as_int", (int) idx);
        break;
      }
      case 1: {
        r.body_case = Probe.BODY_CASE_AS_TEXT;
        r.body_as_text = Values.recode(Values.word("Probe.as_text", (int) idx), cs);
        break;
      }
      case 2: {
        r.body_case = Probe.BODY_CASE_AS_BLOB;
        r.body_as_blob = Values.blob("Probe.as_blob", (int) idx, 16);
        break;
      }
      case 3: {
        r.body_case = Probe.BODY_CASE_AS_STAMP;
        r.body_as_stamp = mkTimestamp((int) idx);
        break;
      }
      default: {
        r.body_case = Probe.BODY_CASE_AS_NOTHING;
        r.body_as_nothing = new Empty();
        break;
      }
    }
    return r;
  }

  public static Empty buildEmpty(String path, int idx, int mode, int repeats, int bulk, int cs) {
    Empty r = new Empty();
    return r;
  }

  public static UploadResultData buildUploadResultData(String path, int idx, int mode, int repeats, int bulk, int cs) {
    UploadResultData r = new UploadResultData();
    if (!(mode == ALL_ABSENT)) {
      r.session_id = Values.recode(Values.guid((path + ".session_id"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.result_id = Values.recode(Values.guid((path + ".result_id"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.data_chunk = Values.bulk(bulk);
    }
    return r;
  }

  public static MetricsBatch buildMetricsBatch(String path, int idx, int mode, int repeats, int bulk, int cs) {
    MetricsBatch r = new MetricsBatch();
    if (!(mode == ALL_ABSENT)) {
      r.id = Values.recode(Values.guid((path + ".id"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.ticks = new long[30];
      for (int j = 0; j < 30; j++)
        r.ticks[j] = Values.scalarI64((path + ".ticks"), (int) (idx * 97 + j));
    }
    if (!(mode == ALL_ABSENT)) {
      r.values = new double[30];
      for (int j = 0; j < 30; j++)
        r.values[j] = Values.scalarDouble((path + ".values"), (int) (idx * 97 + j));
    }
    if (!(mode == ALL_ABSENT)) {
      r.codes = new int[30];
      for (int j = 0; j < 30; j++)
        r.codes[j] = Values.scalarI32((path + ".codes"), (int) (idx * 97 + j));
    }
    if (!(mode == ALL_ABSENT)) {
      r.flags = new boolean[30];
      for (int j = 0; j < 30; j++)
        r.flags[j] = Values.scalarBool((path + ".flags"), (int) (idx * 97 + j));
    }
    if (!(mode == ALL_ABSENT)) {
      r.statuses = new int[30];
      for (int j = 0; j < 30; j++)
        r.statuses[j] = Values.enumValue(TaskStatus.DECLARED, idx * 97 + j);
    }
    return r;
  }

  public static Pair buildPair(String path, int idx, int mode, int repeats, int bulk, int cs) {
    Pair r = new Pair();
    if (!(mode == ALL_ABSENT)) {
      r.key = Values.recode(Values.word((path + ".key"), (int) (idx)), cs);
    }
    if (!(mode == ALL_ABSENT)) {
      r.value = Values.scalarI32((path + ".value"), (int) (idx));
    }
    return r;
  }

  public static ListResultsResponse buildListResultsResponse(String path, int idx, int mode, int repeats, int bulk, int cs) {
    ListResultsResponse r = new ListResultsResponse();
    if (!(mode == ALL_ABSENT)) {
      for (int j = 0; j < repeats; j++)
        r.results.add(buildResultRaw((path + ".results"), j, mode, repeats, bulk, cs));
    }
    if (!(mode == ALL_ABSENT)) {
      r.page = Values.scalarI32((path + ".page"), (int) (idx));
    }
    if (!(mode == ALL_ABSENT)) {
      r.total = Values.scalarI32((path + ".total"), (int) (idx));
    }
    return r;
  }

  public static ListTasksDetailedResponse buildListTasksDetailedResponse(String path, int idx, int mode, int repeats, int bulk, int cs) {
    ListTasksDetailedResponse r = new ListTasksDetailedResponse();
    if (!(mode == ALL_ABSENT)) {
      for (int j = 0; j < repeats; j++)
        r.tasks.add(buildTaskDetailed((path + ".tasks"), j, mode, repeats, bulk, cs));
    }
    if (!(mode == ALL_ABSENT)) {
      r.page = Values.scalarI32((path + ".page"), (int) (idx));
    }
    if (!(mode == ALL_ABSENT)) {
      r.total = Values.scalarI32((path + ".total"), (int) (idx));
    }
    return r;
  }

  public static ListTaskSummaryResponse buildListTaskSummaryResponse(String path, int idx, int mode, int repeats, int bulk, int cs) {
    ListTaskSummaryResponse r = new ListTaskSummaryResponse();
    if (!(mode == ALL_ABSENT)) {
      for (int j = 0; j < repeats; j++)
        r.tasks.add(buildTaskSummary((path + ".tasks"), j, mode, repeats, bulk, cs));
    }
    return r;
  }

  public static ListProbeResponse buildListProbeResponse(String path, int idx, int mode, int repeats, int bulk, int cs) {
    ListProbeResponse r = new ListProbeResponse();
    if (!(mode == ALL_ABSENT)) {
      for (int j = 0; j < repeats; j++)
        r.probes.add(buildProbe((path + ".probes"), j, mode, repeats, bulk, cs));
    }
    return r;
  }

  public static ListMetricsResponse buildListMetricsResponse(String path, int idx, int mode, int repeats, int bulk, int cs) {
    ListMetricsResponse r = new ListMetricsResponse();
    if (!(mode == ALL_ABSENT)) {
      for (int j = 0; j < repeats; j++)
        r.batches.add(buildMetricsBatch((path + ".batches"), j, mode, repeats, bulk, cs));
    }
    return r;
  }

  public static UploadResultDataMessage buildUploadResultDataMessage(String path, int idx, int mode, int repeats, int bulk, int cs) {
    UploadResultDataMessage r = new UploadResultDataMessage();
    if (!(mode == ALL_ABSENT)) {
      r.upload = buildUploadResultData((path + ".upload"), idx, mode, repeats, bulk, cs);
    }
    return r;
  }

  public static DualResponse buildDualResponse(String path, int idx, int mode, int repeats, int bulk, int cs) {
    DualResponse r = new DualResponse();
    if (!(mode == ALL_ABSENT)) {
      for (int j = 0; j < repeats; j++)
        r.left.add(buildPair((path + ".left"), j, mode, repeats, bulk, cs));
    }
    if (!(mode == ALL_ABSENT)) {
      for (int j = 0; j < repeats; j++)
        r.right.add(buildPair((path + ".right"), j, mode, repeats, bulk, cs));
    }
    return r;
  }

  // ---- the payload set ------------------------------------------------

  /** P1.1: ListResultsResponse, full. */
  public static ListResultsResponse payloadP1_1(int cs) {
    ListResultsResponse r = new ListResultsResponse();
    final int[] kReps = {3};
    for (int j = 0; j < 4; j++)
      r.results.add(buildResultRaw("ResultRaw", j, FULL, kReps[j % 1], 0, cs));
    r.page = 1;
    r.total = 4;
    return r;
  }

  /** P1.2: ListResultsResponse, full. */
  public static ListResultsResponse payloadP1_2(int cs) {
    ListResultsResponse r = new ListResultsResponse();
    final int[] kReps = {3};
    for (int j = 0; j < 1000; j++)
      r.results.add(buildResultRaw("ResultRaw", j, FULL, kReps[j % 1], 0, cs));
    r.page = 1;
    r.total = 1000;
    return r;
  }

  /** P1.3: ListResultsResponse, all_absent. */
  public static ListResultsResponse payloadP1_3(int cs) {
    ListResultsResponse r = new ListResultsResponse();
    final int[] kReps = {3};
    for (int j = 0; j < 300; j++)
      r.results.add(buildResultRaw("ResultRaw", j, ALL_ABSENT, kReps[j % 1], 0, cs));
    r.page = 1;
    r.total = 300;
    return r;
  }

  /** P2.1: ListTasksDetailedResponse, full. */
  public static ListTasksDetailedResponse payloadP2_1(int cs) {
    ListTasksDetailedResponse r = new ListTasksDetailedResponse();
    final int[] kReps = {3};
    for (int j = 0; j < 1; j++)
      r.tasks.add(buildTaskDetailed("TaskDetailed", j, FULL, kReps[j % 1], 0, cs));
    r.page = 1;
    r.total = 1;
    return r;
  }

  /** P2.2: ListTasksDetailedResponse, full. */
  public static ListTasksDetailedResponse payloadP2_2(int cs) {
    ListTasksDetailedResponse r = new ListTasksDetailedResponse();
    final int[] kReps = {3};
    for (int j = 0; j < 500; j++)
      r.tasks.add(buildTaskDetailed("TaskDetailed", j, FULL, kReps[j % 1], 0, cs));
    r.page = 1;
    r.total = 500;
    return r;
  }

  /** P2.3: ListTasksDetailedResponse, full. */
  public static ListTasksDetailedResponse payloadP2_3(int cs) {
    ListTasksDetailedResponse r = new ListTasksDetailedResponse();
    final int[] kReps = {30};
    for (int j = 0; j < 125; j++)
      r.tasks.add(buildTaskDetailed("TaskDetailed", j, FULL, kReps[j % 1], 0, cs));
    r.page = 1;
    r.total = 125;
    return r;
  }

  /** P2.4: ListTasksDetailedResponse, full. */
  public static ListTasksDetailedResponse payloadP2_4(int cs) {
    ListTasksDetailedResponse r = new ListTasksDetailedResponse();
    final int[] kReps = {3, 150};
    for (int j = 0; j < 80; j++)
      r.tasks.add(buildTaskDetailed("TaskDetailed", j, FULL, kReps[j % 2], 0, cs));
    r.page = 1;
    r.total = 80;
    return r;
  }

  /** P2.5: ListTasksDetailedResponse, half_absent. */
  public static ListTasksDetailedResponse payloadP2_5(int cs) {
    ListTasksDetailedResponse r = new ListTasksDetailedResponse();
    final int[] kReps = {3};
    for (int j = 0; j < 20; j++)
      r.tasks.add(buildTaskDetailed("TaskDetailed", j, HALF_ABSENT, kReps[j % 1], 0, cs));
    r.page = 1;
    r.total = 20;
    return r;
  }

  /** P3.1: ListProbeResponse, full. */
  public static ListProbeResponse payloadP3_1(int cs) {
    ListProbeResponse r = new ListProbeResponse();
    final int[] kReps = {3};
    for (int j = 0; j < 200; j++)
      r.probes.add(buildProbe("Probe", j, FULL, kReps[j % 1], 0, cs));
    return r;
  }

  /** P4.1: ListTaskSummaryResponse, full. */
  public static ListTaskSummaryResponse payloadP4_1(int cs) {
    ListTaskSummaryResponse r = new ListTaskSummaryResponse();
    final int[] kReps = {3};
    for (int j = 0; j < 200; j++)
      r.tasks.add(buildTaskSummary("TaskSummary", j, FULL, kReps[j % 1], 0, cs));
    return r;
  }

  /** P5.1: UploadResultDataMessage, full. */
  public static UploadResultDataMessage payloadP5_1(int cs) {
    UploadResultDataMessage r = new UploadResultDataMessage();
    r.upload = buildUploadResultData("UploadResultData", 0, FULL, 3, 36, cs);
    return r;
  }

  /** P5.2: UploadResultDataMessage, full. */
  public static UploadResultDataMessage payloadP5_2(int cs) {
    UploadResultDataMessage r = new UploadResultDataMessage();
    r.upload = buildUploadResultData("UploadResultData", 0, FULL, 3, 65536, cs);
    return r;
  }

  /** P5.3: UploadResultDataMessage, full. */
  public static UploadResultDataMessage payloadP5_3(int cs) {
    UploadResultDataMessage r = new UploadResultDataMessage();
    r.upload = buildUploadResultData("UploadResultData", 0, FULL, 3, 1048576, cs);
    return r;
  }

  /** P5.4: UploadResultDataMessage, full. */
  public static UploadResultDataMessage payloadP5_4(int cs) {
    UploadResultDataMessage r = new UploadResultDataMessage();
    r.upload = buildUploadResultData("UploadResultData", 0, FULL, 3, 4194304, cs);
    return r;
  }

  /** P6.1: ListMetricsResponse, full. */
  public static ListMetricsResponse payloadP6_1(int cs) {
    ListMetricsResponse r = new ListMetricsResponse();
    final int[] kReps = {3};
    for (int j = 0; j < 200; j++)
      r.batches.add(buildMetricsBatch("MetricsBatch", j, FULL, kReps[j % 1], 0, cs));
    return r;
  }

  // P7.1 has no builder: no canonical writer can produce it, so M7 is
  // validated by DECODE (design/SHAPES.md, P7.1's row).

  /** Every payload this build covers, by the id design/SHAPES.md uses. */
  public static final String[] COVERED = {"P1.1", "P1.2", "P1.3", "P2.1", "P2.2", "P2.3", "P2.4", "P2.5", "P3.1", "P4.1", "P5.1", "P5.2", "P5.3", "P5.4", "P6.1"};
}
