// @generated by ffi/poc/codec/gen/generate.py from ffi/corpus/corpus.json (reader view) over ffi/schema/shapes.json. Do not edit.

//! The core behind the C ABI, rendered from the plans of `poc/codec/gen/plan.py`.
//! `core-native` is rendered from the SAME plans (`gen/rust_native.py`); the only
//! difference between the two is where a value comes from: a group the host filled
//! here, a facade object there (R-E1).
#![allow(non_snake_case, non_camel_case_types, unused_unsafe, unused_variables,
    unused_assignments, unused_mut, unused_macros, clippy::all)]
use crate::{enc_blob, enc_raw, unk_put, DecCtxImpl, EncCtxImpl, UnkCx};
use ak_abi::*;
use ak_rt::dec::Dec;
use core::ffi::c_void;

/// One learned length-prefix slot per site. Per context, never process-global.
pub const SITES: usize = 122;

#[inline]
unsafe fn enc_task_options_options_entry_group(g: &ak_efix_TaskOptionsOptionsEntry, cx: *mut EncCtxImpl) -> bool {
    if g.key.tc.is_some() && g.key.len != 0 {
        if !enc_blob(cx, 1, 0, &g.key) { return false; }
    }
    if g.value.tc.is_some() && g.value.len != 0 {
        if !enc_blob(cx, 2, 1, &g.value) { return false; }
    }
    true
}

#[inline]
unsafe fn enc_task_options_options_entry_ugroup(g: &ak_ufix_TaskOptionsOptionsEntry, cx: *mut EncCtxImpl) -> bool {
    if g.key.tc.is_some() && g.key.len != 0 {
        if !enc_blob(cx, 1, 0, &g.key) { return false; }
    }
    if g.value.tc.is_some() && g.value.len != 0 {
        if !enc_blob(cx, 2, 1, &g.value) { return false; }
    }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_chunk_element_attrs_entry_group(g: &ak_efix_ChunkElementAttrsEntry, cx: *mut EncCtxImpl) -> bool {
    if g.key.tc.is_some() && g.key.len != 0 {
        if !enc_blob(cx, 1, 2, &g.key) { return false; }
    }
    if g.value.tc.is_some() && g.value.len != 0 {
        if !enc_blob(cx, 2, 3, &g.value) { return false; }
    }
    true
}

#[inline]
unsafe fn enc_chunk_element_attrs_entry_ugroup(g: &ak_ufix_ChunkElementAttrsEntry, cx: *mut EncCtxImpl) -> bool {
    if g.key.tc.is_some() && g.key.len != 0 {
        if !enc_blob(cx, 1, 2, &g.key) { return false; }
    }
    if g.value.tc.is_some() && g.value.len != 0 {
        if !enc_blob(cx, 2, 3, &g.value) { return false; }
    }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_surrogate_attrs_entry_group(g: &ak_efix_SurrogateAttrsEntry, cx: *mut EncCtxImpl) -> bool {
    if g.key.tc.is_some() && g.key.len != 0 {
        if !enc_blob(cx, 1, 4, &g.key) { return false; }
    }
    if g.value.tc.is_some() && g.value.len != 0 {
        if !enc_blob(cx, 2, 5, &g.value) { return false; }
    }
    true
}

#[inline]
unsafe fn enc_surrogate_attrs_entry_ugroup(g: &ak_ufix_SurrogateAttrsEntry, cx: *mut EncCtxImpl) -> bool {
    if g.key.tc.is_some() && g.key.len != 0 {
        if !enc_blob(cx, 1, 4, &g.key) { return false; }
    }
    if g.value.tc.is_some() && g.value.len != 0 {
        if !enc_blob(cx, 2, 5, &g.value) { return false; }
    }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_timestamp_group(g: &ak_efix_Timestamp, cx: *mut EncCtxImpl) -> bool {
    if g.seconds != 0 { (*cx).e.varint_field(1, g.seconds as u64); }
    if g.nanos != 0 { (*cx).e.varint_field(2, g.nanos as i64 as u64); }
    true
}

#[inline]
unsafe fn enc_timestamp_ugroup(g: &ak_ufix_Timestamp, cx: *mut EncCtxImpl) -> bool {
    if g.seconds != 0 { (*cx).e.varint_field(1, g.seconds as u64); }
    if g.nanos != 0 { (*cx).e.varint_field(2, g.nanos as i64 as u64); }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_duration_group(g: &ak_efix_Duration, cx: *mut EncCtxImpl) -> bool {
    if g.seconds != 0 { (*cx).e.varint_field(1, g.seconds as u64); }
    if g.nanos != 0 { (*cx).e.varint_field(2, g.nanos as i64 as u64); }
    true
}

#[inline]
unsafe fn enc_duration_ugroup(g: &ak_ufix_Duration, cx: *mut EncCtxImpl) -> bool {
    if g.seconds != 0 { (*cx).e.varint_field(1, g.seconds as u64); }
    if g.nanos != 0 { (*cx).e.varint_field(2, g.nanos as i64 as u64); }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_result_raw_group(g: &ak_efix_ResultRaw, cx: *mut EncCtxImpl) -> bool {
    if g.session_id.tc.is_some() && g.session_id.len != 0 {
        if !enc_blob(cx, 1, 6, &g.session_id) { return false; }
    }
    if g.name.tc.is_some() && g.name.len != 0 {
        if !enc_blob(cx, 2, 7, &g.name) { return false; }
    }
    if g.owner_task_id.tc.is_some() && g.owner_task_id.len != 0 {
        if !enc_blob(cx, 3, 8, &g.owner_task_id) { return false; }
    }
    if g.status != 0 { (*cx).e.varint_field(4, g.status as i64 as u64); }
    if g.presence & AK_EFIX_RESULTRAW_PRESENT_CREATED_AT != 0 {
        let mk = (*cx).e.begin(5, 9);
        if g.created_at.seconds != 0 { (*cx).e.varint_field(1, g.created_at.seconds as u64); }
        if g.created_at.nanos != 0 { (*cx).e.varint_field(2, g.created_at.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_RESULTRAW_PRESENT_COMPLETED_AT != 0 {
        let mk = (*cx).e.begin(6, 10);
        if g.completed_at.seconds != 0 { (*cx).e.varint_field(1, g.completed_at.seconds as u64); }
        if g.completed_at.nanos != 0 { (*cx).e.varint_field(2, g.completed_at.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.result_id.tc.is_some() && g.result_id.len != 0 {
        if !enc_blob(cx, 8, 11, &g.result_id) { return false; }
    }
    if g.size != 0 { (*cx).e.varint_field(9, g.size as u64); }
    if g.created_by.tc.is_some() && g.created_by.len != 0 {
        if !enc_blob(cx, 10, 12, &g.created_by) { return false; }
    }
    if g.opaque_id.tc.is_some() && g.opaque_id.len != 0 {
        if !enc_blob(cx, 11, 13, &g.opaque_id) { return false; }
    }
    if g.manual_deletion != 0 { (*cx).e.varint_field(12, (g.manual_deletion != 0) as u64); }
    true
}

#[inline]
unsafe fn enc_result_raw_ugroup(g: &ak_ufix_ResultRaw, cx: *mut EncCtxImpl) -> bool {
    if g.session_id.tc.is_some() && g.session_id.len != 0 {
        if !enc_blob(cx, 1, 6, &g.session_id) { return false; }
    }
    if g.name.tc.is_some() && g.name.len != 0 {
        if !enc_blob(cx, 2, 7, &g.name) { return false; }
    }
    if g.owner_task_id.tc.is_some() && g.owner_task_id.len != 0 {
        if !enc_blob(cx, 3, 8, &g.owner_task_id) { return false; }
    }
    if g.status != 0 { (*cx).e.varint_field(4, g.status as i64 as u64); }
    if g.presence & AK_UFIX_RESULTRAW_PRESENT_CREATED_AT != 0 {
        let mk = (*cx).e.begin(5, 9);
        if g.created_at.seconds != 0 { (*cx).e.varint_field(1, g.created_at.seconds as u64); }
        if g.created_at.nanos != 0 { (*cx).e.varint_field(2, g.created_at.nanos as i64 as u64); }
        if !enc_raw(cx, &g.created_at.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_RESULTRAW_PRESENT_COMPLETED_AT != 0 {
        let mk = (*cx).e.begin(6, 10);
        if g.completed_at.seconds != 0 { (*cx).e.varint_field(1, g.completed_at.seconds as u64); }
        if g.completed_at.nanos != 0 { (*cx).e.varint_field(2, g.completed_at.nanos as i64 as u64); }
        if !enc_raw(cx, &g.completed_at.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if g.result_id.tc.is_some() && g.result_id.len != 0 {
        if !enc_blob(cx, 8, 11, &g.result_id) { return false; }
    }
    if g.size != 0 { (*cx).e.varint_field(9, g.size as u64); }
    if g.created_by.tc.is_some() && g.created_by.len != 0 {
        if !enc_blob(cx, 10, 12, &g.created_by) { return false; }
    }
    if g.opaque_id.tc.is_some() && g.opaque_id.len != 0 {
        if !enc_blob(cx, 11, 13, &g.opaque_id) { return false; }
    }
    if g.manual_deletion != 0 { (*cx).e.varint_field(12, (g.manual_deletion != 0) as u64); }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_task_options_group(
    g: &ak_efix_TaskOptions,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_TaskOptions,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_options {
        (*cx).open_tag = 1;
        (*cx).open_site = 14;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if g.presence & AK_EFIX_TASKOPTIONS_PRESENT_MAX_DURATION != 0 {
        let mk = (*cx).e.begin(2, 15);
        if g.max_duration.seconds != 0 { (*cx).e.varint_field(1, g.max_duration.seconds as u64); }
        if g.max_duration.nanos != 0 { (*cx).e.varint_field(2, g.max_duration.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.max_retries != 0 { (*cx).e.varint_field(3, g.max_retries as i64 as u64); }
    if g.priority != 0 { (*cx).e.varint_field(4, g.priority as i64 as u64); }
    if g.partition_id.tc.is_some() && g.partition_id.len != 0 {
        if !enc_blob(cx, 5, 16, &g.partition_id) { return false; }
    }
    if g.application_name.tc.is_some() && g.application_name.len != 0 {
        if !enc_blob(cx, 6, 17, &g.application_name) { return false; }
    }
    if g.application_version.tc.is_some() && g.application_version.len != 0 {
        if !enc_blob(cx, 7, 18, &g.application_version) { return false; }
    }
    if g.application_namespace.tc.is_some() && g.application_namespace.len != 0 {
        if !enc_blob(cx, 8, 19, &g.application_namespace) { return false; }
    }
    if g.application_service.tc.is_some() && g.application_service.len != 0 {
        if !enc_blob(cx, 9, 20, &g.application_service) { return false; }
    }
    if g.engine_type.tc.is_some() && g.engine_type.len != 0 {
        if !enc_blob(cx, 10, 21, &g.engine_type) { return false; }
    }
    true
}

#[inline]
unsafe fn enc_task_options_ugroup(
    g: &ak_ufix_TaskOptions,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_TaskOptions,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_options {
        (*cx).open_tag = 1;
        (*cx).open_site = 14;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if g.presence & AK_UFIX_TASKOPTIONS_PRESENT_MAX_DURATION != 0 {
        let mk = (*cx).e.begin(2, 15);
        if g.max_duration.seconds != 0 { (*cx).e.varint_field(1, g.max_duration.seconds as u64); }
        if g.max_duration.nanos != 0 { (*cx).e.varint_field(2, g.max_duration.nanos as i64 as u64); }
        if !enc_raw(cx, &g.max_duration.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if g.max_retries != 0 { (*cx).e.varint_field(3, g.max_retries as i64 as u64); }
    if g.priority != 0 { (*cx).e.varint_field(4, g.priority as i64 as u64); }
    if g.partition_id.tc.is_some() && g.partition_id.len != 0 {
        if !enc_blob(cx, 5, 16, &g.partition_id) { return false; }
    }
    if g.application_name.tc.is_some() && g.application_name.len != 0 {
        if !enc_blob(cx, 6, 17, &g.application_name) { return false; }
    }
    if g.application_version.tc.is_some() && g.application_version.len != 0 {
        if !enc_blob(cx, 7, 18, &g.application_version) { return false; }
    }
    if g.application_namespace.tc.is_some() && g.application_namespace.len != 0 {
        if !enc_blob(cx, 8, 19, &g.application_namespace) { return false; }
    }
    if g.application_service.tc.is_some() && g.application_service.len != 0 {
        if !enc_blob(cx, 9, 20, &g.application_service) { return false; }
    }
    if g.engine_type.tc.is_some() && g.engine_type.len != 0 {
        if !enc_blob(cx, 10, 21, &g.engine_type) { return false; }
    }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_task_output_group(g: &ak_efix_TaskOutput, cx: *mut EncCtxImpl) -> bool {
    if g.success != 0 { (*cx).e.varint_field(1, (g.success != 0) as u64); }
    if g.error.tc.is_some() && g.error.len != 0 {
        if !enc_blob(cx, 2, 22, &g.error) { return false; }
    }
    true
}

#[inline]
unsafe fn enc_task_output_ugroup(g: &ak_ufix_TaskOutput, cx: *mut EncCtxImpl) -> bool {
    if g.success != 0 { (*cx).e.varint_field(1, (g.success != 0) as u64); }
    if g.error.tc.is_some() && g.error.len != 0 {
        if !enc_blob(cx, 2, 22, &g.error) { return false; }
    }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_task_detailed_group(
    g: &ak_efix_TaskDetailed,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_TaskDetailed,
    obj: *const c_void,
    token: i64,
) -> bool {
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 1, 23, &g.id) { return false; }
    }
    if g.session_id.tc.is_some() && g.session_id.len != 0 {
        if !enc_blob(cx, 2, 24, &g.session_id) { return false; }
    }
    if g.owner_pod_id.tc.is_some() && g.owner_pod_id.len != 0 {
        if !enc_blob(cx, 3, 25, &g.owner_pod_id) { return false; }
    }
    if let Some(lp) = (*vt).loop_parent_task_ids {
        (*cx).open_tag = 4;
        (*cx).open_site = 26;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if let Some(lp) = (*vt).loop_data_dependencies {
        (*cx).open_tag = 5;
        (*cx).open_site = 27;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if let Some(lp) = (*vt).loop_expected_output_ids {
        (*cx).open_tag = 6;
        (*cx).open_site = 28;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if let Some(lp) = (*vt).loop_retry_of_ids {
        (*cx).open_tag = 7;
        (*cx).open_site = 29;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if g.status != 0 { (*cx).e.varint_field(8, g.status as i64 as u64); }
    if g.status_message.tc.is_some() && g.status_message.len != 0 {
        if !enc_blob(cx, 9, 30, &g.status_message) { return false; }
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_OPTIONS != 0 {
        let mk = (*cx).e.begin(10, 31);
        if let Some(lp) = (*vt).loop_options_options {
            (*cx).open_tag = 1;
            (*cx).open_site = 32;
            (*cx).open_kind = 0;
            ak_rt::bump!((*cx).e.c, reverse);
            let rc = lp(cx as *mut ak_enc_ctx, obj, token);
            if rc < 0 { (*cx).e.fail(rc); return false; }
            if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
        }
        if g.options.presence & AK_EFIX_TASKOPTIONS_PRESENT_MAX_DURATION != 0 {
            let mk = (*cx).e.begin(2, 33);
            if g.options.max_duration.seconds != 0 { (*cx).e.varint_field(1, g.options.max_duration.seconds as u64); }
            if g.options.max_duration.nanos != 0 { (*cx).e.varint_field(2, g.options.max_duration.nanos as i64 as u64); }
            (*cx).e.end(mk);
        }
        if g.options.max_retries != 0 { (*cx).e.varint_field(3, g.options.max_retries as i64 as u64); }
        if g.options.priority != 0 { (*cx).e.varint_field(4, g.options.priority as i64 as u64); }
        if g.options.partition_id.tc.is_some() && g.options.partition_id.len != 0 {
            if !enc_blob(cx, 5, 34, &g.options.partition_id) { return false; }
        }
        if g.options.application_name.tc.is_some() && g.options.application_name.len != 0 {
            if !enc_blob(cx, 6, 35, &g.options.application_name) { return false; }
        }
        if g.options.application_version.tc.is_some() && g.options.application_version.len != 0 {
            if !enc_blob(cx, 7, 36, &g.options.application_version) { return false; }
        }
        if g.options.application_namespace.tc.is_some() && g.options.application_namespace.len != 0 {
            if !enc_blob(cx, 8, 37, &g.options.application_namespace) { return false; }
        }
        if g.options.application_service.tc.is_some() && g.options.application_service.len != 0 {
            if !enc_blob(cx, 9, 38, &g.options.application_service) { return false; }
        }
        if g.options.engine_type.tc.is_some() && g.options.engine_type.len != 0 {
            if !enc_blob(cx, 10, 39, &g.options.engine_type) { return false; }
        }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_CREATED_AT != 0 {
        let mk = (*cx).e.begin(11, 40);
        if g.created_at.seconds != 0 { (*cx).e.varint_field(1, g.created_at.seconds as u64); }
        if g.created_at.nanos != 0 { (*cx).e.varint_field(2, g.created_at.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_SUBMITTED_AT != 0 {
        let mk = (*cx).e.begin(12, 41);
        if g.submitted_at.seconds != 0 { (*cx).e.varint_field(1, g.submitted_at.seconds as u64); }
        if g.submitted_at.nanos != 0 { (*cx).e.varint_field(2, g.submitted_at.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_STARTED_AT != 0 {
        let mk = (*cx).e.begin(13, 42);
        if g.started_at.seconds != 0 { (*cx).e.varint_field(1, g.started_at.seconds as u64); }
        if g.started_at.nanos != 0 { (*cx).e.varint_field(2, g.started_at.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_ENDED_AT != 0 {
        let mk = (*cx).e.begin(14, 43);
        if g.ended_at.seconds != 0 { (*cx).e.varint_field(1, g.ended_at.seconds as u64); }
        if g.ended_at.nanos != 0 { (*cx).e.varint_field(2, g.ended_at.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_POD_TTL != 0 {
        let mk = (*cx).e.begin(15, 44);
        if g.pod_ttl.seconds != 0 { (*cx).e.varint_field(1, g.pod_ttl.seconds as u64); }
        if g.pod_ttl.nanos != 0 { (*cx).e.varint_field(2, g.pod_ttl.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_OUTPUT != 0 {
        let mk = (*cx).e.begin(16, 45);
        if g.output.success != 0 { (*cx).e.varint_field(1, (g.output.success != 0) as u64); }
        if g.output.error.tc.is_some() && g.output.error.len != 0 {
            if !enc_blob(cx, 2, 46, &g.output.error) { return false; }
        }
        (*cx).e.end(mk);
    }
    if g.pod_hostname.tc.is_some() && g.pod_hostname.len != 0 {
        if !enc_blob(cx, 17, 47, &g.pod_hostname) { return false; }
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_RECEIVED_AT != 0 {
        let mk = (*cx).e.begin(18, 48);
        if g.received_at.seconds != 0 { (*cx).e.varint_field(1, g.received_at.seconds as u64); }
        if g.received_at.nanos != 0 { (*cx).e.varint_field(2, g.received_at.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_ACQUIRED_AT != 0 {
        let mk = (*cx).e.begin(19, 49);
        if g.acquired_at.seconds != 0 { (*cx).e.varint_field(1, g.acquired_at.seconds as u64); }
        if g.acquired_at.nanos != 0 { (*cx).e.varint_field(2, g.acquired_at.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_CREATION_TO_END_DURATION != 0 {
        let mk = (*cx).e.begin(20, 50);
        if g.creation_to_end_duration.seconds != 0 { (*cx).e.varint_field(1, g.creation_to_end_duration.seconds as u64); }
        if g.creation_to_end_duration.nanos != 0 { (*cx).e.varint_field(2, g.creation_to_end_duration.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_PROCESSING_TO_END_DURATION != 0 {
        let mk = (*cx).e.begin(21, 51);
        if g.processing_to_end_duration.seconds != 0 { (*cx).e.varint_field(1, g.processing_to_end_duration.seconds as u64); }
        if g.processing_to_end_duration.nanos != 0 { (*cx).e.varint_field(2, g.processing_to_end_duration.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.initial_task_id.tc.is_some() && g.initial_task_id.len != 0 {
        if !enc_blob(cx, 22, 52, &g.initial_task_id) { return false; }
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_RECEIVED_TO_END_DURATION != 0 {
        let mk = (*cx).e.begin(23, 53);
        if g.received_to_end_duration.seconds != 0 { (*cx).e.varint_field(1, g.received_to_end_duration.seconds as u64); }
        if g.received_to_end_duration.nanos != 0 { (*cx).e.varint_field(2, g.received_to_end_duration.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_PROCESSED_AT != 0 {
        let mk = (*cx).e.begin(24, 54);
        if g.processed_at.seconds != 0 { (*cx).e.varint_field(1, g.processed_at.seconds as u64); }
        if g.processed_at.nanos != 0 { (*cx).e.varint_field(2, g.processed_at.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_FETCHED_AT != 0 {
        let mk = (*cx).e.begin(25, 55);
        if g.fetched_at.seconds != 0 { (*cx).e.varint_field(1, g.fetched_at.seconds as u64); }
        if g.fetched_at.nanos != 0 { (*cx).e.varint_field(2, g.fetched_at.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.payload_id.tc.is_some() && g.payload_id.len != 0 {
        if !enc_blob(cx, 26, 56, &g.payload_id) { return false; }
    }
    if g.created_by.tc.is_some() && g.created_by.len != 0 {
        if !enc_blob(cx, 27, 57, &g.created_by) { return false; }
    }
    true
}

#[inline]
unsafe fn enc_task_detailed_ugroup(
    g: &ak_ufix_TaskDetailed,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_TaskDetailed,
    obj: *const c_void,
    token: i64,
) -> bool {
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 1, 23, &g.id) { return false; }
    }
    if g.session_id.tc.is_some() && g.session_id.len != 0 {
        if !enc_blob(cx, 2, 24, &g.session_id) { return false; }
    }
    if g.owner_pod_id.tc.is_some() && g.owner_pod_id.len != 0 {
        if !enc_blob(cx, 3, 25, &g.owner_pod_id) { return false; }
    }
    if let Some(lp) = (*vt).loop_parent_task_ids {
        (*cx).open_tag = 4;
        (*cx).open_site = 26;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if let Some(lp) = (*vt).loop_data_dependencies {
        (*cx).open_tag = 5;
        (*cx).open_site = 27;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if let Some(lp) = (*vt).loop_expected_output_ids {
        (*cx).open_tag = 6;
        (*cx).open_site = 28;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if let Some(lp) = (*vt).loop_retry_of_ids {
        (*cx).open_tag = 7;
        (*cx).open_site = 29;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if g.status != 0 { (*cx).e.varint_field(8, g.status as i64 as u64); }
    if g.status_message.tc.is_some() && g.status_message.len != 0 {
        if !enc_blob(cx, 9, 30, &g.status_message) { return false; }
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_OPTIONS != 0 {
        let mk = (*cx).e.begin(10, 31);
        if let Some(lp) = (*vt).loop_options_options {
            (*cx).open_tag = 1;
            (*cx).open_site = 32;
            (*cx).open_kind = 0;
            ak_rt::bump!((*cx).e.c, reverse);
            let rc = lp(cx as *mut ak_enc_ctx, obj, token);
            if rc < 0 { (*cx).e.fail(rc); return false; }
            if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
        }
        if g.options.presence & AK_UFIX_TASKOPTIONS_PRESENT_MAX_DURATION != 0 {
            let mk = (*cx).e.begin(2, 33);
            if g.options.max_duration.seconds != 0 { (*cx).e.varint_field(1, g.options.max_duration.seconds as u64); }
            if g.options.max_duration.nanos != 0 { (*cx).e.varint_field(2, g.options.max_duration.nanos as i64 as u64); }
            if !enc_raw(cx, &g.options.max_duration.unknown) { return false; }
            (*cx).e.end(mk);
        }
        if g.options.max_retries != 0 { (*cx).e.varint_field(3, g.options.max_retries as i64 as u64); }
        if g.options.priority != 0 { (*cx).e.varint_field(4, g.options.priority as i64 as u64); }
        if g.options.partition_id.tc.is_some() && g.options.partition_id.len != 0 {
            if !enc_blob(cx, 5, 34, &g.options.partition_id) { return false; }
        }
        if g.options.application_name.tc.is_some() && g.options.application_name.len != 0 {
            if !enc_blob(cx, 6, 35, &g.options.application_name) { return false; }
        }
        if g.options.application_version.tc.is_some() && g.options.application_version.len != 0 {
            if !enc_blob(cx, 7, 36, &g.options.application_version) { return false; }
        }
        if g.options.application_namespace.tc.is_some() && g.options.application_namespace.len != 0 {
            if !enc_blob(cx, 8, 37, &g.options.application_namespace) { return false; }
        }
        if g.options.application_service.tc.is_some() && g.options.application_service.len != 0 {
            if !enc_blob(cx, 9, 38, &g.options.application_service) { return false; }
        }
        if g.options.engine_type.tc.is_some() && g.options.engine_type.len != 0 {
            if !enc_blob(cx, 10, 39, &g.options.engine_type) { return false; }
        }
        if !enc_raw(cx, &g.options.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_CREATED_AT != 0 {
        let mk = (*cx).e.begin(11, 40);
        if g.created_at.seconds != 0 { (*cx).e.varint_field(1, g.created_at.seconds as u64); }
        if g.created_at.nanos != 0 { (*cx).e.varint_field(2, g.created_at.nanos as i64 as u64); }
        if !enc_raw(cx, &g.created_at.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_SUBMITTED_AT != 0 {
        let mk = (*cx).e.begin(12, 41);
        if g.submitted_at.seconds != 0 { (*cx).e.varint_field(1, g.submitted_at.seconds as u64); }
        if g.submitted_at.nanos != 0 { (*cx).e.varint_field(2, g.submitted_at.nanos as i64 as u64); }
        if !enc_raw(cx, &g.submitted_at.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_STARTED_AT != 0 {
        let mk = (*cx).e.begin(13, 42);
        if g.started_at.seconds != 0 { (*cx).e.varint_field(1, g.started_at.seconds as u64); }
        if g.started_at.nanos != 0 { (*cx).e.varint_field(2, g.started_at.nanos as i64 as u64); }
        if !enc_raw(cx, &g.started_at.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_ENDED_AT != 0 {
        let mk = (*cx).e.begin(14, 43);
        if g.ended_at.seconds != 0 { (*cx).e.varint_field(1, g.ended_at.seconds as u64); }
        if g.ended_at.nanos != 0 { (*cx).e.varint_field(2, g.ended_at.nanos as i64 as u64); }
        if !enc_raw(cx, &g.ended_at.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_POD_TTL != 0 {
        let mk = (*cx).e.begin(15, 44);
        if g.pod_ttl.seconds != 0 { (*cx).e.varint_field(1, g.pod_ttl.seconds as u64); }
        if g.pod_ttl.nanos != 0 { (*cx).e.varint_field(2, g.pod_ttl.nanos as i64 as u64); }
        if !enc_raw(cx, &g.pod_ttl.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_OUTPUT != 0 {
        let mk = (*cx).e.begin(16, 45);
        if g.output.success != 0 { (*cx).e.varint_field(1, (g.output.success != 0) as u64); }
        if g.output.error.tc.is_some() && g.output.error.len != 0 {
            if !enc_blob(cx, 2, 46, &g.output.error) { return false; }
        }
        if !enc_raw(cx, &g.output.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if g.pod_hostname.tc.is_some() && g.pod_hostname.len != 0 {
        if !enc_blob(cx, 17, 47, &g.pod_hostname) { return false; }
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_RECEIVED_AT != 0 {
        let mk = (*cx).e.begin(18, 48);
        if g.received_at.seconds != 0 { (*cx).e.varint_field(1, g.received_at.seconds as u64); }
        if g.received_at.nanos != 0 { (*cx).e.varint_field(2, g.received_at.nanos as i64 as u64); }
        if !enc_raw(cx, &g.received_at.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_ACQUIRED_AT != 0 {
        let mk = (*cx).e.begin(19, 49);
        if g.acquired_at.seconds != 0 { (*cx).e.varint_field(1, g.acquired_at.seconds as u64); }
        if g.acquired_at.nanos != 0 { (*cx).e.varint_field(2, g.acquired_at.nanos as i64 as u64); }
        if !enc_raw(cx, &g.acquired_at.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_CREATION_TO_END_DURATION != 0 {
        let mk = (*cx).e.begin(20, 50);
        if g.creation_to_end_duration.seconds != 0 { (*cx).e.varint_field(1, g.creation_to_end_duration.seconds as u64); }
        if g.creation_to_end_duration.nanos != 0 { (*cx).e.varint_field(2, g.creation_to_end_duration.nanos as i64 as u64); }
        if !enc_raw(cx, &g.creation_to_end_duration.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_PROCESSING_TO_END_DURATION != 0 {
        let mk = (*cx).e.begin(21, 51);
        if g.processing_to_end_duration.seconds != 0 { (*cx).e.varint_field(1, g.processing_to_end_duration.seconds as u64); }
        if g.processing_to_end_duration.nanos != 0 { (*cx).e.varint_field(2, g.processing_to_end_duration.nanos as i64 as u64); }
        if !enc_raw(cx, &g.processing_to_end_duration.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if g.initial_task_id.tc.is_some() && g.initial_task_id.len != 0 {
        if !enc_blob(cx, 22, 52, &g.initial_task_id) { return false; }
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_RECEIVED_TO_END_DURATION != 0 {
        let mk = (*cx).e.begin(23, 53);
        if g.received_to_end_duration.seconds != 0 { (*cx).e.varint_field(1, g.received_to_end_duration.seconds as u64); }
        if g.received_to_end_duration.nanos != 0 { (*cx).e.varint_field(2, g.received_to_end_duration.nanos as i64 as u64); }
        if !enc_raw(cx, &g.received_to_end_duration.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_PROCESSED_AT != 0 {
        let mk = (*cx).e.begin(24, 54);
        if g.processed_at.seconds != 0 { (*cx).e.varint_field(1, g.processed_at.seconds as u64); }
        if g.processed_at.nanos != 0 { (*cx).e.varint_field(2, g.processed_at.nanos as i64 as u64); }
        if !enc_raw(cx, &g.processed_at.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_FETCHED_AT != 0 {
        let mk = (*cx).e.begin(25, 55);
        if g.fetched_at.seconds != 0 { (*cx).e.varint_field(1, g.fetched_at.seconds as u64); }
        if g.fetched_at.nanos != 0 { (*cx).e.varint_field(2, g.fetched_at.nanos as i64 as u64); }
        if !enc_raw(cx, &g.fetched_at.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if g.payload_id.tc.is_some() && g.payload_id.len != 0 {
        if !enc_blob(cx, 26, 56, &g.payload_id) { return false; }
    }
    if g.created_by.tc.is_some() && g.created_by.len != 0 {
        if !enc_blob(cx, 27, 57, &g.created_by) { return false; }
    }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_task_summary_group(
    g: &ak_efix_TaskSummary,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_TaskSummary,
    obj: *const c_void,
    token: i64,
) -> bool {
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 1, 58, &g.id) { return false; }
    }
    if g.session_id.tc.is_some() && g.session_id.len != 0 {
        if !enc_blob(cx, 2, 59, &g.session_id) { return false; }
    }
    if g.presence & AK_EFIX_TASKSUMMARY_PRESENT_OPTIONS != 0 {
        let mk = (*cx).e.begin(3, 60);
        if let Some(lp) = (*vt).loop_options_options {
            (*cx).open_tag = 1;
            (*cx).open_site = 61;
            (*cx).open_kind = 0;
            ak_rt::bump!((*cx).e.c, reverse);
            let rc = lp(cx as *mut ak_enc_ctx, obj, token);
            if rc < 0 { (*cx).e.fail(rc); return false; }
            if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
        }
        if g.options.presence & AK_EFIX_TASKOPTIONS_PRESENT_MAX_DURATION != 0 {
            let mk = (*cx).e.begin(2, 62);
            if g.options.max_duration.seconds != 0 { (*cx).e.varint_field(1, g.options.max_duration.seconds as u64); }
            if g.options.max_duration.nanos != 0 { (*cx).e.varint_field(2, g.options.max_duration.nanos as i64 as u64); }
            (*cx).e.end(mk);
        }
        if g.options.max_retries != 0 { (*cx).e.varint_field(3, g.options.max_retries as i64 as u64); }
        if g.options.priority != 0 { (*cx).e.varint_field(4, g.options.priority as i64 as u64); }
        if g.options.partition_id.tc.is_some() && g.options.partition_id.len != 0 {
            if !enc_blob(cx, 5, 63, &g.options.partition_id) { return false; }
        }
        if g.options.application_name.tc.is_some() && g.options.application_name.len != 0 {
            if !enc_blob(cx, 6, 64, &g.options.application_name) { return false; }
        }
        if g.options.application_version.tc.is_some() && g.options.application_version.len != 0 {
            if !enc_blob(cx, 7, 65, &g.options.application_version) { return false; }
        }
        if g.options.application_namespace.tc.is_some() && g.options.application_namespace.len != 0 {
            if !enc_blob(cx, 8, 66, &g.options.application_namespace) { return false; }
        }
        if g.options.application_service.tc.is_some() && g.options.application_service.len != 0 {
            if !enc_blob(cx, 9, 67, &g.options.application_service) { return false; }
        }
        if g.options.engine_type.tc.is_some() && g.options.engine_type.len != 0 {
            if !enc_blob(cx, 10, 68, &g.options.engine_type) { return false; }
        }
        (*cx).e.end(mk);
    }
    if g.status != 0 { (*cx).e.varint_field(4, g.status as i64 as u64); }
    if g.presence & AK_EFIX_TASKSUMMARY_PRESENT_CREATED_AT != 0 {
        let mk = (*cx).e.begin(5, 69);
        if g.created_at.seconds != 0 { (*cx).e.varint_field(1, g.created_at.seconds as u64); }
        if g.created_at.nanos != 0 { (*cx).e.varint_field(2, g.created_at.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.error.tc.is_some() && g.error.len != 0 {
        if !enc_blob(cx, 8, 70, &g.error) { return false; }
    }
    if g.status_message.tc.is_some() && g.status_message.len != 0 {
        if !enc_blob(cx, 9, 71, &g.status_message) { return false; }
    }
    if g.count_data_dependencies != 0 { (*cx).e.varint_field(11, g.count_data_dependencies as u64); }
    true
}

#[inline]
unsafe fn enc_task_summary_ugroup(
    g: &ak_ufix_TaskSummary,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_TaskSummary,
    obj: *const c_void,
    token: i64,
) -> bool {
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 1, 58, &g.id) { return false; }
    }
    if g.session_id.tc.is_some() && g.session_id.len != 0 {
        if !enc_blob(cx, 2, 59, &g.session_id) { return false; }
    }
    if g.presence & AK_UFIX_TASKSUMMARY_PRESENT_OPTIONS != 0 {
        let mk = (*cx).e.begin(3, 60);
        if let Some(lp) = (*vt).loop_options_options {
            (*cx).open_tag = 1;
            (*cx).open_site = 61;
            (*cx).open_kind = 0;
            ak_rt::bump!((*cx).e.c, reverse);
            let rc = lp(cx as *mut ak_enc_ctx, obj, token);
            if rc < 0 { (*cx).e.fail(rc); return false; }
            if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
        }
        if g.options.presence & AK_UFIX_TASKOPTIONS_PRESENT_MAX_DURATION != 0 {
            let mk = (*cx).e.begin(2, 62);
            if g.options.max_duration.seconds != 0 { (*cx).e.varint_field(1, g.options.max_duration.seconds as u64); }
            if g.options.max_duration.nanos != 0 { (*cx).e.varint_field(2, g.options.max_duration.nanos as i64 as u64); }
            if !enc_raw(cx, &g.options.max_duration.unknown) { return false; }
            (*cx).e.end(mk);
        }
        if g.options.max_retries != 0 { (*cx).e.varint_field(3, g.options.max_retries as i64 as u64); }
        if g.options.priority != 0 { (*cx).e.varint_field(4, g.options.priority as i64 as u64); }
        if g.options.partition_id.tc.is_some() && g.options.partition_id.len != 0 {
            if !enc_blob(cx, 5, 63, &g.options.partition_id) { return false; }
        }
        if g.options.application_name.tc.is_some() && g.options.application_name.len != 0 {
            if !enc_blob(cx, 6, 64, &g.options.application_name) { return false; }
        }
        if g.options.application_version.tc.is_some() && g.options.application_version.len != 0 {
            if !enc_blob(cx, 7, 65, &g.options.application_version) { return false; }
        }
        if g.options.application_namespace.tc.is_some() && g.options.application_namespace.len != 0 {
            if !enc_blob(cx, 8, 66, &g.options.application_namespace) { return false; }
        }
        if g.options.application_service.tc.is_some() && g.options.application_service.len != 0 {
            if !enc_blob(cx, 9, 67, &g.options.application_service) { return false; }
        }
        if g.options.engine_type.tc.is_some() && g.options.engine_type.len != 0 {
            if !enc_blob(cx, 10, 68, &g.options.engine_type) { return false; }
        }
        if !enc_raw(cx, &g.options.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if g.status != 0 { (*cx).e.varint_field(4, g.status as i64 as u64); }
    if g.presence & AK_UFIX_TASKSUMMARY_PRESENT_CREATED_AT != 0 {
        let mk = (*cx).e.begin(5, 69);
        if g.created_at.seconds != 0 { (*cx).e.varint_field(1, g.created_at.seconds as u64); }
        if g.created_at.nanos != 0 { (*cx).e.varint_field(2, g.created_at.nanos as i64 as u64); }
        if !enc_raw(cx, &g.created_at.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if g.error.tc.is_some() && g.error.len != 0 {
        if !enc_blob(cx, 8, 70, &g.error) { return false; }
    }
    if g.status_message.tc.is_some() && g.status_message.len != 0 {
        if !enc_blob(cx, 9, 71, &g.status_message) { return false; }
    }
    if g.count_data_dependencies != 0 { (*cx).e.varint_field(11, g.count_data_dependencies as u64); }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_probe_group(g: &ak_efix_Probe, cx: *mut EncCtxImpl) -> bool {
    // A case this build does not know -- a host generated against a newer
    // descriptor -- is refused before anything is written (plan rule).
    match g.body_case {
        0 | 10 | 11 | 12 | 13 | 14 => {}
        _ => { (*cx).e.fail(AK_ERR_ABI); return false; }
    }
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 1, 72, &g.id) { return false; }
    }
    if g.presence & AK_EFIX_PROBE_PRESENT_OPT_COUNT != 0 { (*cx).e.varint_field(2, g.opt_count as i64 as u64); }
    if g.presence & AK_EFIX_PROBE_PRESENT_OPT_LABEL != 0 {
        if !enc_blob(cx, 3, 73, &g.opt_label) { return false; }
    }
    if g.presence & AK_EFIX_PROBE_PRESENT_OPT_FLAG != 0 { (*cx).e.varint_field(4, (g.opt_flag != 0) as u64); }
    if g.body_case == 10 {
        (*cx).e.varint_field(10, g.body_as_int as u64);
    }
    if g.body_case == 11 {
        if !enc_blob(cx, 11, 75, &g.body_as_text) { return false; }
    }
    if g.body_case == 12 {
        if !enc_blob(cx, 12, 76, &g.body_as_blob) { return false; }
    }
    if g.body_case == 13 {
        let mk = (*cx).e.begin(13, 74);
        if !enc_timestamp_group(&g.body_as_stamp, cx) { return false; }
        (*cx).e.end(mk);
    }
    if g.body_case == 14 {
        let mk = (*cx).e.begin(14, 74);
        if !enc_empty_group(&g.body_as_nothing, cx) { return false; }
        (*cx).e.end(mk);
    }
    true
}

#[inline]
unsafe fn enc_probe_ugroup(g: &ak_ufix_Probe, cx: *mut EncCtxImpl) -> bool {
    // A case this build does not know -- a host generated against a newer
    // descriptor -- is refused before anything is written (plan rule).
    match g.body_case {
        0 | 10 | 11 | 12 | 13 | 14 => {}
        _ => { (*cx).e.fail(AK_ERR_ABI); return false; }
    }
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 1, 72, &g.id) { return false; }
    }
    if g.presence & AK_UFIX_PROBE_PRESENT_OPT_COUNT != 0 { (*cx).e.varint_field(2, g.opt_count as i64 as u64); }
    if g.presence & AK_UFIX_PROBE_PRESENT_OPT_LABEL != 0 {
        if !enc_blob(cx, 3, 73, &g.opt_label) { return false; }
    }
    if g.presence & AK_UFIX_PROBE_PRESENT_OPT_FLAG != 0 { (*cx).e.varint_field(4, (g.opt_flag != 0) as u64); }
    if g.body_case == 10 {
        (*cx).e.varint_field(10, g.body_as_int as u64);
    }
    if g.body_case == 11 {
        if !enc_blob(cx, 11, 75, &g.body_as_text) { return false; }
    }
    if g.body_case == 12 {
        if !enc_blob(cx, 12, 76, &g.body_as_blob) { return false; }
    }
    if g.body_case == 13 {
        let mk = (*cx).e.begin(13, 74);
        if !enc_timestamp_ugroup(&g.body_as_stamp, cx) { return false; }
        (*cx).e.end(mk);
    }
    if g.body_case == 14 {
        let mk = (*cx).e.begin(14, 74);
        if !enc_empty_ugroup(&g.body_as_nothing, cx) { return false; }
        (*cx).e.end(mk);
    }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_empty_group(g: &ak_efix_Empty, cx: *mut EncCtxImpl) -> bool {
    true
}

#[inline]
unsafe fn enc_empty_ugroup(g: &ak_ufix_Empty, cx: *mut EncCtxImpl) -> bool {
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_upload_result_data_group(g: &ak_efix_UploadResultData, cx: *mut EncCtxImpl) -> bool {
    if g.session_id.tc.is_some() && g.session_id.len != 0 {
        if !enc_blob(cx, 1, 77, &g.session_id) { return false; }
    }
    if g.result_id.tc.is_some() && g.result_id.len != 0 {
        if !enc_blob(cx, 2, 78, &g.result_id) { return false; }
    }
    if g.data_chunk.len != 0 {
        if !enc_blob(cx, 3, 79, &g.data_chunk) { return false; }
    }
    true
}

#[inline]
unsafe fn enc_upload_result_data_ugroup(g: &ak_ufix_UploadResultData, cx: *mut EncCtxImpl) -> bool {
    if g.session_id.tc.is_some() && g.session_id.len != 0 {
        if !enc_blob(cx, 1, 77, &g.session_id) { return false; }
    }
    if g.result_id.tc.is_some() && g.result_id.len != 0 {
        if !enc_blob(cx, 2, 78, &g.result_id) { return false; }
    }
    if g.data_chunk.len != 0 {
        if !enc_blob(cx, 3, 79, &g.data_chunk) { return false; }
    }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_metrics_batch_group(
    g: &ak_efix_MetricsBatch,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_MetricsBatch,
    obj: *const c_void,
    token: i64,
) -> bool {
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 1, 80, &g.id) { return false; }
    }
    if let Some(lp) = (*vt).loop_ticks {
        (*cx).open_tag = 2;
        (*cx).open_site = 81;
        (*cx).open_kind = 2;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if let Some(lp) = (*vt).loop_values {
        (*cx).open_tag = 3;
        (*cx).open_site = 82;
        (*cx).open_kind = 4;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if let Some(lp) = (*vt).loop_codes {
        (*cx).open_tag = 4;
        (*cx).open_site = 83;
        (*cx).open_kind = 1;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if let Some(lp) = (*vt).loop_flags {
        (*cx).open_tag = 5;
        (*cx).open_site = 84;
        (*cx).open_kind = 3;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if let Some(lp) = (*vt).loop_statuses {
        (*cx).open_tag = 6;
        (*cx).open_site = 85;
        (*cx).open_kind = 5;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    true
}

#[inline]
unsafe fn enc_metrics_batch_ugroup(
    g: &ak_ufix_MetricsBatch,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_MetricsBatch,
    obj: *const c_void,
    token: i64,
) -> bool {
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 1, 80, &g.id) { return false; }
    }
    if let Some(lp) = (*vt).loop_ticks {
        (*cx).open_tag = 2;
        (*cx).open_site = 81;
        (*cx).open_kind = 2;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if let Some(lp) = (*vt).loop_values {
        (*cx).open_tag = 3;
        (*cx).open_site = 82;
        (*cx).open_kind = 4;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if let Some(lp) = (*vt).loop_codes {
        (*cx).open_tag = 4;
        (*cx).open_site = 83;
        (*cx).open_kind = 1;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if let Some(lp) = (*vt).loop_flags {
        (*cx).open_tag = 5;
        (*cx).open_site = 84;
        (*cx).open_kind = 3;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if let Some(lp) = (*vt).loop_statuses {
        (*cx).open_tag = 6;
        (*cx).open_site = 85;
        (*cx).open_kind = 5;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_pair_group(g: &ak_efix_Pair, cx: *mut EncCtxImpl) -> bool {
    if g.key.tc.is_some() && g.key.len != 0 {
        if !enc_blob(cx, 1, 86, &g.key) { return false; }
    }
    if g.value != 0 { (*cx).e.varint_field(2, g.value as i64 as u64); }
    true
}

#[inline]
unsafe fn enc_pair_ugroup(g: &ak_ufix_Pair, cx: *mut EncCtxImpl) -> bool {
    if g.key.tc.is_some() && g.key.len != 0 {
        if !enc_blob(cx, 1, 86, &g.key) { return false; }
    }
    if g.value != 0 { (*cx).e.varint_field(2, g.value as i64 as u64); }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_list_results_response_group(
    g: &ak_efix_ListResultsResponse,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_ListResultsResponse,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_results {
        (*cx).open_tag = 1;
        (*cx).open_site = 87;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if g.page != 0 { (*cx).e.varint_field(2, g.page as i64 as u64); }
    if g.total != 0 { (*cx).e.varint_field(3, g.total as i64 as u64); }
    true
}

#[inline]
unsafe fn enc_list_results_response_ugroup(
    g: &ak_ufix_ListResultsResponse,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_ListResultsResponse,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_results {
        (*cx).open_tag = 1;
        (*cx).open_site = 87;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if g.page != 0 { (*cx).e.varint_field(2, g.page as i64 as u64); }
    if g.total != 0 { (*cx).e.varint_field(3, g.total as i64 as u64); }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_list_tasks_detailed_response_group(
    g: &ak_efix_ListTasksDetailedResponse,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_ListTasksDetailedResponse,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_tasks {
        (*cx).open_tag = 1;
        (*cx).open_site = 88;
        (*cx).open_kind = 0;
        (*cx).open_vt = (*vt).elem_tasks as *const c_void;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if g.page != 0 { (*cx).e.varint_field(2, g.page as i64 as u64); }
    if g.total != 0 { (*cx).e.varint_field(3, g.total as i64 as u64); }
    true
}

#[inline]
unsafe fn enc_list_tasks_detailed_response_ugroup(
    g: &ak_ufix_ListTasksDetailedResponse,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_ListTasksDetailedResponse,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_tasks {
        (*cx).open_tag = 1;
        (*cx).open_site = 88;
        (*cx).open_kind = 0;
        (*cx).open_vt = (*vt).elem_tasks as *const c_void;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if g.page != 0 { (*cx).e.varint_field(2, g.page as i64 as u64); }
    if g.total != 0 { (*cx).e.varint_field(3, g.total as i64 as u64); }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_list_task_summary_response_group(
    g: &ak_efix_ListTaskSummaryResponse,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_ListTaskSummaryResponse,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_tasks {
        (*cx).open_tag = 1;
        (*cx).open_site = 89;
        (*cx).open_kind = 0;
        (*cx).open_vt = (*vt).elem_tasks as *const c_void;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    true
}

#[inline]
unsafe fn enc_list_task_summary_response_ugroup(
    g: &ak_ufix_ListTaskSummaryResponse,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_ListTaskSummaryResponse,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_tasks {
        (*cx).open_tag = 1;
        (*cx).open_site = 89;
        (*cx).open_kind = 0;
        (*cx).open_vt = (*vt).elem_tasks as *const c_void;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_list_probe_response_group(
    g: &ak_efix_ListProbeResponse,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_ListProbeResponse,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_probes {
        (*cx).open_tag = 1;
        (*cx).open_site = 90;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    true
}

#[inline]
unsafe fn enc_list_probe_response_ugroup(
    g: &ak_ufix_ListProbeResponse,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_ListProbeResponse,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_probes {
        (*cx).open_tag = 1;
        (*cx).open_site = 90;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_list_metrics_response_group(
    g: &ak_efix_ListMetricsResponse,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_ListMetricsResponse,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_batches {
        (*cx).open_tag = 1;
        (*cx).open_site = 91;
        (*cx).open_kind = 0;
        (*cx).open_vt = (*vt).elem_batches as *const c_void;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    true
}

#[inline]
unsafe fn enc_list_metrics_response_ugroup(
    g: &ak_ufix_ListMetricsResponse,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_ListMetricsResponse,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_batches {
        (*cx).open_tag = 1;
        (*cx).open_site = 91;
        (*cx).open_kind = 0;
        (*cx).open_vt = (*vt).elem_batches as *const c_void;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_upload_result_data_message_group(g: &ak_efix_UploadResultDataMessage, cx: *mut EncCtxImpl) -> bool {
    if g.presence & AK_EFIX_UPLOADRESULTDATAMESSAGE_PRESENT_UPLOAD != 0 {
        let mk = (*cx).e.begin(1, 92);
        if g.upload.session_id.tc.is_some() && g.upload.session_id.len != 0 {
            if !enc_blob(cx, 1, 93, &g.upload.session_id) { return false; }
        }
        if g.upload.result_id.tc.is_some() && g.upload.result_id.len != 0 {
            if !enc_blob(cx, 2, 94, &g.upload.result_id) { return false; }
        }
        if g.upload.data_chunk.len != 0 {
            if !enc_blob(cx, 3, 95, &g.upload.data_chunk) { return false; }
        }
        (*cx).e.end(mk);
    }
    true
}

#[inline]
unsafe fn enc_upload_result_data_message_ugroup(g: &ak_ufix_UploadResultDataMessage, cx: *mut EncCtxImpl) -> bool {
    if g.presence & AK_UFIX_UPLOADRESULTDATAMESSAGE_PRESENT_UPLOAD != 0 {
        let mk = (*cx).e.begin(1, 92);
        if g.upload.session_id.tc.is_some() && g.upload.session_id.len != 0 {
            if !enc_blob(cx, 1, 93, &g.upload.session_id) { return false; }
        }
        if g.upload.result_id.tc.is_some() && g.upload.result_id.len != 0 {
            if !enc_blob(cx, 2, 94, &g.upload.result_id) { return false; }
        }
        if g.upload.data_chunk.len != 0 {
            if !enc_blob(cx, 3, 95, &g.upload.data_chunk) { return false; }
        }
        if !enc_raw(cx, &g.upload.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_dual_response_group(
    g: &ak_efix_DualResponse,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_DualResponse,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_left {
        (*cx).open_tag = 1;
        (*cx).open_site = 96;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if let Some(lp) = (*vt).loop_right {
        (*cx).open_tag = 2;
        (*cx).open_site = 97;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    true
}

#[inline]
unsafe fn enc_dual_response_ugroup(
    g: &ak_ufix_DualResponse,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_DualResponse,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_left {
        (*cx).open_tag = 1;
        (*cx).open_site = 96;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if let Some(lp) = (*vt).loop_right {
        (*cx).open_tag = 2;
        (*cx).open_site = 97;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_chunk_leaf_group(g: &ak_efix_ChunkLeaf, cx: *mut EncCtxImpl) -> bool {
    if g.k.tc.is_some() && g.k.len != 0 {
        if !enc_blob(cx, 1, 98, &g.k) { return false; }
    }
    if g.v != 0 { (*cx).e.varint_field(2, g.v as i64 as u64); }
    true
}

#[inline]
unsafe fn enc_chunk_leaf_ugroup(g: &ak_ufix_ChunkLeaf, cx: *mut EncCtxImpl) -> bool {
    if g.k.tc.is_some() && g.k.len != 0 {
        if !enc_blob(cx, 1, 98, &g.k) { return false; }
    }
    if g.v != 0 { (*cx).e.varint_field(2, g.v as i64 as u64); }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_chunk_inner_group(
    g: &ak_efix_ChunkInner,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_ChunkInner,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_marks {
        (*cx).open_tag = 1;
        (*cx).open_site = 99;
        (*cx).open_kind = 2;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if let Some(lp) = (*vt).loop_leaves {
        (*cx).open_tag = 2;
        (*cx).open_site = 100;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    true
}

#[inline]
unsafe fn enc_chunk_inner_ugroup(
    g: &ak_ufix_ChunkInner,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_ChunkInner,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_marks {
        (*cx).open_tag = 1;
        (*cx).open_site = 99;
        (*cx).open_kind = 2;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if let Some(lp) = (*vt).loop_leaves {
        (*cx).open_tag = 2;
        (*cx).open_site = 100;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_chunk_element_group(
    g: &ak_efix_ChunkElement,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_ChunkElement,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_labels {
        (*cx).open_tag = 1;
        (*cx).open_site = 101;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if let Some(lp) = (*vt).loop_attrs {
        (*cx).open_tag = 2;
        (*cx).open_site = 102;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 3, 103, &g.id) { return false; }
    }
    if g.presence & AK_EFIX_CHUNKELEMENT_PRESENT_INNER != 0 {
        let mk = (*cx).e.begin(4, 104);
        if let Some(lp) = (*vt).loop_inner_marks {
            (*cx).open_tag = 1;
            (*cx).open_site = 105;
            (*cx).open_kind = 2;
            ak_rt::bump!((*cx).e.c, reverse);
            let rc = lp(cx as *mut ak_enc_ctx, obj, token);
            if rc < 0 { (*cx).e.fail(rc); return false; }
            if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
        }
        if let Some(lp) = (*vt).loop_inner_leaves {
            (*cx).open_tag = 2;
            (*cx).open_site = 106;
            (*cx).open_kind = 0;
            ak_rt::bump!((*cx).e.c, reverse);
            let rc = lp(cx as *mut ak_enc_ctx, obj, token);
            if rc < 0 { (*cx).e.fail(rc); return false; }
            if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
        }
        (*cx).e.end(mk);
    }
    true
}

#[inline]
unsafe fn enc_chunk_element_ugroup(
    g: &ak_ufix_ChunkElement,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_ChunkElement,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_labels {
        (*cx).open_tag = 1;
        (*cx).open_site = 101;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if let Some(lp) = (*vt).loop_attrs {
        (*cx).open_tag = 2;
        (*cx).open_site = 102;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 3, 103, &g.id) { return false; }
    }
    if g.presence & AK_UFIX_CHUNKELEMENT_PRESENT_INNER != 0 {
        let mk = (*cx).e.begin(4, 104);
        if let Some(lp) = (*vt).loop_inner_marks {
            (*cx).open_tag = 1;
            (*cx).open_site = 105;
            (*cx).open_kind = 2;
            ak_rt::bump!((*cx).e.c, reverse);
            let rc = lp(cx as *mut ak_enc_ctx, obj, token);
            if rc < 0 { (*cx).e.fail(rc); return false; }
            if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
        }
        if let Some(lp) = (*vt).loop_inner_leaves {
            (*cx).open_tag = 2;
            (*cx).open_site = 106;
            (*cx).open_kind = 0;
            ak_rt::bump!((*cx).e.c, reverse);
            let rc = lp(cx as *mut ak_enc_ctx, obj, token);
            if rc < 0 { (*cx).e.fail(rc); return false; }
            if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
        }
        if !enc_raw(cx, &g.inner.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_chunked_response_group(
    g: &ak_efix_ChunkedResponse,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_ChunkedResponse,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_items {
        (*cx).open_tag = 7;
        (*cx).open_site = 107;
        (*cx).open_kind = 0;
        (*cx).open_vt = (*vt).elem_items as *const c_void;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if g.page != 0 { (*cx).e.varint_field(8, g.page as i64 as u64); }
    true
}

#[inline]
unsafe fn enc_chunked_response_ugroup(
    g: &ak_ufix_ChunkedResponse,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_ChunkedResponse,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_items {
        (*cx).open_tag = 7;
        (*cx).open_site = 107;
        (*cx).open_kind = 0;
        (*cx).open_vt = (*vt).elem_items as *const c_void;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if g.page != 0 { (*cx).e.varint_field(8, g.page as i64 as u64); }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_chunked_response_wide_group(
    g: &ak_efix_ChunkedResponseWide,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_ChunkedResponseWide,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_items {
        (*cx).open_tag = 70000;
        (*cx).open_site = 108;
        (*cx).open_kind = 0;
        (*cx).open_vt = (*vt).elem_items as *const c_void;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    true
}

#[inline]
unsafe fn enc_chunked_response_wide_ugroup(
    g: &ak_ufix_ChunkedResponseWide,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_ChunkedResponseWide,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_items {
        (*cx).open_tag = 70000;
        (*cx).open_site = 108;
        (*cx).open_kind = 0;
        (*cx).open_vt = (*vt).elem_items as *const c_void;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_leaf_element_group(g: &ak_efix_LeafElement, cx: *mut EncCtxImpl) -> bool {
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 1, 109, &g.id) { return false; }
    }
    if g.n != 0 { (*cx).e.varint_field(2, g.n as u64); }
    if g.presence & AK_EFIX_LEAFELEMENT_PRESENT_STAMP != 0 {
        let mk = (*cx).e.begin(3, 110);
        if g.stamp.seconds != 0 { (*cx).e.varint_field(1, g.stamp.seconds as u64); }
        if g.stamp.nanos != 0 { (*cx).e.varint_field(2, g.stamp.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    true
}

#[inline]
unsafe fn enc_leaf_element_ugroup(g: &ak_ufix_LeafElement, cx: *mut EncCtxImpl) -> bool {
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 1, 109, &g.id) { return false; }
    }
    if g.n != 0 { (*cx).e.varint_field(2, g.n as u64); }
    if g.presence & AK_UFIX_LEAFELEMENT_PRESENT_STAMP != 0 {
        let mk = (*cx).e.begin(3, 110);
        if g.stamp.seconds != 0 { (*cx).e.varint_field(1, g.stamp.seconds as u64); }
        if g.stamp.nanos != 0 { (*cx).e.varint_field(2, g.stamp.nanos as i64 as u64); }
        if !enc_raw(cx, &g.stamp.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_leaf_response_group(
    g: &ak_efix_LeafResponse,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_LeafResponse,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_items {
        (*cx).open_tag = 9;
        (*cx).open_site = 111;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    true
}

#[inline]
unsafe fn enc_leaf_response_ugroup(
    g: &ak_ufix_LeafResponse,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_LeafResponse,
    obj: *const c_void,
    token: i64,
) -> bool {
    if let Some(lp) = (*vt).loop_items {
        (*cx).open_tag = 9;
        (*cx).open_site = 111;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_surrogate_group(
    g: &ak_efix_Surrogate,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_Surrogate,
    obj: *const c_void,
    token: i64,
) -> bool {
    if g.text.tc.is_some() && g.text.len != 0 {
        if !enc_blob(cx, 1, 112, &g.text) { return false; }
    }
    if g.presence & AK_EFIX_SURROGATE_PRESENT_NESTED != 0 {
        let mk = (*cx).e.begin(2, 113);
        if g.nested.text.tc.is_some() && g.nested.text.len != 0 {
            if !enc_blob(cx, 1, 114, &g.nested.text) { return false; }
        }
        (*cx).e.end(mk);
    }
    if let Some(lp) = (*vt).loop_attrs {
        (*cx).open_tag = 3;
        (*cx).open_site = 115;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if let Some(lp) = (*vt).loop_texts {
        (*cx).open_tag = 4;
        (*cx).open_site = 116;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if g.raw.tc.is_some() && g.raw.len != 0 {
        if !enc_blob(cx, 5, 117, &g.raw) { return false; }
    }
    true
}

#[inline]
unsafe fn enc_surrogate_ugroup(
    g: &ak_ufix_Surrogate,
    cx: *mut EncCtxImpl,
    vt: *const ak_evt_Surrogate,
    obj: *const c_void,
    token: i64,
) -> bool {
    if g.text.tc.is_some() && g.text.len != 0 {
        if !enc_blob(cx, 1, 112, &g.text) { return false; }
    }
    if g.presence & AK_UFIX_SURROGATE_PRESENT_NESTED != 0 {
        let mk = (*cx).e.begin(2, 113);
        if g.nested.text.tc.is_some() && g.nested.text.len != 0 {
            if !enc_blob(cx, 1, 114, &g.nested.text) { return false; }
        }
        if !enc_raw(cx, &g.nested.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if let Some(lp) = (*vt).loop_attrs {
        (*cx).open_tag = 3;
        (*cx).open_site = 115;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if let Some(lp) = (*vt).loop_texts {
        (*cx).open_tag = 4;
        (*cx).open_site = 116;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return false; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return false; }
    }
    if g.raw.tc.is_some() && g.raw.len != 0 {
        if !enc_blob(cx, 5, 117, &g.raw) { return false; }
    }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_surrogate_inner_group(g: &ak_efix_SurrogateInner, cx: *mut EncCtxImpl) -> bool {
    if g.text.tc.is_some() && g.text.len != 0 {
        if !enc_blob(cx, 1, 118, &g.text) { return false; }
    }
    true
}

#[inline]
unsafe fn enc_surrogate_inner_ugroup(g: &ak_ufix_SurrogateInner, cx: *mut EncCtxImpl) -> bool {
    if g.text.tc.is_some() && g.text.len != 0 {
        if !enc_blob(cx, 1, 118, &g.text) { return false; }
    }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[inline]
unsafe fn enc_wire_zoo_group(g: &ak_efix_WireZoo, cx: *mut EncCtxImpl) -> bool {
    if g.v_int32 != 0 { (*cx).e.varint_field(1, g.v_int32 as i64 as u64); }
    if g.v_int64 != 0 { (*cx).e.varint_field(2, g.v_int64 as u64); }
    if g.v_bool != 0 { (*cx).e.varint_field(3, (g.v_bool != 0) as u64); }
    if g.v_double.to_bits() != 0 { (*cx).e.f64_field(4, g.v_double); }
    if g.v_fixed32 != 0 { (*cx).e.fixed32_field(5, g.v_fixed32); }
    if g.v_string.tc.is_some() && g.v_string.len != 0 {
        if !enc_blob(cx, 6, 119, &g.v_string) { return false; }
    }
    if g.v_bytes.tc.is_some() && g.v_bytes.len != 0 {
        if !enc_blob(cx, 7, 120, &g.v_bytes) { return false; }
    }
    if g.v_enum != 0 { (*cx).e.varint_field(8, g.v_enum as i64 as u64); }
    if g.presence & AK_EFIX_WIREZOO_PRESENT_V_MSG != 0 {
        let mk = (*cx).e.begin(9, 121);
        if g.v_msg.seconds != 0 { (*cx).e.varint_field(1, g.v_msg.seconds as u64); }
        if g.v_msg.nanos != 0 { (*cx).e.varint_field(2, g.v_msg.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.v_big_tag != 0 { (*cx).e.varint_field(536870911, g.v_big_tag as i64 as u64); }
    true
}

#[inline]
unsafe fn enc_wire_zoo_ugroup(g: &ak_ufix_WireZoo, cx: *mut EncCtxImpl) -> bool {
    if g.v_int32 != 0 { (*cx).e.varint_field(1, g.v_int32 as i64 as u64); }
    if g.v_int64 != 0 { (*cx).e.varint_field(2, g.v_int64 as u64); }
    if g.v_bool != 0 { (*cx).e.varint_field(3, (g.v_bool != 0) as u64); }
    if g.v_double.to_bits() != 0 { (*cx).e.f64_field(4, g.v_double); }
    if g.v_fixed32 != 0 { (*cx).e.fixed32_field(5, g.v_fixed32); }
    if g.v_string.tc.is_some() && g.v_string.len != 0 {
        if !enc_blob(cx, 6, 119, &g.v_string) { return false; }
    }
    if g.v_bytes.tc.is_some() && g.v_bytes.len != 0 {
        if !enc_blob(cx, 7, 120, &g.v_bytes) { return false; }
    }
    if g.v_enum != 0 { (*cx).e.varint_field(8, g.v_enum as i64 as u64); }
    if g.presence & AK_UFIX_WIREZOO_PRESENT_V_MSG != 0 {
        let mk = (*cx).e.begin(9, 121);
        if g.v_msg.seconds != 0 { (*cx).e.varint_field(1, g.v_msg.seconds as u64); }
        if g.v_msg.nanos != 0 { (*cx).e.varint_field(2, g.v_msg.nanos as i64 as u64); }
        if !enc_raw(cx, &g.v_msg.unknown) { return false; }
        (*cx).e.end(mk);
    }
    if g.v_big_tag != 0 { (*cx).e.varint_field(536870911, g.v_big_tag as i64 as u64); }
    if !enc_raw(cx, &g.unknown) { return false; }
    true
}

#[no_mangle]
pub unsafe extern "C" fn ak_elemu_ChunkElement(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_efix_ChunkElement,
    n: i32,
    tok0: i64,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    // Read the open state into locals BEFORE the run: encoding an
    // element makes reverse calls that open fields of their own.
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    let vt = (*cx).open_vt as *const ak_evt_ChunkElement;
    let obj = (*cx).open_obj;
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_chunk_element_group(&*elems.add(i), cx, vt, obj, tok0 + i as i64) {
            return crate::enc_status(cx);
        }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

/// The same unbatched run over the group that carries the bag.
#[no_mangle]
pub unsafe extern "C" fn ak_uelemu_ChunkElement(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_ufix_ChunkElement,
    n: i32,
    tok0: i64,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    let vt = (*cx).open_vt as *const ak_evt_ChunkElement;
    let obj = (*cx).open_obj;
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_chunk_element_ugroup(&*elems.add(i), cx, vt, obj, tok0 + i as i64) {
            return crate::enc_status(cx);
        }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

#[no_mangle]
pub unsafe extern "C" fn ak_elem_ChunkElementAttrsEntry(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_efix_ChunkElementAttrsEntry,
    n: i32,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_chunk_element_attrs_entry_group(&*elems.add(i), cx) { return crate::enc_status(cx); }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

/// The same run over the group that carries the unknown-field bag.
#[no_mangle]
pub unsafe extern "C" fn ak_uelem_ChunkElementAttrsEntry(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_ufix_ChunkElementAttrsEntry,
    n: i32,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_chunk_element_attrs_entry_ugroup(&*elems.add(i), cx) { return crate::enc_status(cx); }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

#[no_mangle]
pub unsafe extern "C" fn ak_elem_ChunkLeaf(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_efix_ChunkLeaf,
    n: i32,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_chunk_leaf_group(&*elems.add(i), cx) { return crate::enc_status(cx); }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

/// The same run over the group that carries the unknown-field bag.
#[no_mangle]
pub unsafe extern "C" fn ak_uelem_ChunkLeaf(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_ufix_ChunkLeaf,
    n: i32,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_chunk_leaf_ugroup(&*elems.add(i), cx) { return crate::enc_status(cx); }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

#[no_mangle]
pub unsafe extern "C" fn ak_elem_LeafElement(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_efix_LeafElement,
    n: i32,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_leaf_element_group(&*elems.add(i), cx) { return crate::enc_status(cx); }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

/// The same run over the group that carries the unknown-field bag.
#[no_mangle]
pub unsafe extern "C" fn ak_uelem_LeafElement(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_ufix_LeafElement,
    n: i32,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_leaf_element_ugroup(&*elems.add(i), cx) { return crate::enc_status(cx); }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

#[no_mangle]
pub unsafe extern "C" fn ak_elemu_MetricsBatch(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_efix_MetricsBatch,
    n: i32,
    tok0: i64,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    // Read the open state into locals BEFORE the run: encoding an
    // element makes reverse calls that open fields of their own.
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    let vt = (*cx).open_vt as *const ak_evt_MetricsBatch;
    let obj = (*cx).open_obj;
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_metrics_batch_group(&*elems.add(i), cx, vt, obj, tok0 + i as i64) {
            return crate::enc_status(cx);
        }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

/// The same unbatched run over the group that carries the bag.
#[no_mangle]
pub unsafe extern "C" fn ak_uelemu_MetricsBatch(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_ufix_MetricsBatch,
    n: i32,
    tok0: i64,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    let vt = (*cx).open_vt as *const ak_evt_MetricsBatch;
    let obj = (*cx).open_obj;
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_metrics_batch_ugroup(&*elems.add(i), cx, vt, obj, tok0 + i as i64) {
            return crate::enc_status(cx);
        }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

#[no_mangle]
pub unsafe extern "C" fn ak_elem_Pair(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_efix_Pair,
    n: i32,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_pair_group(&*elems.add(i), cx) { return crate::enc_status(cx); }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

/// The same run over the group that carries the unknown-field bag.
#[no_mangle]
pub unsafe extern "C" fn ak_uelem_Pair(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_ufix_Pair,
    n: i32,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_pair_ugroup(&*elems.add(i), cx) { return crate::enc_status(cx); }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

#[no_mangle]
pub unsafe extern "C" fn ak_elem_Probe(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_efix_Probe,
    n: i32,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_probe_group(&*elems.add(i), cx) { return crate::enc_status(cx); }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

/// The same run over the group that carries the unknown-field bag.
#[no_mangle]
pub unsafe extern "C" fn ak_uelem_Probe(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_ufix_Probe,
    n: i32,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_probe_ugroup(&*elems.add(i), cx) { return crate::enc_status(cx); }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

#[no_mangle]
pub unsafe extern "C" fn ak_elem_ResultRaw(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_efix_ResultRaw,
    n: i32,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_result_raw_group(&*elems.add(i), cx) { return crate::enc_status(cx); }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

/// The same run over the group that carries the unknown-field bag.
#[no_mangle]
pub unsafe extern "C" fn ak_uelem_ResultRaw(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_ufix_ResultRaw,
    n: i32,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_result_raw_ugroup(&*elems.add(i), cx) { return crate::enc_status(cx); }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

#[no_mangle]
pub unsafe extern "C" fn ak_elem_SurrogateAttrsEntry(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_efix_SurrogateAttrsEntry,
    n: i32,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_surrogate_attrs_entry_group(&*elems.add(i), cx) { return crate::enc_status(cx); }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

/// The same run over the group that carries the unknown-field bag.
#[no_mangle]
pub unsafe extern "C" fn ak_uelem_SurrogateAttrsEntry(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_ufix_SurrogateAttrsEntry,
    n: i32,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_surrogate_attrs_entry_ugroup(&*elems.add(i), cx) { return crate::enc_status(cx); }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

#[no_mangle]
pub unsafe extern "C" fn ak_elemu_TaskDetailed(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_efix_TaskDetailed,
    n: i32,
    tok0: i64,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    // Read the open state into locals BEFORE the run: encoding an
    // element makes reverse calls that open fields of their own.
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    let vt = (*cx).open_vt as *const ak_evt_TaskDetailed;
    let obj = (*cx).open_obj;
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_task_detailed_group(&*elems.add(i), cx, vt, obj, tok0 + i as i64) {
            return crate::enc_status(cx);
        }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

/// The same unbatched run over the group that carries the bag.
#[no_mangle]
pub unsafe extern "C" fn ak_uelemu_TaskDetailed(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_ufix_TaskDetailed,
    n: i32,
    tok0: i64,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    let vt = (*cx).open_vt as *const ak_evt_TaskDetailed;
    let obj = (*cx).open_obj;
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_task_detailed_ugroup(&*elems.add(i), cx, vt, obj, tok0 + i as i64) {
            return crate::enc_status(cx);
        }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

#[no_mangle]
pub unsafe extern "C" fn ak_elem_TaskOptionsOptionsEntry(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_efix_TaskOptionsOptionsEntry,
    n: i32,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_task_options_options_entry_group(&*elems.add(i), cx) { return crate::enc_status(cx); }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

/// The same run over the group that carries the unknown-field bag.
#[no_mangle]
pub unsafe extern "C" fn ak_uelem_TaskOptionsOptionsEntry(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_ufix_TaskOptionsOptionsEntry,
    n: i32,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_task_options_options_entry_ugroup(&*elems.add(i), cx) { return crate::enc_status(cx); }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

#[no_mangle]
pub unsafe extern "C" fn ak_elemu_TaskSummary(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_efix_TaskSummary,
    n: i32,
    tok0: i64,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    // Read the open state into locals BEFORE the run: encoding an
    // element makes reverse calls that open fields of their own.
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    let vt = (*cx).open_vt as *const ak_evt_TaskSummary;
    let obj = (*cx).open_obj;
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_task_summary_group(&*elems.add(i), cx, vt, obj, tok0 + i as i64) {
            return crate::enc_status(cx);
        }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

/// The same unbatched run over the group that carries the bag.
#[no_mangle]
pub unsafe extern "C" fn ak_uelemu_TaskSummary(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_ufix_TaskSummary,
    n: i32,
    tok0: i64,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    // ABI v1: an element entry point must leave the codec's open-field state as it found
    // it. The host drives iteration over its own container and calls this once per chunk;
    // encoding an element sets the open state for the element's OWN repeated fields, so
    // without this the second chunk reads whatever the first element left behind. It was
    // found as a length-prefix site that would not converge, but the failure is worse than
    // that: `open_tag` is read here too, so a host that chunks would write the later chunks
    // under the inner field's tag. It did not corrupt this payload set only because
    // `ListTasksDetailedResponse.tasks` and `TaskOptions.options` are both tag 1.
    let saved = ((*cx).open_tag, (*cx).open_site, (*cx).open_kind, (*cx).open_vt, (*cx).open_obj);
    // R-D6: a host that already failed this operation and calls in anyway gets the
    // sticky code back and nothing is written.
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    let vt = (*cx).open_vt as *const ak_evt_TaskSummary;
    let obj = (*cx).open_obj;
    for i in 0..n as usize {
        let mk = (*cx).e.begin(tag, site);
        if !enc_task_summary_ugroup(&*elems.add(i), cx, vt, obj, tok0 + i as i64) {
            return crate::enc_status(cx);
        }
        (*cx).e.end(mk);
    }
    (*cx).open_tag = saved.0;
    (*cx).open_site = saved.1;
    (*cx).open_kind = saved.2;
    (*cx).open_vt = saved.3;
    (*cx).open_obj = saved.4;
    AK_OK
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_Timestamp(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_Timestamp,
    fix: *const ak_efix_Timestamp,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.seconds != 0 { (*cx).e.varint_field(1, g.seconds as u64); }
    if g.nanos != 0 { (*cx).e.varint_field(2, g.nanos as i64 as u64); }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_Timestamp(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_Timestamp,
    fix: *const ak_ufix_Timestamp,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.seconds != 0 { (*cx).e.varint_field(1, g.seconds as u64); }
    if g.nanos != 0 { (*cx).e.varint_field(2, g.nanos as i64 as u64); }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_Duration(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_Duration,
    fix: *const ak_efix_Duration,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.seconds != 0 { (*cx).e.varint_field(1, g.seconds as u64); }
    if g.nanos != 0 { (*cx).e.varint_field(2, g.nanos as i64 as u64); }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_Duration(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_Duration,
    fix: *const ak_ufix_Duration,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.seconds != 0 { (*cx).e.varint_field(1, g.seconds as u64); }
    if g.nanos != 0 { (*cx).e.varint_field(2, g.nanos as i64 as u64); }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_ResultRaw(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_ResultRaw,
    fix: *const ak_efix_ResultRaw,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.session_id.tc.is_some() && g.session_id.len != 0 {
        if !enc_blob(cx, 1, 6, &g.session_id) { return crate::enc_status(cx) as isize; }
    }
    if g.name.tc.is_some() && g.name.len != 0 {
        if !enc_blob(cx, 2, 7, &g.name) { return crate::enc_status(cx) as isize; }
    }
    if g.owner_task_id.tc.is_some() && g.owner_task_id.len != 0 {
        if !enc_blob(cx, 3, 8, &g.owner_task_id) { return crate::enc_status(cx) as isize; }
    }
    if g.status != 0 { (*cx).e.varint_field(4, g.status as i64 as u64); }
    if g.presence & AK_EFIX_RESULTRAW_PRESENT_CREATED_AT != 0 {
        let mk = (*cx).e.begin(5, 9);
        if g.created_at.seconds != 0 { (*cx).e.varint_field(1, g.created_at.seconds as u64); }
        if g.created_at.nanos != 0 { (*cx).e.varint_field(2, g.created_at.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_RESULTRAW_PRESENT_COMPLETED_AT != 0 {
        let mk = (*cx).e.begin(6, 10);
        if g.completed_at.seconds != 0 { (*cx).e.varint_field(1, g.completed_at.seconds as u64); }
        if g.completed_at.nanos != 0 { (*cx).e.varint_field(2, g.completed_at.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.result_id.tc.is_some() && g.result_id.len != 0 {
        if !enc_blob(cx, 8, 11, &g.result_id) { return crate::enc_status(cx) as isize; }
    }
    if g.size != 0 { (*cx).e.varint_field(9, g.size as u64); }
    if g.created_by.tc.is_some() && g.created_by.len != 0 {
        if !enc_blob(cx, 10, 12, &g.created_by) { return crate::enc_status(cx) as isize; }
    }
    if g.opaque_id.tc.is_some() && g.opaque_id.len != 0 {
        if !enc_blob(cx, 11, 13, &g.opaque_id) { return crate::enc_status(cx) as isize; }
    }
    if g.manual_deletion != 0 { (*cx).e.varint_field(12, (g.manual_deletion != 0) as u64); }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_ResultRaw(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_ResultRaw,
    fix: *const ak_ufix_ResultRaw,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.session_id.tc.is_some() && g.session_id.len != 0 {
        if !enc_blob(cx, 1, 6, &g.session_id) { return crate::enc_status(cx) as isize; }
    }
    if g.name.tc.is_some() && g.name.len != 0 {
        if !enc_blob(cx, 2, 7, &g.name) { return crate::enc_status(cx) as isize; }
    }
    if g.owner_task_id.tc.is_some() && g.owner_task_id.len != 0 {
        if !enc_blob(cx, 3, 8, &g.owner_task_id) { return crate::enc_status(cx) as isize; }
    }
    if g.status != 0 { (*cx).e.varint_field(4, g.status as i64 as u64); }
    if g.presence & AK_UFIX_RESULTRAW_PRESENT_CREATED_AT != 0 {
        let mk = (*cx).e.begin(5, 9);
        if g.created_at.seconds != 0 { (*cx).e.varint_field(1, g.created_at.seconds as u64); }
        if g.created_at.nanos != 0 { (*cx).e.varint_field(2, g.created_at.nanos as i64 as u64); }
        if !enc_raw(cx, &g.created_at.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_RESULTRAW_PRESENT_COMPLETED_AT != 0 {
        let mk = (*cx).e.begin(6, 10);
        if g.completed_at.seconds != 0 { (*cx).e.varint_field(1, g.completed_at.seconds as u64); }
        if g.completed_at.nanos != 0 { (*cx).e.varint_field(2, g.completed_at.nanos as i64 as u64); }
        if !enc_raw(cx, &g.completed_at.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if g.result_id.tc.is_some() && g.result_id.len != 0 {
        if !enc_blob(cx, 8, 11, &g.result_id) { return crate::enc_status(cx) as isize; }
    }
    if g.size != 0 { (*cx).e.varint_field(9, g.size as u64); }
    if g.created_by.tc.is_some() && g.created_by.len != 0 {
        if !enc_blob(cx, 10, 12, &g.created_by) { return crate::enc_status(cx) as isize; }
    }
    if g.opaque_id.tc.is_some() && g.opaque_id.len != 0 {
        if !enc_blob(cx, 11, 13, &g.opaque_id) { return crate::enc_status(cx) as isize; }
    }
    if g.manual_deletion != 0 { (*cx).e.varint_field(12, (g.manual_deletion != 0) as u64); }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_TaskOptions(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_TaskOptions,
    fix: *const ak_efix_TaskOptions,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_options {
        (*cx).open_tag = 1;
        (*cx).open_site = 14;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if g.presence & AK_EFIX_TASKOPTIONS_PRESENT_MAX_DURATION != 0 {
        let mk = (*cx).e.begin(2, 15);
        if g.max_duration.seconds != 0 { (*cx).e.varint_field(1, g.max_duration.seconds as u64); }
        if g.max_duration.nanos != 0 { (*cx).e.varint_field(2, g.max_duration.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.max_retries != 0 { (*cx).e.varint_field(3, g.max_retries as i64 as u64); }
    if g.priority != 0 { (*cx).e.varint_field(4, g.priority as i64 as u64); }
    if g.partition_id.tc.is_some() && g.partition_id.len != 0 {
        if !enc_blob(cx, 5, 16, &g.partition_id) { return crate::enc_status(cx) as isize; }
    }
    if g.application_name.tc.is_some() && g.application_name.len != 0 {
        if !enc_blob(cx, 6, 17, &g.application_name) { return crate::enc_status(cx) as isize; }
    }
    if g.application_version.tc.is_some() && g.application_version.len != 0 {
        if !enc_blob(cx, 7, 18, &g.application_version) { return crate::enc_status(cx) as isize; }
    }
    if g.application_namespace.tc.is_some() && g.application_namespace.len != 0 {
        if !enc_blob(cx, 8, 19, &g.application_namespace) { return crate::enc_status(cx) as isize; }
    }
    if g.application_service.tc.is_some() && g.application_service.len != 0 {
        if !enc_blob(cx, 9, 20, &g.application_service) { return crate::enc_status(cx) as isize; }
    }
    if g.engine_type.tc.is_some() && g.engine_type.len != 0 {
        if !enc_blob(cx, 10, 21, &g.engine_type) { return crate::enc_status(cx) as isize; }
    }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_TaskOptions(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_TaskOptions,
    fix: *const ak_ufix_TaskOptions,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_options {
        (*cx).open_tag = 1;
        (*cx).open_site = 14;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if g.presence & AK_UFIX_TASKOPTIONS_PRESENT_MAX_DURATION != 0 {
        let mk = (*cx).e.begin(2, 15);
        if g.max_duration.seconds != 0 { (*cx).e.varint_field(1, g.max_duration.seconds as u64); }
        if g.max_duration.nanos != 0 { (*cx).e.varint_field(2, g.max_duration.nanos as i64 as u64); }
        if !enc_raw(cx, &g.max_duration.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if g.max_retries != 0 { (*cx).e.varint_field(3, g.max_retries as i64 as u64); }
    if g.priority != 0 { (*cx).e.varint_field(4, g.priority as i64 as u64); }
    if g.partition_id.tc.is_some() && g.partition_id.len != 0 {
        if !enc_blob(cx, 5, 16, &g.partition_id) { return crate::enc_status(cx) as isize; }
    }
    if g.application_name.tc.is_some() && g.application_name.len != 0 {
        if !enc_blob(cx, 6, 17, &g.application_name) { return crate::enc_status(cx) as isize; }
    }
    if g.application_version.tc.is_some() && g.application_version.len != 0 {
        if !enc_blob(cx, 7, 18, &g.application_version) { return crate::enc_status(cx) as isize; }
    }
    if g.application_namespace.tc.is_some() && g.application_namespace.len != 0 {
        if !enc_blob(cx, 8, 19, &g.application_namespace) { return crate::enc_status(cx) as isize; }
    }
    if g.application_service.tc.is_some() && g.application_service.len != 0 {
        if !enc_blob(cx, 9, 20, &g.application_service) { return crate::enc_status(cx) as isize; }
    }
    if g.engine_type.tc.is_some() && g.engine_type.len != 0 {
        if !enc_blob(cx, 10, 21, &g.engine_type) { return crate::enc_status(cx) as isize; }
    }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_TaskOutput(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_TaskOutput,
    fix: *const ak_efix_TaskOutput,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.success != 0 { (*cx).e.varint_field(1, (g.success != 0) as u64); }
    if g.error.tc.is_some() && g.error.len != 0 {
        if !enc_blob(cx, 2, 22, &g.error) { return crate::enc_status(cx) as isize; }
    }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_TaskOutput(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_TaskOutput,
    fix: *const ak_ufix_TaskOutput,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.success != 0 { (*cx).e.varint_field(1, (g.success != 0) as u64); }
    if g.error.tc.is_some() && g.error.len != 0 {
        if !enc_blob(cx, 2, 22, &g.error) { return crate::enc_status(cx) as isize; }
    }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_TaskDetailed(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_TaskDetailed,
    fix: *const ak_efix_TaskDetailed,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 1, 23, &g.id) { return crate::enc_status(cx) as isize; }
    }
    if g.session_id.tc.is_some() && g.session_id.len != 0 {
        if !enc_blob(cx, 2, 24, &g.session_id) { return crate::enc_status(cx) as isize; }
    }
    if g.owner_pod_id.tc.is_some() && g.owner_pod_id.len != 0 {
        if !enc_blob(cx, 3, 25, &g.owner_pod_id) { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_parent_task_ids {
        (*cx).open_tag = 4;
        (*cx).open_site = 26;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_data_dependencies {
        (*cx).open_tag = 5;
        (*cx).open_site = 27;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_expected_output_ids {
        (*cx).open_tag = 6;
        (*cx).open_site = 28;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_retry_of_ids {
        (*cx).open_tag = 7;
        (*cx).open_site = 29;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if g.status != 0 { (*cx).e.varint_field(8, g.status as i64 as u64); }
    if g.status_message.tc.is_some() && g.status_message.len != 0 {
        if !enc_blob(cx, 9, 30, &g.status_message) { return crate::enc_status(cx) as isize; }
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_OPTIONS != 0 {
        let mk = (*cx).e.begin(10, 31);
        if let Some(lp) = (*vt).loop_options_options {
            (*cx).open_tag = 1;
            (*cx).open_site = 32;
            (*cx).open_kind = 0;
            ak_rt::bump!((*cx).e.c, reverse);
            let rc = lp(cx as *mut ak_enc_ctx, obj, token);
            if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
            if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
        }
        if g.options.presence & AK_EFIX_TASKOPTIONS_PRESENT_MAX_DURATION != 0 {
            let mk = (*cx).e.begin(2, 33);
            if g.options.max_duration.seconds != 0 { (*cx).e.varint_field(1, g.options.max_duration.seconds as u64); }
            if g.options.max_duration.nanos != 0 { (*cx).e.varint_field(2, g.options.max_duration.nanos as i64 as u64); }
            (*cx).e.end(mk);
        }
        if g.options.max_retries != 0 { (*cx).e.varint_field(3, g.options.max_retries as i64 as u64); }
        if g.options.priority != 0 { (*cx).e.varint_field(4, g.options.priority as i64 as u64); }
        if g.options.partition_id.tc.is_some() && g.options.partition_id.len != 0 {
            if !enc_blob(cx, 5, 34, &g.options.partition_id) { return crate::enc_status(cx) as isize; }
        }
        if g.options.application_name.tc.is_some() && g.options.application_name.len != 0 {
            if !enc_blob(cx, 6, 35, &g.options.application_name) { return crate::enc_status(cx) as isize; }
        }
        if g.options.application_version.tc.is_some() && g.options.application_version.len != 0 {
            if !enc_blob(cx, 7, 36, &g.options.application_version) { return crate::enc_status(cx) as isize; }
        }
        if g.options.application_namespace.tc.is_some() && g.options.application_namespace.len != 0 {
            if !enc_blob(cx, 8, 37, &g.options.application_namespace) { return crate::enc_status(cx) as isize; }
        }
        if g.options.application_service.tc.is_some() && g.options.application_service.len != 0 {
            if !enc_blob(cx, 9, 38, &g.options.application_service) { return crate::enc_status(cx) as isize; }
        }
        if g.options.engine_type.tc.is_some() && g.options.engine_type.len != 0 {
            if !enc_blob(cx, 10, 39, &g.options.engine_type) { return crate::enc_status(cx) as isize; }
        }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_CREATED_AT != 0 {
        let mk = (*cx).e.begin(11, 40);
        if g.created_at.seconds != 0 { (*cx).e.varint_field(1, g.created_at.seconds as u64); }
        if g.created_at.nanos != 0 { (*cx).e.varint_field(2, g.created_at.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_SUBMITTED_AT != 0 {
        let mk = (*cx).e.begin(12, 41);
        if g.submitted_at.seconds != 0 { (*cx).e.varint_field(1, g.submitted_at.seconds as u64); }
        if g.submitted_at.nanos != 0 { (*cx).e.varint_field(2, g.submitted_at.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_STARTED_AT != 0 {
        let mk = (*cx).e.begin(13, 42);
        if g.started_at.seconds != 0 { (*cx).e.varint_field(1, g.started_at.seconds as u64); }
        if g.started_at.nanos != 0 { (*cx).e.varint_field(2, g.started_at.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_ENDED_AT != 0 {
        let mk = (*cx).e.begin(14, 43);
        if g.ended_at.seconds != 0 { (*cx).e.varint_field(1, g.ended_at.seconds as u64); }
        if g.ended_at.nanos != 0 { (*cx).e.varint_field(2, g.ended_at.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_POD_TTL != 0 {
        let mk = (*cx).e.begin(15, 44);
        if g.pod_ttl.seconds != 0 { (*cx).e.varint_field(1, g.pod_ttl.seconds as u64); }
        if g.pod_ttl.nanos != 0 { (*cx).e.varint_field(2, g.pod_ttl.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_OUTPUT != 0 {
        let mk = (*cx).e.begin(16, 45);
        if g.output.success != 0 { (*cx).e.varint_field(1, (g.output.success != 0) as u64); }
        if g.output.error.tc.is_some() && g.output.error.len != 0 {
            if !enc_blob(cx, 2, 46, &g.output.error) { return crate::enc_status(cx) as isize; }
        }
        (*cx).e.end(mk);
    }
    if g.pod_hostname.tc.is_some() && g.pod_hostname.len != 0 {
        if !enc_blob(cx, 17, 47, &g.pod_hostname) { return crate::enc_status(cx) as isize; }
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_RECEIVED_AT != 0 {
        let mk = (*cx).e.begin(18, 48);
        if g.received_at.seconds != 0 { (*cx).e.varint_field(1, g.received_at.seconds as u64); }
        if g.received_at.nanos != 0 { (*cx).e.varint_field(2, g.received_at.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_ACQUIRED_AT != 0 {
        let mk = (*cx).e.begin(19, 49);
        if g.acquired_at.seconds != 0 { (*cx).e.varint_field(1, g.acquired_at.seconds as u64); }
        if g.acquired_at.nanos != 0 { (*cx).e.varint_field(2, g.acquired_at.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_CREATION_TO_END_DURATION != 0 {
        let mk = (*cx).e.begin(20, 50);
        if g.creation_to_end_duration.seconds != 0 { (*cx).e.varint_field(1, g.creation_to_end_duration.seconds as u64); }
        if g.creation_to_end_duration.nanos != 0 { (*cx).e.varint_field(2, g.creation_to_end_duration.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_PROCESSING_TO_END_DURATION != 0 {
        let mk = (*cx).e.begin(21, 51);
        if g.processing_to_end_duration.seconds != 0 { (*cx).e.varint_field(1, g.processing_to_end_duration.seconds as u64); }
        if g.processing_to_end_duration.nanos != 0 { (*cx).e.varint_field(2, g.processing_to_end_duration.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.initial_task_id.tc.is_some() && g.initial_task_id.len != 0 {
        if !enc_blob(cx, 22, 52, &g.initial_task_id) { return crate::enc_status(cx) as isize; }
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_RECEIVED_TO_END_DURATION != 0 {
        let mk = (*cx).e.begin(23, 53);
        if g.received_to_end_duration.seconds != 0 { (*cx).e.varint_field(1, g.received_to_end_duration.seconds as u64); }
        if g.received_to_end_duration.nanos != 0 { (*cx).e.varint_field(2, g.received_to_end_duration.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_PROCESSED_AT != 0 {
        let mk = (*cx).e.begin(24, 54);
        if g.processed_at.seconds != 0 { (*cx).e.varint_field(1, g.processed_at.seconds as u64); }
        if g.processed_at.nanos != 0 { (*cx).e.varint_field(2, g.processed_at.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.presence & AK_EFIX_TASKDETAILED_PRESENT_FETCHED_AT != 0 {
        let mk = (*cx).e.begin(25, 55);
        if g.fetched_at.seconds != 0 { (*cx).e.varint_field(1, g.fetched_at.seconds as u64); }
        if g.fetched_at.nanos != 0 { (*cx).e.varint_field(2, g.fetched_at.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.payload_id.tc.is_some() && g.payload_id.len != 0 {
        if !enc_blob(cx, 26, 56, &g.payload_id) { return crate::enc_status(cx) as isize; }
    }
    if g.created_by.tc.is_some() && g.created_by.len != 0 {
        if !enc_blob(cx, 27, 57, &g.created_by) { return crate::enc_status(cx) as isize; }
    }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_TaskDetailed(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_TaskDetailed,
    fix: *const ak_ufix_TaskDetailed,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 1, 23, &g.id) { return crate::enc_status(cx) as isize; }
    }
    if g.session_id.tc.is_some() && g.session_id.len != 0 {
        if !enc_blob(cx, 2, 24, &g.session_id) { return crate::enc_status(cx) as isize; }
    }
    if g.owner_pod_id.tc.is_some() && g.owner_pod_id.len != 0 {
        if !enc_blob(cx, 3, 25, &g.owner_pod_id) { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_parent_task_ids {
        (*cx).open_tag = 4;
        (*cx).open_site = 26;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_data_dependencies {
        (*cx).open_tag = 5;
        (*cx).open_site = 27;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_expected_output_ids {
        (*cx).open_tag = 6;
        (*cx).open_site = 28;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_retry_of_ids {
        (*cx).open_tag = 7;
        (*cx).open_site = 29;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if g.status != 0 { (*cx).e.varint_field(8, g.status as i64 as u64); }
    if g.status_message.tc.is_some() && g.status_message.len != 0 {
        if !enc_blob(cx, 9, 30, &g.status_message) { return crate::enc_status(cx) as isize; }
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_OPTIONS != 0 {
        let mk = (*cx).e.begin(10, 31);
        if let Some(lp) = (*vt).loop_options_options {
            (*cx).open_tag = 1;
            (*cx).open_site = 32;
            (*cx).open_kind = 0;
            ak_rt::bump!((*cx).e.c, reverse);
            let rc = lp(cx as *mut ak_enc_ctx, obj, token);
            if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
            if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
        }
        if g.options.presence & AK_UFIX_TASKOPTIONS_PRESENT_MAX_DURATION != 0 {
            let mk = (*cx).e.begin(2, 33);
            if g.options.max_duration.seconds != 0 { (*cx).e.varint_field(1, g.options.max_duration.seconds as u64); }
            if g.options.max_duration.nanos != 0 { (*cx).e.varint_field(2, g.options.max_duration.nanos as i64 as u64); }
            if !enc_raw(cx, &g.options.max_duration.unknown) { return crate::enc_status(cx) as isize; }
            (*cx).e.end(mk);
        }
        if g.options.max_retries != 0 { (*cx).e.varint_field(3, g.options.max_retries as i64 as u64); }
        if g.options.priority != 0 { (*cx).e.varint_field(4, g.options.priority as i64 as u64); }
        if g.options.partition_id.tc.is_some() && g.options.partition_id.len != 0 {
            if !enc_blob(cx, 5, 34, &g.options.partition_id) { return crate::enc_status(cx) as isize; }
        }
        if g.options.application_name.tc.is_some() && g.options.application_name.len != 0 {
            if !enc_blob(cx, 6, 35, &g.options.application_name) { return crate::enc_status(cx) as isize; }
        }
        if g.options.application_version.tc.is_some() && g.options.application_version.len != 0 {
            if !enc_blob(cx, 7, 36, &g.options.application_version) { return crate::enc_status(cx) as isize; }
        }
        if g.options.application_namespace.tc.is_some() && g.options.application_namespace.len != 0 {
            if !enc_blob(cx, 8, 37, &g.options.application_namespace) { return crate::enc_status(cx) as isize; }
        }
        if g.options.application_service.tc.is_some() && g.options.application_service.len != 0 {
            if !enc_blob(cx, 9, 38, &g.options.application_service) { return crate::enc_status(cx) as isize; }
        }
        if g.options.engine_type.tc.is_some() && g.options.engine_type.len != 0 {
            if !enc_blob(cx, 10, 39, &g.options.engine_type) { return crate::enc_status(cx) as isize; }
        }
        if !enc_raw(cx, &g.options.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_CREATED_AT != 0 {
        let mk = (*cx).e.begin(11, 40);
        if g.created_at.seconds != 0 { (*cx).e.varint_field(1, g.created_at.seconds as u64); }
        if g.created_at.nanos != 0 { (*cx).e.varint_field(2, g.created_at.nanos as i64 as u64); }
        if !enc_raw(cx, &g.created_at.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_SUBMITTED_AT != 0 {
        let mk = (*cx).e.begin(12, 41);
        if g.submitted_at.seconds != 0 { (*cx).e.varint_field(1, g.submitted_at.seconds as u64); }
        if g.submitted_at.nanos != 0 { (*cx).e.varint_field(2, g.submitted_at.nanos as i64 as u64); }
        if !enc_raw(cx, &g.submitted_at.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_STARTED_AT != 0 {
        let mk = (*cx).e.begin(13, 42);
        if g.started_at.seconds != 0 { (*cx).e.varint_field(1, g.started_at.seconds as u64); }
        if g.started_at.nanos != 0 { (*cx).e.varint_field(2, g.started_at.nanos as i64 as u64); }
        if !enc_raw(cx, &g.started_at.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_ENDED_AT != 0 {
        let mk = (*cx).e.begin(14, 43);
        if g.ended_at.seconds != 0 { (*cx).e.varint_field(1, g.ended_at.seconds as u64); }
        if g.ended_at.nanos != 0 { (*cx).e.varint_field(2, g.ended_at.nanos as i64 as u64); }
        if !enc_raw(cx, &g.ended_at.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_POD_TTL != 0 {
        let mk = (*cx).e.begin(15, 44);
        if g.pod_ttl.seconds != 0 { (*cx).e.varint_field(1, g.pod_ttl.seconds as u64); }
        if g.pod_ttl.nanos != 0 { (*cx).e.varint_field(2, g.pod_ttl.nanos as i64 as u64); }
        if !enc_raw(cx, &g.pod_ttl.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_OUTPUT != 0 {
        let mk = (*cx).e.begin(16, 45);
        if g.output.success != 0 { (*cx).e.varint_field(1, (g.output.success != 0) as u64); }
        if g.output.error.tc.is_some() && g.output.error.len != 0 {
            if !enc_blob(cx, 2, 46, &g.output.error) { return crate::enc_status(cx) as isize; }
        }
        if !enc_raw(cx, &g.output.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if g.pod_hostname.tc.is_some() && g.pod_hostname.len != 0 {
        if !enc_blob(cx, 17, 47, &g.pod_hostname) { return crate::enc_status(cx) as isize; }
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_RECEIVED_AT != 0 {
        let mk = (*cx).e.begin(18, 48);
        if g.received_at.seconds != 0 { (*cx).e.varint_field(1, g.received_at.seconds as u64); }
        if g.received_at.nanos != 0 { (*cx).e.varint_field(2, g.received_at.nanos as i64 as u64); }
        if !enc_raw(cx, &g.received_at.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_ACQUIRED_AT != 0 {
        let mk = (*cx).e.begin(19, 49);
        if g.acquired_at.seconds != 0 { (*cx).e.varint_field(1, g.acquired_at.seconds as u64); }
        if g.acquired_at.nanos != 0 { (*cx).e.varint_field(2, g.acquired_at.nanos as i64 as u64); }
        if !enc_raw(cx, &g.acquired_at.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_CREATION_TO_END_DURATION != 0 {
        let mk = (*cx).e.begin(20, 50);
        if g.creation_to_end_duration.seconds != 0 { (*cx).e.varint_field(1, g.creation_to_end_duration.seconds as u64); }
        if g.creation_to_end_duration.nanos != 0 { (*cx).e.varint_field(2, g.creation_to_end_duration.nanos as i64 as u64); }
        if !enc_raw(cx, &g.creation_to_end_duration.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_PROCESSING_TO_END_DURATION != 0 {
        let mk = (*cx).e.begin(21, 51);
        if g.processing_to_end_duration.seconds != 0 { (*cx).e.varint_field(1, g.processing_to_end_duration.seconds as u64); }
        if g.processing_to_end_duration.nanos != 0 { (*cx).e.varint_field(2, g.processing_to_end_duration.nanos as i64 as u64); }
        if !enc_raw(cx, &g.processing_to_end_duration.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if g.initial_task_id.tc.is_some() && g.initial_task_id.len != 0 {
        if !enc_blob(cx, 22, 52, &g.initial_task_id) { return crate::enc_status(cx) as isize; }
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_RECEIVED_TO_END_DURATION != 0 {
        let mk = (*cx).e.begin(23, 53);
        if g.received_to_end_duration.seconds != 0 { (*cx).e.varint_field(1, g.received_to_end_duration.seconds as u64); }
        if g.received_to_end_duration.nanos != 0 { (*cx).e.varint_field(2, g.received_to_end_duration.nanos as i64 as u64); }
        if !enc_raw(cx, &g.received_to_end_duration.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_PROCESSED_AT != 0 {
        let mk = (*cx).e.begin(24, 54);
        if g.processed_at.seconds != 0 { (*cx).e.varint_field(1, g.processed_at.seconds as u64); }
        if g.processed_at.nanos != 0 { (*cx).e.varint_field(2, g.processed_at.nanos as i64 as u64); }
        if !enc_raw(cx, &g.processed_at.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if g.presence & AK_UFIX_TASKDETAILED_PRESENT_FETCHED_AT != 0 {
        let mk = (*cx).e.begin(25, 55);
        if g.fetched_at.seconds != 0 { (*cx).e.varint_field(1, g.fetched_at.seconds as u64); }
        if g.fetched_at.nanos != 0 { (*cx).e.varint_field(2, g.fetched_at.nanos as i64 as u64); }
        if !enc_raw(cx, &g.fetched_at.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if g.payload_id.tc.is_some() && g.payload_id.len != 0 {
        if !enc_blob(cx, 26, 56, &g.payload_id) { return crate::enc_status(cx) as isize; }
    }
    if g.created_by.tc.is_some() && g.created_by.len != 0 {
        if !enc_blob(cx, 27, 57, &g.created_by) { return crate::enc_status(cx) as isize; }
    }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_TaskSummary(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_TaskSummary,
    fix: *const ak_efix_TaskSummary,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 1, 58, &g.id) { return crate::enc_status(cx) as isize; }
    }
    if g.session_id.tc.is_some() && g.session_id.len != 0 {
        if !enc_blob(cx, 2, 59, &g.session_id) { return crate::enc_status(cx) as isize; }
    }
    if g.presence & AK_EFIX_TASKSUMMARY_PRESENT_OPTIONS != 0 {
        let mk = (*cx).e.begin(3, 60);
        if let Some(lp) = (*vt).loop_options_options {
            (*cx).open_tag = 1;
            (*cx).open_site = 61;
            (*cx).open_kind = 0;
            ak_rt::bump!((*cx).e.c, reverse);
            let rc = lp(cx as *mut ak_enc_ctx, obj, token);
            if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
            if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
        }
        if g.options.presence & AK_EFIX_TASKOPTIONS_PRESENT_MAX_DURATION != 0 {
            let mk = (*cx).e.begin(2, 62);
            if g.options.max_duration.seconds != 0 { (*cx).e.varint_field(1, g.options.max_duration.seconds as u64); }
            if g.options.max_duration.nanos != 0 { (*cx).e.varint_field(2, g.options.max_duration.nanos as i64 as u64); }
            (*cx).e.end(mk);
        }
        if g.options.max_retries != 0 { (*cx).e.varint_field(3, g.options.max_retries as i64 as u64); }
        if g.options.priority != 0 { (*cx).e.varint_field(4, g.options.priority as i64 as u64); }
        if g.options.partition_id.tc.is_some() && g.options.partition_id.len != 0 {
            if !enc_blob(cx, 5, 63, &g.options.partition_id) { return crate::enc_status(cx) as isize; }
        }
        if g.options.application_name.tc.is_some() && g.options.application_name.len != 0 {
            if !enc_blob(cx, 6, 64, &g.options.application_name) { return crate::enc_status(cx) as isize; }
        }
        if g.options.application_version.tc.is_some() && g.options.application_version.len != 0 {
            if !enc_blob(cx, 7, 65, &g.options.application_version) { return crate::enc_status(cx) as isize; }
        }
        if g.options.application_namespace.tc.is_some() && g.options.application_namespace.len != 0 {
            if !enc_blob(cx, 8, 66, &g.options.application_namespace) { return crate::enc_status(cx) as isize; }
        }
        if g.options.application_service.tc.is_some() && g.options.application_service.len != 0 {
            if !enc_blob(cx, 9, 67, &g.options.application_service) { return crate::enc_status(cx) as isize; }
        }
        if g.options.engine_type.tc.is_some() && g.options.engine_type.len != 0 {
            if !enc_blob(cx, 10, 68, &g.options.engine_type) { return crate::enc_status(cx) as isize; }
        }
        (*cx).e.end(mk);
    }
    if g.status != 0 { (*cx).e.varint_field(4, g.status as i64 as u64); }
    if g.presence & AK_EFIX_TASKSUMMARY_PRESENT_CREATED_AT != 0 {
        let mk = (*cx).e.begin(5, 69);
        if g.created_at.seconds != 0 { (*cx).e.varint_field(1, g.created_at.seconds as u64); }
        if g.created_at.nanos != 0 { (*cx).e.varint_field(2, g.created_at.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.error.tc.is_some() && g.error.len != 0 {
        if !enc_blob(cx, 8, 70, &g.error) { return crate::enc_status(cx) as isize; }
    }
    if g.status_message.tc.is_some() && g.status_message.len != 0 {
        if !enc_blob(cx, 9, 71, &g.status_message) { return crate::enc_status(cx) as isize; }
    }
    if g.count_data_dependencies != 0 { (*cx).e.varint_field(11, g.count_data_dependencies as u64); }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_TaskSummary(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_TaskSummary,
    fix: *const ak_ufix_TaskSummary,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 1, 58, &g.id) { return crate::enc_status(cx) as isize; }
    }
    if g.session_id.tc.is_some() && g.session_id.len != 0 {
        if !enc_blob(cx, 2, 59, &g.session_id) { return crate::enc_status(cx) as isize; }
    }
    if g.presence & AK_UFIX_TASKSUMMARY_PRESENT_OPTIONS != 0 {
        let mk = (*cx).e.begin(3, 60);
        if let Some(lp) = (*vt).loop_options_options {
            (*cx).open_tag = 1;
            (*cx).open_site = 61;
            (*cx).open_kind = 0;
            ak_rt::bump!((*cx).e.c, reverse);
            let rc = lp(cx as *mut ak_enc_ctx, obj, token);
            if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
            if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
        }
        if g.options.presence & AK_UFIX_TASKOPTIONS_PRESENT_MAX_DURATION != 0 {
            let mk = (*cx).e.begin(2, 62);
            if g.options.max_duration.seconds != 0 { (*cx).e.varint_field(1, g.options.max_duration.seconds as u64); }
            if g.options.max_duration.nanos != 0 { (*cx).e.varint_field(2, g.options.max_duration.nanos as i64 as u64); }
            if !enc_raw(cx, &g.options.max_duration.unknown) { return crate::enc_status(cx) as isize; }
            (*cx).e.end(mk);
        }
        if g.options.max_retries != 0 { (*cx).e.varint_field(3, g.options.max_retries as i64 as u64); }
        if g.options.priority != 0 { (*cx).e.varint_field(4, g.options.priority as i64 as u64); }
        if g.options.partition_id.tc.is_some() && g.options.partition_id.len != 0 {
            if !enc_blob(cx, 5, 63, &g.options.partition_id) { return crate::enc_status(cx) as isize; }
        }
        if g.options.application_name.tc.is_some() && g.options.application_name.len != 0 {
            if !enc_blob(cx, 6, 64, &g.options.application_name) { return crate::enc_status(cx) as isize; }
        }
        if g.options.application_version.tc.is_some() && g.options.application_version.len != 0 {
            if !enc_blob(cx, 7, 65, &g.options.application_version) { return crate::enc_status(cx) as isize; }
        }
        if g.options.application_namespace.tc.is_some() && g.options.application_namespace.len != 0 {
            if !enc_blob(cx, 8, 66, &g.options.application_namespace) { return crate::enc_status(cx) as isize; }
        }
        if g.options.application_service.tc.is_some() && g.options.application_service.len != 0 {
            if !enc_blob(cx, 9, 67, &g.options.application_service) { return crate::enc_status(cx) as isize; }
        }
        if g.options.engine_type.tc.is_some() && g.options.engine_type.len != 0 {
            if !enc_blob(cx, 10, 68, &g.options.engine_type) { return crate::enc_status(cx) as isize; }
        }
        if !enc_raw(cx, &g.options.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if g.status != 0 { (*cx).e.varint_field(4, g.status as i64 as u64); }
    if g.presence & AK_UFIX_TASKSUMMARY_PRESENT_CREATED_AT != 0 {
        let mk = (*cx).e.begin(5, 69);
        if g.created_at.seconds != 0 { (*cx).e.varint_field(1, g.created_at.seconds as u64); }
        if g.created_at.nanos != 0 { (*cx).e.varint_field(2, g.created_at.nanos as i64 as u64); }
        if !enc_raw(cx, &g.created_at.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if g.error.tc.is_some() && g.error.len != 0 {
        if !enc_blob(cx, 8, 70, &g.error) { return crate::enc_status(cx) as isize; }
    }
    if g.status_message.tc.is_some() && g.status_message.len != 0 {
        if !enc_blob(cx, 9, 71, &g.status_message) { return crate::enc_status(cx) as isize; }
    }
    if g.count_data_dependencies != 0 { (*cx).e.varint_field(11, g.count_data_dependencies as u64); }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_Probe(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_Probe,
    fix: *const ak_efix_Probe,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    // A case this build does not know -- a host generated against a newer
    // descriptor -- is refused before anything is written (plan rule).
    match g.body_case {
        0 | 10 | 11 | 12 | 13 | 14 => {}
        _ => { (*cx).e.fail(AK_ERR_ABI); return crate::enc_status(cx) as isize; }
    }
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 1, 72, &g.id) { return crate::enc_status(cx) as isize; }
    }
    if g.presence & AK_EFIX_PROBE_PRESENT_OPT_COUNT != 0 { (*cx).e.varint_field(2, g.opt_count as i64 as u64); }
    if g.presence & AK_EFIX_PROBE_PRESENT_OPT_LABEL != 0 {
        if !enc_blob(cx, 3, 73, &g.opt_label) { return crate::enc_status(cx) as isize; }
    }
    if g.presence & AK_EFIX_PROBE_PRESENT_OPT_FLAG != 0 { (*cx).e.varint_field(4, (g.opt_flag != 0) as u64); }
    if g.body_case == 10 {
        (*cx).e.varint_field(10, g.body_as_int as u64);
    }
    if g.body_case == 11 {
        if !enc_blob(cx, 11, 75, &g.body_as_text) { return crate::enc_status(cx) as isize; }
    }
    if g.body_case == 12 {
        if !enc_blob(cx, 12, 76, &g.body_as_blob) { return crate::enc_status(cx) as isize; }
    }
    if g.body_case == 13 {
        let mk = (*cx).e.begin(13, 74);
        if !enc_timestamp_group(&g.body_as_stamp, cx) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if g.body_case == 14 {
        let mk = (*cx).e.begin(14, 74);
        if !enc_empty_group(&g.body_as_nothing, cx) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_Probe(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_Probe,
    fix: *const ak_ufix_Probe,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    // A case this build does not know -- a host generated against a newer
    // descriptor -- is refused before anything is written (plan rule).
    match g.body_case {
        0 | 10 | 11 | 12 | 13 | 14 => {}
        _ => { (*cx).e.fail(AK_ERR_ABI); return crate::enc_status(cx) as isize; }
    }
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 1, 72, &g.id) { return crate::enc_status(cx) as isize; }
    }
    if g.presence & AK_UFIX_PROBE_PRESENT_OPT_COUNT != 0 { (*cx).e.varint_field(2, g.opt_count as i64 as u64); }
    if g.presence & AK_UFIX_PROBE_PRESENT_OPT_LABEL != 0 {
        if !enc_blob(cx, 3, 73, &g.opt_label) { return crate::enc_status(cx) as isize; }
    }
    if g.presence & AK_UFIX_PROBE_PRESENT_OPT_FLAG != 0 { (*cx).e.varint_field(4, (g.opt_flag != 0) as u64); }
    if g.body_case == 10 {
        (*cx).e.varint_field(10, g.body_as_int as u64);
    }
    if g.body_case == 11 {
        if !enc_blob(cx, 11, 75, &g.body_as_text) { return crate::enc_status(cx) as isize; }
    }
    if g.body_case == 12 {
        if !enc_blob(cx, 12, 76, &g.body_as_blob) { return crate::enc_status(cx) as isize; }
    }
    if g.body_case == 13 {
        let mk = (*cx).e.begin(13, 74);
        if !enc_timestamp_ugroup(&g.body_as_stamp, cx) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if g.body_case == 14 {
        let mk = (*cx).e.begin(14, 74);
        if !enc_empty_ugroup(&g.body_as_nothing, cx) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_Empty(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_Empty,
    fix: *const ak_efix_Empty,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_Empty(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_Empty,
    fix: *const ak_ufix_Empty,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_UploadResultData(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_UploadResultData,
    fix: *const ak_efix_UploadResultData,
    direct: *const u8,
    direct_len: usize,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    (*cx).direct = direct;
    (*cx).direct_len = direct_len;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.session_id.tc.is_some() && g.session_id.len != 0 {
        if !enc_blob(cx, 1, 77, &g.session_id) { return crate::enc_status(cx) as isize; }
    }
    if g.result_id.tc.is_some() && g.result_id.len != 0 {
        if !enc_blob(cx, 2, 78, &g.result_id) { return crate::enc_status(cx) as isize; }
    }
    if g.data_chunk.len != 0 {
        if !enc_blob(cx, 3, 79, &g.data_chunk) { return crate::enc_status(cx) as isize; }
    }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_UploadResultData(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_UploadResultData,
    fix: *const ak_ufix_UploadResultData,
    direct: *const u8,
    direct_len: usize,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    (*cx).direct = direct;
    (*cx).direct_len = direct_len;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.session_id.tc.is_some() && g.session_id.len != 0 {
        if !enc_blob(cx, 1, 77, &g.session_id) { return crate::enc_status(cx) as isize; }
    }
    if g.result_id.tc.is_some() && g.result_id.len != 0 {
        if !enc_blob(cx, 2, 78, &g.result_id) { return crate::enc_status(cx) as isize; }
    }
    if g.data_chunk.len != 0 {
        if !enc_blob(cx, 3, 79, &g.data_chunk) { return crate::enc_status(cx) as isize; }
    }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_MetricsBatch(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_MetricsBatch,
    fix: *const ak_efix_MetricsBatch,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 1, 80, &g.id) { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_ticks {
        (*cx).open_tag = 2;
        (*cx).open_site = 81;
        (*cx).open_kind = 2;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_values {
        (*cx).open_tag = 3;
        (*cx).open_site = 82;
        (*cx).open_kind = 4;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_codes {
        (*cx).open_tag = 4;
        (*cx).open_site = 83;
        (*cx).open_kind = 1;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_flags {
        (*cx).open_tag = 5;
        (*cx).open_site = 84;
        (*cx).open_kind = 3;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_statuses {
        (*cx).open_tag = 6;
        (*cx).open_site = 85;
        (*cx).open_kind = 5;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_MetricsBatch(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_MetricsBatch,
    fix: *const ak_ufix_MetricsBatch,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 1, 80, &g.id) { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_ticks {
        (*cx).open_tag = 2;
        (*cx).open_site = 81;
        (*cx).open_kind = 2;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_values {
        (*cx).open_tag = 3;
        (*cx).open_site = 82;
        (*cx).open_kind = 4;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_codes {
        (*cx).open_tag = 4;
        (*cx).open_site = 83;
        (*cx).open_kind = 1;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_flags {
        (*cx).open_tag = 5;
        (*cx).open_site = 84;
        (*cx).open_kind = 3;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_statuses {
        (*cx).open_tag = 6;
        (*cx).open_site = 85;
        (*cx).open_kind = 5;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_Pair(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_Pair,
    fix: *const ak_efix_Pair,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.key.tc.is_some() && g.key.len != 0 {
        if !enc_blob(cx, 1, 86, &g.key) { return crate::enc_status(cx) as isize; }
    }
    if g.value != 0 { (*cx).e.varint_field(2, g.value as i64 as u64); }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_Pair(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_Pair,
    fix: *const ak_ufix_Pair,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.key.tc.is_some() && g.key.len != 0 {
        if !enc_blob(cx, 1, 86, &g.key) { return crate::enc_status(cx) as isize; }
    }
    if g.value != 0 { (*cx).e.varint_field(2, g.value as i64 as u64); }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_ListResultsResponse(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_ListResultsResponse,
    fix: *const ak_efix_ListResultsResponse,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_results {
        (*cx).open_tag = 1;
        (*cx).open_site = 87;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if g.page != 0 { (*cx).e.varint_field(2, g.page as i64 as u64); }
    if g.total != 0 { (*cx).e.varint_field(3, g.total as i64 as u64); }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_ListResultsResponse(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_ListResultsResponse,
    fix: *const ak_ufix_ListResultsResponse,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_results {
        (*cx).open_tag = 1;
        (*cx).open_site = 87;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if g.page != 0 { (*cx).e.varint_field(2, g.page as i64 as u64); }
    if g.total != 0 { (*cx).e.varint_field(3, g.total as i64 as u64); }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_ListTasksDetailedResponse(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_ListTasksDetailedResponse,
    fix: *const ak_efix_ListTasksDetailedResponse,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_tasks {
        (*cx).open_tag = 1;
        (*cx).open_site = 88;
        (*cx).open_kind = 0;
        (*cx).open_vt = (*vt).elem_tasks as *const c_void;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if g.page != 0 { (*cx).e.varint_field(2, g.page as i64 as u64); }
    if g.total != 0 { (*cx).e.varint_field(3, g.total as i64 as u64); }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_ListTasksDetailedResponse(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_ListTasksDetailedResponse,
    fix: *const ak_ufix_ListTasksDetailedResponse,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_tasks {
        (*cx).open_tag = 1;
        (*cx).open_site = 88;
        (*cx).open_kind = 0;
        (*cx).open_vt = (*vt).elem_tasks as *const c_void;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if g.page != 0 { (*cx).e.varint_field(2, g.page as i64 as u64); }
    if g.total != 0 { (*cx).e.varint_field(3, g.total as i64 as u64); }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_ListTaskSummaryResponse(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_ListTaskSummaryResponse,
    fix: *const ak_efix_ListTaskSummaryResponse,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_tasks {
        (*cx).open_tag = 1;
        (*cx).open_site = 89;
        (*cx).open_kind = 0;
        (*cx).open_vt = (*vt).elem_tasks as *const c_void;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_ListTaskSummaryResponse(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_ListTaskSummaryResponse,
    fix: *const ak_ufix_ListTaskSummaryResponse,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_tasks {
        (*cx).open_tag = 1;
        (*cx).open_site = 89;
        (*cx).open_kind = 0;
        (*cx).open_vt = (*vt).elem_tasks as *const c_void;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_ListProbeResponse(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_ListProbeResponse,
    fix: *const ak_efix_ListProbeResponse,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_probes {
        (*cx).open_tag = 1;
        (*cx).open_site = 90;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_ListProbeResponse(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_ListProbeResponse,
    fix: *const ak_ufix_ListProbeResponse,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_probes {
        (*cx).open_tag = 1;
        (*cx).open_site = 90;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_ListMetricsResponse(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_ListMetricsResponse,
    fix: *const ak_efix_ListMetricsResponse,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_batches {
        (*cx).open_tag = 1;
        (*cx).open_site = 91;
        (*cx).open_kind = 0;
        (*cx).open_vt = (*vt).elem_batches as *const c_void;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_ListMetricsResponse(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_ListMetricsResponse,
    fix: *const ak_ufix_ListMetricsResponse,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_batches {
        (*cx).open_tag = 1;
        (*cx).open_site = 91;
        (*cx).open_kind = 0;
        (*cx).open_vt = (*vt).elem_batches as *const c_void;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_UploadResultDataMessage(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_UploadResultDataMessage,
    fix: *const ak_efix_UploadResultDataMessage,
    direct: *const u8,
    direct_len: usize,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    (*cx).direct = direct;
    (*cx).direct_len = direct_len;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.presence & AK_EFIX_UPLOADRESULTDATAMESSAGE_PRESENT_UPLOAD != 0 {
        let mk = (*cx).e.begin(1, 92);
        if g.upload.session_id.tc.is_some() && g.upload.session_id.len != 0 {
            if !enc_blob(cx, 1, 93, &g.upload.session_id) { return crate::enc_status(cx) as isize; }
        }
        if g.upload.result_id.tc.is_some() && g.upload.result_id.len != 0 {
            if !enc_blob(cx, 2, 94, &g.upload.result_id) { return crate::enc_status(cx) as isize; }
        }
        if g.upload.data_chunk.len != 0 {
            if !enc_blob(cx, 3, 95, &g.upload.data_chunk) { return crate::enc_status(cx) as isize; }
        }
        (*cx).e.end(mk);
    }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_UploadResultDataMessage(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_UploadResultDataMessage,
    fix: *const ak_ufix_UploadResultDataMessage,
    direct: *const u8,
    direct_len: usize,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    (*cx).direct = direct;
    (*cx).direct_len = direct_len;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.presence & AK_UFIX_UPLOADRESULTDATAMESSAGE_PRESENT_UPLOAD != 0 {
        let mk = (*cx).e.begin(1, 92);
        if g.upload.session_id.tc.is_some() && g.upload.session_id.len != 0 {
            if !enc_blob(cx, 1, 93, &g.upload.session_id) { return crate::enc_status(cx) as isize; }
        }
        if g.upload.result_id.tc.is_some() && g.upload.result_id.len != 0 {
            if !enc_blob(cx, 2, 94, &g.upload.result_id) { return crate::enc_status(cx) as isize; }
        }
        if g.upload.data_chunk.len != 0 {
            if !enc_blob(cx, 3, 95, &g.upload.data_chunk) { return crate::enc_status(cx) as isize; }
        }
        if !enc_raw(cx, &g.upload.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_DualResponse(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_DualResponse,
    fix: *const ak_efix_DualResponse,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_left {
        (*cx).open_tag = 1;
        (*cx).open_site = 96;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_right {
        (*cx).open_tag = 2;
        (*cx).open_site = 97;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_DualResponse(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_DualResponse,
    fix: *const ak_ufix_DualResponse,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_left {
        (*cx).open_tag = 1;
        (*cx).open_site = 96;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_right {
        (*cx).open_tag = 2;
        (*cx).open_site = 97;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_ChunkLeaf(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_ChunkLeaf,
    fix: *const ak_efix_ChunkLeaf,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.k.tc.is_some() && g.k.len != 0 {
        if !enc_blob(cx, 1, 98, &g.k) { return crate::enc_status(cx) as isize; }
    }
    if g.v != 0 { (*cx).e.varint_field(2, g.v as i64 as u64); }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_ChunkLeaf(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_ChunkLeaf,
    fix: *const ak_ufix_ChunkLeaf,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.k.tc.is_some() && g.k.len != 0 {
        if !enc_blob(cx, 1, 98, &g.k) { return crate::enc_status(cx) as isize; }
    }
    if g.v != 0 { (*cx).e.varint_field(2, g.v as i64 as u64); }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_ChunkInner(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_ChunkInner,
    fix: *const ak_efix_ChunkInner,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_marks {
        (*cx).open_tag = 1;
        (*cx).open_site = 99;
        (*cx).open_kind = 2;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_leaves {
        (*cx).open_tag = 2;
        (*cx).open_site = 100;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_ChunkInner(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_ChunkInner,
    fix: *const ak_ufix_ChunkInner,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_marks {
        (*cx).open_tag = 1;
        (*cx).open_site = 99;
        (*cx).open_kind = 2;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_leaves {
        (*cx).open_tag = 2;
        (*cx).open_site = 100;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_ChunkElement(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_ChunkElement,
    fix: *const ak_efix_ChunkElement,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_labels {
        (*cx).open_tag = 1;
        (*cx).open_site = 101;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_attrs {
        (*cx).open_tag = 2;
        (*cx).open_site = 102;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 3, 103, &g.id) { return crate::enc_status(cx) as isize; }
    }
    if g.presence & AK_EFIX_CHUNKELEMENT_PRESENT_INNER != 0 {
        let mk = (*cx).e.begin(4, 104);
        if let Some(lp) = (*vt).loop_inner_marks {
            (*cx).open_tag = 1;
            (*cx).open_site = 105;
            (*cx).open_kind = 2;
            ak_rt::bump!((*cx).e.c, reverse);
            let rc = lp(cx as *mut ak_enc_ctx, obj, token);
            if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
            if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
        }
        if let Some(lp) = (*vt).loop_inner_leaves {
            (*cx).open_tag = 2;
            (*cx).open_site = 106;
            (*cx).open_kind = 0;
            ak_rt::bump!((*cx).e.c, reverse);
            let rc = lp(cx as *mut ak_enc_ctx, obj, token);
            if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
            if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
        }
        (*cx).e.end(mk);
    }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_ChunkElement(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_ChunkElement,
    fix: *const ak_ufix_ChunkElement,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_labels {
        (*cx).open_tag = 1;
        (*cx).open_site = 101;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_attrs {
        (*cx).open_tag = 2;
        (*cx).open_site = 102;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 3, 103, &g.id) { return crate::enc_status(cx) as isize; }
    }
    if g.presence & AK_UFIX_CHUNKELEMENT_PRESENT_INNER != 0 {
        let mk = (*cx).e.begin(4, 104);
        if let Some(lp) = (*vt).loop_inner_marks {
            (*cx).open_tag = 1;
            (*cx).open_site = 105;
            (*cx).open_kind = 2;
            ak_rt::bump!((*cx).e.c, reverse);
            let rc = lp(cx as *mut ak_enc_ctx, obj, token);
            if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
            if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
        }
        if let Some(lp) = (*vt).loop_inner_leaves {
            (*cx).open_tag = 2;
            (*cx).open_site = 106;
            (*cx).open_kind = 0;
            ak_rt::bump!((*cx).e.c, reverse);
            let rc = lp(cx as *mut ak_enc_ctx, obj, token);
            if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
            if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
        }
        if !enc_raw(cx, &g.inner.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_ChunkedResponse(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_ChunkedResponse,
    fix: *const ak_efix_ChunkedResponse,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_items {
        (*cx).open_tag = 7;
        (*cx).open_site = 107;
        (*cx).open_kind = 0;
        (*cx).open_vt = (*vt).elem_items as *const c_void;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if g.page != 0 { (*cx).e.varint_field(8, g.page as i64 as u64); }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_ChunkedResponse(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_ChunkedResponse,
    fix: *const ak_ufix_ChunkedResponse,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_items {
        (*cx).open_tag = 7;
        (*cx).open_site = 107;
        (*cx).open_kind = 0;
        (*cx).open_vt = (*vt).elem_items as *const c_void;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if g.page != 0 { (*cx).e.varint_field(8, g.page as i64 as u64); }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_ChunkedResponseWide(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_ChunkedResponseWide,
    fix: *const ak_efix_ChunkedResponseWide,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_items {
        (*cx).open_tag = 70000;
        (*cx).open_site = 108;
        (*cx).open_kind = 0;
        (*cx).open_vt = (*vt).elem_items as *const c_void;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_ChunkedResponseWide(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_ChunkedResponseWide,
    fix: *const ak_ufix_ChunkedResponseWide,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_items {
        (*cx).open_tag = 70000;
        (*cx).open_site = 108;
        (*cx).open_kind = 0;
        (*cx).open_vt = (*vt).elem_items as *const c_void;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_LeafElement(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_LeafElement,
    fix: *const ak_efix_LeafElement,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 1, 109, &g.id) { return crate::enc_status(cx) as isize; }
    }
    if g.n != 0 { (*cx).e.varint_field(2, g.n as u64); }
    if g.presence & AK_EFIX_LEAFELEMENT_PRESENT_STAMP != 0 {
        let mk = (*cx).e.begin(3, 110);
        if g.stamp.seconds != 0 { (*cx).e.varint_field(1, g.stamp.seconds as u64); }
        if g.stamp.nanos != 0 { (*cx).e.varint_field(2, g.stamp.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_LeafElement(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_LeafElement,
    fix: *const ak_ufix_LeafElement,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.id.tc.is_some() && g.id.len != 0 {
        if !enc_blob(cx, 1, 109, &g.id) { return crate::enc_status(cx) as isize; }
    }
    if g.n != 0 { (*cx).e.varint_field(2, g.n as u64); }
    if g.presence & AK_UFIX_LEAFELEMENT_PRESENT_STAMP != 0 {
        let mk = (*cx).e.begin(3, 110);
        if g.stamp.seconds != 0 { (*cx).e.varint_field(1, g.stamp.seconds as u64); }
        if g.stamp.nanos != 0 { (*cx).e.varint_field(2, g.stamp.nanos as i64 as u64); }
        if !enc_raw(cx, &g.stamp.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_LeafResponse(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_LeafResponse,
    fix: *const ak_efix_LeafResponse,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_items {
        (*cx).open_tag = 9;
        (*cx).open_site = 111;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_LeafResponse(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_LeafResponse,
    fix: *const ak_ufix_LeafResponse,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if let Some(lp) = (*vt).loop_items {
        (*cx).open_tag = 9;
        (*cx).open_site = 111;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_Surrogate(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_Surrogate,
    fix: *const ak_efix_Surrogate,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.text.tc.is_some() && g.text.len != 0 {
        if !enc_blob(cx, 1, 112, &g.text) { return crate::enc_status(cx) as isize; }
    }
    if g.presence & AK_EFIX_SURROGATE_PRESENT_NESTED != 0 {
        let mk = (*cx).e.begin(2, 113);
        if g.nested.text.tc.is_some() && g.nested.text.len != 0 {
            if !enc_blob(cx, 1, 114, &g.nested.text) { return crate::enc_status(cx) as isize; }
        }
        (*cx).e.end(mk);
    }
    if let Some(lp) = (*vt).loop_attrs {
        (*cx).open_tag = 3;
        (*cx).open_site = 115;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_texts {
        (*cx).open_tag = 4;
        (*cx).open_site = 116;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if g.raw.tc.is_some() && g.raw.len != 0 {
        if !enc_blob(cx, 5, 117, &g.raw) { return crate::enc_status(cx) as isize; }
    }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_Surrogate(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_Surrogate,
    fix: *const ak_ufix_Surrogate,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.text.tc.is_some() && g.text.len != 0 {
        if !enc_blob(cx, 1, 112, &g.text) { return crate::enc_status(cx) as isize; }
    }
    if g.presence & AK_UFIX_SURROGATE_PRESENT_NESTED != 0 {
        let mk = (*cx).e.begin(2, 113);
        if g.nested.text.tc.is_some() && g.nested.text.len != 0 {
            if !enc_blob(cx, 1, 114, &g.nested.text) { return crate::enc_status(cx) as isize; }
        }
        if !enc_raw(cx, &g.nested.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if let Some(lp) = (*vt).loop_attrs {
        (*cx).open_tag = 3;
        (*cx).open_site = 115;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if let Some(lp) = (*vt).loop_texts {
        (*cx).open_tag = 4;
        (*cx).open_site = 116;
        (*cx).open_kind = 0;
        ak_rt::bump!((*cx).e.c, reverse);
        let rc = lp(cx as *mut ak_enc_ctx, obj, token);
        if rc < 0 { (*cx).e.fail(rc); return crate::enc_status(cx) as isize; }
        if (*cx).e.err != 0 || (*cx).hdr.err != AK_OK { return crate::enc_status(cx) as isize; }
    }
    if g.raw.tc.is_some() && g.raw.len != 0 {
        if !enc_blob(cx, 5, 117, &g.raw) { return crate::enc_status(cx) as isize; }
    }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_SurrogateInner(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_SurrogateInner,
    fix: *const ak_efix_SurrogateInner,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.text.tc.is_some() && g.text.len != 0 {
        if !enc_blob(cx, 1, 118, &g.text) { return crate::enc_status(cx) as isize; }
    }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_SurrogateInner(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_SurrogateInner,
    fix: *const ak_ufix_SurrogateInner,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.text.tc.is_some() && g.text.len != 0 {
        if !enc_blob(cx, 1, 118, &g.text) { return crate::enc_status(cx) as isize; }
    }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_encode_WireZoo(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_WireZoo,
    fix: *const ak_efix_WireZoo,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.v_int32 != 0 { (*cx).e.varint_field(1, g.v_int32 as i64 as u64); }
    if g.v_int64 != 0 { (*cx).e.varint_field(2, g.v_int64 as u64); }
    if g.v_bool != 0 { (*cx).e.varint_field(3, (g.v_bool != 0) as u64); }
    if g.v_double.to_bits() != 0 { (*cx).e.f64_field(4, g.v_double); }
    if g.v_fixed32 != 0 { (*cx).e.fixed32_field(5, g.v_fixed32); }
    if g.v_string.tc.is_some() && g.v_string.len != 0 {
        if !enc_blob(cx, 6, 119, &g.v_string) { return crate::enc_status(cx) as isize; }
    }
    if g.v_bytes.tc.is_some() && g.v_bytes.len != 0 {
        if !enc_blob(cx, 7, 120, &g.v_bytes) { return crate::enc_status(cx) as isize; }
    }
    if g.v_enum != 0 { (*cx).e.varint_field(8, g.v_enum as i64 as u64); }
    if g.presence & AK_EFIX_WIREZOO_PRESENT_V_MSG != 0 {
        let mk = (*cx).e.begin(9, 121);
        if g.v_msg.seconds != 0 { (*cx).e.varint_field(1, g.v_msg.seconds as u64); }
        if g.v_msg.nanos != 0 { (*cx).e.varint_field(2, g.v_msg.nanos as i64 as u64); }
        (*cx).e.end(mk);
    }
    if g.v_big_tag != 0 { (*cx).e.varint_field(536870911, g.v_big_tag as i64 as u64); }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_uencode_WireZoo(
    obj: *const c_void,
    ctx: *mut ak_enc_ctx,
    vt: *const ak_evt_WireZoo,
    fix: *const ak_ufix_WireZoo,
) -> isize {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED as isize;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    (*cx).open_obj = obj;
    let g = &*fix;
    let token = AK_TOKEN_ROOT;
    let _ = token;
    if g.v_int32 != 0 { (*cx).e.varint_field(1, g.v_int32 as i64 as u64); }
    if g.v_int64 != 0 { (*cx).e.varint_field(2, g.v_int64 as u64); }
    if g.v_bool != 0 { (*cx).e.varint_field(3, (g.v_bool != 0) as u64); }
    if g.v_double.to_bits() != 0 { (*cx).e.f64_field(4, g.v_double); }
    if g.v_fixed32 != 0 { (*cx).e.fixed32_field(5, g.v_fixed32); }
    if g.v_string.tc.is_some() && g.v_string.len != 0 {
        if !enc_blob(cx, 6, 119, &g.v_string) { return crate::enc_status(cx) as isize; }
    }
    if g.v_bytes.tc.is_some() && g.v_bytes.len != 0 {
        if !enc_blob(cx, 7, 120, &g.v_bytes) { return crate::enc_status(cx) as isize; }
    }
    if g.v_enum != 0 { (*cx).e.varint_field(8, g.v_enum as i64 as u64); }
    if g.presence & AK_UFIX_WIREZOO_PRESENT_V_MSG != 0 {
        let mk = (*cx).e.begin(9, 121);
        if g.v_msg.seconds != 0 { (*cx).e.varint_field(1, g.v_msg.seconds as u64); }
        if g.v_msg.nanos != 0 { (*cx).e.varint_field(2, g.v_msg.nanos as i64 as u64); }
        if !enc_raw(cx, &g.v_msg.unknown) { return crate::enc_status(cx) as isize; }
        (*cx).e.end(mk);
    }
    if g.v_big_tag != 0 { (*cx).e.varint_field(536870911, g.v_big_tag as i64 as u64); }
    if !enc_raw(cx, &g.unknown) { return crate::enc_status(cx) as isize; }
    let st = crate::enc_status(cx);
    if st != AK_OK { return st as isize; }
    (*cx).e.buf.len() as isize
}

#[no_mangle]
pub unsafe extern "C" fn ak_blob_run(
    ctx: *mut ak_enc_ctx,
    elems: *const ak_str,
    n: i32,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    for i in 0..n as usize {
        if !enc_blob(cx, tag, site, &*elems.add(i)) { return crate::enc_status(cx); }
    }
    AK_OK
}

#[no_mangle]
pub unsafe extern "C" fn ak_run_i32(ctx: *mut ak_enc_ctx, p: *const i32, n: usize) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    if n == 0 { return AK_OK; }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    let mk = (*cx).e.begin(tag, site);
    // The wire encoding comes from the schema and lives in the context,
    // so bool and enum need no symbols of their own.
    for i in 0..n {
        let v = *p.add(i);
        (*cx).e.varint(v as i64 as u64);
    }
    (*cx).e.end(mk);
    AK_OK
}

#[no_mangle]
pub unsafe extern "C" fn ak_run_i64(ctx: *mut ak_enc_ctx, p: *const i64, n: usize) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    if n == 0 { return AK_OK; }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    let mk = (*cx).e.begin(tag, site);
    // The wire encoding comes from the schema and lives in the context,
    // so bool and enum need no symbols of their own.
    for i in 0..n {
        let v = *p.add(i);
        (*cx).e.varint(v as u64);
    }
    (*cx).e.end(mk);
    AK_OK
}

#[no_mangle]
pub unsafe extern "C" fn ak_run_f64(ctx: *mut ak_enc_ctx, p: *const f64, n: usize) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    if n == 0 { return AK_OK; }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    let mk = (*cx).e.begin(tag, site);
    // The wire encoding comes from the schema and lives in the context,
    // so bool and enum need no symbols of their own.
    for i in 0..n {
        let v = *p.add(i);
        (*cx).e.buf.extend_from_slice(&v.to_le_bytes());
    }
    (*cx).e.end(mk);
    AK_OK
}

#[no_mangle]
pub unsafe extern "C" fn ak_run_u8(ctx: *mut ak_enc_ctx, p: *const u8, n: usize) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let cx = ctx as *mut EncCtxImpl;
    ak_rt::bump!((*cx).e.c, forward);
    if crate::enc_status(cx) != AK_OK { return crate::enc_status(cx); }
    if n == 0 { return AK_OK; }
    let (tag, site) = ((*cx).open_tag, (*cx).open_site);
    let mk = (*cx).e.begin(tag, site);
    // The wire encoding comes from the schema and lives in the context,
    // so bool and enum need no symbols of their own.
    for i in 0..n {
        let v = *p.add(i);
        (*cx).e.varint(v as u64);
    }
    (*cx).e.end(mk);
    AK_OK
}

#[inline]
unsafe fn dec_chunk_element_attrs_entry_fix(d: &mut Dec, base: usize, u: UnkCx) -> ak_dfix_ChunkElementAttrsEntry {
    let mut out = ak_dfix_ChunkElementAttrsEntry::ZERO;
    dec_chunk_element_attrs_entry_fix_into(d, base, u, &mut out);
    out
}

/// Decode INTO an existing group: what a oneof's message member needs to merge a
/// repeated occurrence (plan rule), and what `dec_chunk_element_attrs_entry_fix` wraps.
#[inline]
unsafe fn dec_chunk_element_attrs_entry_fix_into(d: &mut Dec, base: usize, u: UnkCx, out: &mut ak_dfix_ChunkElementAttrsEntry) {
    #[allow(unused_variables)]
    let buf0 = d.buf;
    let base0 = base;
    let mut cur = 0u32;
    macro_rules! flush { () => { }; }
    while !d.at_end() {
        // Decision 11: where this field's tag-and-value run starts, so an
        // unknown one can be copied into the message's own buffer.
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.key = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.value = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            _ => {
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
}

#[inline]
unsafe fn dec_chunk_leaf_fix(d: &mut Dec, base: usize, u: UnkCx) -> ak_dfix_ChunkLeaf {
    let mut out = ak_dfix_ChunkLeaf::ZERO;
    dec_chunk_leaf_fix_into(d, base, u, &mut out);
    out
}

/// Decode INTO an existing group: what a oneof's message member needs to merge a
/// repeated occurrence (plan rule), and what `dec_chunk_leaf_fix` wraps.
#[inline]
unsafe fn dec_chunk_leaf_fix_into(d: &mut Dec, base: usize, u: UnkCx, out: &mut ak_dfix_ChunkLeaf) {
    #[allow(unused_variables)]
    let buf0 = d.buf;
    let base0 = base;
    let mut cur = 0u32;
    macro_rules! flush { () => { }; }
    while !d.at_end() {
        // Decision 11: where this field's tag-and-value run starts, so an
        // unknown one can be copied into the message's own buffer.
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.k = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.v = d.varint() as i32;
            }
            _ => {
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
}

#[inline]
unsafe fn dec_empty_fix(d: &mut Dec, base: usize, u: UnkCx) -> ak_dfix_Empty {
    let mut out = ak_dfix_Empty::ZERO;
    dec_empty_fix_into(d, base, u, &mut out);
    out
}

/// Decode INTO an existing group: what a oneof's message member needs to merge a
/// repeated occurrence (plan rule), and what `dec_empty_fix` wraps.
#[inline]
unsafe fn dec_empty_fix_into(d: &mut Dec, base: usize, u: UnkCx, out: &mut ak_dfix_Empty) {
    #[allow(unused_variables)]
    let buf0 = d.buf;
    let base0 = base;
    let mut cur = 0u32;
    macro_rules! flush { () => { }; }
    while !d.at_end() {
        // Decision 11: where this field's tag-and-value run starts, so an
        // unknown one can be copied into the message's own buffer.
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            _ => {
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
}

#[inline]
unsafe fn dec_leaf_element_fix(d: &mut Dec, base: usize, u: UnkCx) -> ak_dfix_LeafElement {
    let mut out = ak_dfix_LeafElement::ZERO;
    dec_leaf_element_fix_into(d, base, u, &mut out);
    out
}

/// Decode INTO an existing group: what a oneof's message member needs to merge a
/// repeated occurrence (plan rule), and what `dec_leaf_element_fix` wraps.
#[inline]
unsafe fn dec_leaf_element_fix_into(d: &mut Dec, base: usize, u: UnkCx, out: &mut ak_dfix_LeafElement) {
    #[allow(unused_variables)]
    let buf0 = d.buf;
    let base0 = base;
    let mut cur = 0u32;
    macro_rules! flush { () => { }; }
    while !d.at_end() {
        // Decision 11: where this field's tag-and-value run starts, so an
        // unknown one can be copied into the message's own buffer.
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.n = d.varint() as i64;
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_LEAFELEMENT_PRESENT_STAMP;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.stamp.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.stamp.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(1), &mut out.stamp.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            _ => {
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
}

#[inline]
unsafe fn dec_pair_fix(d: &mut Dec, base: usize, u: UnkCx) -> ak_dfix_Pair {
    let mut out = ak_dfix_Pair::ZERO;
    dec_pair_fix_into(d, base, u, &mut out);
    out
}

/// Decode INTO an existing group: what a oneof's message member needs to merge a
/// repeated occurrence (plan rule), and what `dec_pair_fix` wraps.
#[inline]
unsafe fn dec_pair_fix_into(d: &mut Dec, base: usize, u: UnkCx, out: &mut ak_dfix_Pair) {
    #[allow(unused_variables)]
    let buf0 = d.buf;
    let base0 = base;
    let mut cur = 0u32;
    macro_rules! flush { () => { }; }
    while !d.at_end() {
        // Decision 11: where this field's tag-and-value run starts, so an
        // unknown one can be copied into the message's own buffer.
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.key = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.value = d.varint() as i32;
            }
            _ => {
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
}

#[inline]
unsafe fn dec_probe_fix(d: &mut Dec, base: usize, u: UnkCx) -> ak_dfix_Probe {
    let mut out = ak_dfix_Probe::ZERO;
    dec_probe_fix_into(d, base, u, &mut out);
    out
}

/// Decode INTO an existing group: what a oneof's message member needs to merge a
/// repeated occurrence (plan rule), and what `dec_probe_fix` wraps.
#[inline]
unsafe fn dec_probe_fix_into(d: &mut Dec, base: usize, u: UnkCx, out: &mut ak_dfix_Probe) {
    #[allow(unused_variables)]
    let buf0 = d.buf;
    let base0 = base;
    let mut cur = 0u32;
    macro_rules! flush { () => { }; }
    while !d.at_end() {
        // Decision 11: where this field's tag-and-value run starts, so an
        // unknown one can be copied into the message's own buffer.
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.opt_count = d.varint() as i32;
                out.presence |= AK_DFIX_PROBE_PRESENT_OPT_COUNT;
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.opt_label = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
                out.presence |= AK_DFIX_PROBE_PRESENT_OPT_LABEL;
            }
            4 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.opt_flag = (d.varint() != 0) as u8;
                out.presence |= AK_DFIX_PROBE_PRESENT_OPT_FLAG;
            }
            10 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.body_as_int = d.varint() as i64;
                // Last one wins: a later member replaces the case.
                out.body_case = 10;
            }
            11 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.body_as_text = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
                // Last one wins: a later member replaces the case.
                out.body_case = 11;
            }
            12 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                out.body_as_blob = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
                // Last one wins: a later member replaces the case.
                out.body_case = 12;
            }
            13 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                let mut os = Dec::new(&buf0[off..off + n]);
                // Plan rule: the SAME member again merges; another member,
                // or none, starts from empty.
                // Decision 11 rule 4: the oneof's ONE buffer moves, emptied, from the
                // previous message member's slot into this member's.
                if out.body_case != 13 {
                    // At most one member slot holds it (the invariant this keeps).
                    let mut k = ::core::mem::replace(&mut out.body_as_stamp.unknown, ak_unk_buf { data: ::core::ptr::null_mut(), len: 0, cap: 0 });
                    let t = ::core::mem::replace(&mut out.body_as_nothing.unknown, ak_unk_buf { data: ::core::ptr::null_mut(), len: 0, cap: 0 });
                    if !t.data.is_null() { k = t; }
                    out.body_as_stamp = ak_dfix_Timestamp::ZERO;
                    out.body_as_stamp.unknown = ak_unk_buf { data: k.data, len: 0, cap: k.cap };
                }
                dec_timestamp_fix_into(&mut os, base0 + off, u.at(1), &mut out.body_as_stamp);
                if os.err != 0 { d.err = os.err; }
                // Last one wins: a later member replaces the case.
                out.body_case = 13;
            }
            14 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                let mut os = Dec::new(&buf0[off..off + n]);
                // Plan rule: the SAME member again merges; another member,
                // or none, starts from empty.
                // Decision 11 rule 4: the oneof's ONE buffer moves, emptied, from the
                // previous message member's slot into this member's.
                if out.body_case != 14 {
                    // At most one member slot holds it (the invariant this keeps).
                    let mut k = ::core::mem::replace(&mut out.body_as_nothing.unknown, ak_unk_buf { data: ::core::ptr::null_mut(), len: 0, cap: 0 });
                    let t = ::core::mem::replace(&mut out.body_as_stamp.unknown, ak_unk_buf { data: ::core::ptr::null_mut(), len: 0, cap: 0 });
                    if !t.data.is_null() { k = t; }
                    out.body_as_nothing = ak_dfix_Empty::ZERO;
                    out.body_as_nothing.unknown = ak_unk_buf { data: k.data, len: 0, cap: k.cap };
                }
                dec_empty_fix_into(&mut os, base0 + off, u.at(1), &mut out.body_as_nothing);
                if os.err != 0 { d.err = os.err; }
                // Last one wins: a later member replaces the case.
                out.body_case = 14;
            }
            _ => {
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
}

#[inline]
unsafe fn dec_result_raw_fix(d: &mut Dec, base: usize, u: UnkCx) -> ak_dfix_ResultRaw {
    let mut out = ak_dfix_ResultRaw::ZERO;
    dec_result_raw_fix_into(d, base, u, &mut out);
    out
}

/// Decode INTO an existing group: what a oneof's message member needs to merge a
/// repeated occurrence (plan rule), and what `dec_result_raw_fix` wraps.
#[inline]
unsafe fn dec_result_raw_fix_into(d: &mut Dec, base: usize, u: UnkCx, out: &mut ak_dfix_ResultRaw) {
    #[allow(unused_variables)]
    let buf0 = d.buf;
    let base0 = base;
    let mut cur = 0u32;
    macro_rules! flush { () => { }; }
    while !d.at_end() {
        // Decision 11: where this field's tag-and-value run starts, so an
        // unknown one can be copied into the message's own buffer.
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.session_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.name = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.owner_task_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            4 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.status = d.varint() as i32;
            }
            5 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_RESULTRAW_PRESENT_CREATED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.created_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.created_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(1), &mut out.created_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            6 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_RESULTRAW_PRESENT_COMPLETED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.completed_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.completed_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(2), &mut out.completed_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            8 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.result_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            9 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.size = d.varint() as i64;
            }
            10 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.created_by = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            11 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                out.opaque_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            12 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.manual_deletion = (d.varint() != 0) as u8;
            }
            _ => {
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
}

#[inline]
unsafe fn dec_surrogate_attrs_entry_fix(d: &mut Dec, base: usize, u: UnkCx) -> ak_dfix_SurrogateAttrsEntry {
    let mut out = ak_dfix_SurrogateAttrsEntry::ZERO;
    dec_surrogate_attrs_entry_fix_into(d, base, u, &mut out);
    out
}

/// Decode INTO an existing group: what a oneof's message member needs to merge a
/// repeated occurrence (plan rule), and what `dec_surrogate_attrs_entry_fix` wraps.
#[inline]
unsafe fn dec_surrogate_attrs_entry_fix_into(d: &mut Dec, base: usize, u: UnkCx, out: &mut ak_dfix_SurrogateAttrsEntry) {
    #[allow(unused_variables)]
    let buf0 = d.buf;
    let base0 = base;
    let mut cur = 0u32;
    macro_rules! flush { () => { }; }
    while !d.at_end() {
        // Decision 11: where this field's tag-and-value run starts, so an
        // unknown one can be copied into the message's own buffer.
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.key = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.value = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            _ => {
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
}

#[inline]
unsafe fn dec_task_options_options_entry_fix(d: &mut Dec, base: usize, u: UnkCx) -> ak_dfix_TaskOptionsOptionsEntry {
    let mut out = ak_dfix_TaskOptionsOptionsEntry::ZERO;
    dec_task_options_options_entry_fix_into(d, base, u, &mut out);
    out
}

/// Decode INTO an existing group: what a oneof's message member needs to merge a
/// repeated occurrence (plan rule), and what `dec_task_options_options_entry_fix` wraps.
#[inline]
unsafe fn dec_task_options_options_entry_fix_into(d: &mut Dec, base: usize, u: UnkCx, out: &mut ak_dfix_TaskOptionsOptionsEntry) {
    #[allow(unused_variables)]
    let buf0 = d.buf;
    let base0 = base;
    let mut cur = 0u32;
    macro_rules! flush { () => { }; }
    while !d.at_end() {
        // Decision 11: where this field's tag-and-value run starts, so an
        // unknown one can be copied into the message's own buffer.
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.key = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.value = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            _ => {
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
}

#[inline]
unsafe fn dec_timestamp_fix(d: &mut Dec, base: usize, u: UnkCx) -> ak_dfix_Timestamp {
    let mut out = ak_dfix_Timestamp::ZERO;
    dec_timestamp_fix_into(d, base, u, &mut out);
    out
}

/// Decode INTO an existing group: what a oneof's message member needs to merge a
/// repeated occurrence (plan rule), and what `dec_timestamp_fix` wraps.
#[inline]
unsafe fn dec_timestamp_fix_into(d: &mut Dec, base: usize, u: UnkCx, out: &mut ak_dfix_Timestamp) {
    #[allow(unused_variables)]
    let buf0 = d.buf;
    let base0 = base;
    let mut cur = 0u32;
    macro_rules! flush { () => { }; }
    while !d.at_end() {
        // Decision 11: where this field's tag-and-value run starts, so an
        // unknown one can be copied into the message's own buffer.
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.seconds = d.varint() as i64;
            }
            2 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.nanos = d.varint() as i32;
            }
            _ => {
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
}

/// `TaskDetailed` is not batchable: it carries repeated or map fields of its own,
/// so there would be nothing to attach the inner elements to. Two calls
/// per element -- `new` then `apply` -- plus one run per inner field that
/// occurred (ABI v1 section 7.2).
unsafe fn dec_list_tasks_detailed_response_tasks_element(
    ctx: *mut ak_dec_ctx,
    dcx: *mut DecCtxImpl,
    obj: *mut c_void,
    vt: *const ak_dvt_ListTasksDetailedResponse,
    d: &mut Dec,
    base: usize,
    u: UnkCx,
) {
    let tok = match (*vt).new_tasks {
        Some(f) => {
            ak_rt::bump!((*dcx).c, reverse);
            f(ctx, obj)
        }
        None => return,
    };
    if (*dcx).hdr.err != AK_OK { d.err = (*dcx).hdr.err; return; }
    if tok < 0 { crate::ak_fail(ctx as *mut c_void, AK_ERR_HOST, ::core::ptr::null(), 0); d.err = AK_ERR_HOST; return; }
    let mut out = ak_dfix_TaskDetailed::ZERO;
    #[allow(unused_variables)]
    let buf0 = d.buf;
    let base0 = base;
    // Optimisation D2: one 8-aligned arena shared by every loop slot below (one
    // slot is open at a time; each keeps its own element budget).
    let mut arena: [::core::mem::MaybeUninit<u64>; ak_rt::ARENA_BYTES / 8] =
        [const { ::core::mem::MaybeUninit::uninit() }; ak_rt::ARENA_BYTES / 8];
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_PARENT_TASK_IDS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_PARENT_TASK_IDS * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_parent_task_ids: usize = 0;
    let a_parent_task_ids: *mut ak_span = arena.as_mut_ptr() as *mut ak_span;
    #[allow(unused_macros)]
    macro_rules! at_parent_task_ids { () => { a_parent_task_ids.add(n_parent_task_ids) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_DATA_DEPENDENCIES: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_DATA_DEPENDENCIES * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_data_dependencies: usize = 0;
    let a_data_dependencies: *mut ak_span = arena.as_mut_ptr() as *mut ak_span;
    #[allow(unused_macros)]
    macro_rules! at_data_dependencies { () => { a_data_dependencies.add(n_data_dependencies) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_EXPECTED_OUTPUT_IDS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_EXPECTED_OUTPUT_IDS * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_expected_output_ids: usize = 0;
    let a_expected_output_ids: *mut ak_span = arena.as_mut_ptr() as *mut ak_span;
    #[allow(unused_macros)]
    macro_rules! at_expected_output_ids { () => { a_expected_output_ids.add(n_expected_output_ids) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_RETRY_OF_IDS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_RETRY_OF_IDS * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_retry_of_ids: usize = 0;
    let a_retry_of_ids: *mut ak_span = arena.as_mut_ptr() as *mut ak_span;
    #[allow(unused_macros)]
    macro_rules! at_retry_of_ids { () => { a_retry_of_ids.add(n_retry_of_ids) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_OPTIONS_OPTIONS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>());
    const _: () = assert!(N_OPTIONS_OPTIONS * ::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_TaskOptionsOptionsEntry>() <= 8);
    let mut n_options_options: usize = 0;
    let a_options_options: *mut ak_dfix_TaskOptionsOptionsEntry = arena.as_mut_ptr() as *mut ak_dfix_TaskOptionsOptionsEntry;
    #[allow(unused_macros)]
    macro_rules! at_options_options { () => { a_options_options.add(n_options_options) }; }
    macro_rules! flush_parent_task_ids {
        () => {
            if n_parent_task_ids > 0 {
                if let Some(add) = (*vt).add_tasks_parent_task_ids {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, tok, a_parent_task_ids as *const ak_span, n_parent_task_ids as i32);
                    }
                }
                n_parent_task_ids = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_data_dependencies {
        () => {
            if n_data_dependencies > 0 {
                if let Some(add) = (*vt).add_tasks_data_dependencies {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, tok, a_data_dependencies as *const ak_span, n_data_dependencies as i32);
                    }
                }
                n_data_dependencies = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_expected_output_ids {
        () => {
            if n_expected_output_ids > 0 {
                if let Some(add) = (*vt).add_tasks_expected_output_ids {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, tok, a_expected_output_ids as *const ak_span, n_expected_output_ids as i32);
                    }
                }
                n_expected_output_ids = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_retry_of_ids {
        () => {
            if n_retry_of_ids > 0 {
                if let Some(add) = (*vt).add_tasks_retry_of_ids {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, tok, a_retry_of_ids as *const ak_span, n_retry_of_ids as i32);
                    }
                }
                n_retry_of_ids = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_options_options {
        () => {
            if n_options_options > 0 {
                if let Some(add) = (*vt).add_tasks_options_options {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, tok, a_options_options as *const ak_dfix_TaskOptionsOptionsEntry, n_options_options as i32);
                    }
                }
                n_options_options = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_parent_task_ids!();
            flush_data_dependencies!();
            flush_expected_output_ids!();
            flush_retry_of_ids!();
            flush_options_options!();
        };
    }
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.session_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.owner_pod_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            4 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_parent_task_ids == N_PARENT_TASK_IDS { flush_parent_task_ids!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_parent_task_ids!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_parent_task_ids += 1;
            }
            5 if wire == 2 => {
                if cur != 2 { flush!(); cur = 2; }
                if n_data_dependencies == N_DATA_DEPENDENCIES { flush_data_dependencies!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_data_dependencies!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_data_dependencies += 1;
            }
            6 if wire == 2 => {
                if cur != 3 { flush!(); cur = 3; }
                if n_expected_output_ids == N_EXPECTED_OUTPUT_IDS { flush_expected_output_ids!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_expected_output_ids!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_expected_output_ids += 1;
            }
            7 if wire == 2 => {
                if cur != 4 { flush!(); cur = 4; }
                if n_retry_of_ids == N_RETRY_OF_IDS { flush_retry_of_ids!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_retry_of_ids!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_retry_of_ids += 1;
            }
            8 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.status = d.varint() as i32;
            }
            9 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.status_message = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            10 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_OPTIONS;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 2 => {
                        if cur != 5 { flush!(); cur = 5; }
                        if n_options_options == N_OPTIONS_OPTIONS { flush_options_options!(); }
                        let (off, n) = c1.len_body();
                        let mut es = Dec::new(&buf1[off..off + n]);
                        let p = at_options_options!();
                        p.write(ak_dfix_TaskOptionsOptionsEntry::ZERO);
                        dec_task_options_options_entry_fix_into(&mut es, base1 + off, u.at(2), &mut *p);
                        if es.err != 0 { c1.err = es.err; }
                        n_options_options += 1;
                    }
                    2 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (coff, clen) = c1.len_body();
                        let base2 = base1 + coff;
                        let buf2 = &buf1[coff..coff + clen];
                        let mut c2 = Dec::new(buf2);
                        out.options.presence |= AK_DFIX_TASKOPTIONS_PRESENT_MAX_DURATION;
                        while !c2.at_end() {
                            let s0 = c2.pos;
                            let k = c2.varint();
                            if c2.err != 0 { break; }
                            let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                            if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c2.err = ak_rt::ERR_MALFORMED; break; }
                            match tag {
                            1 if wire == 0 => {
                                if cur != 0 { flush!(); cur = 0; }
                                out.options.max_duration.seconds = c2.varint() as i64;
                            }
                            2 if wire == 0 => {
                                if cur != 0 { flush!(); cur = 0; }
                                out.options.max_duration.nanos = c2.varint() as i32;
                            }
                                _ => {
                                    c2.skip(tag, wire);
                                    // Decision 11: the inlined child's OWN buffer.
                                    if c2.err == 0 && !u.pos.is_null() {
                                        let rc = unk_put(u.at(3), &mut out.options.max_duration.unknown, &buf2[s0..c2.pos]);
                                        if rc != 0 { c2.err = rc; }
                                    }
                                }
                            }
                        }
                        if c2.err != 0 { c1.err = c2.err; }
                    }
                    3 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.options.max_retries = c1.varint() as i32;
                    }
                    4 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.options.priority = c1.varint() as i32;
                    }
                    5 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.partition_id = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    6 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_name = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    7 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_version = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    8 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_namespace = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    9 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_service = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    10 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.engine_type = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(1), &mut out.options.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            11 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_CREATED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.created_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.created_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(4), &mut out.created_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            12 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_SUBMITTED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.submitted_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.submitted_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(5), &mut out.submitted_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            13 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_STARTED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.started_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.started_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(6), &mut out.started_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            14 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_ENDED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.ended_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.ended_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(7), &mut out.ended_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            15 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_POD_TTL;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.pod_ttl.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.pod_ttl.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(8), &mut out.pod_ttl.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            16 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_OUTPUT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.output.success = (c1.varint() != 0) as u8;
                    }
                    2 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.output.error = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(9), &mut out.output.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            17 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.pod_hostname = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            18 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_RECEIVED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.received_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.received_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(10), &mut out.received_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            19 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_ACQUIRED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.acquired_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.acquired_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(11), &mut out.acquired_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            20 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_CREATION_TO_END_DURATION;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.creation_to_end_duration.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.creation_to_end_duration.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(12), &mut out.creation_to_end_duration.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            21 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_PROCESSING_TO_END_DURATION;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.processing_to_end_duration.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.processing_to_end_duration.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(13), &mut out.processing_to_end_duration.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            22 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.initial_task_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            23 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_RECEIVED_TO_END_DURATION;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.received_to_end_duration.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.received_to_end_duration.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(14), &mut out.received_to_end_duration.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            24 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_PROCESSED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.processed_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.processed_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(15), &mut out.processed_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            25 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_FETCHED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.fetched_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.fetched_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(16), &mut out.fetched_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            26 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.payload_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            27 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.created_by = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    if d.err == 0 {
        if let Some(ap) = (*vt).apply_tasks {
            ak_rt::bump!((*dcx).c, reverse);
            ap(ctx, obj, tok, &out);
            if (*dcx).hdr.err != AK_OK { d.err = (*dcx).hdr.err; }
        }
    }
}

/// `TaskSummary` is not batchable: it carries repeated or map fields of its own,
/// so there would be nothing to attach the inner elements to. Two calls
/// per element -- `new` then `apply` -- plus one run per inner field that
/// occurred (ABI v1 section 7.2).
unsafe fn dec_list_task_summary_response_tasks_element(
    ctx: *mut ak_dec_ctx,
    dcx: *mut DecCtxImpl,
    obj: *mut c_void,
    vt: *const ak_dvt_ListTaskSummaryResponse,
    d: &mut Dec,
    base: usize,
    u: UnkCx,
) {
    let tok = match (*vt).new_tasks {
        Some(f) => {
            ak_rt::bump!((*dcx).c, reverse);
            f(ctx, obj)
        }
        None => return,
    };
    if (*dcx).hdr.err != AK_OK { d.err = (*dcx).hdr.err; return; }
    if tok < 0 { crate::ak_fail(ctx as *mut c_void, AK_ERR_HOST, ::core::ptr::null(), 0); d.err = AK_ERR_HOST; return; }
    let mut out = ak_dfix_TaskSummary::ZERO;
    #[allow(unused_variables)]
    let buf0 = d.buf;
    let base0 = base;
    // Optimisation D2: one 8-aligned arena shared by every loop slot below (one
    // slot is open at a time; each keeps its own element budget).
    let mut arena: [::core::mem::MaybeUninit<u64>; ak_rt::ARENA_BYTES / 8] =
        [const { ::core::mem::MaybeUninit::uninit() }; ak_rt::ARENA_BYTES / 8];
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_OPTIONS_OPTIONS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>());
    const _: () = assert!(N_OPTIONS_OPTIONS * ::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_TaskOptionsOptionsEntry>() <= 8);
    let mut n_options_options: usize = 0;
    let a_options_options: *mut ak_dfix_TaskOptionsOptionsEntry = arena.as_mut_ptr() as *mut ak_dfix_TaskOptionsOptionsEntry;
    #[allow(unused_macros)]
    macro_rules! at_options_options { () => { a_options_options.add(n_options_options) }; }
    macro_rules! flush_options_options {
        () => {
            if n_options_options > 0 {
                if let Some(add) = (*vt).add_tasks_options_options {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, tok, a_options_options as *const ak_dfix_TaskOptionsOptionsEntry, n_options_options as i32);
                    }
                }
                n_options_options = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_options_options!();
        };
    }
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.session_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKSUMMARY_PRESENT_OPTIONS;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 2 => {
                        if cur != 1 { flush!(); cur = 1; }
                        if n_options_options == N_OPTIONS_OPTIONS { flush_options_options!(); }
                        let (off, n) = c1.len_body();
                        let mut es = Dec::new(&buf1[off..off + n]);
                        let p = at_options_options!();
                        p.write(ak_dfix_TaskOptionsOptionsEntry::ZERO);
                        dec_task_options_options_entry_fix_into(&mut es, base1 + off, u.at(2), &mut *p);
                        if es.err != 0 { c1.err = es.err; }
                        n_options_options += 1;
                    }
                    2 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (coff, clen) = c1.len_body();
                        let base2 = base1 + coff;
                        let buf2 = &buf1[coff..coff + clen];
                        let mut c2 = Dec::new(buf2);
                        out.options.presence |= AK_DFIX_TASKOPTIONS_PRESENT_MAX_DURATION;
                        while !c2.at_end() {
                            let s0 = c2.pos;
                            let k = c2.varint();
                            if c2.err != 0 { break; }
                            let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                            if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c2.err = ak_rt::ERR_MALFORMED; break; }
                            match tag {
                            1 if wire == 0 => {
                                if cur != 0 { flush!(); cur = 0; }
                                out.options.max_duration.seconds = c2.varint() as i64;
                            }
                            2 if wire == 0 => {
                                if cur != 0 { flush!(); cur = 0; }
                                out.options.max_duration.nanos = c2.varint() as i32;
                            }
                                _ => {
                                    c2.skip(tag, wire);
                                    // Decision 11: the inlined child's OWN buffer.
                                    if c2.err == 0 && !u.pos.is_null() {
                                        let rc = unk_put(u.at(3), &mut out.options.max_duration.unknown, &buf2[s0..c2.pos]);
                                        if rc != 0 { c2.err = rc; }
                                    }
                                }
                            }
                        }
                        if c2.err != 0 { c1.err = c2.err; }
                    }
                    3 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.options.max_retries = c1.varint() as i32;
                    }
                    4 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.options.priority = c1.varint() as i32;
                    }
                    5 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.partition_id = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    6 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_name = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    7 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_version = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    8 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_namespace = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    9 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_service = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    10 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.engine_type = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(1), &mut out.options.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            4 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.status = d.varint() as i32;
            }
            5 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKSUMMARY_PRESENT_CREATED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.created_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.created_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(4), &mut out.created_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            8 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.error = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            9 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.status_message = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            11 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.count_data_dependencies = d.varint() as i64;
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    if d.err == 0 {
        if let Some(ap) = (*vt).apply_tasks {
            ak_rt::bump!((*dcx).c, reverse);
            ap(ctx, obj, tok, &out);
            if (*dcx).hdr.err != AK_OK { d.err = (*dcx).hdr.err; }
        }
    }
}

/// `MetricsBatch` is not batchable: it carries repeated or map fields of its own,
/// so there would be nothing to attach the inner elements to. Two calls
/// per element -- `new` then `apply` -- plus one run per inner field that
/// occurred (ABI v1 section 7.2).
unsafe fn dec_list_metrics_response_batches_element(
    ctx: *mut ak_dec_ctx,
    dcx: *mut DecCtxImpl,
    obj: *mut c_void,
    vt: *const ak_dvt_ListMetricsResponse,
    d: &mut Dec,
    base: usize,
    u: UnkCx,
) {
    let tok = match (*vt).new_batches {
        Some(f) => {
            ak_rt::bump!((*dcx).c, reverse);
            f(ctx, obj)
        }
        None => return,
    };
    if (*dcx).hdr.err != AK_OK { d.err = (*dcx).hdr.err; return; }
    if tok < 0 { crate::ak_fail(ctx as *mut c_void, AK_ERR_HOST, ::core::ptr::null(), 0); d.err = AK_ERR_HOST; return; }
    let mut out = ak_dfix_MetricsBatch::ZERO;
    #[allow(unused_variables)]
    let buf0 = d.buf;
    let base0 = base;
    // Optimisation D2: one 8-aligned arena shared by every loop slot below (one
    // slot is open at a time; each keeps its own element budget).
    let mut arena: [::core::mem::MaybeUninit<u64>; ak_rt::ARENA_BYTES / 8] =
        [const { ::core::mem::MaybeUninit::uninit() }; ak_rt::ARENA_BYTES / 8];
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_TICKS: usize = ak_rt::arena_n(::core::mem::size_of::<i64>());
    const _: () = assert!(N_TICKS * ::core::mem::size_of::<i64>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<i64>() <= 8);
    let mut n_ticks: usize = 0;
    let a_ticks: *mut i64 = arena.as_mut_ptr() as *mut i64;
    #[allow(unused_macros)]
    macro_rules! at_ticks { () => { a_ticks.add(n_ticks) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_VALUES: usize = ak_rt::arena_n(::core::mem::size_of::<f64>());
    const _: () = assert!(N_VALUES * ::core::mem::size_of::<f64>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<f64>() <= 8);
    let mut n_values: usize = 0;
    let a_values: *mut f64 = arena.as_mut_ptr() as *mut f64;
    #[allow(unused_macros)]
    macro_rules! at_values { () => { a_values.add(n_values) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_CODES: usize = ak_rt::arena_n(::core::mem::size_of::<i32>());
    const _: () = assert!(N_CODES * ::core::mem::size_of::<i32>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<i32>() <= 8);
    let mut n_codes: usize = 0;
    let a_codes: *mut i32 = arena.as_mut_ptr() as *mut i32;
    #[allow(unused_macros)]
    macro_rules! at_codes { () => { a_codes.add(n_codes) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_FLAGS: usize = ak_rt::arena_n(::core::mem::size_of::<u8>());
    const _: () = assert!(N_FLAGS * ::core::mem::size_of::<u8>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<u8>() <= 8);
    let mut n_flags: usize = 0;
    let a_flags: *mut u8 = arena.as_mut_ptr() as *mut u8;
    #[allow(unused_macros)]
    macro_rules! at_flags { () => { a_flags.add(n_flags) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_STATUSES: usize = ak_rt::arena_n(::core::mem::size_of::<i32>());
    const _: () = assert!(N_STATUSES * ::core::mem::size_of::<i32>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<i32>() <= 8);
    let mut n_statuses: usize = 0;
    let a_statuses: *mut i32 = arena.as_mut_ptr() as *mut i32;
    #[allow(unused_macros)]
    macro_rules! at_statuses { () => { a_statuses.add(n_statuses) }; }
    macro_rules! flush_ticks {
        () => {
            if n_ticks > 0 {
                if let Some(add) = (*vt).add_batches_ticks {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, tok, a_ticks as *const i64, n_ticks as i32);
                    }
                }
                n_ticks = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_values {
        () => {
            if n_values > 0 {
                if let Some(add) = (*vt).add_batches_values {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, tok, a_values as *const f64, n_values as i32);
                    }
                }
                n_values = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_codes {
        () => {
            if n_codes > 0 {
                if let Some(add) = (*vt).add_batches_codes {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, tok, a_codes as *const i32, n_codes as i32);
                    }
                }
                n_codes = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_flags {
        () => {
            if n_flags > 0 {
                if let Some(add) = (*vt).add_batches_flags {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, tok, a_flags as *const u8, n_flags as i32);
                    }
                }
                n_flags = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_statuses {
        () => {
            if n_statuses > 0 {
                if let Some(add) = (*vt).add_batches_statuses {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, tok, a_statuses as *const i32, n_statuses as i32);
                    }
                }
                n_statuses = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_ticks!();
            flush_values!();
            flush_codes!();
            flush_flags!();
            flush_statuses!();
        };
    }
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 0 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_ticks == N_TICKS { flush_ticks!(); }
                let v = d.varint() as i64;
                if d.err == 0 { at_ticks!().write(v); n_ticks += 1; }
            }
            2 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                let (off, n) = d.len_body();
                let mut ps = Dec::new(&buf0[off..off + n]);
                while !ps.at_end() {
                    if n_ticks == N_TICKS { flush_ticks!(); }
                    let v = ps.varint() as i64;
                    if ps.err != 0 { break; }
                    at_ticks!().write(v);
                    n_ticks += 1;
                }
                if ps.err != 0 { d.err = ps.err; }
            }
            3 if wire == 1 => {
                if cur != 2 { flush!(); cur = 2; }
                if n_values == N_VALUES { flush_values!(); }
                let v = d.f64();
                if d.err == 0 { at_values!().write(v); n_values += 1; }
            }
            3 if wire == 2 => {
                if cur != 2 { flush!(); cur = 2; }
                let (off, n) = d.len_body();
                let mut ps = Dec::new(&buf0[off..off + n]);
                while !ps.at_end() {
                    if n_values == N_VALUES { flush_values!(); }
                    let v = ps.f64();
                    if ps.err != 0 { break; }
                    at_values!().write(v);
                    n_values += 1;
                }
                if ps.err != 0 { d.err = ps.err; }
            }
            4 if wire == 0 => {
                if cur != 3 { flush!(); cur = 3; }
                if n_codes == N_CODES { flush_codes!(); }
                let v = d.varint() as i32;
                if d.err == 0 { at_codes!().write(v); n_codes += 1; }
            }
            4 if wire == 2 => {
                if cur != 3 { flush!(); cur = 3; }
                let (off, n) = d.len_body();
                let mut ps = Dec::new(&buf0[off..off + n]);
                while !ps.at_end() {
                    if n_codes == N_CODES { flush_codes!(); }
                    let v = ps.varint() as i32;
                    if ps.err != 0 { break; }
                    at_codes!().write(v);
                    n_codes += 1;
                }
                if ps.err != 0 { d.err = ps.err; }
            }
            5 if wire == 0 => {
                if cur != 4 { flush!(); cur = 4; }
                if n_flags == N_FLAGS { flush_flags!(); }
                let v = (d.varint() != 0) as u8;
                if d.err == 0 { at_flags!().write(v); n_flags += 1; }
            }
            5 if wire == 2 => {
                if cur != 4 { flush!(); cur = 4; }
                let (off, n) = d.len_body();
                let mut ps = Dec::new(&buf0[off..off + n]);
                while !ps.at_end() {
                    if n_flags == N_FLAGS { flush_flags!(); }
                    let v = (ps.varint() != 0) as u8;
                    if ps.err != 0 { break; }
                    at_flags!().write(v);
                    n_flags += 1;
                }
                if ps.err != 0 { d.err = ps.err; }
            }
            6 if wire == 0 => {
                if cur != 5 { flush!(); cur = 5; }
                if n_statuses == N_STATUSES { flush_statuses!(); }
                let v = d.varint() as i32;
                if d.err == 0 { at_statuses!().write(v); n_statuses += 1; }
            }
            6 if wire == 2 => {
                if cur != 5 { flush!(); cur = 5; }
                let (off, n) = d.len_body();
                let mut ps = Dec::new(&buf0[off..off + n]);
                while !ps.at_end() {
                    if n_statuses == N_STATUSES { flush_statuses!(); }
                    let v = ps.varint() as i32;
                    if ps.err != 0 { break; }
                    at_statuses!().write(v);
                    n_statuses += 1;
                }
                if ps.err != 0 { d.err = ps.err; }
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    if d.err == 0 {
        if let Some(ap) = (*vt).apply_batches {
            ak_rt::bump!((*dcx).c, reverse);
            ap(ctx, obj, tok, &out);
            if (*dcx).hdr.err != AK_OK { d.err = (*dcx).hdr.err; }
        }
    }
}

/// `ChunkElement` is not batchable: it carries repeated or map fields of its own,
/// so there would be nothing to attach the inner elements to. Two calls
/// per element -- `new` then `apply` -- plus one run per inner field that
/// occurred (ABI v1 section 7.2).
unsafe fn dec_chunked_response_items_element(
    ctx: *mut ak_dec_ctx,
    dcx: *mut DecCtxImpl,
    obj: *mut c_void,
    vt: *const ak_dvt_ChunkedResponse,
    d: &mut Dec,
    base: usize,
    u: UnkCx,
) {
    let tok = match (*vt).new_items {
        Some(f) => {
            ak_rt::bump!((*dcx).c, reverse);
            f(ctx, obj)
        }
        None => return,
    };
    if (*dcx).hdr.err != AK_OK { d.err = (*dcx).hdr.err; return; }
    if tok < 0 { crate::ak_fail(ctx as *mut c_void, AK_ERR_HOST, ::core::ptr::null(), 0); d.err = AK_ERR_HOST; return; }
    let mut out = ak_dfix_ChunkElement::ZERO;
    #[allow(unused_variables)]
    let buf0 = d.buf;
    let base0 = base;
    // Optimisation D2: one 8-aligned arena shared by every loop slot below (one
    // slot is open at a time; each keeps its own element budget).
    let mut arena: [::core::mem::MaybeUninit<u64>; ak_rt::ARENA_BYTES / 8] =
        [const { ::core::mem::MaybeUninit::uninit() }; ak_rt::ARENA_BYTES / 8];
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_LABELS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_LABELS * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_labels: usize = 0;
    let a_labels: *mut ak_span = arena.as_mut_ptr() as *mut ak_span;
    #[allow(unused_macros)]
    macro_rules! at_labels { () => { a_labels.add(n_labels) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_ATTRS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_ChunkElementAttrsEntry>());
    const _: () = assert!(N_ATTRS * ::core::mem::size_of::<ak_dfix_ChunkElementAttrsEntry>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_ChunkElementAttrsEntry>() <= 8);
    let mut n_attrs: usize = 0;
    let a_attrs: *mut ak_dfix_ChunkElementAttrsEntry = arena.as_mut_ptr() as *mut ak_dfix_ChunkElementAttrsEntry;
    #[allow(unused_macros)]
    macro_rules! at_attrs { () => { a_attrs.add(n_attrs) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_INNER_MARKS: usize = ak_rt::arena_n(::core::mem::size_of::<i64>());
    const _: () = assert!(N_INNER_MARKS * ::core::mem::size_of::<i64>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<i64>() <= 8);
    let mut n_inner_marks: usize = 0;
    let a_inner_marks: *mut i64 = arena.as_mut_ptr() as *mut i64;
    #[allow(unused_macros)]
    macro_rules! at_inner_marks { () => { a_inner_marks.add(n_inner_marks) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_INNER_LEAVES: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_ChunkLeaf>());
    const _: () = assert!(N_INNER_LEAVES * ::core::mem::size_of::<ak_dfix_ChunkLeaf>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_ChunkLeaf>() <= 8);
    let mut n_inner_leaves: usize = 0;
    let a_inner_leaves: *mut ak_dfix_ChunkLeaf = arena.as_mut_ptr() as *mut ak_dfix_ChunkLeaf;
    #[allow(unused_macros)]
    macro_rules! at_inner_leaves { () => { a_inner_leaves.add(n_inner_leaves) }; }
    macro_rules! flush_labels {
        () => {
            if n_labels > 0 {
                if let Some(add) = (*vt).add_items_labels {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, tok, a_labels as *const ak_span, n_labels as i32);
                    }
                }
                n_labels = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_attrs {
        () => {
            if n_attrs > 0 {
                if let Some(add) = (*vt).add_items_attrs {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, tok, a_attrs as *const ak_dfix_ChunkElementAttrsEntry, n_attrs as i32);
                    }
                }
                n_attrs = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_inner_marks {
        () => {
            if n_inner_marks > 0 {
                if let Some(add) = (*vt).add_items_inner_marks {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, tok, a_inner_marks as *const i64, n_inner_marks as i32);
                    }
                }
                n_inner_marks = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_inner_leaves {
        () => {
            if n_inner_leaves > 0 {
                if let Some(add) = (*vt).add_items_inner_leaves {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, tok, a_inner_leaves as *const ak_dfix_ChunkLeaf, n_inner_leaves as i32);
                    }
                }
                n_inner_leaves = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_labels!();
            flush_attrs!();
            flush_inner_marks!();
            flush_inner_leaves!();
        };
    }
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_labels == N_LABELS { flush_labels!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_labels!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_labels += 1;
            }
            2 if wire == 2 => {
                if cur != 2 { flush!(); cur = 2; }
                if n_attrs == N_ATTRS { flush_attrs!(); }
                let (off, n) = d.len_body();
                let mut es = Dec::new(&buf0[off..off + n]);
                let p = at_attrs!();
                p.write(ak_dfix_ChunkElementAttrsEntry::ZERO);
                dec_chunk_element_attrs_entry_fix_into(&mut es, base0 + off, u.at(1), &mut *p);
                if es.err != 0 { d.err = es.err; }
                n_attrs += 1;
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            4 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_CHUNKELEMENT_PRESENT_INNER;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 3 { flush!(); cur = 3; }
                        if n_inner_marks == N_INNER_MARKS { flush_inner_marks!(); }
                        let v = c1.varint() as i64;
                        if c1.err == 0 { at_inner_marks!().write(v); n_inner_marks += 1; }
                    }
                    1 if wire == 2 => {
                        if cur != 3 { flush!(); cur = 3; }
                        let (off, n) = c1.len_body();
                        let mut ps = Dec::new(&buf1[off..off + n]);
                        while !ps.at_end() {
                            if n_inner_marks == N_INNER_MARKS { flush_inner_marks!(); }
                            let v = ps.varint() as i64;
                            if ps.err != 0 { break; }
                            at_inner_marks!().write(v);
                            n_inner_marks += 1;
                        }
                        if ps.err != 0 { c1.err = ps.err; }
                    }
                    2 if wire == 2 => {
                        if cur != 4 { flush!(); cur = 4; }
                        if n_inner_leaves == N_INNER_LEAVES { flush_inner_leaves!(); }
                        let (off, n) = c1.len_body();
                        let mut es = Dec::new(&buf1[off..off + n]);
                        let p = at_inner_leaves!();
                        p.write(ak_dfix_ChunkLeaf::ZERO);
                        dec_chunk_leaf_fix_into(&mut es, base1 + off, u.at(3), &mut *p);
                        if es.err != 0 { c1.err = es.err; }
                        n_inner_leaves += 1;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(2), &mut out.inner.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    if d.err == 0 {
        if let Some(ap) = (*vt).apply_items {
            ak_rt::bump!((*dcx).c, reverse);
            ap(ctx, obj, tok, &out);
            if (*dcx).hdr.err != AK_OK { d.err = (*dcx).hdr.err; }
        }
    }
}

/// `ChunkElement` is not batchable: it carries repeated or map fields of its own,
/// so there would be nothing to attach the inner elements to. Two calls
/// per element -- `new` then `apply` -- plus one run per inner field that
/// occurred (ABI v1 section 7.2).
unsafe fn dec_chunked_response_wide_items_element(
    ctx: *mut ak_dec_ctx,
    dcx: *mut DecCtxImpl,
    obj: *mut c_void,
    vt: *const ak_dvt_ChunkedResponseWide,
    d: &mut Dec,
    base: usize,
    u: UnkCx,
) {
    let tok = match (*vt).new_items {
        Some(f) => {
            ak_rt::bump!((*dcx).c, reverse);
            f(ctx, obj)
        }
        None => return,
    };
    if (*dcx).hdr.err != AK_OK { d.err = (*dcx).hdr.err; return; }
    if tok < 0 { crate::ak_fail(ctx as *mut c_void, AK_ERR_HOST, ::core::ptr::null(), 0); d.err = AK_ERR_HOST; return; }
    let mut out = ak_dfix_ChunkElement::ZERO;
    #[allow(unused_variables)]
    let buf0 = d.buf;
    let base0 = base;
    // Optimisation D2: one 8-aligned arena shared by every loop slot below (one
    // slot is open at a time; each keeps its own element budget).
    let mut arena: [::core::mem::MaybeUninit<u64>; ak_rt::ARENA_BYTES / 8] =
        [const { ::core::mem::MaybeUninit::uninit() }; ak_rt::ARENA_BYTES / 8];
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_LABELS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_LABELS * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_labels: usize = 0;
    let a_labels: *mut ak_span = arena.as_mut_ptr() as *mut ak_span;
    #[allow(unused_macros)]
    macro_rules! at_labels { () => { a_labels.add(n_labels) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_ATTRS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_ChunkElementAttrsEntry>());
    const _: () = assert!(N_ATTRS * ::core::mem::size_of::<ak_dfix_ChunkElementAttrsEntry>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_ChunkElementAttrsEntry>() <= 8);
    let mut n_attrs: usize = 0;
    let a_attrs: *mut ak_dfix_ChunkElementAttrsEntry = arena.as_mut_ptr() as *mut ak_dfix_ChunkElementAttrsEntry;
    #[allow(unused_macros)]
    macro_rules! at_attrs { () => { a_attrs.add(n_attrs) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_INNER_MARKS: usize = ak_rt::arena_n(::core::mem::size_of::<i64>());
    const _: () = assert!(N_INNER_MARKS * ::core::mem::size_of::<i64>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<i64>() <= 8);
    let mut n_inner_marks: usize = 0;
    let a_inner_marks: *mut i64 = arena.as_mut_ptr() as *mut i64;
    #[allow(unused_macros)]
    macro_rules! at_inner_marks { () => { a_inner_marks.add(n_inner_marks) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_INNER_LEAVES: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_ChunkLeaf>());
    const _: () = assert!(N_INNER_LEAVES * ::core::mem::size_of::<ak_dfix_ChunkLeaf>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_ChunkLeaf>() <= 8);
    let mut n_inner_leaves: usize = 0;
    let a_inner_leaves: *mut ak_dfix_ChunkLeaf = arena.as_mut_ptr() as *mut ak_dfix_ChunkLeaf;
    #[allow(unused_macros)]
    macro_rules! at_inner_leaves { () => { a_inner_leaves.add(n_inner_leaves) }; }
    macro_rules! flush_labels {
        () => {
            if n_labels > 0 {
                if let Some(add) = (*vt).add_items_labels {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, tok, a_labels as *const ak_span, n_labels as i32);
                    }
                }
                n_labels = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_attrs {
        () => {
            if n_attrs > 0 {
                if let Some(add) = (*vt).add_items_attrs {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, tok, a_attrs as *const ak_dfix_ChunkElementAttrsEntry, n_attrs as i32);
                    }
                }
                n_attrs = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_inner_marks {
        () => {
            if n_inner_marks > 0 {
                if let Some(add) = (*vt).add_items_inner_marks {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, tok, a_inner_marks as *const i64, n_inner_marks as i32);
                    }
                }
                n_inner_marks = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_inner_leaves {
        () => {
            if n_inner_leaves > 0 {
                if let Some(add) = (*vt).add_items_inner_leaves {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, tok, a_inner_leaves as *const ak_dfix_ChunkLeaf, n_inner_leaves as i32);
                    }
                }
                n_inner_leaves = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_labels!();
            flush_attrs!();
            flush_inner_marks!();
            flush_inner_leaves!();
        };
    }
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_labels == N_LABELS { flush_labels!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_labels!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_labels += 1;
            }
            2 if wire == 2 => {
                if cur != 2 { flush!(); cur = 2; }
                if n_attrs == N_ATTRS { flush_attrs!(); }
                let (off, n) = d.len_body();
                let mut es = Dec::new(&buf0[off..off + n]);
                let p = at_attrs!();
                p.write(ak_dfix_ChunkElementAttrsEntry::ZERO);
                dec_chunk_element_attrs_entry_fix_into(&mut es, base0 + off, u.at(1), &mut *p);
                if es.err != 0 { d.err = es.err; }
                n_attrs += 1;
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            4 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_CHUNKELEMENT_PRESENT_INNER;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 3 { flush!(); cur = 3; }
                        if n_inner_marks == N_INNER_MARKS { flush_inner_marks!(); }
                        let v = c1.varint() as i64;
                        if c1.err == 0 { at_inner_marks!().write(v); n_inner_marks += 1; }
                    }
                    1 if wire == 2 => {
                        if cur != 3 { flush!(); cur = 3; }
                        let (off, n) = c1.len_body();
                        let mut ps = Dec::new(&buf1[off..off + n]);
                        while !ps.at_end() {
                            if n_inner_marks == N_INNER_MARKS { flush_inner_marks!(); }
                            let v = ps.varint() as i64;
                            if ps.err != 0 { break; }
                            at_inner_marks!().write(v);
                            n_inner_marks += 1;
                        }
                        if ps.err != 0 { c1.err = ps.err; }
                    }
                    2 if wire == 2 => {
                        if cur != 4 { flush!(); cur = 4; }
                        if n_inner_leaves == N_INNER_LEAVES { flush_inner_leaves!(); }
                        let (off, n) = c1.len_body();
                        let mut es = Dec::new(&buf1[off..off + n]);
                        let p = at_inner_leaves!();
                        p.write(ak_dfix_ChunkLeaf::ZERO);
                        dec_chunk_leaf_fix_into(&mut es, base1 + off, u.at(3), &mut *p);
                        if es.err != 0 { c1.err = es.err; }
                        n_inner_leaves += 1;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(2), &mut out.inner.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    if d.err == 0 {
        if let Some(ap) = (*vt).apply_items {
            ak_rt::bump!((*dcx).c, reverse);
            ap(ctx, obj, tok, &out);
            if (*dcx).hdr.err != AK_OK { d.err = (*dcx).hdr.err; }
        }
    }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_Timestamp(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_Timestamp,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 1 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_Timestamp::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.seconds = d.varint() as i64;
            }
            2 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.nanos = d.varint() as i32;
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_Duration(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_Duration,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 2 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_Duration::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.seconds = d.varint() as i64;
            }
            2 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.nanos = d.varint() as i32;
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_ResultRaw(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_ResultRaw,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 3 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_ResultRaw::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.session_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.name = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.owner_task_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            4 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.status = d.varint() as i32;
            }
            5 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_RESULTRAW_PRESENT_CREATED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.created_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.created_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(1), &mut out.created_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            6 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_RESULTRAW_PRESENT_COMPLETED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.completed_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.completed_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(2), &mut out.completed_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            8 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.result_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            9 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.size = d.varint() as i64;
            }
            10 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.created_by = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            11 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                out.opaque_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            12 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.manual_deletion = (d.varint() != 0) as u8;
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_TaskOptions(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_TaskOptions,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 4 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_TaskOptions::ZERO;
    // Optimisation D2: one 8-aligned arena shared by every loop slot below (one
    // slot is open at a time; each keeps its own element budget).
    let mut arena: [::core::mem::MaybeUninit<u64>; ak_rt::ARENA_BYTES / 8] =
        [const { ::core::mem::MaybeUninit::uninit() }; ak_rt::ARENA_BYTES / 8];
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_OPTIONS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>());
    const _: () = assert!(N_OPTIONS * ::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_TaskOptionsOptionsEntry>() <= 8);
    let mut n_options: usize = 0;
    let a_options: *mut ak_dfix_TaskOptionsOptionsEntry = arena.as_mut_ptr() as *mut ak_dfix_TaskOptionsOptionsEntry;
    #[allow(unused_macros)]
    macro_rules! at_options { () => { a_options.add(n_options) }; }
    macro_rules! flush_options {
        () => {
            if n_options > 0 {
                if let Some(add) = (*vt).add_options {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_options as *const ak_dfix_TaskOptionsOptionsEntry, n_options as i32);
                    }
                }
                n_options = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_options!();
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_options == N_OPTIONS { flush_options!(); }
                let (off, n) = d.len_body();
                let mut es = Dec::new(&buf0[off..off + n]);
                let p = at_options!();
                p.write(ak_dfix_TaskOptionsOptionsEntry::ZERO);
                dec_task_options_options_entry_fix_into(&mut es, base0 + off, u.at(1), &mut *p);
                if es.err != 0 { d.err = es.err; }
                n_options += 1;
            }
            2 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKOPTIONS_PRESENT_MAX_DURATION;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.max_duration.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.max_duration.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(2), &mut out.max_duration.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            3 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.max_retries = d.varint() as i32;
            }
            4 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.priority = d.varint() as i32;
            }
            5 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.partition_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            6 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.application_name = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            7 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.application_version = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            8 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.application_namespace = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            9 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.application_service = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            10 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.engine_type = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_TaskOutput(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_TaskOutput,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 5 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_TaskOutput::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.success = (d.varint() != 0) as u8;
            }
            2 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.error = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_TaskDetailed(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_TaskDetailed,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 6 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_TaskDetailed::ZERO;
    // Optimisation D2: one 8-aligned arena shared by every loop slot below (one
    // slot is open at a time; each keeps its own element budget).
    let mut arena: [::core::mem::MaybeUninit<u64>; ak_rt::ARENA_BYTES / 8] =
        [const { ::core::mem::MaybeUninit::uninit() }; ak_rt::ARENA_BYTES / 8];
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_PARENT_TASK_IDS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_PARENT_TASK_IDS * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_parent_task_ids: usize = 0;
    let a_parent_task_ids: *mut ak_span = arena.as_mut_ptr() as *mut ak_span;
    #[allow(unused_macros)]
    macro_rules! at_parent_task_ids { () => { a_parent_task_ids.add(n_parent_task_ids) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_DATA_DEPENDENCIES: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_DATA_DEPENDENCIES * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_data_dependencies: usize = 0;
    let a_data_dependencies: *mut ak_span = arena.as_mut_ptr() as *mut ak_span;
    #[allow(unused_macros)]
    macro_rules! at_data_dependencies { () => { a_data_dependencies.add(n_data_dependencies) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_EXPECTED_OUTPUT_IDS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_EXPECTED_OUTPUT_IDS * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_expected_output_ids: usize = 0;
    let a_expected_output_ids: *mut ak_span = arena.as_mut_ptr() as *mut ak_span;
    #[allow(unused_macros)]
    macro_rules! at_expected_output_ids { () => { a_expected_output_ids.add(n_expected_output_ids) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_RETRY_OF_IDS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_RETRY_OF_IDS * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_retry_of_ids: usize = 0;
    let a_retry_of_ids: *mut ak_span = arena.as_mut_ptr() as *mut ak_span;
    #[allow(unused_macros)]
    macro_rules! at_retry_of_ids { () => { a_retry_of_ids.add(n_retry_of_ids) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_OPTIONS_OPTIONS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>());
    const _: () = assert!(N_OPTIONS_OPTIONS * ::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_TaskOptionsOptionsEntry>() <= 8);
    let mut n_options_options: usize = 0;
    let a_options_options: *mut ak_dfix_TaskOptionsOptionsEntry = arena.as_mut_ptr() as *mut ak_dfix_TaskOptionsOptionsEntry;
    #[allow(unused_macros)]
    macro_rules! at_options_options { () => { a_options_options.add(n_options_options) }; }
    macro_rules! flush_parent_task_ids {
        () => {
            if n_parent_task_ids > 0 {
                if let Some(add) = (*vt).add_parent_task_ids {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_parent_task_ids as *const ak_span, n_parent_task_ids as i32);
                    }
                }
                n_parent_task_ids = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_data_dependencies {
        () => {
            if n_data_dependencies > 0 {
                if let Some(add) = (*vt).add_data_dependencies {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_data_dependencies as *const ak_span, n_data_dependencies as i32);
                    }
                }
                n_data_dependencies = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_expected_output_ids {
        () => {
            if n_expected_output_ids > 0 {
                if let Some(add) = (*vt).add_expected_output_ids {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_expected_output_ids as *const ak_span, n_expected_output_ids as i32);
                    }
                }
                n_expected_output_ids = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_retry_of_ids {
        () => {
            if n_retry_of_ids > 0 {
                if let Some(add) = (*vt).add_retry_of_ids {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_retry_of_ids as *const ak_span, n_retry_of_ids as i32);
                    }
                }
                n_retry_of_ids = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_options_options {
        () => {
            if n_options_options > 0 {
                if let Some(add) = (*vt).add_options_options {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_options_options as *const ak_dfix_TaskOptionsOptionsEntry, n_options_options as i32);
                    }
                }
                n_options_options = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_parent_task_ids!();
            flush_data_dependencies!();
            flush_expected_output_ids!();
            flush_retry_of_ids!();
            flush_options_options!();
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.session_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.owner_pod_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            4 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_parent_task_ids == N_PARENT_TASK_IDS { flush_parent_task_ids!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_parent_task_ids!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_parent_task_ids += 1;
            }
            5 if wire == 2 => {
                if cur != 2 { flush!(); cur = 2; }
                if n_data_dependencies == N_DATA_DEPENDENCIES { flush_data_dependencies!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_data_dependencies!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_data_dependencies += 1;
            }
            6 if wire == 2 => {
                if cur != 3 { flush!(); cur = 3; }
                if n_expected_output_ids == N_EXPECTED_OUTPUT_IDS { flush_expected_output_ids!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_expected_output_ids!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_expected_output_ids += 1;
            }
            7 if wire == 2 => {
                if cur != 4 { flush!(); cur = 4; }
                if n_retry_of_ids == N_RETRY_OF_IDS { flush_retry_of_ids!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_retry_of_ids!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_retry_of_ids += 1;
            }
            8 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.status = d.varint() as i32;
            }
            9 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.status_message = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            10 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_OPTIONS;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 2 => {
                        if cur != 5 { flush!(); cur = 5; }
                        if n_options_options == N_OPTIONS_OPTIONS { flush_options_options!(); }
                        let (off, n) = c1.len_body();
                        let mut es = Dec::new(&buf1[off..off + n]);
                        let p = at_options_options!();
                        p.write(ak_dfix_TaskOptionsOptionsEntry::ZERO);
                        dec_task_options_options_entry_fix_into(&mut es, base1 + off, u.at(2), &mut *p);
                        if es.err != 0 { c1.err = es.err; }
                        n_options_options += 1;
                    }
                    2 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (coff, clen) = c1.len_body();
                        let base2 = base1 + coff;
                        let buf2 = &buf1[coff..coff + clen];
                        let mut c2 = Dec::new(buf2);
                        out.options.presence |= AK_DFIX_TASKOPTIONS_PRESENT_MAX_DURATION;
                        while !c2.at_end() {
                            let s0 = c2.pos;
                            let k = c2.varint();
                            if c2.err != 0 { break; }
                            let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                            if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c2.err = ak_rt::ERR_MALFORMED; break; }
                            match tag {
                            1 if wire == 0 => {
                                if cur != 0 { flush!(); cur = 0; }
                                out.options.max_duration.seconds = c2.varint() as i64;
                            }
                            2 if wire == 0 => {
                                if cur != 0 { flush!(); cur = 0; }
                                out.options.max_duration.nanos = c2.varint() as i32;
                            }
                                _ => {
                                    c2.skip(tag, wire);
                                    // Decision 11: the inlined child's OWN buffer.
                                    if c2.err == 0 && !u.pos.is_null() {
                                        let rc = unk_put(u.at(3), &mut out.options.max_duration.unknown, &buf2[s0..c2.pos]);
                                        if rc != 0 { c2.err = rc; }
                                    }
                                }
                            }
                        }
                        if c2.err != 0 { c1.err = c2.err; }
                    }
                    3 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.options.max_retries = c1.varint() as i32;
                    }
                    4 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.options.priority = c1.varint() as i32;
                    }
                    5 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.partition_id = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    6 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_name = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    7 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_version = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    8 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_namespace = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    9 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_service = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    10 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.engine_type = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(1), &mut out.options.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            11 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_CREATED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.created_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.created_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(4), &mut out.created_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            12 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_SUBMITTED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.submitted_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.submitted_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(5), &mut out.submitted_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            13 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_STARTED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.started_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.started_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(6), &mut out.started_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            14 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_ENDED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.ended_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.ended_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(7), &mut out.ended_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            15 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_POD_TTL;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.pod_ttl.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.pod_ttl.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(8), &mut out.pod_ttl.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            16 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_OUTPUT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.output.success = (c1.varint() != 0) as u8;
                    }
                    2 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.output.error = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(9), &mut out.output.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            17 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.pod_hostname = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            18 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_RECEIVED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.received_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.received_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(10), &mut out.received_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            19 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_ACQUIRED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.acquired_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.acquired_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(11), &mut out.acquired_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            20 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_CREATION_TO_END_DURATION;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.creation_to_end_duration.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.creation_to_end_duration.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(12), &mut out.creation_to_end_duration.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            21 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_PROCESSING_TO_END_DURATION;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.processing_to_end_duration.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.processing_to_end_duration.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(13), &mut out.processing_to_end_duration.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            22 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.initial_task_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            23 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_RECEIVED_TO_END_DURATION;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.received_to_end_duration.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.received_to_end_duration.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(14), &mut out.received_to_end_duration.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            24 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_PROCESSED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.processed_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.processed_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(15), &mut out.processed_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            25 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_FETCHED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.fetched_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.fetched_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(16), &mut out.fetched_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            26 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.payload_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            27 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.created_by = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_TaskSummary(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_TaskSummary,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 7 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_TaskSummary::ZERO;
    // Optimisation D2: one 8-aligned arena shared by every loop slot below (one
    // slot is open at a time; each keeps its own element budget).
    let mut arena: [::core::mem::MaybeUninit<u64>; ak_rt::ARENA_BYTES / 8] =
        [const { ::core::mem::MaybeUninit::uninit() }; ak_rt::ARENA_BYTES / 8];
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_OPTIONS_OPTIONS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>());
    const _: () = assert!(N_OPTIONS_OPTIONS * ::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_TaskOptionsOptionsEntry>() <= 8);
    let mut n_options_options: usize = 0;
    let a_options_options: *mut ak_dfix_TaskOptionsOptionsEntry = arena.as_mut_ptr() as *mut ak_dfix_TaskOptionsOptionsEntry;
    #[allow(unused_macros)]
    macro_rules! at_options_options { () => { a_options_options.add(n_options_options) }; }
    macro_rules! flush_options_options {
        () => {
            if n_options_options > 0 {
                if let Some(add) = (*vt).add_options_options {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_options_options as *const ak_dfix_TaskOptionsOptionsEntry, n_options_options as i32);
                    }
                }
                n_options_options = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_options_options!();
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.session_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKSUMMARY_PRESENT_OPTIONS;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 2 => {
                        if cur != 1 { flush!(); cur = 1; }
                        if n_options_options == N_OPTIONS_OPTIONS { flush_options_options!(); }
                        let (off, n) = c1.len_body();
                        let mut es = Dec::new(&buf1[off..off + n]);
                        let p = at_options_options!();
                        p.write(ak_dfix_TaskOptionsOptionsEntry::ZERO);
                        dec_task_options_options_entry_fix_into(&mut es, base1 + off, u.at(2), &mut *p);
                        if es.err != 0 { c1.err = es.err; }
                        n_options_options += 1;
                    }
                    2 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (coff, clen) = c1.len_body();
                        let base2 = base1 + coff;
                        let buf2 = &buf1[coff..coff + clen];
                        let mut c2 = Dec::new(buf2);
                        out.options.presence |= AK_DFIX_TASKOPTIONS_PRESENT_MAX_DURATION;
                        while !c2.at_end() {
                            let s0 = c2.pos;
                            let k = c2.varint();
                            if c2.err != 0 { break; }
                            let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                            if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c2.err = ak_rt::ERR_MALFORMED; break; }
                            match tag {
                            1 if wire == 0 => {
                                if cur != 0 { flush!(); cur = 0; }
                                out.options.max_duration.seconds = c2.varint() as i64;
                            }
                            2 if wire == 0 => {
                                if cur != 0 { flush!(); cur = 0; }
                                out.options.max_duration.nanos = c2.varint() as i32;
                            }
                                _ => {
                                    c2.skip(tag, wire);
                                    // Decision 11: the inlined child's OWN buffer.
                                    if c2.err == 0 && !u.pos.is_null() {
                                        let rc = unk_put(u.at(3), &mut out.options.max_duration.unknown, &buf2[s0..c2.pos]);
                                        if rc != 0 { c2.err = rc; }
                                    }
                                }
                            }
                        }
                        if c2.err != 0 { c1.err = c2.err; }
                    }
                    3 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.options.max_retries = c1.varint() as i32;
                    }
                    4 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.options.priority = c1.varint() as i32;
                    }
                    5 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.partition_id = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    6 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_name = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    7 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_version = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    8 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_namespace = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    9 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_service = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    10 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.engine_type = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(1), &mut out.options.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            4 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.status = d.varint() as i32;
            }
            5 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKSUMMARY_PRESENT_CREATED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.created_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.created_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(4), &mut out.created_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            8 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.error = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            9 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.status_message = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            11 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.count_data_dependencies = d.varint() as i64;
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_Probe(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_Probe,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 8 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_Probe::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.opt_count = d.varint() as i32;
                out.presence |= AK_DFIX_PROBE_PRESENT_OPT_COUNT;
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.opt_label = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
                out.presence |= AK_DFIX_PROBE_PRESENT_OPT_LABEL;
            }
            4 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.opt_flag = (d.varint() != 0) as u8;
                out.presence |= AK_DFIX_PROBE_PRESENT_OPT_FLAG;
            }
            10 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.body_as_int = d.varint() as i64;
                // Last one wins: a later member replaces the case.
                out.body_case = 10;
            }
            11 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.body_as_text = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
                // Last one wins: a later member replaces the case.
                out.body_case = 11;
            }
            12 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                out.body_as_blob = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
                // Last one wins: a later member replaces the case.
                out.body_case = 12;
            }
            13 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                let mut os = Dec::new(&buf0[off..off + n]);
                // Plan rule: the SAME member again merges; another member,
                // or none, starts from empty.
                // Decision 11 rule 4: the oneof's ONE buffer moves, emptied, from the
                // previous message member's slot into this member's.
                if out.body_case != 13 {
                    // At most one member slot holds it (the invariant this keeps).
                    let mut k = ::core::mem::replace(&mut out.body_as_stamp.unknown, ak_unk_buf { data: ::core::ptr::null_mut(), len: 0, cap: 0 });
                    let t = ::core::mem::replace(&mut out.body_as_nothing.unknown, ak_unk_buf { data: ::core::ptr::null_mut(), len: 0, cap: 0 });
                    if !t.data.is_null() { k = t; }
                    out.body_as_stamp = ak_dfix_Timestamp::ZERO;
                    out.body_as_stamp.unknown = ak_unk_buf { data: k.data, len: 0, cap: k.cap };
                }
                dec_timestamp_fix_into(&mut os, base0 + off, u.at(1), &mut out.body_as_stamp);
                if os.err != 0 { d.err = os.err; }
                // Last one wins: a later member replaces the case.
                out.body_case = 13;
            }
            14 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                let mut os = Dec::new(&buf0[off..off + n]);
                // Plan rule: the SAME member again merges; another member,
                // or none, starts from empty.
                // Decision 11 rule 4: the oneof's ONE buffer moves, emptied, from the
                // previous message member's slot into this member's.
                if out.body_case != 14 {
                    // At most one member slot holds it (the invariant this keeps).
                    let mut k = ::core::mem::replace(&mut out.body_as_nothing.unknown, ak_unk_buf { data: ::core::ptr::null_mut(), len: 0, cap: 0 });
                    let t = ::core::mem::replace(&mut out.body_as_stamp.unknown, ak_unk_buf { data: ::core::ptr::null_mut(), len: 0, cap: 0 });
                    if !t.data.is_null() { k = t; }
                    out.body_as_nothing = ak_dfix_Empty::ZERO;
                    out.body_as_nothing.unknown = ak_unk_buf { data: k.data, len: 0, cap: k.cap };
                }
                dec_empty_fix_into(&mut os, base0 + off, u.at(1), &mut out.body_as_nothing);
                if os.err != 0 { d.err = os.err; }
                // Last one wins: a later member replaces the case.
                out.body_case = 14;
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_Empty(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_Empty,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 9 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_Empty::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_UploadResultData(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_UploadResultData,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 10 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_UploadResultData::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.session_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.result_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                out.data_chunk = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_MetricsBatch(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_MetricsBatch,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 11 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_MetricsBatch::ZERO;
    // Optimisation D2: one 8-aligned arena shared by every loop slot below (one
    // slot is open at a time; each keeps its own element budget).
    let mut arena: [::core::mem::MaybeUninit<u64>; ak_rt::ARENA_BYTES / 8] =
        [const { ::core::mem::MaybeUninit::uninit() }; ak_rt::ARENA_BYTES / 8];
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_TICKS: usize = ak_rt::arena_n(::core::mem::size_of::<i64>());
    const _: () = assert!(N_TICKS * ::core::mem::size_of::<i64>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<i64>() <= 8);
    let mut n_ticks: usize = 0;
    let a_ticks: *mut i64 = arena.as_mut_ptr() as *mut i64;
    #[allow(unused_macros)]
    macro_rules! at_ticks { () => { a_ticks.add(n_ticks) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_VALUES: usize = ak_rt::arena_n(::core::mem::size_of::<f64>());
    const _: () = assert!(N_VALUES * ::core::mem::size_of::<f64>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<f64>() <= 8);
    let mut n_values: usize = 0;
    let a_values: *mut f64 = arena.as_mut_ptr() as *mut f64;
    #[allow(unused_macros)]
    macro_rules! at_values { () => { a_values.add(n_values) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_CODES: usize = ak_rt::arena_n(::core::mem::size_of::<i32>());
    const _: () = assert!(N_CODES * ::core::mem::size_of::<i32>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<i32>() <= 8);
    let mut n_codes: usize = 0;
    let a_codes: *mut i32 = arena.as_mut_ptr() as *mut i32;
    #[allow(unused_macros)]
    macro_rules! at_codes { () => { a_codes.add(n_codes) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_FLAGS: usize = ak_rt::arena_n(::core::mem::size_of::<u8>());
    const _: () = assert!(N_FLAGS * ::core::mem::size_of::<u8>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<u8>() <= 8);
    let mut n_flags: usize = 0;
    let a_flags: *mut u8 = arena.as_mut_ptr() as *mut u8;
    #[allow(unused_macros)]
    macro_rules! at_flags { () => { a_flags.add(n_flags) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_STATUSES: usize = ak_rt::arena_n(::core::mem::size_of::<i32>());
    const _: () = assert!(N_STATUSES * ::core::mem::size_of::<i32>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<i32>() <= 8);
    let mut n_statuses: usize = 0;
    let a_statuses: *mut i32 = arena.as_mut_ptr() as *mut i32;
    #[allow(unused_macros)]
    macro_rules! at_statuses { () => { a_statuses.add(n_statuses) }; }
    macro_rules! flush_ticks {
        () => {
            if n_ticks > 0 {
                if let Some(add) = (*vt).add_ticks {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_ticks as *const i64, n_ticks as i32);
                    }
                }
                n_ticks = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_values {
        () => {
            if n_values > 0 {
                if let Some(add) = (*vt).add_values {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_values as *const f64, n_values as i32);
                    }
                }
                n_values = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_codes {
        () => {
            if n_codes > 0 {
                if let Some(add) = (*vt).add_codes {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_codes as *const i32, n_codes as i32);
                    }
                }
                n_codes = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_flags {
        () => {
            if n_flags > 0 {
                if let Some(add) = (*vt).add_flags {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_flags as *const u8, n_flags as i32);
                    }
                }
                n_flags = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_statuses {
        () => {
            if n_statuses > 0 {
                if let Some(add) = (*vt).add_statuses {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_statuses as *const i32, n_statuses as i32);
                    }
                }
                n_statuses = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_ticks!();
            flush_values!();
            flush_codes!();
            flush_flags!();
            flush_statuses!();
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 0 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_ticks == N_TICKS { flush_ticks!(); }
                let v = d.varint() as i64;
                if d.err == 0 { at_ticks!().write(v); n_ticks += 1; }
            }
            2 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                let (off, n) = d.len_body();
                let mut ps = Dec::new(&buf0[off..off + n]);
                while !ps.at_end() {
                    if n_ticks == N_TICKS { flush_ticks!(); }
                    let v = ps.varint() as i64;
                    if ps.err != 0 { break; }
                    at_ticks!().write(v);
                    n_ticks += 1;
                }
                if ps.err != 0 { d.err = ps.err; }
            }
            3 if wire == 1 => {
                if cur != 2 { flush!(); cur = 2; }
                if n_values == N_VALUES { flush_values!(); }
                let v = d.f64();
                if d.err == 0 { at_values!().write(v); n_values += 1; }
            }
            3 if wire == 2 => {
                if cur != 2 { flush!(); cur = 2; }
                let (off, n) = d.len_body();
                let mut ps = Dec::new(&buf0[off..off + n]);
                while !ps.at_end() {
                    if n_values == N_VALUES { flush_values!(); }
                    let v = ps.f64();
                    if ps.err != 0 { break; }
                    at_values!().write(v);
                    n_values += 1;
                }
                if ps.err != 0 { d.err = ps.err; }
            }
            4 if wire == 0 => {
                if cur != 3 { flush!(); cur = 3; }
                if n_codes == N_CODES { flush_codes!(); }
                let v = d.varint() as i32;
                if d.err == 0 { at_codes!().write(v); n_codes += 1; }
            }
            4 if wire == 2 => {
                if cur != 3 { flush!(); cur = 3; }
                let (off, n) = d.len_body();
                let mut ps = Dec::new(&buf0[off..off + n]);
                while !ps.at_end() {
                    if n_codes == N_CODES { flush_codes!(); }
                    let v = ps.varint() as i32;
                    if ps.err != 0 { break; }
                    at_codes!().write(v);
                    n_codes += 1;
                }
                if ps.err != 0 { d.err = ps.err; }
            }
            5 if wire == 0 => {
                if cur != 4 { flush!(); cur = 4; }
                if n_flags == N_FLAGS { flush_flags!(); }
                let v = (d.varint() != 0) as u8;
                if d.err == 0 { at_flags!().write(v); n_flags += 1; }
            }
            5 if wire == 2 => {
                if cur != 4 { flush!(); cur = 4; }
                let (off, n) = d.len_body();
                let mut ps = Dec::new(&buf0[off..off + n]);
                while !ps.at_end() {
                    if n_flags == N_FLAGS { flush_flags!(); }
                    let v = (ps.varint() != 0) as u8;
                    if ps.err != 0 { break; }
                    at_flags!().write(v);
                    n_flags += 1;
                }
                if ps.err != 0 { d.err = ps.err; }
            }
            6 if wire == 0 => {
                if cur != 5 { flush!(); cur = 5; }
                if n_statuses == N_STATUSES { flush_statuses!(); }
                let v = d.varint() as i32;
                if d.err == 0 { at_statuses!().write(v); n_statuses += 1; }
            }
            6 if wire == 2 => {
                if cur != 5 { flush!(); cur = 5; }
                let (off, n) = d.len_body();
                let mut ps = Dec::new(&buf0[off..off + n]);
                while !ps.at_end() {
                    if n_statuses == N_STATUSES { flush_statuses!(); }
                    let v = ps.varint() as i32;
                    if ps.err != 0 { break; }
                    at_statuses!().write(v);
                    n_statuses += 1;
                }
                if ps.err != 0 { d.err = ps.err; }
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_Pair(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_Pair,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 12 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_Pair::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.key = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.value = d.varint() as i32;
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_ListResultsResponse(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_ListResultsResponse,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 13 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_ListResultsResponse::ZERO;
    // Optimisation D2: one 8-aligned arena shared by every loop slot below (one
    // slot is open at a time; each keeps its own element budget).
    let mut arena: [::core::mem::MaybeUninit<u64>; ak_rt::ARENA_BYTES / 8] =
        [const { ::core::mem::MaybeUninit::uninit() }; ak_rt::ARENA_BYTES / 8];
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_RESULTS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_ResultRaw>());
    const _: () = assert!(N_RESULTS * ::core::mem::size_of::<ak_dfix_ResultRaw>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_ResultRaw>() <= 8);
    let mut n_results: usize = 0;
    let a_results: *mut ak_dfix_ResultRaw = arena.as_mut_ptr() as *mut ak_dfix_ResultRaw;
    #[allow(unused_macros)]
    macro_rules! at_results { () => { a_results.add(n_results) }; }
    macro_rules! flush_results {
        () => {
            if n_results > 0 {
                if let Some(add) = (*vt).add_results {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_results as *const ak_dfix_ResultRaw, n_results as i32);
                    }
                }
                n_results = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_results!();
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_results == N_RESULTS { flush_results!(); }
                let (off, n) = d.len_body();
                let mut es = Dec::new(&buf0[off..off + n]);
                let p = at_results!();
                p.write(ak_dfix_ResultRaw::ZERO);
                dec_result_raw_fix_into(&mut es, base0 + off, u.at(1), &mut *p);
                if es.err != 0 { d.err = es.err; }
                n_results += 1;
            }
            2 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.page = d.varint() as i32;
            }
            3 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.total = d.varint() as i32;
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_ListTasksDetailedResponse(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_ListTasksDetailedResponse,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 14 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_ListTasksDetailedResponse::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf0[off..off + n]);
                dec_list_tasks_detailed_response_tasks_element(ctx, dcx, obj, vt, &mut sub, base0 + off, u.at(1));
                if sub.err != 0 { d.err = sub.err; }
            }
            2 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.page = d.varint() as i32;
            }
            3 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.total = d.varint() as i32;
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_ListTaskSummaryResponse(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_ListTaskSummaryResponse,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 15 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_ListTaskSummaryResponse::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf0[off..off + n]);
                dec_list_task_summary_response_tasks_element(ctx, dcx, obj, vt, &mut sub, base0 + off, u.at(1));
                if sub.err != 0 { d.err = sub.err; }
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_ListProbeResponse(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_ListProbeResponse,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 16 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_ListProbeResponse::ZERO;
    // Optimisation D2: one 8-aligned arena shared by every loop slot below (one
    // slot is open at a time; each keeps its own element budget).
    let mut arena: [::core::mem::MaybeUninit<u64>; ak_rt::ARENA_BYTES / 8] =
        [const { ::core::mem::MaybeUninit::uninit() }; ak_rt::ARENA_BYTES / 8];
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_PROBES: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_Probe>());
    const _: () = assert!(N_PROBES * ::core::mem::size_of::<ak_dfix_Probe>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_Probe>() <= 8);
    let mut n_probes: usize = 0;
    let a_probes: *mut ak_dfix_Probe = arena.as_mut_ptr() as *mut ak_dfix_Probe;
    #[allow(unused_macros)]
    macro_rules! at_probes { () => { a_probes.add(n_probes) }; }
    macro_rules! flush_probes {
        () => {
            if n_probes > 0 {
                if let Some(add) = (*vt).add_probes {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_probes as *const ak_dfix_Probe, n_probes as i32);
                    }
                }
                n_probes = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_probes!();
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_probes == N_PROBES { flush_probes!(); }
                let (off, n) = d.len_body();
                let mut es = Dec::new(&buf0[off..off + n]);
                let p = at_probes!();
                p.write(ak_dfix_Probe::ZERO);
                dec_probe_fix_into(&mut es, base0 + off, u.at(1), &mut *p);
                if es.err != 0 { d.err = es.err; }
                n_probes += 1;
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_ListMetricsResponse(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_ListMetricsResponse,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 17 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_ListMetricsResponse::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf0[off..off + n]);
                dec_list_metrics_response_batches_element(ctx, dcx, obj, vt, &mut sub, base0 + off, u.at(1));
                if sub.err != 0 { d.err = sub.err; }
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_UploadResultDataMessage(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_UploadResultDataMessage,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 18 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_UploadResultDataMessage::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_UPLOADRESULTDATAMESSAGE_PRESENT_UPLOAD;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.upload.session_id = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    2 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.upload.result_id = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    3 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        out.upload.data_chunk = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(1), &mut out.upload.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_DualResponse(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_DualResponse,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 19 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_DualResponse::ZERO;
    // Optimisation D2: one 8-aligned arena shared by every loop slot below (one
    // slot is open at a time; each keeps its own element budget).
    let mut arena: [::core::mem::MaybeUninit<u64>; ak_rt::ARENA_BYTES / 8] =
        [const { ::core::mem::MaybeUninit::uninit() }; ak_rt::ARENA_BYTES / 8];
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_LEFT: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_Pair>());
    const _: () = assert!(N_LEFT * ::core::mem::size_of::<ak_dfix_Pair>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_Pair>() <= 8);
    let mut n_left: usize = 0;
    let a_left: *mut ak_dfix_Pair = arena.as_mut_ptr() as *mut ak_dfix_Pair;
    #[allow(unused_macros)]
    macro_rules! at_left { () => { a_left.add(n_left) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_RIGHT: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_Pair>());
    const _: () = assert!(N_RIGHT * ::core::mem::size_of::<ak_dfix_Pair>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_Pair>() <= 8);
    let mut n_right: usize = 0;
    let a_right: *mut ak_dfix_Pair = arena.as_mut_ptr() as *mut ak_dfix_Pair;
    #[allow(unused_macros)]
    macro_rules! at_right { () => { a_right.add(n_right) }; }
    macro_rules! flush_left {
        () => {
            if n_left > 0 {
                if let Some(add) = (*vt).add_left {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_left as *const ak_dfix_Pair, n_left as i32);
                    }
                }
                n_left = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_right {
        () => {
            if n_right > 0 {
                if let Some(add) = (*vt).add_right {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_right as *const ak_dfix_Pair, n_right as i32);
                    }
                }
                n_right = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_left!();
            flush_right!();
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_left == N_LEFT { flush_left!(); }
                let (off, n) = d.len_body();
                let mut es = Dec::new(&buf0[off..off + n]);
                let p = at_left!();
                p.write(ak_dfix_Pair::ZERO);
                dec_pair_fix_into(&mut es, base0 + off, u.at(1), &mut *p);
                if es.err != 0 { d.err = es.err; }
                n_left += 1;
            }
            2 if wire == 2 => {
                if cur != 2 { flush!(); cur = 2; }
                if n_right == N_RIGHT { flush_right!(); }
                let (off, n) = d.len_body();
                let mut es = Dec::new(&buf0[off..off + n]);
                let p = at_right!();
                p.write(ak_dfix_Pair::ZERO);
                dec_pair_fix_into(&mut es, base0 + off, u.at(2), &mut *p);
                if es.err != 0 { d.err = es.err; }
                n_right += 1;
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_ChunkLeaf(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_ChunkLeaf,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 20 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_ChunkLeaf::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.k = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.v = d.varint() as i32;
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_ChunkInner(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_ChunkInner,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 21 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_ChunkInner::ZERO;
    // Optimisation D2: one 8-aligned arena shared by every loop slot below (one
    // slot is open at a time; each keeps its own element budget).
    let mut arena: [::core::mem::MaybeUninit<u64>; ak_rt::ARENA_BYTES / 8] =
        [const { ::core::mem::MaybeUninit::uninit() }; ak_rt::ARENA_BYTES / 8];
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_MARKS: usize = ak_rt::arena_n(::core::mem::size_of::<i64>());
    const _: () = assert!(N_MARKS * ::core::mem::size_of::<i64>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<i64>() <= 8);
    let mut n_marks: usize = 0;
    let a_marks: *mut i64 = arena.as_mut_ptr() as *mut i64;
    #[allow(unused_macros)]
    macro_rules! at_marks { () => { a_marks.add(n_marks) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_LEAVES: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_ChunkLeaf>());
    const _: () = assert!(N_LEAVES * ::core::mem::size_of::<ak_dfix_ChunkLeaf>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_ChunkLeaf>() <= 8);
    let mut n_leaves: usize = 0;
    let a_leaves: *mut ak_dfix_ChunkLeaf = arena.as_mut_ptr() as *mut ak_dfix_ChunkLeaf;
    #[allow(unused_macros)]
    macro_rules! at_leaves { () => { a_leaves.add(n_leaves) }; }
    macro_rules! flush_marks {
        () => {
            if n_marks > 0 {
                if let Some(add) = (*vt).add_marks {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_marks as *const i64, n_marks as i32);
                    }
                }
                n_marks = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_leaves {
        () => {
            if n_leaves > 0 {
                if let Some(add) = (*vt).add_leaves {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_leaves as *const ak_dfix_ChunkLeaf, n_leaves as i32);
                    }
                }
                n_leaves = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_marks!();
            flush_leaves!();
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 0 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_marks == N_MARKS { flush_marks!(); }
                let v = d.varint() as i64;
                if d.err == 0 { at_marks!().write(v); n_marks += 1; }
            }
            1 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                let (off, n) = d.len_body();
                let mut ps = Dec::new(&buf0[off..off + n]);
                while !ps.at_end() {
                    if n_marks == N_MARKS { flush_marks!(); }
                    let v = ps.varint() as i64;
                    if ps.err != 0 { break; }
                    at_marks!().write(v);
                    n_marks += 1;
                }
                if ps.err != 0 { d.err = ps.err; }
            }
            2 if wire == 2 => {
                if cur != 2 { flush!(); cur = 2; }
                if n_leaves == N_LEAVES { flush_leaves!(); }
                let (off, n) = d.len_body();
                let mut es = Dec::new(&buf0[off..off + n]);
                let p = at_leaves!();
                p.write(ak_dfix_ChunkLeaf::ZERO);
                dec_chunk_leaf_fix_into(&mut es, base0 + off, u.at(1), &mut *p);
                if es.err != 0 { d.err = es.err; }
                n_leaves += 1;
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_ChunkElement(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_ChunkElement,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 22 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_ChunkElement::ZERO;
    // Optimisation D2: one 8-aligned arena shared by every loop slot below (one
    // slot is open at a time; each keeps its own element budget).
    let mut arena: [::core::mem::MaybeUninit<u64>; ak_rt::ARENA_BYTES / 8] =
        [const { ::core::mem::MaybeUninit::uninit() }; ak_rt::ARENA_BYTES / 8];
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_LABELS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_LABELS * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_labels: usize = 0;
    let a_labels: *mut ak_span = arena.as_mut_ptr() as *mut ak_span;
    #[allow(unused_macros)]
    macro_rules! at_labels { () => { a_labels.add(n_labels) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_ATTRS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_ChunkElementAttrsEntry>());
    const _: () = assert!(N_ATTRS * ::core::mem::size_of::<ak_dfix_ChunkElementAttrsEntry>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_ChunkElementAttrsEntry>() <= 8);
    let mut n_attrs: usize = 0;
    let a_attrs: *mut ak_dfix_ChunkElementAttrsEntry = arena.as_mut_ptr() as *mut ak_dfix_ChunkElementAttrsEntry;
    #[allow(unused_macros)]
    macro_rules! at_attrs { () => { a_attrs.add(n_attrs) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_INNER_MARKS: usize = ak_rt::arena_n(::core::mem::size_of::<i64>());
    const _: () = assert!(N_INNER_MARKS * ::core::mem::size_of::<i64>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<i64>() <= 8);
    let mut n_inner_marks: usize = 0;
    let a_inner_marks: *mut i64 = arena.as_mut_ptr() as *mut i64;
    #[allow(unused_macros)]
    macro_rules! at_inner_marks { () => { a_inner_marks.add(n_inner_marks) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_INNER_LEAVES: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_ChunkLeaf>());
    const _: () = assert!(N_INNER_LEAVES * ::core::mem::size_of::<ak_dfix_ChunkLeaf>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_ChunkLeaf>() <= 8);
    let mut n_inner_leaves: usize = 0;
    let a_inner_leaves: *mut ak_dfix_ChunkLeaf = arena.as_mut_ptr() as *mut ak_dfix_ChunkLeaf;
    #[allow(unused_macros)]
    macro_rules! at_inner_leaves { () => { a_inner_leaves.add(n_inner_leaves) }; }
    macro_rules! flush_labels {
        () => {
            if n_labels > 0 {
                if let Some(add) = (*vt).add_labels {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_labels as *const ak_span, n_labels as i32);
                    }
                }
                n_labels = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_attrs {
        () => {
            if n_attrs > 0 {
                if let Some(add) = (*vt).add_attrs {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_attrs as *const ak_dfix_ChunkElementAttrsEntry, n_attrs as i32);
                    }
                }
                n_attrs = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_inner_marks {
        () => {
            if n_inner_marks > 0 {
                if let Some(add) = (*vt).add_inner_marks {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_inner_marks as *const i64, n_inner_marks as i32);
                    }
                }
                n_inner_marks = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_inner_leaves {
        () => {
            if n_inner_leaves > 0 {
                if let Some(add) = (*vt).add_inner_leaves {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_inner_leaves as *const ak_dfix_ChunkLeaf, n_inner_leaves as i32);
                    }
                }
                n_inner_leaves = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_labels!();
            flush_attrs!();
            flush_inner_marks!();
            flush_inner_leaves!();
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_labels == N_LABELS { flush_labels!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_labels!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_labels += 1;
            }
            2 if wire == 2 => {
                if cur != 2 { flush!(); cur = 2; }
                if n_attrs == N_ATTRS { flush_attrs!(); }
                let (off, n) = d.len_body();
                let mut es = Dec::new(&buf0[off..off + n]);
                let p = at_attrs!();
                p.write(ak_dfix_ChunkElementAttrsEntry::ZERO);
                dec_chunk_element_attrs_entry_fix_into(&mut es, base0 + off, u.at(1), &mut *p);
                if es.err != 0 { d.err = es.err; }
                n_attrs += 1;
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            4 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_CHUNKELEMENT_PRESENT_INNER;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 3 { flush!(); cur = 3; }
                        if n_inner_marks == N_INNER_MARKS { flush_inner_marks!(); }
                        let v = c1.varint() as i64;
                        if c1.err == 0 { at_inner_marks!().write(v); n_inner_marks += 1; }
                    }
                    1 if wire == 2 => {
                        if cur != 3 { flush!(); cur = 3; }
                        let (off, n) = c1.len_body();
                        let mut ps = Dec::new(&buf1[off..off + n]);
                        while !ps.at_end() {
                            if n_inner_marks == N_INNER_MARKS { flush_inner_marks!(); }
                            let v = ps.varint() as i64;
                            if ps.err != 0 { break; }
                            at_inner_marks!().write(v);
                            n_inner_marks += 1;
                        }
                        if ps.err != 0 { c1.err = ps.err; }
                    }
                    2 if wire == 2 => {
                        if cur != 4 { flush!(); cur = 4; }
                        if n_inner_leaves == N_INNER_LEAVES { flush_inner_leaves!(); }
                        let (off, n) = c1.len_body();
                        let mut es = Dec::new(&buf1[off..off + n]);
                        let p = at_inner_leaves!();
                        p.write(ak_dfix_ChunkLeaf::ZERO);
                        dec_chunk_leaf_fix_into(&mut es, base1 + off, u.at(3), &mut *p);
                        if es.err != 0 { c1.err = es.err; }
                        n_inner_leaves += 1;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(2), &mut out.inner.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_ChunkedResponse(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_ChunkedResponse,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 23 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_ChunkedResponse::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            7 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf0[off..off + n]);
                dec_chunked_response_items_element(ctx, dcx, obj, vt, &mut sub, base0 + off, u.at(1));
                if sub.err != 0 { d.err = sub.err; }
            }
            8 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.page = d.varint() as i32;
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_ChunkedResponseWide(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_ChunkedResponseWide,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 24 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_ChunkedResponseWide::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            70000 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf0[off..off + n]);
                dec_chunked_response_wide_items_element(ctx, dcx, obj, vt, &mut sub, base0 + off, u.at(1));
                if sub.err != 0 { d.err = sub.err; }
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_LeafElement(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_LeafElement,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 25 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_LeafElement::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.n = d.varint() as i64;
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_LEAFELEMENT_PRESENT_STAMP;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.stamp.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.stamp.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(1), &mut out.stamp.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_LeafResponse(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_LeafResponse,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 26 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_LeafResponse::ZERO;
    // Optimisation D2: one 8-aligned arena shared by every loop slot below (one
    // slot is open at a time; each keeps its own element budget).
    let mut arena: [::core::mem::MaybeUninit<u64>; ak_rt::ARENA_BYTES / 8] =
        [const { ::core::mem::MaybeUninit::uninit() }; ak_rt::ARENA_BYTES / 8];
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_ITEMS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_LeafElement>());
    const _: () = assert!(N_ITEMS * ::core::mem::size_of::<ak_dfix_LeafElement>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_LeafElement>() <= 8);
    let mut n_items: usize = 0;
    let a_items: *mut ak_dfix_LeafElement = arena.as_mut_ptr() as *mut ak_dfix_LeafElement;
    #[allow(unused_macros)]
    macro_rules! at_items { () => { a_items.add(n_items) }; }
    macro_rules! flush_items {
        () => {
            if n_items > 0 {
                if let Some(add) = (*vt).add_items {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_items as *const ak_dfix_LeafElement, n_items as i32);
                    }
                }
                n_items = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_items!();
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            9 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_items == N_ITEMS { flush_items!(); }
                let (off, n) = d.len_body();
                let mut es = Dec::new(&buf0[off..off + n]);
                let p = at_items!();
                p.write(ak_dfix_LeafElement::ZERO);
                dec_leaf_element_fix_into(&mut es, base0 + off, u.at(1), &mut *p);
                if es.err != 0 { d.err = es.err; }
                n_items += 1;
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_Surrogate(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_Surrogate,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 27 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_Surrogate::ZERO;
    // Optimisation D2: one 8-aligned arena shared by every loop slot below (one
    // slot is open at a time; each keeps its own element budget).
    let mut arena: [::core::mem::MaybeUninit<u64>; ak_rt::ARENA_BYTES / 8] =
        [const { ::core::mem::MaybeUninit::uninit() }; ak_rt::ARENA_BYTES / 8];
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_ATTRS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_SurrogateAttrsEntry>());
    const _: () = assert!(N_ATTRS * ::core::mem::size_of::<ak_dfix_SurrogateAttrsEntry>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_SurrogateAttrsEntry>() <= 8);
    let mut n_attrs: usize = 0;
    let a_attrs: *mut ak_dfix_SurrogateAttrsEntry = arena.as_mut_ptr() as *mut ak_dfix_SurrogateAttrsEntry;
    #[allow(unused_macros)]
    macro_rules! at_attrs { () => { a_attrs.add(n_attrs) }; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_TEXTS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_TEXTS * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_texts: usize = 0;
    let a_texts: *mut ak_span = arena.as_mut_ptr() as *mut ak_span;
    #[allow(unused_macros)]
    macro_rules! at_texts { () => { a_texts.add(n_texts) }; }
    macro_rules! flush_attrs {
        () => {
            if n_attrs > 0 {
                if let Some(add) = (*vt).add_attrs {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_attrs as *const ak_dfix_SurrogateAttrsEntry, n_attrs as i32);
                    }
                }
                n_attrs = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush_texts {
        () => {
            if n_texts > 0 {
                if let Some(add) = (*vt).add_texts {
                    if (*dcx).hdr.err == AK_OK {
                        ak_rt::bump!((*dcx).c, reverse);
                        add(ctx, obj, AK_TOKEN_ROOT, a_texts as *const ak_span, n_texts as i32);
                    }
                }
                n_texts = 0;
                if (*dcx).hdr.err != AK_OK && d.err == 0 { d.err = (*dcx).hdr.err; }
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_attrs!();
            flush_texts!();
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.text = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_SURROGATE_PRESENT_NESTED;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.nested.text = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(1), &mut out.nested.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            3 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_attrs == N_ATTRS { flush_attrs!(); }
                let (off, n) = d.len_body();
                let mut es = Dec::new(&buf0[off..off + n]);
                let p = at_attrs!();
                p.write(ak_dfix_SurrogateAttrsEntry::ZERO);
                dec_surrogate_attrs_entry_fix_into(&mut es, base0 + off, u.at(2), &mut *p);
                if es.err != 0 { d.err = es.err; }
                n_attrs += 1;
            }
            4 if wire == 2 => {
                if cur != 2 { flush!(); cur = 2; }
                if n_texts == N_TEXTS { flush_texts!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_texts!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_texts += 1;
            }
            5 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                out.raw = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_SurrogateInner(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_SurrogateInner,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 28 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_SurrogateInner::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.text = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

#[no_mangle]
pub unsafe extern "C" fn ak_decode_WireZoo(
    ctx: *mut ak_dec_ctx,
    obj: *mut c_void,
    buf: *const u8,
    len: usize,
    vt: *const ak_dvt_WireZoo,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // ABI v1 section 5: the slot is sticky and the FIRST error wins, which is a rule
    // about ONE operation. A new decode starts here, so nothing can be lost by
    // clearing it and a context that carried a failure would otherwise poison every
    // later decode. Doing it here rather than in the host costs no extra crossing
    // and takes the obligation off the binding author.
    (*dcx).hdr.err = AK_OK;
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 29 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: spans are (u32, u32) into this buffer, so a buffer longer than
    // u32::MAX would alias offsets and lengths. Reject at entry rather than
    // truncate. `usize` is 64-bit on every host this ships to.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_WireZoo::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    // Decision 11: the positions this context is armed with, or none (drop mode:
    // every capture below is one null test). The context is bound to its root.
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.v_int32 = d.varint() as i32;
            }
            2 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.v_int64 = d.varint() as i64;
            }
            3 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.v_bool = (d.varint() != 0) as u8;
            }
            4 if wire == 1 => {
                if cur != 0 { flush!(); cur = 0; }
                out.v_double = d.f64();
            }
            5 if wire == 5 => {
                if cur != 0 { flush!(); cur = 0; }
                out.v_fixed32 = d.fixed32();
            }
            6 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.v_string = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            7 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                out.v_bytes = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            8 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.v_enum = d.varint() as i32;
            }
            9 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_WIREZOO_PRESENT_V_MSG;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.v_msg.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.v_msg.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(1), &mut out.v_msg.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            536870911 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.v_big_tag = d.varint() as i32;
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
    }
    // R-D6: `apply` only if nothing -- the reader or the host -- failed.
    if d.err == 0 && (*dcx).hdr.err == AK_OK {
        if let Some(apply) = (*vt).apply {
            ak_rt::bump!((*dcx).c, reverse);
            apply(ctx, obj, &out);
        }
    }
    // The host may have failed the operation from inside a reverse call; the
    // sticky slot in the context is where it said so (ABI v1 section 5).
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_Timestamp_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_TIMESTAMP: [(usize, bool); 1] = [
    (::core::mem::offset_of!(ak_dec_Timestamp_opts, self_), false),
];

/// Decision 11: re-arm every unknown-field position of `Timestamp` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_Timestamp(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_Timestamp_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 1 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_TIMESTAMP);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `Timestamp`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_Timestamp(opts: *mut ak_dec_Timestamp_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(1);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_TIMESTAMP);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_Duration_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_DURATION: [(usize, bool); 1] = [
    (::core::mem::offset_of!(ak_dec_Duration_opts, self_), false),
];

/// Decision 11: re-arm every unknown-field position of `Duration` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_Duration(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_Duration_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 2 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_DURATION);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `Duration`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_Duration(opts: *mut ak_dec_Duration_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(2);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_DURATION);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_ResultRaw_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_RESULTRAW: [(usize, bool); 3] = [
    (::core::mem::offset_of!(ak_dec_ResultRaw_opts, self_), false),
    (::core::mem::offset_of!(ak_dec_ResultRaw_opts, created_at), false),
    (::core::mem::offset_of!(ak_dec_ResultRaw_opts, completed_at), false),
];

/// Decision 11: re-arm every unknown-field position of `ResultRaw` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_ResultRaw(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_ResultRaw_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 3 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_RESULTRAW);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `ResultRaw`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_ResultRaw(opts: *mut ak_dec_ResultRaw_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(3);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_RESULTRAW);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_TaskOptions_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_TASKOPTIONS: [(usize, bool); 3] = [
    (::core::mem::offset_of!(ak_dec_TaskOptions_opts, self_), false),
    (::core::mem::offset_of!(ak_dec_TaskOptions_opts, options), true),
    (::core::mem::offset_of!(ak_dec_TaskOptions_opts, max_duration), false),
];

/// Decision 11: re-arm every unknown-field position of `TaskOptions` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_TaskOptions(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_TaskOptions_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 4 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_TASKOPTIONS);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `TaskOptions`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_TaskOptions(opts: *mut ak_dec_TaskOptions_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(4);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_TASKOPTIONS);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_TaskOutput_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_TASKOUTPUT: [(usize, bool); 1] = [
    (::core::mem::offset_of!(ak_dec_TaskOutput_opts, self_), false),
];

/// Decision 11: re-arm every unknown-field position of `TaskOutput` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_TaskOutput(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_TaskOutput_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 5 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_TASKOUTPUT);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `TaskOutput`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_TaskOutput(opts: *mut ak_dec_TaskOutput_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(5);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_TASKOUTPUT);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_TaskDetailed_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_TASKDETAILED: [(usize, bool); 17] = [
    (::core::mem::offset_of!(ak_dec_TaskDetailed_opts, self_), false),
    (::core::mem::offset_of!(ak_dec_TaskDetailed_opts, options), false),
    (::core::mem::offset_of!(ak_dec_TaskDetailed_opts, options_options), true),
    (::core::mem::offset_of!(ak_dec_TaskDetailed_opts, options_max_duration), false),
    (::core::mem::offset_of!(ak_dec_TaskDetailed_opts, created_at), false),
    (::core::mem::offset_of!(ak_dec_TaskDetailed_opts, submitted_at), false),
    (::core::mem::offset_of!(ak_dec_TaskDetailed_opts, started_at), false),
    (::core::mem::offset_of!(ak_dec_TaskDetailed_opts, ended_at), false),
    (::core::mem::offset_of!(ak_dec_TaskDetailed_opts, pod_ttl), false),
    (::core::mem::offset_of!(ak_dec_TaskDetailed_opts, output), false),
    (::core::mem::offset_of!(ak_dec_TaskDetailed_opts, received_at), false),
    (::core::mem::offset_of!(ak_dec_TaskDetailed_opts, acquired_at), false),
    (::core::mem::offset_of!(ak_dec_TaskDetailed_opts, creation_to_end_duration), false),
    (::core::mem::offset_of!(ak_dec_TaskDetailed_opts, processing_to_end_duration), false),
    (::core::mem::offset_of!(ak_dec_TaskDetailed_opts, received_to_end_duration), false),
    (::core::mem::offset_of!(ak_dec_TaskDetailed_opts, processed_at), false),
    (::core::mem::offset_of!(ak_dec_TaskDetailed_opts, fetched_at), false),
];

/// Decision 11: re-arm every unknown-field position of `TaskDetailed` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_TaskDetailed(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_TaskDetailed_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 6 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_TASKDETAILED);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `TaskDetailed`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_TaskDetailed(opts: *mut ak_dec_TaskDetailed_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(6);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_TASKDETAILED);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_TaskSummary_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_TASKSUMMARY: [(usize, bool); 5] = [
    (::core::mem::offset_of!(ak_dec_TaskSummary_opts, self_), false),
    (::core::mem::offset_of!(ak_dec_TaskSummary_opts, options), false),
    (::core::mem::offset_of!(ak_dec_TaskSummary_opts, options_options), true),
    (::core::mem::offset_of!(ak_dec_TaskSummary_opts, options_max_duration), false),
    (::core::mem::offset_of!(ak_dec_TaskSummary_opts, created_at), false),
];

/// Decision 11: re-arm every unknown-field position of `TaskSummary` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_TaskSummary(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_TaskSummary_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 7 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_TASKSUMMARY);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `TaskSummary`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_TaskSummary(opts: *mut ak_dec_TaskSummary_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(7);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_TASKSUMMARY);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_Probe_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_PROBE: [(usize, bool); 2] = [
    (::core::mem::offset_of!(ak_dec_Probe_opts, self_), false),
    (::core::mem::offset_of!(ak_dec_Probe_opts, body), false),
];

/// Decision 11: re-arm every unknown-field position of `Probe` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_Probe(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_Probe_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 8 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_PROBE);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `Probe`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_Probe(opts: *mut ak_dec_Probe_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(8);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_PROBE);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_Empty_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_EMPTY: [(usize, bool); 1] = [
    (::core::mem::offset_of!(ak_dec_Empty_opts, self_), false),
];

/// Decision 11: re-arm every unknown-field position of `Empty` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_Empty(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_Empty_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 9 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_EMPTY);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `Empty`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_Empty(opts: *mut ak_dec_Empty_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(9);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_EMPTY);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_UploadResultData_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_UPLOADRESULTDATA: [(usize, bool); 1] = [
    (::core::mem::offset_of!(ak_dec_UploadResultData_opts, self_), false),
];

/// Decision 11: re-arm every unknown-field position of `UploadResultData` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_UploadResultData(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_UploadResultData_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 10 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_UPLOADRESULTDATA);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `UploadResultData`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_UploadResultData(opts: *mut ak_dec_UploadResultData_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(10);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_UPLOADRESULTDATA);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_MetricsBatch_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_METRICSBATCH: [(usize, bool); 1] = [
    (::core::mem::offset_of!(ak_dec_MetricsBatch_opts, self_), false),
];

/// Decision 11: re-arm every unknown-field position of `MetricsBatch` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_MetricsBatch(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_MetricsBatch_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 11 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_METRICSBATCH);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `MetricsBatch`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_MetricsBatch(opts: *mut ak_dec_MetricsBatch_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(11);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_METRICSBATCH);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_Pair_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_PAIR: [(usize, bool); 1] = [
    (::core::mem::offset_of!(ak_dec_Pair_opts, self_), false),
];

/// Decision 11: re-arm every unknown-field position of `Pair` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_Pair(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_Pair_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 12 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_PAIR);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `Pair`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_Pair(opts: *mut ak_dec_Pair_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(12);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_PAIR);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_ListResultsResponse_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_LISTRESULTSRESPONSE: [(usize, bool); 4] = [
    (::core::mem::offset_of!(ak_dec_ListResultsResponse_opts, self_), false),
    (::core::mem::offset_of!(ak_dec_ListResultsResponse_opts, results), true),
    (::core::mem::offset_of!(ak_dec_ListResultsResponse_opts, results_created_at), true),
    (::core::mem::offset_of!(ak_dec_ListResultsResponse_opts, results_completed_at), true),
];

/// Decision 11: re-arm every unknown-field position of `ListResultsResponse` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_ListResultsResponse(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_ListResultsResponse_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 13 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_LISTRESULTSRESPONSE);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `ListResultsResponse`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_ListResultsResponse(opts: *mut ak_dec_ListResultsResponse_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(13);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_LISTRESULTSRESPONSE);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_ListTasksDetailedResponse_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_LISTTASKSDETAILEDRESPONSE: [(usize, bool); 18] = [
    (::core::mem::offset_of!(ak_dec_ListTasksDetailedResponse_opts, self_), false),
    (::core::mem::offset_of!(ak_dec_ListTasksDetailedResponse_opts, tasks), true),
    (::core::mem::offset_of!(ak_dec_ListTasksDetailedResponse_opts, tasks_options), true),
    (::core::mem::offset_of!(ak_dec_ListTasksDetailedResponse_opts, tasks_options_options), true),
    (::core::mem::offset_of!(ak_dec_ListTasksDetailedResponse_opts, tasks_options_max_duration), true),
    (::core::mem::offset_of!(ak_dec_ListTasksDetailedResponse_opts, tasks_created_at), true),
    (::core::mem::offset_of!(ak_dec_ListTasksDetailedResponse_opts, tasks_submitted_at), true),
    (::core::mem::offset_of!(ak_dec_ListTasksDetailedResponse_opts, tasks_started_at), true),
    (::core::mem::offset_of!(ak_dec_ListTasksDetailedResponse_opts, tasks_ended_at), true),
    (::core::mem::offset_of!(ak_dec_ListTasksDetailedResponse_opts, tasks_pod_ttl), true),
    (::core::mem::offset_of!(ak_dec_ListTasksDetailedResponse_opts, tasks_output), true),
    (::core::mem::offset_of!(ak_dec_ListTasksDetailedResponse_opts, tasks_received_at), true),
    (::core::mem::offset_of!(ak_dec_ListTasksDetailedResponse_opts, tasks_acquired_at), true),
    (::core::mem::offset_of!(ak_dec_ListTasksDetailedResponse_opts, tasks_creation_to_end_duration), true),
    (::core::mem::offset_of!(ak_dec_ListTasksDetailedResponse_opts, tasks_processing_to_end_duration), true),
    (::core::mem::offset_of!(ak_dec_ListTasksDetailedResponse_opts, tasks_received_to_end_duration), true),
    (::core::mem::offset_of!(ak_dec_ListTasksDetailedResponse_opts, tasks_processed_at), true),
    (::core::mem::offset_of!(ak_dec_ListTasksDetailedResponse_opts, tasks_fetched_at), true),
];

/// Decision 11: re-arm every unknown-field position of `ListTasksDetailedResponse` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_ListTasksDetailedResponse(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_ListTasksDetailedResponse_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 14 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_LISTTASKSDETAILEDRESPONSE);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `ListTasksDetailedResponse`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_ListTasksDetailedResponse(opts: *mut ak_dec_ListTasksDetailedResponse_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(14);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_LISTTASKSDETAILEDRESPONSE);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_ListTaskSummaryResponse_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_LISTTASKSUMMARYRESPONSE: [(usize, bool); 6] = [
    (::core::mem::offset_of!(ak_dec_ListTaskSummaryResponse_opts, self_), false),
    (::core::mem::offset_of!(ak_dec_ListTaskSummaryResponse_opts, tasks), true),
    (::core::mem::offset_of!(ak_dec_ListTaskSummaryResponse_opts, tasks_options), true),
    (::core::mem::offset_of!(ak_dec_ListTaskSummaryResponse_opts, tasks_options_options), true),
    (::core::mem::offset_of!(ak_dec_ListTaskSummaryResponse_opts, tasks_options_max_duration), true),
    (::core::mem::offset_of!(ak_dec_ListTaskSummaryResponse_opts, tasks_created_at), true),
];

/// Decision 11: re-arm every unknown-field position of `ListTaskSummaryResponse` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_ListTaskSummaryResponse(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_ListTaskSummaryResponse_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 15 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_LISTTASKSUMMARYRESPONSE);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `ListTaskSummaryResponse`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_ListTaskSummaryResponse(opts: *mut ak_dec_ListTaskSummaryResponse_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(15);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_LISTTASKSUMMARYRESPONSE);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_ListProbeResponse_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_LISTPROBERESPONSE: [(usize, bool); 3] = [
    (::core::mem::offset_of!(ak_dec_ListProbeResponse_opts, self_), false),
    (::core::mem::offset_of!(ak_dec_ListProbeResponse_opts, probes), true),
    (::core::mem::offset_of!(ak_dec_ListProbeResponse_opts, probes_body), true),
];

/// Decision 11: re-arm every unknown-field position of `ListProbeResponse` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_ListProbeResponse(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_ListProbeResponse_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 16 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_LISTPROBERESPONSE);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `ListProbeResponse`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_ListProbeResponse(opts: *mut ak_dec_ListProbeResponse_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(16);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_LISTPROBERESPONSE);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_ListMetricsResponse_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_LISTMETRICSRESPONSE: [(usize, bool); 2] = [
    (::core::mem::offset_of!(ak_dec_ListMetricsResponse_opts, self_), false),
    (::core::mem::offset_of!(ak_dec_ListMetricsResponse_opts, batches), true),
];

/// Decision 11: re-arm every unknown-field position of `ListMetricsResponse` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_ListMetricsResponse(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_ListMetricsResponse_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 17 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_LISTMETRICSRESPONSE);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `ListMetricsResponse`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_ListMetricsResponse(opts: *mut ak_dec_ListMetricsResponse_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(17);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_LISTMETRICSRESPONSE);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_UploadResultDataMessage_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_UPLOADRESULTDATAMESSAGE: [(usize, bool); 2] = [
    (::core::mem::offset_of!(ak_dec_UploadResultDataMessage_opts, self_), false),
    (::core::mem::offset_of!(ak_dec_UploadResultDataMessage_opts, upload), false),
];

/// Decision 11: re-arm every unknown-field position of `UploadResultDataMessage` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_UploadResultDataMessage(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_UploadResultDataMessage_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 18 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_UPLOADRESULTDATAMESSAGE);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `UploadResultDataMessage`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_UploadResultDataMessage(opts: *mut ak_dec_UploadResultDataMessage_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(18);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_UPLOADRESULTDATAMESSAGE);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_DualResponse_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_DUALRESPONSE: [(usize, bool); 3] = [
    (::core::mem::offset_of!(ak_dec_DualResponse_opts, self_), false),
    (::core::mem::offset_of!(ak_dec_DualResponse_opts, left), true),
    (::core::mem::offset_of!(ak_dec_DualResponse_opts, right), true),
];

/// Decision 11: re-arm every unknown-field position of `DualResponse` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_DualResponse(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_DualResponse_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 19 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_DUALRESPONSE);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `DualResponse`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_DualResponse(opts: *mut ak_dec_DualResponse_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(19);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_DUALRESPONSE);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_ChunkLeaf_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_CHUNKLEAF: [(usize, bool); 1] = [
    (::core::mem::offset_of!(ak_dec_ChunkLeaf_opts, self_), false),
];

/// Decision 11: re-arm every unknown-field position of `ChunkLeaf` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_ChunkLeaf(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_ChunkLeaf_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 20 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_CHUNKLEAF);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `ChunkLeaf`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_ChunkLeaf(opts: *mut ak_dec_ChunkLeaf_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(20);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_CHUNKLEAF);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_ChunkInner_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_CHUNKINNER: [(usize, bool); 2] = [
    (::core::mem::offset_of!(ak_dec_ChunkInner_opts, self_), false),
    (::core::mem::offset_of!(ak_dec_ChunkInner_opts, leaves), true),
];

/// Decision 11: re-arm every unknown-field position of `ChunkInner` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_ChunkInner(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_ChunkInner_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 21 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_CHUNKINNER);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `ChunkInner`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_ChunkInner(opts: *mut ak_dec_ChunkInner_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(21);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_CHUNKINNER);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_ChunkElement_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_CHUNKELEMENT: [(usize, bool); 4] = [
    (::core::mem::offset_of!(ak_dec_ChunkElement_opts, self_), false),
    (::core::mem::offset_of!(ak_dec_ChunkElement_opts, attrs), true),
    (::core::mem::offset_of!(ak_dec_ChunkElement_opts, inner), false),
    (::core::mem::offset_of!(ak_dec_ChunkElement_opts, inner_leaves), true),
];

/// Decision 11: re-arm every unknown-field position of `ChunkElement` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_ChunkElement(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_ChunkElement_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 22 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_CHUNKELEMENT);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `ChunkElement`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_ChunkElement(opts: *mut ak_dec_ChunkElement_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(22);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_CHUNKELEMENT);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_ChunkedResponse_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_CHUNKEDRESPONSE: [(usize, bool); 5] = [
    (::core::mem::offset_of!(ak_dec_ChunkedResponse_opts, self_), false),
    (::core::mem::offset_of!(ak_dec_ChunkedResponse_opts, items), true),
    (::core::mem::offset_of!(ak_dec_ChunkedResponse_opts, items_attrs), true),
    (::core::mem::offset_of!(ak_dec_ChunkedResponse_opts, items_inner), true),
    (::core::mem::offset_of!(ak_dec_ChunkedResponse_opts, items_inner_leaves), true),
];

/// Decision 11: re-arm every unknown-field position of `ChunkedResponse` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_ChunkedResponse(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_ChunkedResponse_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 23 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_CHUNKEDRESPONSE);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `ChunkedResponse`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_ChunkedResponse(opts: *mut ak_dec_ChunkedResponse_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(23);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_CHUNKEDRESPONSE);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_ChunkedResponseWide_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_CHUNKEDRESPONSEWIDE: [(usize, bool); 5] = [
    (::core::mem::offset_of!(ak_dec_ChunkedResponseWide_opts, self_), false),
    (::core::mem::offset_of!(ak_dec_ChunkedResponseWide_opts, items), true),
    (::core::mem::offset_of!(ak_dec_ChunkedResponseWide_opts, items_attrs), true),
    (::core::mem::offset_of!(ak_dec_ChunkedResponseWide_opts, items_inner), true),
    (::core::mem::offset_of!(ak_dec_ChunkedResponseWide_opts, items_inner_leaves), true),
];

/// Decision 11: re-arm every unknown-field position of `ChunkedResponseWide` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_ChunkedResponseWide(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_ChunkedResponseWide_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 24 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_CHUNKEDRESPONSEWIDE);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `ChunkedResponseWide`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_ChunkedResponseWide(opts: *mut ak_dec_ChunkedResponseWide_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(24);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_CHUNKEDRESPONSEWIDE);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_LeafElement_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_LEAFELEMENT: [(usize, bool); 2] = [
    (::core::mem::offset_of!(ak_dec_LeafElement_opts, self_), false),
    (::core::mem::offset_of!(ak_dec_LeafElement_opts, stamp), false),
];

/// Decision 11: re-arm every unknown-field position of `LeafElement` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_LeafElement(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_LeafElement_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 25 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_LEAFELEMENT);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `LeafElement`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_LeafElement(opts: *mut ak_dec_LeafElement_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(25);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_LEAFELEMENT);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_LeafResponse_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_LEAFRESPONSE: [(usize, bool); 3] = [
    (::core::mem::offset_of!(ak_dec_LeafResponse_opts, self_), false),
    (::core::mem::offset_of!(ak_dec_LeafResponse_opts, items), true),
    (::core::mem::offset_of!(ak_dec_LeafResponse_opts, items_stamp), true),
];

/// Decision 11: re-arm every unknown-field position of `LeafResponse` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_LeafResponse(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_LeafResponse_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 26 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_LEAFRESPONSE);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `LeafResponse`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_LeafResponse(opts: *mut ak_dec_LeafResponse_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(26);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_LEAFRESPONSE);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_Surrogate_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_SURROGATE: [(usize, bool); 3] = [
    (::core::mem::offset_of!(ak_dec_Surrogate_opts, self_), false),
    (::core::mem::offset_of!(ak_dec_Surrogate_opts, nested), false),
    (::core::mem::offset_of!(ak_dec_Surrogate_opts, attrs), true),
];

/// Decision 11: re-arm every unknown-field position of `Surrogate` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_Surrogate(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_Surrogate_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 27 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_SURROGATE);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `Surrogate`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_Surrogate(opts: *mut ak_dec_Surrogate_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(27);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_SURROGATE);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_SurrogateInner_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_SURROGATEINNER: [(usize, bool); 1] = [
    (::core::mem::offset_of!(ak_dec_SurrogateInner_opts, self_), false),
];

/// Decision 11: re-arm every unknown-field position of `SurrogateInner` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_SurrogateInner(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_SurrogateInner_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 28 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_SURROGATEINNER);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `SurrogateInner`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_SurrogateInner(opts: *mut ak_dec_SurrogateInner_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(28);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_SURROGATEINNER);
    ctx
}

/// Decision 11 rule 1: where each position's entry sits in `ak_dec_WireZoo_opts`, and whether it
/// is a pool; the core reads the host's struct IN PLACE through these offsets.
const UNK_LAYOUT_WIREZOO: [(usize, bool); 2] = [
    (::core::mem::offset_of!(ak_dec_WireZoo_opts, self_), false),
    (::core::mem::offset_of!(ak_dec_WireZoo_opts, v_msg), false),
];

/// Decision 11: re-arm every unknown-field position of `WireZoo` from `opts`, read in
/// place (NULL = drop everywhere). Rule 6: only a context bound to this root.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_reset_WireZoo(ctx: *mut ak_dec_ctx, opts: *mut ak_dec_WireZoo_opts) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    if ctx.is_null() { return AK_ERR_INVALID_STATE; }
    let dcx = ctx as *mut DecCtxImpl;
    if (*dcx).root != 29 { return AK_ERR_INVALID_STATE; }
    crate::unk_arm(dcx, opts as *mut u8, &UNK_LAYOUT_WIREZOO);
    AK_OK
}

/// Decision 11 rule 6: a decode context BOUND to `WireZoo`, armed with `opts`.
#[no_mangle]
pub unsafe extern "C" fn ak_dec_ctx_new_WireZoo(opts: *mut ak_dec_WireZoo_opts) -> *mut ak_dec_ctx {
    let ctx = crate::dec_ctx_alloc(29);
    crate::unk_arm(ctx as *mut DecCtxImpl, opts as *mut u8, &UNK_LAYOUT_WIREZOO);
    ctx
}

// ====================================================================================
// The PULL family (ABI v1 section 7.1). `ak_parse_*` makes NO reverse call:
// it deposits into the host-owned context's record buffer, and the host reads
// it afterwards with `ak_bdr_drain` or `ak_bdr_ptr`. On the JVM that is what
// lets a parse run inside a critical section without first copying the wire
// buffer into native scratch; on a host whose reverse call is cheap it is a
// materialisation nobody needs, and the measurement says which is which.
//
// The traversal is `dec_walk`, shared verbatim with the push family above.
// ====================================================================================

/// Pull form of the non-leaf element run. `new` becomes a minted token and
/// an OP_NEW record, `apply` becomes an OP_APPLY_ELEM record; the host's
/// replay does what its `new_tasks`/`apply_tasks` would have done.
unsafe fn dec_list_tasks_detailed_response_tasks_element_pull(
    dcx: *mut DecCtxImpl,
    d: &mut Dec,
    base: usize,
    u: UnkCx,
) {
    // The codec MINTS the token because there is nobody to ask during a parse.
    // A token is an index (ABI v1 section 10), and the host's replay pushes its
    // elements in the order the records arrive, so index i names the same object
    // on both sides without either holding an address.
    let tok = (*dcx).bdr.mint();
    (*dcx).bdr.push(ak_rt::bdr::OP_NEW, 65536, tok, 0, ::core::ptr::null(), 0);
    let mut out = ak_dfix_TaskDetailed::ZERO;
    #[allow(unused_variables)]
    let buf0 = d.buf;
    let base0 = base;
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_PARENT_TASK_IDS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_PARENT_TASK_IDS * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_parent_task_ids: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_parent_task_ids: *mut ak_span = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_parent_task_ids { () => {{ if n_parent_task_ids == 0 { a_parent_task_ids = (*dcx).bdr.open_run(N_PARENT_TASK_IDS * ::core::mem::size_of::<ak_span>()) as *mut ak_span; } a_parent_task_ids.add(n_parent_task_ids) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_DATA_DEPENDENCIES: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_DATA_DEPENDENCIES * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_data_dependencies: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_data_dependencies: *mut ak_span = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_data_dependencies { () => {{ if n_data_dependencies == 0 { a_data_dependencies = (*dcx).bdr.open_run(N_DATA_DEPENDENCIES * ::core::mem::size_of::<ak_span>()) as *mut ak_span; } a_data_dependencies.add(n_data_dependencies) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_EXPECTED_OUTPUT_IDS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_EXPECTED_OUTPUT_IDS * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_expected_output_ids: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_expected_output_ids: *mut ak_span = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_expected_output_ids { () => {{ if n_expected_output_ids == 0 { a_expected_output_ids = (*dcx).bdr.open_run(N_EXPECTED_OUTPUT_IDS * ::core::mem::size_of::<ak_span>()) as *mut ak_span; } a_expected_output_ids.add(n_expected_output_ids) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_RETRY_OF_IDS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_RETRY_OF_IDS * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_retry_of_ids: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_retry_of_ids: *mut ak_span = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_retry_of_ids { () => {{ if n_retry_of_ids == 0 { a_retry_of_ids = (*dcx).bdr.open_run(N_RETRY_OF_IDS * ::core::mem::size_of::<ak_span>()) as *mut ak_span; } a_retry_of_ids.add(n_retry_of_ids) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_OPTIONS_OPTIONS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>());
    const _: () = assert!(N_OPTIONS_OPTIONS * ::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_TaskOptionsOptionsEntry>() <= 8);
    let mut n_options_options: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_options_options: *mut ak_dfix_TaskOptionsOptionsEntry = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_options_options { () => {{ if n_options_options == 0 { a_options_options = (*dcx).bdr.open_run(N_OPTIONS_OPTIONS * ::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>()) as *mut ak_dfix_TaskOptionsOptionsEntry; } a_options_options.add(n_options_options) }}; }
    macro_rules! flush_parent_task_ids {
        () => {
            if n_parent_task_ids > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    65537,
                    tok,
                    n_parent_task_ids as u32,
                    n_parent_task_ids * ::core::mem::size_of::<ak_span>(),
                );
                n_parent_task_ids = 0;
            }
        };
    }
    macro_rules! flush_data_dependencies {
        () => {
            if n_data_dependencies > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    65538,
                    tok,
                    n_data_dependencies as u32,
                    n_data_dependencies * ::core::mem::size_of::<ak_span>(),
                );
                n_data_dependencies = 0;
            }
        };
    }
    macro_rules! flush_expected_output_ids {
        () => {
            if n_expected_output_ids > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    65539,
                    tok,
                    n_expected_output_ids as u32,
                    n_expected_output_ids * ::core::mem::size_of::<ak_span>(),
                );
                n_expected_output_ids = 0;
            }
        };
    }
    macro_rules! flush_retry_of_ids {
        () => {
            if n_retry_of_ids > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    65540,
                    tok,
                    n_retry_of_ids as u32,
                    n_retry_of_ids * ::core::mem::size_of::<ak_span>(),
                );
                n_retry_of_ids = 0;
            }
        };
    }
    macro_rules! flush_options_options {
        () => {
            if n_options_options > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    65541,
                    tok,
                    n_options_options as u32,
                    n_options_options * ::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>(),
                );
                n_options_options = 0;
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_parent_task_ids!();
            flush_data_dependencies!();
            flush_expected_output_ids!();
            flush_retry_of_ids!();
            flush_options_options!();
        };
    }
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.session_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.owner_pod_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            4 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_parent_task_ids == N_PARENT_TASK_IDS { flush_parent_task_ids!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_parent_task_ids!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_parent_task_ids += 1;
            }
            5 if wire == 2 => {
                if cur != 2 { flush!(); cur = 2; }
                if n_data_dependencies == N_DATA_DEPENDENCIES { flush_data_dependencies!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_data_dependencies!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_data_dependencies += 1;
            }
            6 if wire == 2 => {
                if cur != 3 { flush!(); cur = 3; }
                if n_expected_output_ids == N_EXPECTED_OUTPUT_IDS { flush_expected_output_ids!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_expected_output_ids!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_expected_output_ids += 1;
            }
            7 if wire == 2 => {
                if cur != 4 { flush!(); cur = 4; }
                if n_retry_of_ids == N_RETRY_OF_IDS { flush_retry_of_ids!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_retry_of_ids!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_retry_of_ids += 1;
            }
            8 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.status = d.varint() as i32;
            }
            9 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.status_message = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            10 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_OPTIONS;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 2 => {
                        if cur != 5 { flush!(); cur = 5; }
                        if n_options_options == N_OPTIONS_OPTIONS { flush_options_options!(); }
                        let (off, n) = c1.len_body();
                        let mut es = Dec::new(&buf1[off..off + n]);
                        let p = at_options_options!();
                        p.write(ak_dfix_TaskOptionsOptionsEntry::ZERO);
                        dec_task_options_options_entry_fix_into(&mut es, base1 + off, u.at(2), &mut *p);
                        if es.err != 0 { c1.err = es.err; }
                        n_options_options += 1;
                    }
                    2 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (coff, clen) = c1.len_body();
                        let base2 = base1 + coff;
                        let buf2 = &buf1[coff..coff + clen];
                        let mut c2 = Dec::new(buf2);
                        out.options.presence |= AK_DFIX_TASKOPTIONS_PRESENT_MAX_DURATION;
                        while !c2.at_end() {
                            let s0 = c2.pos;
                            let k = c2.varint();
                            if c2.err != 0 { break; }
                            let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                            if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c2.err = ak_rt::ERR_MALFORMED; break; }
                            match tag {
                            1 if wire == 0 => {
                                if cur != 0 { flush!(); cur = 0; }
                                out.options.max_duration.seconds = c2.varint() as i64;
                            }
                            2 if wire == 0 => {
                                if cur != 0 { flush!(); cur = 0; }
                                out.options.max_duration.nanos = c2.varint() as i32;
                            }
                                _ => {
                                    c2.skip(tag, wire);
                                    // Decision 11: the inlined child's OWN buffer.
                                    if c2.err == 0 && !u.pos.is_null() {
                                        let rc = unk_put(u.at(3), &mut out.options.max_duration.unknown, &buf2[s0..c2.pos]);
                                        if rc != 0 { c2.err = rc; }
                                    }
                                }
                            }
                        }
                        if c2.err != 0 { c1.err = c2.err; }
                    }
                    3 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.options.max_retries = c1.varint() as i32;
                    }
                    4 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.options.priority = c1.varint() as i32;
                    }
                    5 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.partition_id = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    6 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_name = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    7 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_version = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    8 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_namespace = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    9 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_service = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    10 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.engine_type = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(1), &mut out.options.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            11 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_CREATED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.created_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.created_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(4), &mut out.created_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            12 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_SUBMITTED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.submitted_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.submitted_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(5), &mut out.submitted_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            13 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_STARTED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.started_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.started_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(6), &mut out.started_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            14 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_ENDED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.ended_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.ended_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(7), &mut out.ended_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            15 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_POD_TTL;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.pod_ttl.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.pod_ttl.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(8), &mut out.pod_ttl.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            16 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_OUTPUT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.output.success = (c1.varint() != 0) as u8;
                    }
                    2 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.output.error = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(9), &mut out.output.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            17 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.pod_hostname = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            18 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_RECEIVED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.received_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.received_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(10), &mut out.received_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            19 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_ACQUIRED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.acquired_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.acquired_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(11), &mut out.acquired_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            20 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_CREATION_TO_END_DURATION;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.creation_to_end_duration.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.creation_to_end_duration.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(12), &mut out.creation_to_end_duration.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            21 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_PROCESSING_TO_END_DURATION;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.processing_to_end_duration.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.processing_to_end_duration.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(13), &mut out.processing_to_end_duration.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            22 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.initial_task_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            23 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_RECEIVED_TO_END_DURATION;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.received_to_end_duration.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.received_to_end_duration.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(14), &mut out.received_to_end_duration.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            24 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_PROCESSED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.processed_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.processed_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(15), &mut out.processed_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            25 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_FETCHED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.fetched_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.fetched_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(16), &mut out.fetched_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            26 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.payload_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            27 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.created_by = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY_ELEM,
            65536,
            tok,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_TaskDetailed>(),
        );
    }
}

/// Pull form of the non-leaf element run. `new` becomes a minted token and
/// an OP_NEW record, `apply` becomes an OP_APPLY_ELEM record; the host's
/// replay does what its `new_tasks`/`apply_tasks` would have done.
unsafe fn dec_list_task_summary_response_tasks_element_pull(
    dcx: *mut DecCtxImpl,
    d: &mut Dec,
    base: usize,
    u: UnkCx,
) {
    // The codec MINTS the token because there is nobody to ask during a parse.
    // A token is an index (ABI v1 section 10), and the host's replay pushes its
    // elements in the order the records arrive, so index i names the same object
    // on both sides without either holding an address.
    let tok = (*dcx).bdr.mint();
    (*dcx).bdr.push(ak_rt::bdr::OP_NEW, 65536, tok, 0, ::core::ptr::null(), 0);
    let mut out = ak_dfix_TaskSummary::ZERO;
    #[allow(unused_variables)]
    let buf0 = d.buf;
    let base0 = base;
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_OPTIONS_OPTIONS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>());
    const _: () = assert!(N_OPTIONS_OPTIONS * ::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_TaskOptionsOptionsEntry>() <= 8);
    let mut n_options_options: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_options_options: *mut ak_dfix_TaskOptionsOptionsEntry = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_options_options { () => {{ if n_options_options == 0 { a_options_options = (*dcx).bdr.open_run(N_OPTIONS_OPTIONS * ::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>()) as *mut ak_dfix_TaskOptionsOptionsEntry; } a_options_options.add(n_options_options) }}; }
    macro_rules! flush_options_options {
        () => {
            if n_options_options > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    65537,
                    tok,
                    n_options_options as u32,
                    n_options_options * ::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>(),
                );
                n_options_options = 0;
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_options_options!();
        };
    }
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.session_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKSUMMARY_PRESENT_OPTIONS;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 2 => {
                        if cur != 1 { flush!(); cur = 1; }
                        if n_options_options == N_OPTIONS_OPTIONS { flush_options_options!(); }
                        let (off, n) = c1.len_body();
                        let mut es = Dec::new(&buf1[off..off + n]);
                        let p = at_options_options!();
                        p.write(ak_dfix_TaskOptionsOptionsEntry::ZERO);
                        dec_task_options_options_entry_fix_into(&mut es, base1 + off, u.at(2), &mut *p);
                        if es.err != 0 { c1.err = es.err; }
                        n_options_options += 1;
                    }
                    2 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (coff, clen) = c1.len_body();
                        let base2 = base1 + coff;
                        let buf2 = &buf1[coff..coff + clen];
                        let mut c2 = Dec::new(buf2);
                        out.options.presence |= AK_DFIX_TASKOPTIONS_PRESENT_MAX_DURATION;
                        while !c2.at_end() {
                            let s0 = c2.pos;
                            let k = c2.varint();
                            if c2.err != 0 { break; }
                            let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                            if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c2.err = ak_rt::ERR_MALFORMED; break; }
                            match tag {
                            1 if wire == 0 => {
                                if cur != 0 { flush!(); cur = 0; }
                                out.options.max_duration.seconds = c2.varint() as i64;
                            }
                            2 if wire == 0 => {
                                if cur != 0 { flush!(); cur = 0; }
                                out.options.max_duration.nanos = c2.varint() as i32;
                            }
                                _ => {
                                    c2.skip(tag, wire);
                                    // Decision 11: the inlined child's OWN buffer.
                                    if c2.err == 0 && !u.pos.is_null() {
                                        let rc = unk_put(u.at(3), &mut out.options.max_duration.unknown, &buf2[s0..c2.pos]);
                                        if rc != 0 { c2.err = rc; }
                                    }
                                }
                            }
                        }
                        if c2.err != 0 { c1.err = c2.err; }
                    }
                    3 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.options.max_retries = c1.varint() as i32;
                    }
                    4 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.options.priority = c1.varint() as i32;
                    }
                    5 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.partition_id = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    6 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_name = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    7 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_version = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    8 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_namespace = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    9 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_service = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    10 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.engine_type = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(1), &mut out.options.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            4 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.status = d.varint() as i32;
            }
            5 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKSUMMARY_PRESENT_CREATED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.created_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.created_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(4), &mut out.created_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            8 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.error = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            9 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.status_message = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            11 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.count_data_dependencies = d.varint() as i64;
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY_ELEM,
            65536,
            tok,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_TaskSummary>(),
        );
    }
}

/// Pull form of the non-leaf element run. `new` becomes a minted token and
/// an OP_NEW record, `apply` becomes an OP_APPLY_ELEM record; the host's
/// replay does what its `new_batches`/`apply_batches` would have done.
unsafe fn dec_list_metrics_response_batches_element_pull(
    dcx: *mut DecCtxImpl,
    d: &mut Dec,
    base: usize,
    u: UnkCx,
) {
    // The codec MINTS the token because there is nobody to ask during a parse.
    // A token is an index (ABI v1 section 10), and the host's replay pushes its
    // elements in the order the records arrive, so index i names the same object
    // on both sides without either holding an address.
    let tok = (*dcx).bdr.mint();
    (*dcx).bdr.push(ak_rt::bdr::OP_NEW, 65536, tok, 0, ::core::ptr::null(), 0);
    let mut out = ak_dfix_MetricsBatch::ZERO;
    #[allow(unused_variables)]
    let buf0 = d.buf;
    let base0 = base;
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_TICKS: usize = ak_rt::arena_n(::core::mem::size_of::<i64>());
    const _: () = assert!(N_TICKS * ::core::mem::size_of::<i64>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<i64>() <= 8);
    let mut n_ticks: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_ticks: *mut i64 = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_ticks { () => {{ if n_ticks == 0 { a_ticks = (*dcx).bdr.open_run(N_TICKS * ::core::mem::size_of::<i64>()) as *mut i64; } a_ticks.add(n_ticks) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_VALUES: usize = ak_rt::arena_n(::core::mem::size_of::<f64>());
    const _: () = assert!(N_VALUES * ::core::mem::size_of::<f64>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<f64>() <= 8);
    let mut n_values: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_values: *mut f64 = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_values { () => {{ if n_values == 0 { a_values = (*dcx).bdr.open_run(N_VALUES * ::core::mem::size_of::<f64>()) as *mut f64; } a_values.add(n_values) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_CODES: usize = ak_rt::arena_n(::core::mem::size_of::<i32>());
    const _: () = assert!(N_CODES * ::core::mem::size_of::<i32>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<i32>() <= 8);
    let mut n_codes: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_codes: *mut i32 = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_codes { () => {{ if n_codes == 0 { a_codes = (*dcx).bdr.open_run(N_CODES * ::core::mem::size_of::<i32>()) as *mut i32; } a_codes.add(n_codes) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_FLAGS: usize = ak_rt::arena_n(::core::mem::size_of::<u8>());
    const _: () = assert!(N_FLAGS * ::core::mem::size_of::<u8>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<u8>() <= 8);
    let mut n_flags: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_flags: *mut u8 = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_flags { () => {{ if n_flags == 0 { a_flags = (*dcx).bdr.open_run(N_FLAGS * ::core::mem::size_of::<u8>()) as *mut u8; } a_flags.add(n_flags) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_STATUSES: usize = ak_rt::arena_n(::core::mem::size_of::<i32>());
    const _: () = assert!(N_STATUSES * ::core::mem::size_of::<i32>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<i32>() <= 8);
    let mut n_statuses: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_statuses: *mut i32 = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_statuses { () => {{ if n_statuses == 0 { a_statuses = (*dcx).bdr.open_run(N_STATUSES * ::core::mem::size_of::<i32>()) as *mut i32; } a_statuses.add(n_statuses) }}; }
    macro_rules! flush_ticks {
        () => {
            if n_ticks > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    65537,
                    tok,
                    n_ticks as u32,
                    n_ticks * ::core::mem::size_of::<i64>(),
                );
                n_ticks = 0;
            }
        };
    }
    macro_rules! flush_values {
        () => {
            if n_values > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    65538,
                    tok,
                    n_values as u32,
                    n_values * ::core::mem::size_of::<f64>(),
                );
                n_values = 0;
            }
        };
    }
    macro_rules! flush_codes {
        () => {
            if n_codes > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    65539,
                    tok,
                    n_codes as u32,
                    n_codes * ::core::mem::size_of::<i32>(),
                );
                n_codes = 0;
            }
        };
    }
    macro_rules! flush_flags {
        () => {
            if n_flags > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    65540,
                    tok,
                    n_flags as u32,
                    n_flags * ::core::mem::size_of::<u8>(),
                );
                n_flags = 0;
            }
        };
    }
    macro_rules! flush_statuses {
        () => {
            if n_statuses > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    65541,
                    tok,
                    n_statuses as u32,
                    n_statuses * ::core::mem::size_of::<i32>(),
                );
                n_statuses = 0;
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_ticks!();
            flush_values!();
            flush_codes!();
            flush_flags!();
            flush_statuses!();
        };
    }
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 0 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_ticks == N_TICKS { flush_ticks!(); }
                let v = d.varint() as i64;
                if d.err == 0 { at_ticks!().write(v); n_ticks += 1; }
            }
            2 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                let (off, n) = d.len_body();
                let mut ps = Dec::new(&buf0[off..off + n]);
                while !ps.at_end() {
                    if n_ticks == N_TICKS { flush_ticks!(); }
                    let v = ps.varint() as i64;
                    if ps.err != 0 { break; }
                    at_ticks!().write(v);
                    n_ticks += 1;
                }
                if ps.err != 0 { d.err = ps.err; }
            }
            3 if wire == 1 => {
                if cur != 2 { flush!(); cur = 2; }
                if n_values == N_VALUES { flush_values!(); }
                let v = d.f64();
                if d.err == 0 { at_values!().write(v); n_values += 1; }
            }
            3 if wire == 2 => {
                if cur != 2 { flush!(); cur = 2; }
                let (off, n) = d.len_body();
                let mut ps = Dec::new(&buf0[off..off + n]);
                while !ps.at_end() {
                    if n_values == N_VALUES { flush_values!(); }
                    let v = ps.f64();
                    if ps.err != 0 { break; }
                    at_values!().write(v);
                    n_values += 1;
                }
                if ps.err != 0 { d.err = ps.err; }
            }
            4 if wire == 0 => {
                if cur != 3 { flush!(); cur = 3; }
                if n_codes == N_CODES { flush_codes!(); }
                let v = d.varint() as i32;
                if d.err == 0 { at_codes!().write(v); n_codes += 1; }
            }
            4 if wire == 2 => {
                if cur != 3 { flush!(); cur = 3; }
                let (off, n) = d.len_body();
                let mut ps = Dec::new(&buf0[off..off + n]);
                while !ps.at_end() {
                    if n_codes == N_CODES { flush_codes!(); }
                    let v = ps.varint() as i32;
                    if ps.err != 0 { break; }
                    at_codes!().write(v);
                    n_codes += 1;
                }
                if ps.err != 0 { d.err = ps.err; }
            }
            5 if wire == 0 => {
                if cur != 4 { flush!(); cur = 4; }
                if n_flags == N_FLAGS { flush_flags!(); }
                let v = (d.varint() != 0) as u8;
                if d.err == 0 { at_flags!().write(v); n_flags += 1; }
            }
            5 if wire == 2 => {
                if cur != 4 { flush!(); cur = 4; }
                let (off, n) = d.len_body();
                let mut ps = Dec::new(&buf0[off..off + n]);
                while !ps.at_end() {
                    if n_flags == N_FLAGS { flush_flags!(); }
                    let v = (ps.varint() != 0) as u8;
                    if ps.err != 0 { break; }
                    at_flags!().write(v);
                    n_flags += 1;
                }
                if ps.err != 0 { d.err = ps.err; }
            }
            6 if wire == 0 => {
                if cur != 5 { flush!(); cur = 5; }
                if n_statuses == N_STATUSES { flush_statuses!(); }
                let v = d.varint() as i32;
                if d.err == 0 { at_statuses!().write(v); n_statuses += 1; }
            }
            6 if wire == 2 => {
                if cur != 5 { flush!(); cur = 5; }
                let (off, n) = d.len_body();
                let mut ps = Dec::new(&buf0[off..off + n]);
                while !ps.at_end() {
                    if n_statuses == N_STATUSES { flush_statuses!(); }
                    let v = ps.varint() as i32;
                    if ps.err != 0 { break; }
                    at_statuses!().write(v);
                    n_statuses += 1;
                }
                if ps.err != 0 { d.err = ps.err; }
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY_ELEM,
            65536,
            tok,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_MetricsBatch>(),
        );
    }
}

/// Pull form of the non-leaf element run. `new` becomes a minted token and
/// an OP_NEW record, `apply` becomes an OP_APPLY_ELEM record; the host's
/// replay does what its `new_items`/`apply_items` would have done.
unsafe fn dec_chunked_response_items_element_pull(
    dcx: *mut DecCtxImpl,
    d: &mut Dec,
    base: usize,
    u: UnkCx,
) {
    // The codec MINTS the token because there is nobody to ask during a parse.
    // A token is an index (ABI v1 section 10), and the host's replay pushes its
    // elements in the order the records arrive, so index i names the same object
    // on both sides without either holding an address.
    let tok = (*dcx).bdr.mint();
    (*dcx).bdr.push(ak_rt::bdr::OP_NEW, 65536, tok, 0, ::core::ptr::null(), 0);
    let mut out = ak_dfix_ChunkElement::ZERO;
    #[allow(unused_variables)]
    let buf0 = d.buf;
    let base0 = base;
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_LABELS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_LABELS * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_labels: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_labels: *mut ak_span = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_labels { () => {{ if n_labels == 0 { a_labels = (*dcx).bdr.open_run(N_LABELS * ::core::mem::size_of::<ak_span>()) as *mut ak_span; } a_labels.add(n_labels) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_ATTRS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_ChunkElementAttrsEntry>());
    const _: () = assert!(N_ATTRS * ::core::mem::size_of::<ak_dfix_ChunkElementAttrsEntry>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_ChunkElementAttrsEntry>() <= 8);
    let mut n_attrs: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_attrs: *mut ak_dfix_ChunkElementAttrsEntry = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_attrs { () => {{ if n_attrs == 0 { a_attrs = (*dcx).bdr.open_run(N_ATTRS * ::core::mem::size_of::<ak_dfix_ChunkElementAttrsEntry>()) as *mut ak_dfix_ChunkElementAttrsEntry; } a_attrs.add(n_attrs) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_INNER_MARKS: usize = ak_rt::arena_n(::core::mem::size_of::<i64>());
    const _: () = assert!(N_INNER_MARKS * ::core::mem::size_of::<i64>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<i64>() <= 8);
    let mut n_inner_marks: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_inner_marks: *mut i64 = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_inner_marks { () => {{ if n_inner_marks == 0 { a_inner_marks = (*dcx).bdr.open_run(N_INNER_MARKS * ::core::mem::size_of::<i64>()) as *mut i64; } a_inner_marks.add(n_inner_marks) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_INNER_LEAVES: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_ChunkLeaf>());
    const _: () = assert!(N_INNER_LEAVES * ::core::mem::size_of::<ak_dfix_ChunkLeaf>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_ChunkLeaf>() <= 8);
    let mut n_inner_leaves: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_inner_leaves: *mut ak_dfix_ChunkLeaf = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_inner_leaves { () => {{ if n_inner_leaves == 0 { a_inner_leaves = (*dcx).bdr.open_run(N_INNER_LEAVES * ::core::mem::size_of::<ak_dfix_ChunkLeaf>()) as *mut ak_dfix_ChunkLeaf; } a_inner_leaves.add(n_inner_leaves) }}; }
    macro_rules! flush_labels {
        () => {
            if n_labels > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    65537,
                    tok,
                    n_labels as u32,
                    n_labels * ::core::mem::size_of::<ak_span>(),
                );
                n_labels = 0;
            }
        };
    }
    macro_rules! flush_attrs {
        () => {
            if n_attrs > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    65538,
                    tok,
                    n_attrs as u32,
                    n_attrs * ::core::mem::size_of::<ak_dfix_ChunkElementAttrsEntry>(),
                );
                n_attrs = 0;
            }
        };
    }
    macro_rules! flush_inner_marks {
        () => {
            if n_inner_marks > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    65539,
                    tok,
                    n_inner_marks as u32,
                    n_inner_marks * ::core::mem::size_of::<i64>(),
                );
                n_inner_marks = 0;
            }
        };
    }
    macro_rules! flush_inner_leaves {
        () => {
            if n_inner_leaves > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    65540,
                    tok,
                    n_inner_leaves as u32,
                    n_inner_leaves * ::core::mem::size_of::<ak_dfix_ChunkLeaf>(),
                );
                n_inner_leaves = 0;
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_labels!();
            flush_attrs!();
            flush_inner_marks!();
            flush_inner_leaves!();
        };
    }
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_labels == N_LABELS { flush_labels!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_labels!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_labels += 1;
            }
            2 if wire == 2 => {
                if cur != 2 { flush!(); cur = 2; }
                if n_attrs == N_ATTRS { flush_attrs!(); }
                let (off, n) = d.len_body();
                let mut es = Dec::new(&buf0[off..off + n]);
                let p = at_attrs!();
                p.write(ak_dfix_ChunkElementAttrsEntry::ZERO);
                dec_chunk_element_attrs_entry_fix_into(&mut es, base0 + off, u.at(1), &mut *p);
                if es.err != 0 { d.err = es.err; }
                n_attrs += 1;
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            4 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_CHUNKELEMENT_PRESENT_INNER;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 3 { flush!(); cur = 3; }
                        if n_inner_marks == N_INNER_MARKS { flush_inner_marks!(); }
                        let v = c1.varint() as i64;
                        if c1.err == 0 { at_inner_marks!().write(v); n_inner_marks += 1; }
                    }
                    1 if wire == 2 => {
                        if cur != 3 { flush!(); cur = 3; }
                        let (off, n) = c1.len_body();
                        let mut ps = Dec::new(&buf1[off..off + n]);
                        while !ps.at_end() {
                            if n_inner_marks == N_INNER_MARKS { flush_inner_marks!(); }
                            let v = ps.varint() as i64;
                            if ps.err != 0 { break; }
                            at_inner_marks!().write(v);
                            n_inner_marks += 1;
                        }
                        if ps.err != 0 { c1.err = ps.err; }
                    }
                    2 if wire == 2 => {
                        if cur != 4 { flush!(); cur = 4; }
                        if n_inner_leaves == N_INNER_LEAVES { flush_inner_leaves!(); }
                        let (off, n) = c1.len_body();
                        let mut es = Dec::new(&buf1[off..off + n]);
                        let p = at_inner_leaves!();
                        p.write(ak_dfix_ChunkLeaf::ZERO);
                        dec_chunk_leaf_fix_into(&mut es, base1 + off, u.at(3), &mut *p);
                        if es.err != 0 { c1.err = es.err; }
                        n_inner_leaves += 1;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(2), &mut out.inner.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY_ELEM,
            65536,
            tok,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_ChunkElement>(),
        );
    }
}

/// Pull form of the non-leaf element run. `new` becomes a minted token and
/// an OP_NEW record, `apply` becomes an OP_APPLY_ELEM record; the host's
/// replay does what its `new_items`/`apply_items` would have done.
unsafe fn dec_chunked_response_wide_items_element_pull(
    dcx: *mut DecCtxImpl,
    d: &mut Dec,
    base: usize,
    u: UnkCx,
) {
    // The codec MINTS the token because there is nobody to ask during a parse.
    // A token is an index (ABI v1 section 10), and the host's replay pushes its
    // elements in the order the records arrive, so index i names the same object
    // on both sides without either holding an address.
    let tok = (*dcx).bdr.mint();
    (*dcx).bdr.push(ak_rt::bdr::OP_NEW, 65536, tok, 0, ::core::ptr::null(), 0);
    let mut out = ak_dfix_ChunkElement::ZERO;
    #[allow(unused_variables)]
    let buf0 = d.buf;
    let base0 = base;
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_LABELS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_LABELS * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_labels: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_labels: *mut ak_span = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_labels { () => {{ if n_labels == 0 { a_labels = (*dcx).bdr.open_run(N_LABELS * ::core::mem::size_of::<ak_span>()) as *mut ak_span; } a_labels.add(n_labels) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_ATTRS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_ChunkElementAttrsEntry>());
    const _: () = assert!(N_ATTRS * ::core::mem::size_of::<ak_dfix_ChunkElementAttrsEntry>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_ChunkElementAttrsEntry>() <= 8);
    let mut n_attrs: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_attrs: *mut ak_dfix_ChunkElementAttrsEntry = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_attrs { () => {{ if n_attrs == 0 { a_attrs = (*dcx).bdr.open_run(N_ATTRS * ::core::mem::size_of::<ak_dfix_ChunkElementAttrsEntry>()) as *mut ak_dfix_ChunkElementAttrsEntry; } a_attrs.add(n_attrs) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_INNER_MARKS: usize = ak_rt::arena_n(::core::mem::size_of::<i64>());
    const _: () = assert!(N_INNER_MARKS * ::core::mem::size_of::<i64>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<i64>() <= 8);
    let mut n_inner_marks: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_inner_marks: *mut i64 = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_inner_marks { () => {{ if n_inner_marks == 0 { a_inner_marks = (*dcx).bdr.open_run(N_INNER_MARKS * ::core::mem::size_of::<i64>()) as *mut i64; } a_inner_marks.add(n_inner_marks) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_INNER_LEAVES: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_ChunkLeaf>());
    const _: () = assert!(N_INNER_LEAVES * ::core::mem::size_of::<ak_dfix_ChunkLeaf>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_ChunkLeaf>() <= 8);
    let mut n_inner_leaves: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_inner_leaves: *mut ak_dfix_ChunkLeaf = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_inner_leaves { () => {{ if n_inner_leaves == 0 { a_inner_leaves = (*dcx).bdr.open_run(N_INNER_LEAVES * ::core::mem::size_of::<ak_dfix_ChunkLeaf>()) as *mut ak_dfix_ChunkLeaf; } a_inner_leaves.add(n_inner_leaves) }}; }
    macro_rules! flush_labels {
        () => {
            if n_labels > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    65537,
                    tok,
                    n_labels as u32,
                    n_labels * ::core::mem::size_of::<ak_span>(),
                );
                n_labels = 0;
            }
        };
    }
    macro_rules! flush_attrs {
        () => {
            if n_attrs > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    65538,
                    tok,
                    n_attrs as u32,
                    n_attrs * ::core::mem::size_of::<ak_dfix_ChunkElementAttrsEntry>(),
                );
                n_attrs = 0;
            }
        };
    }
    macro_rules! flush_inner_marks {
        () => {
            if n_inner_marks > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    65539,
                    tok,
                    n_inner_marks as u32,
                    n_inner_marks * ::core::mem::size_of::<i64>(),
                );
                n_inner_marks = 0;
            }
        };
    }
    macro_rules! flush_inner_leaves {
        () => {
            if n_inner_leaves > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    65540,
                    tok,
                    n_inner_leaves as u32,
                    n_inner_leaves * ::core::mem::size_of::<ak_dfix_ChunkLeaf>(),
                );
                n_inner_leaves = 0;
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_labels!();
            flush_attrs!();
            flush_inner_marks!();
            flush_inner_leaves!();
        };
    }
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_labels == N_LABELS { flush_labels!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_labels!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_labels += 1;
            }
            2 if wire == 2 => {
                if cur != 2 { flush!(); cur = 2; }
                if n_attrs == N_ATTRS { flush_attrs!(); }
                let (off, n) = d.len_body();
                let mut es = Dec::new(&buf0[off..off + n]);
                let p = at_attrs!();
                p.write(ak_dfix_ChunkElementAttrsEntry::ZERO);
                dec_chunk_element_attrs_entry_fix_into(&mut es, base0 + off, u.at(1), &mut *p);
                if es.err != 0 { d.err = es.err; }
                n_attrs += 1;
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            4 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_CHUNKELEMENT_PRESENT_INNER;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 3 { flush!(); cur = 3; }
                        if n_inner_marks == N_INNER_MARKS { flush_inner_marks!(); }
                        let v = c1.varint() as i64;
                        if c1.err == 0 { at_inner_marks!().write(v); n_inner_marks += 1; }
                    }
                    1 if wire == 2 => {
                        if cur != 3 { flush!(); cur = 3; }
                        let (off, n) = c1.len_body();
                        let mut ps = Dec::new(&buf1[off..off + n]);
                        while !ps.at_end() {
                            if n_inner_marks == N_INNER_MARKS { flush_inner_marks!(); }
                            let v = ps.varint() as i64;
                            if ps.err != 0 { break; }
                            at_inner_marks!().write(v);
                            n_inner_marks += 1;
                        }
                        if ps.err != 0 { c1.err = ps.err; }
                    }
                    2 if wire == 2 => {
                        if cur != 4 { flush!(); cur = 4; }
                        if n_inner_leaves == N_INNER_LEAVES { flush_inner_leaves!(); }
                        let (off, n) = c1.len_body();
                        let mut es = Dec::new(&buf1[off..off + n]);
                        let p = at_inner_leaves!();
                        p.write(ak_dfix_ChunkLeaf::ZERO);
                        dec_chunk_leaf_fix_into(&mut es, base1 + off, u.at(3), &mut *p);
                        if es.err != 0 { c1.err = es.err; }
                        n_inner_leaves += 1;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(2), &mut out.inner.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY_ELEM,
            65536,
            tok,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_ChunkElement>(),
        );
    }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_Timestamp(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 1 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_Timestamp::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.seconds = d.varint() as i64;
            }
            2 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.nanos = d.varint() as i32;
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_Timestamp>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_Duration(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 2 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_Duration::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.seconds = d.varint() as i64;
            }
            2 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.nanos = d.varint() as i32;
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_Duration>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_ResultRaw(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 3 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_ResultRaw::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.session_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.name = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.owner_task_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            4 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.status = d.varint() as i32;
            }
            5 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_RESULTRAW_PRESENT_CREATED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.created_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.created_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(1), &mut out.created_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            6 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_RESULTRAW_PRESENT_COMPLETED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.completed_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.completed_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(2), &mut out.completed_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            8 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.result_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            9 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.size = d.varint() as i64;
            }
            10 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.created_by = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            11 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                out.opaque_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            12 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.manual_deletion = (d.varint() != 0) as u8;
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_ResultRaw>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_TaskOptions(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 4 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_TaskOptions::ZERO;
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_OPTIONS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>());
    const _: () = assert!(N_OPTIONS * ::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_TaskOptionsOptionsEntry>() <= 8);
    let mut n_options: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_options: *mut ak_dfix_TaskOptionsOptionsEntry = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_options { () => {{ if n_options == 0 { a_options = (*dcx).bdr.open_run(N_OPTIONS * ::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>()) as *mut ak_dfix_TaskOptionsOptionsEntry; } a_options.add(n_options) }}; }
    macro_rules! flush_options {
        () => {
            if n_options > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    1,
                    AK_TOKEN_ROOT,
                    n_options as u32,
                    n_options * ::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>(),
                );
                n_options = 0;
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_options!();
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_options == N_OPTIONS { flush_options!(); }
                let (off, n) = d.len_body();
                let mut es = Dec::new(&buf0[off..off + n]);
                let p = at_options!();
                p.write(ak_dfix_TaskOptionsOptionsEntry::ZERO);
                dec_task_options_options_entry_fix_into(&mut es, base0 + off, u.at(1), &mut *p);
                if es.err != 0 { d.err = es.err; }
                n_options += 1;
            }
            2 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKOPTIONS_PRESENT_MAX_DURATION;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.max_duration.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.max_duration.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(2), &mut out.max_duration.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            3 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.max_retries = d.varint() as i32;
            }
            4 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.priority = d.varint() as i32;
            }
            5 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.partition_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            6 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.application_name = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            7 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.application_version = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            8 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.application_namespace = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            9 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.application_service = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            10 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.engine_type = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_TaskOptions>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_TaskOutput(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 5 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_TaskOutput::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.success = (d.varint() != 0) as u8;
            }
            2 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.error = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_TaskOutput>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_TaskDetailed(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 6 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_TaskDetailed::ZERO;
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_PARENT_TASK_IDS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_PARENT_TASK_IDS * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_parent_task_ids: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_parent_task_ids: *mut ak_span = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_parent_task_ids { () => {{ if n_parent_task_ids == 0 { a_parent_task_ids = (*dcx).bdr.open_run(N_PARENT_TASK_IDS * ::core::mem::size_of::<ak_span>()) as *mut ak_span; } a_parent_task_ids.add(n_parent_task_ids) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_DATA_DEPENDENCIES: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_DATA_DEPENDENCIES * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_data_dependencies: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_data_dependencies: *mut ak_span = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_data_dependencies { () => {{ if n_data_dependencies == 0 { a_data_dependencies = (*dcx).bdr.open_run(N_DATA_DEPENDENCIES * ::core::mem::size_of::<ak_span>()) as *mut ak_span; } a_data_dependencies.add(n_data_dependencies) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_EXPECTED_OUTPUT_IDS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_EXPECTED_OUTPUT_IDS * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_expected_output_ids: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_expected_output_ids: *mut ak_span = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_expected_output_ids { () => {{ if n_expected_output_ids == 0 { a_expected_output_ids = (*dcx).bdr.open_run(N_EXPECTED_OUTPUT_IDS * ::core::mem::size_of::<ak_span>()) as *mut ak_span; } a_expected_output_ids.add(n_expected_output_ids) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_RETRY_OF_IDS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_RETRY_OF_IDS * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_retry_of_ids: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_retry_of_ids: *mut ak_span = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_retry_of_ids { () => {{ if n_retry_of_ids == 0 { a_retry_of_ids = (*dcx).bdr.open_run(N_RETRY_OF_IDS * ::core::mem::size_of::<ak_span>()) as *mut ak_span; } a_retry_of_ids.add(n_retry_of_ids) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_OPTIONS_OPTIONS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>());
    const _: () = assert!(N_OPTIONS_OPTIONS * ::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_TaskOptionsOptionsEntry>() <= 8);
    let mut n_options_options: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_options_options: *mut ak_dfix_TaskOptionsOptionsEntry = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_options_options { () => {{ if n_options_options == 0 { a_options_options = (*dcx).bdr.open_run(N_OPTIONS_OPTIONS * ::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>()) as *mut ak_dfix_TaskOptionsOptionsEntry; } a_options_options.add(n_options_options) }}; }
    macro_rules! flush_parent_task_ids {
        () => {
            if n_parent_task_ids > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    1,
                    AK_TOKEN_ROOT,
                    n_parent_task_ids as u32,
                    n_parent_task_ids * ::core::mem::size_of::<ak_span>(),
                );
                n_parent_task_ids = 0;
            }
        };
    }
    macro_rules! flush_data_dependencies {
        () => {
            if n_data_dependencies > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    2,
                    AK_TOKEN_ROOT,
                    n_data_dependencies as u32,
                    n_data_dependencies * ::core::mem::size_of::<ak_span>(),
                );
                n_data_dependencies = 0;
            }
        };
    }
    macro_rules! flush_expected_output_ids {
        () => {
            if n_expected_output_ids > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    3,
                    AK_TOKEN_ROOT,
                    n_expected_output_ids as u32,
                    n_expected_output_ids * ::core::mem::size_of::<ak_span>(),
                );
                n_expected_output_ids = 0;
            }
        };
    }
    macro_rules! flush_retry_of_ids {
        () => {
            if n_retry_of_ids > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    4,
                    AK_TOKEN_ROOT,
                    n_retry_of_ids as u32,
                    n_retry_of_ids * ::core::mem::size_of::<ak_span>(),
                );
                n_retry_of_ids = 0;
            }
        };
    }
    macro_rules! flush_options_options {
        () => {
            if n_options_options > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    5,
                    AK_TOKEN_ROOT,
                    n_options_options as u32,
                    n_options_options * ::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>(),
                );
                n_options_options = 0;
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_parent_task_ids!();
            flush_data_dependencies!();
            flush_expected_output_ids!();
            flush_retry_of_ids!();
            flush_options_options!();
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.session_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.owner_pod_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            4 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_parent_task_ids == N_PARENT_TASK_IDS { flush_parent_task_ids!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_parent_task_ids!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_parent_task_ids += 1;
            }
            5 if wire == 2 => {
                if cur != 2 { flush!(); cur = 2; }
                if n_data_dependencies == N_DATA_DEPENDENCIES { flush_data_dependencies!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_data_dependencies!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_data_dependencies += 1;
            }
            6 if wire == 2 => {
                if cur != 3 { flush!(); cur = 3; }
                if n_expected_output_ids == N_EXPECTED_OUTPUT_IDS { flush_expected_output_ids!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_expected_output_ids!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_expected_output_ids += 1;
            }
            7 if wire == 2 => {
                if cur != 4 { flush!(); cur = 4; }
                if n_retry_of_ids == N_RETRY_OF_IDS { flush_retry_of_ids!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_retry_of_ids!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_retry_of_ids += 1;
            }
            8 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.status = d.varint() as i32;
            }
            9 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.status_message = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            10 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_OPTIONS;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 2 => {
                        if cur != 5 { flush!(); cur = 5; }
                        if n_options_options == N_OPTIONS_OPTIONS { flush_options_options!(); }
                        let (off, n) = c1.len_body();
                        let mut es = Dec::new(&buf1[off..off + n]);
                        let p = at_options_options!();
                        p.write(ak_dfix_TaskOptionsOptionsEntry::ZERO);
                        dec_task_options_options_entry_fix_into(&mut es, base1 + off, u.at(2), &mut *p);
                        if es.err != 0 { c1.err = es.err; }
                        n_options_options += 1;
                    }
                    2 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (coff, clen) = c1.len_body();
                        let base2 = base1 + coff;
                        let buf2 = &buf1[coff..coff + clen];
                        let mut c2 = Dec::new(buf2);
                        out.options.presence |= AK_DFIX_TASKOPTIONS_PRESENT_MAX_DURATION;
                        while !c2.at_end() {
                            let s0 = c2.pos;
                            let k = c2.varint();
                            if c2.err != 0 { break; }
                            let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                            if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c2.err = ak_rt::ERR_MALFORMED; break; }
                            match tag {
                            1 if wire == 0 => {
                                if cur != 0 { flush!(); cur = 0; }
                                out.options.max_duration.seconds = c2.varint() as i64;
                            }
                            2 if wire == 0 => {
                                if cur != 0 { flush!(); cur = 0; }
                                out.options.max_duration.nanos = c2.varint() as i32;
                            }
                                _ => {
                                    c2.skip(tag, wire);
                                    // Decision 11: the inlined child's OWN buffer.
                                    if c2.err == 0 && !u.pos.is_null() {
                                        let rc = unk_put(u.at(3), &mut out.options.max_duration.unknown, &buf2[s0..c2.pos]);
                                        if rc != 0 { c2.err = rc; }
                                    }
                                }
                            }
                        }
                        if c2.err != 0 { c1.err = c2.err; }
                    }
                    3 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.options.max_retries = c1.varint() as i32;
                    }
                    4 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.options.priority = c1.varint() as i32;
                    }
                    5 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.partition_id = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    6 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_name = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    7 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_version = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    8 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_namespace = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    9 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_service = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    10 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.engine_type = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(1), &mut out.options.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            11 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_CREATED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.created_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.created_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(4), &mut out.created_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            12 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_SUBMITTED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.submitted_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.submitted_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(5), &mut out.submitted_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            13 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_STARTED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.started_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.started_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(6), &mut out.started_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            14 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_ENDED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.ended_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.ended_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(7), &mut out.ended_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            15 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_POD_TTL;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.pod_ttl.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.pod_ttl.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(8), &mut out.pod_ttl.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            16 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_OUTPUT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.output.success = (c1.varint() != 0) as u8;
                    }
                    2 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.output.error = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(9), &mut out.output.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            17 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.pod_hostname = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            18 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_RECEIVED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.received_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.received_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(10), &mut out.received_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            19 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_ACQUIRED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.acquired_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.acquired_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(11), &mut out.acquired_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            20 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_CREATION_TO_END_DURATION;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.creation_to_end_duration.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.creation_to_end_duration.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(12), &mut out.creation_to_end_duration.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            21 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_PROCESSING_TO_END_DURATION;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.processing_to_end_duration.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.processing_to_end_duration.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(13), &mut out.processing_to_end_duration.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            22 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.initial_task_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            23 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_RECEIVED_TO_END_DURATION;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.received_to_end_duration.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.received_to_end_duration.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(14), &mut out.received_to_end_duration.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            24 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_PROCESSED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.processed_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.processed_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(15), &mut out.processed_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            25 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKDETAILED_PRESENT_FETCHED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.fetched_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.fetched_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(16), &mut out.fetched_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            26 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.payload_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            27 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.created_by = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_TaskDetailed>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_TaskSummary(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 7 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_TaskSummary::ZERO;
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_OPTIONS_OPTIONS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>());
    const _: () = assert!(N_OPTIONS_OPTIONS * ::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_TaskOptionsOptionsEntry>() <= 8);
    let mut n_options_options: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_options_options: *mut ak_dfix_TaskOptionsOptionsEntry = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_options_options { () => {{ if n_options_options == 0 { a_options_options = (*dcx).bdr.open_run(N_OPTIONS_OPTIONS * ::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>()) as *mut ak_dfix_TaskOptionsOptionsEntry; } a_options_options.add(n_options_options) }}; }
    macro_rules! flush_options_options {
        () => {
            if n_options_options > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    1,
                    AK_TOKEN_ROOT,
                    n_options_options as u32,
                    n_options_options * ::core::mem::size_of::<ak_dfix_TaskOptionsOptionsEntry>(),
                );
                n_options_options = 0;
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_options_options!();
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.session_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKSUMMARY_PRESENT_OPTIONS;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 2 => {
                        if cur != 1 { flush!(); cur = 1; }
                        if n_options_options == N_OPTIONS_OPTIONS { flush_options_options!(); }
                        let (off, n) = c1.len_body();
                        let mut es = Dec::new(&buf1[off..off + n]);
                        let p = at_options_options!();
                        p.write(ak_dfix_TaskOptionsOptionsEntry::ZERO);
                        dec_task_options_options_entry_fix_into(&mut es, base1 + off, u.at(2), &mut *p);
                        if es.err != 0 { c1.err = es.err; }
                        n_options_options += 1;
                    }
                    2 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (coff, clen) = c1.len_body();
                        let base2 = base1 + coff;
                        let buf2 = &buf1[coff..coff + clen];
                        let mut c2 = Dec::new(buf2);
                        out.options.presence |= AK_DFIX_TASKOPTIONS_PRESENT_MAX_DURATION;
                        while !c2.at_end() {
                            let s0 = c2.pos;
                            let k = c2.varint();
                            if c2.err != 0 { break; }
                            let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                            if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c2.err = ak_rt::ERR_MALFORMED; break; }
                            match tag {
                            1 if wire == 0 => {
                                if cur != 0 { flush!(); cur = 0; }
                                out.options.max_duration.seconds = c2.varint() as i64;
                            }
                            2 if wire == 0 => {
                                if cur != 0 { flush!(); cur = 0; }
                                out.options.max_duration.nanos = c2.varint() as i32;
                            }
                                _ => {
                                    c2.skip(tag, wire);
                                    // Decision 11: the inlined child's OWN buffer.
                                    if c2.err == 0 && !u.pos.is_null() {
                                        let rc = unk_put(u.at(3), &mut out.options.max_duration.unknown, &buf2[s0..c2.pos]);
                                        if rc != 0 { c2.err = rc; }
                                    }
                                }
                            }
                        }
                        if c2.err != 0 { c1.err = c2.err; }
                    }
                    3 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.options.max_retries = c1.varint() as i32;
                    }
                    4 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.options.priority = c1.varint() as i32;
                    }
                    5 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.partition_id = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    6 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_name = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    7 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_version = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    8 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_namespace = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    9 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.application_service = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    10 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.options.engine_type = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(1), &mut out.options.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            4 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.status = d.varint() as i32;
            }
            5 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_TASKSUMMARY_PRESENT_CREATED_AT;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.created_at.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.created_at.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(4), &mut out.created_at.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            8 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.error = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            9 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.status_message = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            11 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.count_data_dependencies = d.varint() as i64;
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_TaskSummary>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_Probe(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 8 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_Probe::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.opt_count = d.varint() as i32;
                out.presence |= AK_DFIX_PROBE_PRESENT_OPT_COUNT;
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.opt_label = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
                out.presence |= AK_DFIX_PROBE_PRESENT_OPT_LABEL;
            }
            4 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.opt_flag = (d.varint() != 0) as u8;
                out.presence |= AK_DFIX_PROBE_PRESENT_OPT_FLAG;
            }
            10 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.body_as_int = d.varint() as i64;
                // Last one wins: a later member replaces the case.
                out.body_case = 10;
            }
            11 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.body_as_text = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
                // Last one wins: a later member replaces the case.
                out.body_case = 11;
            }
            12 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                out.body_as_blob = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
                // Last one wins: a later member replaces the case.
                out.body_case = 12;
            }
            13 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                let mut os = Dec::new(&buf0[off..off + n]);
                // Plan rule: the SAME member again merges; another member,
                // or none, starts from empty.
                // Decision 11 rule 4: the oneof's ONE buffer moves, emptied, from the
                // previous message member's slot into this member's.
                if out.body_case != 13 {
                    // At most one member slot holds it (the invariant this keeps).
                    let mut k = ::core::mem::replace(&mut out.body_as_stamp.unknown, ak_unk_buf { data: ::core::ptr::null_mut(), len: 0, cap: 0 });
                    let t = ::core::mem::replace(&mut out.body_as_nothing.unknown, ak_unk_buf { data: ::core::ptr::null_mut(), len: 0, cap: 0 });
                    if !t.data.is_null() { k = t; }
                    out.body_as_stamp = ak_dfix_Timestamp::ZERO;
                    out.body_as_stamp.unknown = ak_unk_buf { data: k.data, len: 0, cap: k.cap };
                }
                dec_timestamp_fix_into(&mut os, base0 + off, u.at(1), &mut out.body_as_stamp);
                if os.err != 0 { d.err = os.err; }
                // Last one wins: a later member replaces the case.
                out.body_case = 13;
            }
            14 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                let mut os = Dec::new(&buf0[off..off + n]);
                // Plan rule: the SAME member again merges; another member,
                // or none, starts from empty.
                // Decision 11 rule 4: the oneof's ONE buffer moves, emptied, from the
                // previous message member's slot into this member's.
                if out.body_case != 14 {
                    // At most one member slot holds it (the invariant this keeps).
                    let mut k = ::core::mem::replace(&mut out.body_as_nothing.unknown, ak_unk_buf { data: ::core::ptr::null_mut(), len: 0, cap: 0 });
                    let t = ::core::mem::replace(&mut out.body_as_stamp.unknown, ak_unk_buf { data: ::core::ptr::null_mut(), len: 0, cap: 0 });
                    if !t.data.is_null() { k = t; }
                    out.body_as_nothing = ak_dfix_Empty::ZERO;
                    out.body_as_nothing.unknown = ak_unk_buf { data: k.data, len: 0, cap: k.cap };
                }
                dec_empty_fix_into(&mut os, base0 + off, u.at(1), &mut out.body_as_nothing);
                if os.err != 0 { d.err = os.err; }
                // Last one wins: a later member replaces the case.
                out.body_case = 14;
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_Probe>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_Empty(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 9 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_Empty::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_Empty>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_UploadResultData(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 10 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_UploadResultData::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.session_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.result_id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                out.data_chunk = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_UploadResultData>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_MetricsBatch(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 11 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_MetricsBatch::ZERO;
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_TICKS: usize = ak_rt::arena_n(::core::mem::size_of::<i64>());
    const _: () = assert!(N_TICKS * ::core::mem::size_of::<i64>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<i64>() <= 8);
    let mut n_ticks: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_ticks: *mut i64 = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_ticks { () => {{ if n_ticks == 0 { a_ticks = (*dcx).bdr.open_run(N_TICKS * ::core::mem::size_of::<i64>()) as *mut i64; } a_ticks.add(n_ticks) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_VALUES: usize = ak_rt::arena_n(::core::mem::size_of::<f64>());
    const _: () = assert!(N_VALUES * ::core::mem::size_of::<f64>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<f64>() <= 8);
    let mut n_values: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_values: *mut f64 = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_values { () => {{ if n_values == 0 { a_values = (*dcx).bdr.open_run(N_VALUES * ::core::mem::size_of::<f64>()) as *mut f64; } a_values.add(n_values) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_CODES: usize = ak_rt::arena_n(::core::mem::size_of::<i32>());
    const _: () = assert!(N_CODES * ::core::mem::size_of::<i32>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<i32>() <= 8);
    let mut n_codes: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_codes: *mut i32 = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_codes { () => {{ if n_codes == 0 { a_codes = (*dcx).bdr.open_run(N_CODES * ::core::mem::size_of::<i32>()) as *mut i32; } a_codes.add(n_codes) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_FLAGS: usize = ak_rt::arena_n(::core::mem::size_of::<u8>());
    const _: () = assert!(N_FLAGS * ::core::mem::size_of::<u8>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<u8>() <= 8);
    let mut n_flags: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_flags: *mut u8 = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_flags { () => {{ if n_flags == 0 { a_flags = (*dcx).bdr.open_run(N_FLAGS * ::core::mem::size_of::<u8>()) as *mut u8; } a_flags.add(n_flags) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_STATUSES: usize = ak_rt::arena_n(::core::mem::size_of::<i32>());
    const _: () = assert!(N_STATUSES * ::core::mem::size_of::<i32>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<i32>() <= 8);
    let mut n_statuses: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_statuses: *mut i32 = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_statuses { () => {{ if n_statuses == 0 { a_statuses = (*dcx).bdr.open_run(N_STATUSES * ::core::mem::size_of::<i32>()) as *mut i32; } a_statuses.add(n_statuses) }}; }
    macro_rules! flush_ticks {
        () => {
            if n_ticks > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    1,
                    AK_TOKEN_ROOT,
                    n_ticks as u32,
                    n_ticks * ::core::mem::size_of::<i64>(),
                );
                n_ticks = 0;
            }
        };
    }
    macro_rules! flush_values {
        () => {
            if n_values > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    2,
                    AK_TOKEN_ROOT,
                    n_values as u32,
                    n_values * ::core::mem::size_of::<f64>(),
                );
                n_values = 0;
            }
        };
    }
    macro_rules! flush_codes {
        () => {
            if n_codes > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    3,
                    AK_TOKEN_ROOT,
                    n_codes as u32,
                    n_codes * ::core::mem::size_of::<i32>(),
                );
                n_codes = 0;
            }
        };
    }
    macro_rules! flush_flags {
        () => {
            if n_flags > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    4,
                    AK_TOKEN_ROOT,
                    n_flags as u32,
                    n_flags * ::core::mem::size_of::<u8>(),
                );
                n_flags = 0;
            }
        };
    }
    macro_rules! flush_statuses {
        () => {
            if n_statuses > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    5,
                    AK_TOKEN_ROOT,
                    n_statuses as u32,
                    n_statuses * ::core::mem::size_of::<i32>(),
                );
                n_statuses = 0;
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_ticks!();
            flush_values!();
            flush_codes!();
            flush_flags!();
            flush_statuses!();
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 0 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_ticks == N_TICKS { flush_ticks!(); }
                let v = d.varint() as i64;
                if d.err == 0 { at_ticks!().write(v); n_ticks += 1; }
            }
            2 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                let (off, n) = d.len_body();
                let mut ps = Dec::new(&buf0[off..off + n]);
                while !ps.at_end() {
                    if n_ticks == N_TICKS { flush_ticks!(); }
                    let v = ps.varint() as i64;
                    if ps.err != 0 { break; }
                    at_ticks!().write(v);
                    n_ticks += 1;
                }
                if ps.err != 0 { d.err = ps.err; }
            }
            3 if wire == 1 => {
                if cur != 2 { flush!(); cur = 2; }
                if n_values == N_VALUES { flush_values!(); }
                let v = d.f64();
                if d.err == 0 { at_values!().write(v); n_values += 1; }
            }
            3 if wire == 2 => {
                if cur != 2 { flush!(); cur = 2; }
                let (off, n) = d.len_body();
                let mut ps = Dec::new(&buf0[off..off + n]);
                while !ps.at_end() {
                    if n_values == N_VALUES { flush_values!(); }
                    let v = ps.f64();
                    if ps.err != 0 { break; }
                    at_values!().write(v);
                    n_values += 1;
                }
                if ps.err != 0 { d.err = ps.err; }
            }
            4 if wire == 0 => {
                if cur != 3 { flush!(); cur = 3; }
                if n_codes == N_CODES { flush_codes!(); }
                let v = d.varint() as i32;
                if d.err == 0 { at_codes!().write(v); n_codes += 1; }
            }
            4 if wire == 2 => {
                if cur != 3 { flush!(); cur = 3; }
                let (off, n) = d.len_body();
                let mut ps = Dec::new(&buf0[off..off + n]);
                while !ps.at_end() {
                    if n_codes == N_CODES { flush_codes!(); }
                    let v = ps.varint() as i32;
                    if ps.err != 0 { break; }
                    at_codes!().write(v);
                    n_codes += 1;
                }
                if ps.err != 0 { d.err = ps.err; }
            }
            5 if wire == 0 => {
                if cur != 4 { flush!(); cur = 4; }
                if n_flags == N_FLAGS { flush_flags!(); }
                let v = (d.varint() != 0) as u8;
                if d.err == 0 { at_flags!().write(v); n_flags += 1; }
            }
            5 if wire == 2 => {
                if cur != 4 { flush!(); cur = 4; }
                let (off, n) = d.len_body();
                let mut ps = Dec::new(&buf0[off..off + n]);
                while !ps.at_end() {
                    if n_flags == N_FLAGS { flush_flags!(); }
                    let v = (ps.varint() != 0) as u8;
                    if ps.err != 0 { break; }
                    at_flags!().write(v);
                    n_flags += 1;
                }
                if ps.err != 0 { d.err = ps.err; }
            }
            6 if wire == 0 => {
                if cur != 5 { flush!(); cur = 5; }
                if n_statuses == N_STATUSES { flush_statuses!(); }
                let v = d.varint() as i32;
                if d.err == 0 { at_statuses!().write(v); n_statuses += 1; }
            }
            6 if wire == 2 => {
                if cur != 5 { flush!(); cur = 5; }
                let (off, n) = d.len_body();
                let mut ps = Dec::new(&buf0[off..off + n]);
                while !ps.at_end() {
                    if n_statuses == N_STATUSES { flush_statuses!(); }
                    let v = ps.varint() as i32;
                    if ps.err != 0 { break; }
                    at_statuses!().write(v);
                    n_statuses += 1;
                }
                if ps.err != 0 { d.err = ps.err; }
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_MetricsBatch>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_Pair(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 12 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_Pair::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.key = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.value = d.varint() as i32;
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_Pair>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_ListResultsResponse(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 13 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_ListResultsResponse::ZERO;
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_RESULTS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_ResultRaw>());
    const _: () = assert!(N_RESULTS * ::core::mem::size_of::<ak_dfix_ResultRaw>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_ResultRaw>() <= 8);
    let mut n_results: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_results: *mut ak_dfix_ResultRaw = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_results { () => {{ if n_results == 0 { a_results = (*dcx).bdr.open_run(N_RESULTS * ::core::mem::size_of::<ak_dfix_ResultRaw>()) as *mut ak_dfix_ResultRaw; } a_results.add(n_results) }}; }
    macro_rules! flush_results {
        () => {
            if n_results > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    1,
                    AK_TOKEN_ROOT,
                    n_results as u32,
                    n_results * ::core::mem::size_of::<ak_dfix_ResultRaw>(),
                );
                n_results = 0;
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_results!();
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_results == N_RESULTS { flush_results!(); }
                let (off, n) = d.len_body();
                let mut es = Dec::new(&buf0[off..off + n]);
                let p = at_results!();
                p.write(ak_dfix_ResultRaw::ZERO);
                dec_result_raw_fix_into(&mut es, base0 + off, u.at(1), &mut *p);
                if es.err != 0 { d.err = es.err; }
                n_results += 1;
            }
            2 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.page = d.varint() as i32;
            }
            3 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.total = d.varint() as i32;
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_ListResultsResponse>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_ListTasksDetailedResponse(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 14 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_ListTasksDetailedResponse::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf0[off..off + n]);
                dec_list_tasks_detailed_response_tasks_element_pull(dcx, &mut sub, base0 + off, u.at(1));
                if sub.err != 0 { d.err = sub.err; }
            }
            2 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.page = d.varint() as i32;
            }
            3 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.total = d.varint() as i32;
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_ListTasksDetailedResponse>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_ListTaskSummaryResponse(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 15 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_ListTaskSummaryResponse::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf0[off..off + n]);
                dec_list_task_summary_response_tasks_element_pull(dcx, &mut sub, base0 + off, u.at(1));
                if sub.err != 0 { d.err = sub.err; }
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_ListTaskSummaryResponse>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_ListProbeResponse(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 16 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_ListProbeResponse::ZERO;
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_PROBES: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_Probe>());
    const _: () = assert!(N_PROBES * ::core::mem::size_of::<ak_dfix_Probe>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_Probe>() <= 8);
    let mut n_probes: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_probes: *mut ak_dfix_Probe = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_probes { () => {{ if n_probes == 0 { a_probes = (*dcx).bdr.open_run(N_PROBES * ::core::mem::size_of::<ak_dfix_Probe>()) as *mut ak_dfix_Probe; } a_probes.add(n_probes) }}; }
    macro_rules! flush_probes {
        () => {
            if n_probes > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    1,
                    AK_TOKEN_ROOT,
                    n_probes as u32,
                    n_probes * ::core::mem::size_of::<ak_dfix_Probe>(),
                );
                n_probes = 0;
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_probes!();
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_probes == N_PROBES { flush_probes!(); }
                let (off, n) = d.len_body();
                let mut es = Dec::new(&buf0[off..off + n]);
                let p = at_probes!();
                p.write(ak_dfix_Probe::ZERO);
                dec_probe_fix_into(&mut es, base0 + off, u.at(1), &mut *p);
                if es.err != 0 { d.err = es.err; }
                n_probes += 1;
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_ListProbeResponse>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_ListMetricsResponse(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 17 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_ListMetricsResponse::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf0[off..off + n]);
                dec_list_metrics_response_batches_element_pull(dcx, &mut sub, base0 + off, u.at(1));
                if sub.err != 0 { d.err = sub.err; }
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_ListMetricsResponse>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_UploadResultDataMessage(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 18 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_UploadResultDataMessage::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_UPLOADRESULTDATAMESSAGE_PRESENT_UPLOAD;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.upload.session_id = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    2 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.upload.result_id = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                    3 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        out.upload.data_chunk = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(1), &mut out.upload.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_UploadResultDataMessage>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_DualResponse(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 19 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_DualResponse::ZERO;
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_LEFT: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_Pair>());
    const _: () = assert!(N_LEFT * ::core::mem::size_of::<ak_dfix_Pair>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_Pair>() <= 8);
    let mut n_left: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_left: *mut ak_dfix_Pair = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_left { () => {{ if n_left == 0 { a_left = (*dcx).bdr.open_run(N_LEFT * ::core::mem::size_of::<ak_dfix_Pair>()) as *mut ak_dfix_Pair; } a_left.add(n_left) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_RIGHT: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_Pair>());
    const _: () = assert!(N_RIGHT * ::core::mem::size_of::<ak_dfix_Pair>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_Pair>() <= 8);
    let mut n_right: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_right: *mut ak_dfix_Pair = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_right { () => {{ if n_right == 0 { a_right = (*dcx).bdr.open_run(N_RIGHT * ::core::mem::size_of::<ak_dfix_Pair>()) as *mut ak_dfix_Pair; } a_right.add(n_right) }}; }
    macro_rules! flush_left {
        () => {
            if n_left > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    1,
                    AK_TOKEN_ROOT,
                    n_left as u32,
                    n_left * ::core::mem::size_of::<ak_dfix_Pair>(),
                );
                n_left = 0;
            }
        };
    }
    macro_rules! flush_right {
        () => {
            if n_right > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    2,
                    AK_TOKEN_ROOT,
                    n_right as u32,
                    n_right * ::core::mem::size_of::<ak_dfix_Pair>(),
                );
                n_right = 0;
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_left!();
            flush_right!();
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_left == N_LEFT { flush_left!(); }
                let (off, n) = d.len_body();
                let mut es = Dec::new(&buf0[off..off + n]);
                let p = at_left!();
                p.write(ak_dfix_Pair::ZERO);
                dec_pair_fix_into(&mut es, base0 + off, u.at(1), &mut *p);
                if es.err != 0 { d.err = es.err; }
                n_left += 1;
            }
            2 if wire == 2 => {
                if cur != 2 { flush!(); cur = 2; }
                if n_right == N_RIGHT { flush_right!(); }
                let (off, n) = d.len_body();
                let mut es = Dec::new(&buf0[off..off + n]);
                let p = at_right!();
                p.write(ak_dfix_Pair::ZERO);
                dec_pair_fix_into(&mut es, base0 + off, u.at(2), &mut *p);
                if es.err != 0 { d.err = es.err; }
                n_right += 1;
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_DualResponse>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_ChunkLeaf(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 20 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_ChunkLeaf::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.k = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.v = d.varint() as i32;
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_ChunkLeaf>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_ChunkInner(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 21 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_ChunkInner::ZERO;
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_MARKS: usize = ak_rt::arena_n(::core::mem::size_of::<i64>());
    const _: () = assert!(N_MARKS * ::core::mem::size_of::<i64>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<i64>() <= 8);
    let mut n_marks: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_marks: *mut i64 = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_marks { () => {{ if n_marks == 0 { a_marks = (*dcx).bdr.open_run(N_MARKS * ::core::mem::size_of::<i64>()) as *mut i64; } a_marks.add(n_marks) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_LEAVES: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_ChunkLeaf>());
    const _: () = assert!(N_LEAVES * ::core::mem::size_of::<ak_dfix_ChunkLeaf>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_ChunkLeaf>() <= 8);
    let mut n_leaves: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_leaves: *mut ak_dfix_ChunkLeaf = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_leaves { () => {{ if n_leaves == 0 { a_leaves = (*dcx).bdr.open_run(N_LEAVES * ::core::mem::size_of::<ak_dfix_ChunkLeaf>()) as *mut ak_dfix_ChunkLeaf; } a_leaves.add(n_leaves) }}; }
    macro_rules! flush_marks {
        () => {
            if n_marks > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    1,
                    AK_TOKEN_ROOT,
                    n_marks as u32,
                    n_marks * ::core::mem::size_of::<i64>(),
                );
                n_marks = 0;
            }
        };
    }
    macro_rules! flush_leaves {
        () => {
            if n_leaves > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    2,
                    AK_TOKEN_ROOT,
                    n_leaves as u32,
                    n_leaves * ::core::mem::size_of::<ak_dfix_ChunkLeaf>(),
                );
                n_leaves = 0;
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_marks!();
            flush_leaves!();
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 0 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_marks == N_MARKS { flush_marks!(); }
                let v = d.varint() as i64;
                if d.err == 0 { at_marks!().write(v); n_marks += 1; }
            }
            1 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                let (off, n) = d.len_body();
                let mut ps = Dec::new(&buf0[off..off + n]);
                while !ps.at_end() {
                    if n_marks == N_MARKS { flush_marks!(); }
                    let v = ps.varint() as i64;
                    if ps.err != 0 { break; }
                    at_marks!().write(v);
                    n_marks += 1;
                }
                if ps.err != 0 { d.err = ps.err; }
            }
            2 if wire == 2 => {
                if cur != 2 { flush!(); cur = 2; }
                if n_leaves == N_LEAVES { flush_leaves!(); }
                let (off, n) = d.len_body();
                let mut es = Dec::new(&buf0[off..off + n]);
                let p = at_leaves!();
                p.write(ak_dfix_ChunkLeaf::ZERO);
                dec_chunk_leaf_fix_into(&mut es, base0 + off, u.at(1), &mut *p);
                if es.err != 0 { d.err = es.err; }
                n_leaves += 1;
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_ChunkInner>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_ChunkElement(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 22 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_ChunkElement::ZERO;
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_LABELS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_LABELS * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_labels: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_labels: *mut ak_span = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_labels { () => {{ if n_labels == 0 { a_labels = (*dcx).bdr.open_run(N_LABELS * ::core::mem::size_of::<ak_span>()) as *mut ak_span; } a_labels.add(n_labels) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_ATTRS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_ChunkElementAttrsEntry>());
    const _: () = assert!(N_ATTRS * ::core::mem::size_of::<ak_dfix_ChunkElementAttrsEntry>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_ChunkElementAttrsEntry>() <= 8);
    let mut n_attrs: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_attrs: *mut ak_dfix_ChunkElementAttrsEntry = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_attrs { () => {{ if n_attrs == 0 { a_attrs = (*dcx).bdr.open_run(N_ATTRS * ::core::mem::size_of::<ak_dfix_ChunkElementAttrsEntry>()) as *mut ak_dfix_ChunkElementAttrsEntry; } a_attrs.add(n_attrs) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_INNER_MARKS: usize = ak_rt::arena_n(::core::mem::size_of::<i64>());
    const _: () = assert!(N_INNER_MARKS * ::core::mem::size_of::<i64>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<i64>() <= 8);
    let mut n_inner_marks: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_inner_marks: *mut i64 = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_inner_marks { () => {{ if n_inner_marks == 0 { a_inner_marks = (*dcx).bdr.open_run(N_INNER_MARKS * ::core::mem::size_of::<i64>()) as *mut i64; } a_inner_marks.add(n_inner_marks) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_INNER_LEAVES: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_ChunkLeaf>());
    const _: () = assert!(N_INNER_LEAVES * ::core::mem::size_of::<ak_dfix_ChunkLeaf>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_ChunkLeaf>() <= 8);
    let mut n_inner_leaves: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_inner_leaves: *mut ak_dfix_ChunkLeaf = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_inner_leaves { () => {{ if n_inner_leaves == 0 { a_inner_leaves = (*dcx).bdr.open_run(N_INNER_LEAVES * ::core::mem::size_of::<ak_dfix_ChunkLeaf>()) as *mut ak_dfix_ChunkLeaf; } a_inner_leaves.add(n_inner_leaves) }}; }
    macro_rules! flush_labels {
        () => {
            if n_labels > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    1,
                    AK_TOKEN_ROOT,
                    n_labels as u32,
                    n_labels * ::core::mem::size_of::<ak_span>(),
                );
                n_labels = 0;
            }
        };
    }
    macro_rules! flush_attrs {
        () => {
            if n_attrs > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    2,
                    AK_TOKEN_ROOT,
                    n_attrs as u32,
                    n_attrs * ::core::mem::size_of::<ak_dfix_ChunkElementAttrsEntry>(),
                );
                n_attrs = 0;
            }
        };
    }
    macro_rules! flush_inner_marks {
        () => {
            if n_inner_marks > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    3,
                    AK_TOKEN_ROOT,
                    n_inner_marks as u32,
                    n_inner_marks * ::core::mem::size_of::<i64>(),
                );
                n_inner_marks = 0;
            }
        };
    }
    macro_rules! flush_inner_leaves {
        () => {
            if n_inner_leaves > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    4,
                    AK_TOKEN_ROOT,
                    n_inner_leaves as u32,
                    n_inner_leaves * ::core::mem::size_of::<ak_dfix_ChunkLeaf>(),
                );
                n_inner_leaves = 0;
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_labels!();
            flush_attrs!();
            flush_inner_marks!();
            flush_inner_leaves!();
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_labels == N_LABELS { flush_labels!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_labels!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_labels += 1;
            }
            2 if wire == 2 => {
                if cur != 2 { flush!(); cur = 2; }
                if n_attrs == N_ATTRS { flush_attrs!(); }
                let (off, n) = d.len_body();
                let mut es = Dec::new(&buf0[off..off + n]);
                let p = at_attrs!();
                p.write(ak_dfix_ChunkElementAttrsEntry::ZERO);
                dec_chunk_element_attrs_entry_fix_into(&mut es, base0 + off, u.at(1), &mut *p);
                if es.err != 0 { d.err = es.err; }
                n_attrs += 1;
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            4 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_CHUNKELEMENT_PRESENT_INNER;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 3 { flush!(); cur = 3; }
                        if n_inner_marks == N_INNER_MARKS { flush_inner_marks!(); }
                        let v = c1.varint() as i64;
                        if c1.err == 0 { at_inner_marks!().write(v); n_inner_marks += 1; }
                    }
                    1 if wire == 2 => {
                        if cur != 3 { flush!(); cur = 3; }
                        let (off, n) = c1.len_body();
                        let mut ps = Dec::new(&buf1[off..off + n]);
                        while !ps.at_end() {
                            if n_inner_marks == N_INNER_MARKS { flush_inner_marks!(); }
                            let v = ps.varint() as i64;
                            if ps.err != 0 { break; }
                            at_inner_marks!().write(v);
                            n_inner_marks += 1;
                        }
                        if ps.err != 0 { c1.err = ps.err; }
                    }
                    2 if wire == 2 => {
                        if cur != 4 { flush!(); cur = 4; }
                        if n_inner_leaves == N_INNER_LEAVES { flush_inner_leaves!(); }
                        let (off, n) = c1.len_body();
                        let mut es = Dec::new(&buf1[off..off + n]);
                        let p = at_inner_leaves!();
                        p.write(ak_dfix_ChunkLeaf::ZERO);
                        dec_chunk_leaf_fix_into(&mut es, base1 + off, u.at(3), &mut *p);
                        if es.err != 0 { c1.err = es.err; }
                        n_inner_leaves += 1;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(2), &mut out.inner.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_ChunkElement>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_ChunkedResponse(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 23 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_ChunkedResponse::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            7 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf0[off..off + n]);
                dec_chunked_response_items_element_pull(dcx, &mut sub, base0 + off, u.at(1));
                if sub.err != 0 { d.err = sub.err; }
            }
            8 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.page = d.varint() as i32;
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_ChunkedResponse>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_ChunkedResponseWide(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 24 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_ChunkedResponseWide::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            70000 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                let mut sub = Dec::new(&buf0[off..off + n]);
                dec_chunked_response_wide_items_element_pull(dcx, &mut sub, base0 + off, u.at(1));
                if sub.err != 0 { d.err = sub.err; }
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_ChunkedResponseWide>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_LeafElement(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 25 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_LeafElement::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.id = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.n = d.varint() as i64;
            }
            3 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_LEAFELEMENT_PRESENT_STAMP;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.stamp.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.stamp.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(1), &mut out.stamp.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_LeafElement>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_LeafResponse(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 26 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_LeafResponse::ZERO;
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_ITEMS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_LeafElement>());
    const _: () = assert!(N_ITEMS * ::core::mem::size_of::<ak_dfix_LeafElement>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_LeafElement>() <= 8);
    let mut n_items: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_items: *mut ak_dfix_LeafElement = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_items { () => {{ if n_items == 0 { a_items = (*dcx).bdr.open_run(N_ITEMS * ::core::mem::size_of::<ak_dfix_LeafElement>()) as *mut ak_dfix_LeafElement; } a_items.add(n_items) }}; }
    macro_rules! flush_items {
        () => {
            if n_items > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    1,
                    AK_TOKEN_ROOT,
                    n_items as u32,
                    n_items * ::core::mem::size_of::<ak_dfix_LeafElement>(),
                );
                n_items = 0;
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_items!();
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            9 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_items == N_ITEMS { flush_items!(); }
                let (off, n) = d.len_body();
                let mut es = Dec::new(&buf0[off..off + n]);
                let p = at_items!();
                p.write(ak_dfix_LeafElement::ZERO);
                dec_leaf_element_fix_into(&mut es, base0 + off, u.at(1), &mut *p);
                if es.err != 0 { d.err = es.err; }
                n_items += 1;
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_LeafResponse>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_Surrogate(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 27 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_Surrogate::ZERO;
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_ATTRS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_dfix_SurrogateAttrsEntry>());
    const _: () = assert!(N_ATTRS * ::core::mem::size_of::<ak_dfix_SurrogateAttrsEntry>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_dfix_SurrogateAttrsEntry>() <= 8);
    let mut n_attrs: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_attrs: *mut ak_dfix_SurrogateAttrsEntry = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_attrs { () => {{ if n_attrs == 0 { a_attrs = (*dcx).bdr.open_run(N_ATTRS * ::core::mem::size_of::<ak_dfix_SurrogateAttrsEntry>()) as *mut ak_dfix_SurrogateAttrsEntry; } a_attrs.add(n_attrs) }}; }
    // ABI v1 7.3: a byte budget divided by the group size, not an element
    // count, so the scratch is the same 32 KB whatever the schema does.
    const N_TEXTS: usize = ak_rt::arena_n(::core::mem::size_of::<ak_span>());
    const _: () = assert!(N_TEXTS * ::core::mem::size_of::<ak_span>() <= ak_rt::ARENA_BYTES && ::core::mem::align_of::<ak_span>() <= 8);
    let mut n_texts: usize = 0;
    // Optimisation D4: the run's elements go straight into the record buffer.
    let mut a_texts: *mut ak_span = ::core::ptr::null_mut();
    #[allow(unused_macros)]
    macro_rules! at_texts { () => {{ if n_texts == 0 { a_texts = (*dcx).bdr.open_run(N_TEXTS * ::core::mem::size_of::<ak_span>()) as *mut ak_span; } a_texts.add(n_texts) }}; }
    macro_rules! flush_attrs {
        () => {
            if n_attrs > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    1,
                    AK_TOKEN_ROOT,
                    n_attrs as u32,
                    n_attrs * ::core::mem::size_of::<ak_dfix_SurrogateAttrsEntry>(),
                );
                n_attrs = 0;
            }
        };
    }
    macro_rules! flush_texts {
        () => {
            if n_texts > 0 {
                // No call: the run was decoded into the record buffer (D4); close
                // its record. The host reads it after `ak_parse_*` returns.
                (*dcx).bdr.close_run(
                    ak_rt::bdr::OP_ADD,
                    2,
                    AK_TOKEN_ROOT,
                    n_texts as u32,
                    n_texts * ::core::mem::size_of::<ak_span>(),
                );
                n_texts = 0;
            }
        };
    }
    macro_rules! flush {
        () => {
            flush_attrs!();
            flush_texts!();
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.text = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            2 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_SURROGATE_PRESENT_NESTED;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 2 => {
                        if cur != 0 { flush!(); cur = 0; }
                        let (off, n) = c1.len_body();
                        if n != 0 && ak_rt::strings::check_utf8(&buf1[off..off + n]).is_err() { c1.err = ak_rt::ERR_TRANSCODE; }
                        out.nested.text = ak_span { off: (base1 + off) as u32, len: n as u32, coder: 0 };
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(1), &mut out.nested.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            3 if wire == 2 => {
                if cur != 1 { flush!(); cur = 1; }
                if n_attrs == N_ATTRS { flush_attrs!(); }
                let (off, n) = d.len_body();
                let mut es = Dec::new(&buf0[off..off + n]);
                let p = at_attrs!();
                p.write(ak_dfix_SurrogateAttrsEntry::ZERO);
                dec_surrogate_attrs_entry_fix_into(&mut es, base0 + off, u.at(2), &mut *p);
                if es.err != 0 { d.err = es.err; }
                n_attrs += 1;
            }
            4 if wire == 2 => {
                if cur != 2 { flush!(); cur = 2; }
                if n_texts == N_TEXTS { flush_texts!(); }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                at_texts!().write(ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 });
                n_texts += 1;
            }
            5 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                out.raw = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_Surrogate>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_SurrogateInner(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 28 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_SurrogateInner::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.text = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_SurrogateInner>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}

/// Pull family (ABI v1 section 7.1): parse into the context, zero upcalls.
///
/// The host then calls `ak_bdr_footprint` and either `ak_bdr_drain` (copy the
/// records into its own memory, which is what a managed host must do) or
/// `ak_bdr_ptr` (walk them in place, which is what a native host does).
#[no_mangle]
pub unsafe extern "C" fn ak_parse_WireZoo(
    ctx: *mut ak_dec_ctx,
    buf: *const u8,
    len: usize,
) -> i32 {
    // ABI v1 section 3: every entry point requires `ak_init`.
    #[cfg(feature = "init-guard")]
    if !crate::ak_init_ok() {
        return AK_ERR_UNINITIALIZED;
    }
    let dcx = ctx as *mut DecCtxImpl;
    ak_rt::bump!((*dcx).c, forward);
    // Same rule as `ak_decode_*` (D17): a new operation clears the sticky slot,
    // so a rejected parse cannot poison every later one on this context.
    (*dcx).hdr.err = AK_OK;
    (*dcx).bdr.reset();
    // Decision 11 rule 6: the context is bound to its root; another root is refused.
    if (*dcx).root != 29 { (*dcx).hdr.err = AK_ERR_INVALID_STATE; return AK_ERR_INVALID_STATE; }
    // R-D9: reject a buffer longer than u32::MAX before it can alias a span.
    if len > u32::MAX as usize {
        (*dcx).hdr.err = AK_ERR_LIMIT;
        return AK_ERR_LIMIT;
    }
    let buf0 = ::core::slice::from_raw_parts(buf, len);
    let base0 = 0usize;
    let mut d = Dec::new(buf0);
    let mut out = ak_dfix_WireZoo::ZERO;
    macro_rules! flush {
        () => {
        };
    }
    let u = UnkCx::root(dcx);
    let mut cur = 0u32;
    while !d.at_end() {
        let s0 = d.pos;
        let k = d.varint();
        if d.err != 0 { break; }
        let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
        if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { d.err = ak_rt::ERR_MALFORMED; break; }
        match tag {
            1 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.v_int32 = d.varint() as i32;
            }
            2 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.v_int64 = d.varint() as i64;
            }
            3 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.v_bool = (d.varint() != 0) as u8;
            }
            4 if wire == 1 => {
                if cur != 0 { flush!(); cur = 0; }
                out.v_double = d.f64();
            }
            5 if wire == 5 => {
                if cur != 0 { flush!(); cur = 0; }
                out.v_fixed32 = d.fixed32();
            }
            6 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                if n != 0 && ak_rt::strings::check_utf8(&buf0[off..off + n]).is_err() { d.err = ak_rt::ERR_TRANSCODE; }
                out.v_string = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            7 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (off, n) = d.len_body();
                out.v_bytes = ak_span { off: (base0 + off) as u32, len: n as u32, coder: 0 };
            }
            8 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.v_enum = d.varint() as i32;
            }
            9 if wire == 2 => {
                if cur != 0 { flush!(); cur = 0; }
                let (coff, clen) = d.len_body();
                let base1 = base0 + coff;
                let buf1 = &buf0[coff..coff + clen];
                let mut c1 = Dec::new(buf1);
                out.presence |= AK_DFIX_WIREZOO_PRESENT_V_MSG;
                while !c1.at_end() {
                    let s0 = c1.pos;
                    let k = c1.varint();
                    if c1.err != 0 { break; }
                    let (tag, wire) = ((k >> 3) as u32, (k & 7) as u32);
                    if tag == 0 || (k >> 3) > ak_rt::MAX_FIELD_NUMBER { c1.err = ak_rt::ERR_MALFORMED; break; }
                    match tag {
                    1 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.v_msg.seconds = c1.varint() as i64;
                    }
                    2 if wire == 0 => {
                        if cur != 0 { flush!(); cur = 0; }
                        out.v_msg.nanos = c1.varint() as i32;
                    }
                        _ => {
                            c1.skip(tag, wire);
                            // Decision 11: the inlined child's OWN buffer.
                            if c1.err == 0 && !u.pos.is_null() {
                                let rc = unk_put(u.at(1), &mut out.v_msg.unknown, &buf1[s0..c1.pos]);
                                if rc != 0 { c1.err = rc; }
                            }
                        }
                    }
                }
                if c1.err != 0 { d.err = c1.err; }
            }
            536870911 if wire == 0 => {
                if cur != 0 { flush!(); cur = 0; }
                out.v_big_tag = d.varint() as i32;
            }
            // Decision 11 (WP5 step 7): the same capture as push; the buffers ride in
            // the groups the records carry and pass to the host when it reads them.
            _ => {
                if cur != 0 { flush!(); cur = 0; }
                d.skip(tag, wire);
                if d.err == 0 && !u.pos.is_null() {
                    let rc = unk_put(u, &mut out.unknown, &buf0[s0..d.pos]);
                    if rc != 0 { d.err = rc; }
                }
            }
        }
    }
    if d.err == 0 {
        flush!();
        // The root group LAST, exactly where the push family calls `apply`, so the
        // record stream is the call sequence push would have made, in its order.
        (*dcx).bdr.push(
            ak_rt::bdr::OP_APPLY,
            0,
            AK_TOKEN_ROOT,
            1,
            &out as *const _ as *const u8,
            ::core::mem::size_of::<ak_dfix_WireZoo>(),
        );
    }
    if (*dcx).hdr.err != AK_OK { (*dcx).hdr.err } else if d.err != 0 { d.err } else { AK_OK }
}
